以下是基于你的基础计划，进行完善后的**企业级知识库构建方案**。

---

### 总体架构图

```mermaid
graph TD
    A[原始 JSON Schema] --> B(数据清洗与重组 ETL);
    B --> C{数据增强 Data Augmentation};
    C -->|生成| D[语义描述块 (Semantic Block)];
    C -->|生成| E[元数据 Payload];
    D --> F[Embedding 模型];
    F --> G[向量数据库 (ChromaDB)];
    E --> G;
    
    User[用户 Query] --> H[Query 预处理/扩展];
    H --> I[Embedding];
    I --> G;
    G --> J[Top-K 候选结果];
    J --> K[重排序 (Rerank)];
    K --> L[最终 Context];
```

---

### 第一阶段：构建“语义描述块” (Smart Serialization)

不要简单地把 JSON 扁平化。我们需要构建一个**专门面向 Embedding 模型阅读**的文本格式。这个格式应该类似一份“功能说明书”或“Markdown 文档”。

#### 1. 策略：权重分级与模板化
我们将 JSON 的内容分为三类，权重不同：
*   **高权重（核心语义）**：`name`, `description`, `keywords`, `usage_guides`。
*   **中权重（功能细节）**：`parameters` 中的 `description`，端口含义。
*   **低权重（噪音，剔除）**：`id`, `z`, `wires`, `template_json`, `ui_control` 等代码实现细节。

#### 2. 实现代码示例 (Python)

编写一个转换器，将 JSON 转化为自然语言文本（Document）。

```python
def serialize_module_to_text(module_json):
    """
    将模块 JSON 转换为富含语义的自然语言文本块。
    """
    lines = []
    
    # 1. 核心身份 (增加权重)
    lines.append(f"模块名称: {module_json.get('name')} ({module_json.get('id')})")
    lines.append(f"模块类别: {module_json.get('category')}")
    
    # 2. 核心描述 (这是检索的关键)
    desc = module_json.get('description', '')
    lines.append(f"功能描述: {desc}")
    
    # 3. 关键词扩展
    keywords = ", ".join(module_json.get('keywords', []))
    lines.append(f"关键词: {keywords}")
    
    # 4. 使用场景 (非常重要，匹配用户意图)
    guides = module_json.get('usage_guides', [])
    if guides:
        lines.append("适用场景:")
        for guide in guides:
            lines.append(f"- {guide}")
            
    # 5. 参数语义 (只提取参数名和描述，忽略数据类型等技术细节)
    params = module_json.get('parameters', {}).get('properties', {})
    if params:
        lines.append("参数功能:")
        for key, info in params.items():
            # 跳过纯技术参数，只保留业务参数
            if key not in ['x', 'y', 'wires']: 
                lines.append(f"- {key}: {info.get('description')}")

    # 6. 端口语义 (利用我们上一轮设计的 io_topology)
    topo = module_json.get('io_topology', {})
    lines.append("输入输出端口:")
    for inp in topo.get('fixed_inputs', []):
        lines.append(f"- 输入: {inp.get('label')} ({inp.get('description', '')})")
    
    return "\n".join(lines)
```

**生成的文本块示例（Linear）：**
> 模块名称: 线性变换 (Linear Transform)
> 模块类别: 逻辑模块/数学变换
> 功能描述: 线性变换模块用于将输入信号按线性比例映射到新的数值范围。常用于量程转换、归一化或工程量换算...
> 关键词: 线性, 比例映射, 量程转换...
> 适用场景:
> - 当传感器输出范围与控制逻辑所需范围不一致时...
> 参数功能:
> - inputD: 输入范围是否动态...
> - inputA: 输入范围起始值...
> 输入输出端口:
> - 输入: 对象 (输入值)

---

### 第二阶段：数据增强 (Data Augmentation) - *专家建议*

仅仅依靠模块自身的描述，有时无法覆盖用户的**口语化表达**。
例如：用户可能会说“把 4-20mA 转成 0-100度”，但他不知道这叫“线性变换”。

**建议方案：** 在入库前，利用 LLM (如 GPT-4) 为每个模块生成 5-10 个**假设性用户提问 (Hypothetical Questions)**，并将这些问题拼接到 Embedding 文本块的末尾。

*   **Prompt:** "你是一个工业自动化专家。阅读以下模块描述，列出 5 个用户可能为了寻找该模块而输入的搜索指令（不要包含模块名称）。"
*   **生成的文本追加:**
    > ... (上面的描述)
    > **潜在用户指令:**
    > - 如何把温度传感器的信号转换成百分比？
    > - 计算两个数值范围的比例映射
    > - 模拟量输入值校准公式
    > - y = kx + b 运算

这会极大地提高**语义匹配**的命中率。

---

### 第三阶段：向量数据库入库 (Storage Strategy)

使用 ChromaDB 时，Payload (Metadata) 的设计非常关键，它决定了你能否进行**混合检索 (Hybrid Search)**。

#### 1. Metadata 设计
不要只存 JSON，要把常用的过滤字段提取出来。

```python
metadata = {
    "module_id": "linear",
    "category_l1": "逻辑模块",     # 一级分类
    "category_l2": "数学变换",     # 二级分类
    "has_dynamic_ports": True,    # 是否有动态端口（高级过滤用）
    "json_payload": json.dumps(module_json) # 完整的 JSON 字符串
}
```

#### 2. 入库代码逻辑

```python
import chromadb
from chromadb.utils import embedding_functions

# 初始化 client
client = chromadb.PersistentClient(path="./knowledge_db")
collection = client.get_or_create_collection(name="modules_v1")

# 准备数据
doc_text = serialize_module_to_text(module_json) + "\n" + generated_hypothetical_questions
doc_id = module_json['id']

# 入库
collection.upsert(
    documents=[doc_text],      # 用于生成向量的内容
    metadatas=[metadata],      # 用于过滤和后续逻辑的元数据
    ids=[doc_id]               # 唯一 ID
)
```

---

### 第四阶段：检索策略 (Runtime Retrieval)

当检索智能体工作时，不要只做简单的 `query`。

#### 1. 关键词 + 向量 混合检索 (推荐)
如果用户输入了非常明确的词，比如“Modbus”，纯向量搜索可能会因为语义泛化而匹配到不相干的通讯模块。
*   先用 Metadata 里的 `category` 或 `keywords` 做初步筛选（如果 ChromaDB 版本支持全文检索更好，不支持则依靠 Vector）。

#### 2. 阈值过滤 (Thresholding)
设置一个 `distance` 或 `similarity` 阈值。如果匹配度太低，说明知识库里没有这个模块，应该返回“未找到”，而不是强行返回一个不相关的模块。

#### 3. 检索结果处理
检索智能体拿到 Top-3 的 Payload 后，提取出 `json_payload`，将其中的 `parameters_schema` 和 `description` 传递给**规划智能体**。

---

### 总结与改进清单

1.  **序列化 (ETL)**：不要使用原始 JSON。编写一个 `serialize` 函数，提取 Name, Description, Usage, Params, Ports，并转化为 Markdown 格式。
2.  **数据增强**：使用 LLM 生成“潜在用户指令 (Hypothetical Queries)”，并追加到 Embedding 文本中，解决“意图与术语不匹配”的问题。
3.  **元数据结构**：在 Metadata 中存储层级分类 (`category`)，以便未来支持“只搜索数学类模块”这样的指令。
4.  **版本控制**：给 Schema 加上 version 字段。如果 JSON 结构升级了，需要能识别并重建索引。

这个方案既保证了向量的语义密度，又保留了完整的工程描述文件，是目前 RAG 落地的最佳实践之一。