# 工作流节点输入输出说明

> 最后核对时间：2026-04-27
> 核对依据：`workflow.py`、`workflow_trace.py`、`utils/phase3_contracts.py`、`utils/workflow_runtime.py`、`agents/*.py`、`app/services/workflow_state_projection.py`、`app/api/models.py`、`frontend/src/types/workflow.ts`。
> 定位：节点契约速查。全局状态、API/前端定位与优先级见 [工作流总结文档](工作流总结文档.md)。

## 1. 阅读规则

- 本文只描述当前正式代码路径，不描述历史设计稿。
- `workflow.py` 与 `workflow_trace.py` 共用同一套正式拓扑。
- `WorkflowState` 的正式字段以 `workflow.WorkflowState` 与 `build_initial_state(...)` 为准。
- API 与前端只消费投影 DTO，不是节点输入输出的事实源。
- 旧字段 `retrieval_context / execution_plan / generated_code / execution_result / validation_result / next_step` 已退出正式 `WorkflowState`。
- LLM 增强当前是默认关闭的节点内可选能力；不新增正式主链节点，不改变 LangGraph 拓扑，不让 LLM 直接生成 `compiled_artifact` 或最终 flows JSON。

## 2. 主链拓扑

```text
analysis
  -> ambiguity_router
ambiguity_router
  -> clarification_review | retrieval
clarification_review
  -> clarification_review | clarification_apply | retrieval
clarification_apply
  -> retrieval | END
retrieval
  -> architecture_planning
architecture_planning
  -> architecture_review
architecture_review
  -> architecture_review | architecture_feedback_apply | subsystem_planning
architecture_feedback_apply
  -> architecture_planning | subsystem_planning | END
subsystem_planning
  -> global_assembly
  -> coding
  -> verification
  -> repair_router
repair_router
  -> END | repair_agent
repair_agent
  -> subsystem_planning | global_assembly | coding | END
```

## 3. `WorkflowState` 字段总览

| 字段 | 生产/更新方 | 主要消费者 | 说明 |
| --- | --- | --- | --- |
| `user_query` | 入口 | `analysis`、`retrieval` | 用户自然语言需求 |
| `analysis_result` | `analysis`、`ambiguity_router`、`clarification_apply` | `ambiguity_router`、`retrieval` | Analysis 原始结果，含 `clarification_signals`；可选 `engineering_analysis` 记录工程需求编译诊断 |
| `requirement_spec` | `analysis`、`clarification_apply`、`architecture_feedback_apply` | planner、retrieval、assembly、verification | 正式需求入口；可选 `engineering` 扩展承载 AHU 点位、回路、联锁、通讯、命名、缺失项、目标形态和检索线索 |
| `retrieval_bundle` | `retrieval` | planner、assembly、coding | 正式检索资产包；`metadata` 可记录 LLM rewrite、结构化 query 采用情况、reranker 诊断和 top asset IDs |
| `decomposition_result` | `architecture_planning`、planning repair | review、subsystem planning、repair | 系统拆解结果；可带 LLM advisor warnings |
| `architecture_plan` | `architecture_planning`、planning repair | review、subsystem planning、assembly、verification、repair | 系统级骨架；`pattern_bindings[*].llm_advisory` 可记录架构 advisor 诊断 |
| `subsystem_plan_map` | `subsystem_planning`、assembly repair | assembly、verification、repair | 子系统局部 IR；`template_binding.llm_advisory` 可记录接口适配诊断 |
| `assembled_graph_ir` | `global_assembly`、compile repair | coding、verification、repair | 全局 Graph IR |
| `compiled_artifact` | `coding` | verification、API result | JSON 编译产物 |
| `verification_report` | `verification` | `repair_router`、API diagnostics | 结构验收结果 |
| `final_output` | `verification`、review apply、trace/API runner | 外部调用方、API result | 对外结果摘要 |
| `route_decision` | `repair_router`、`repair_agent` | repair 条件边、trace、API diagnostics | repair 路由 |
| `repair_context` | `repair_agent` | repair 条件边、trace | 最近一轮修补上下文 |
| `repair_history` | `repair_agent` | trace、API diagnostics | 修补审计历史 |
| `retry_budget` | 初始状态、repair 节点 | `repair_router`、`repair_agent` | scope 预算，默认 planning/assembly/compile 各 2 |
| `retry_counts_by_scope` | 初始状态、repair 节点 | `repair_router`、`repair_agent` | scope 已消耗计数 |
| `retry_count` | 初始状态、repair 节点 | trace | `retry_counts_by_scope` 聚合影子值 |
| `hitl_stage` | router / review 节点 | review 节点、API projection | 当前 review 阶段 |
| `review_request` | review 节点 | API/前端、review 条件边 | 冻结后的 review 请求 |
| `review_response` | review 节点 / resume | apply 节点、review 条件边 | 人工或 assumed 回复 |
| `review_history` | review / apply 节点 | trace、review artifacts | review 生命周期记录 |
| `review_enabled` | review 节点 | review 条件边 | 当前是否允许真正 `interrupt` |
| `review_required` | `ambiguity_router`、`architecture_review` | review 条件边 | 是否需要 review |
| `review_status` | router / review / apply | review 条件边、API projection | 常规值见第 5 节 |
| `review_id` | review 节点 | API resume 校验 | 当前 review 稳定 ID |
| `clarification_round` | `clarification_apply` | trace / 审计 | 澄清回写轮次 |
| `architecture_feedback_patch` | `architecture_feedback_apply` | architecture feedback 条件边 | 架构反馈补丁摘要 |
| `enable_hitl_clarification` | 入口 | `clarification_review` | clarification HITL 开关 |
| `enable_hitl_architecture_review` | 入口 | `architecture_review` | architecture review HITL 开关 |
| `enable_repair_agent` | 入口 | `repair_router` | 自动修复节点开关；默认 false，只有 true 时 repairable issue 才进入 `repair_agent` |
| `current_step` | 多数节点 | trace、API projection | 当前步骤标签 |
| `debug_history` | 初始状态 / 测试桩 | 无正式决策消费者 | 历史预留 |

## 4. 节点契约

### `analysis`

- 输入：`user_query`
- 输出：`analysis_result`、`requirement_spec`、`current_step=analysis_completed`
- 下游：`ambiguity_router`、`retrieval`
- 可选增强：`ANALYSIS_USE_ENGINEERING_COMPILER=True` 且相关 LLM 可用时，调用工程需求编译器生成结构化补丁，合并到 `requirement_spec.engineering`，并写入 `analysis_result.engineering_analysis` 诊断。
- 增强输出：复杂 AHU 请求可在 `requirement_spec.engineering` 写入 `points`、`control_loops`、`interlocks`、`communication`、`naming_convention`、`missing_required_fields`、`target_profile`、`retrieval_hints`；`target_profile` 描述参考 `AHU程序/flows_*.json` 的目标页签、子流程数量、body 节点下限和标准对象组，`retrieval_hints` 提供 `pattern_queries/template_queries/atomic_queries`。
- 兜底行为：真实 LLM 对复杂 AHU 返回空工程补丁时，工程编译器会生成保守工程画像，逻辑点位允许 `explicit=false` 且不填通讯地址，同时把点表、设备数量、通讯地址等缺失写入 `missing_required_fields`。
- 不变量：`requirement_spec` 是正式规划入口；`clarification_signals` 是前置澄清分支的输入；工程需求编译失败时必须回退 deterministic `build_requirement_spec(...)`；LLM 不允许直接写 `architecture_plan`、`subsystem_plan_map`、`assembled_graph_ir` 或最终 flows JSON。

### `ambiguity_router`

- 输入：`analysis_result`、`requirement_spec`
- 输出：`review_required`、`hitl_stage`、`review_status`、`current_step=ambiguity_routed`；必要时补写 `analysis_result.clarification_signals`
- 路由：`review_required=True -> clarification_review`，否则 `retrieval`
- 不变量：不修改检索、规划、装配、编译产物。

### `clarification_review`

- 输入：`analysis_result`、`requirement_spec`、`review_required`、`enable_hitl_clarification`、review 状态字段
- 输出：`review_request`、`review_response`、`review_history`、`review_enabled`、`review_status`、`review_id`、`hitl_stage`、`current_step`
- 路由：
  - `review_required=False`：跳过到 `retrieval`
  - `review_enabled=True` 且 `review_status=pending`：`interrupt(request)` 后回到本节点
  - 已回答或有 decision：进入 `clarification_apply`
  - 不启用 HITL：走 `assumed` 旁路到 `retrieval`
- 不变量：不修改 `retrieval_bundle / architecture_plan / subsystem_plan_map`。

### `clarification_apply`

- 输入：`review_response`、`analysis_result`、`requirement_spec`、`clarification_round`
- 输出：更新后的 `analysis_result`、`requirement_spec`、`clarification_round`、`review_history`、`review_status`、`hitl_stage=none`、`current_step=clarification_applied`
- 路由：`decision=reject -> END`，否则 `retrieval`
- 不变量：只回写需求侧输入；`reject` 时写 `final_output["review_abort"]`。

### `retrieval`

- 输入：`user_query`、`analysis_result`、`requirement_spec`
- 输出：`retrieval_bundle`、`current_step=retrieval_completed`
- 下游：`architecture_planning`
- 可选增强：`LLM_ENHANCEMENT_ENABLED=True` 且 `RETRIEVAL_USE_LLM_REWRITE=True` 时，可调用 LLM 查询改写；`RETRIEVAL_USE_CROSS_ENCODER_RERANK=True` 时，可对已召回的 `subflow_templates / system_patterns` 做 Cross-Encoder rerank。
- rewrite 输入：消费 `user_query`、`analysis_result` 与 `requirement_spec`，其中 `requirement_spec.engineering.retrieval_hints` 可作为 deterministic supplement。
- rewrite 输出：可包含 `query_variants`、`pattern_queries`、`template_queries`、`atomic_queries`、`asset_queries`、`normalized_terms`、`risk_flags`；`asset_queries` 仅表达 `asset_type/subsystem_id/intent/query/must_match_terms/port_roles`，不选择具体资产 ID。
- 兜底行为：若 LLM rewrite 为空但需求是 AHU，`retrieval` 会根据 `engineering.retrieval_hints` 或内置 AHU taxonomy 补充 system pattern、subflow template、atomic module 查询；诊断写入 `retrieval_bundle.metadata.llm_rewrite.deterministic_supplement_used`。
- rerank provider：`RETRIEVAL_RERANKER_PROVIDER` 支持本地 `bge/sentence-transformers/cross-encoder` 和 `siliconflow` API；API 路径使用 `SILICONFLOW_API_KEY`、`SILICONFLOW_BASE_URL` 与 `RETRIEVAL_RERANK_TIMEOUT_S`。
- 不变量：正式节点只回填 `retrieval_bundle`；仓库级 `RetrievalAgent.retrieve()` compat API 已删除；rewrite/rerank 只改变查询变体或候选排序，不创造不存在的 `module_type / template_id / pattern_id`。

### `architecture_planning`

- 输入：`requirement_spec`、`retrieval_bundle`
- 输出：`decomposition_result`、`architecture_plan`、`current_step=architecture_planned`
- 下游：`architecture_review`
- 可选增强：`LLM_ENHANCEMENT_ENABLED=True` 且 `ARCHITECTURE_USE_LLM_ADVISOR=True` 时，可在 deterministic draft 后调用架构 advisor，采用通过本地白名单校验的 page / subsystem / template preference / shared signal advisory patch。
- 写回位置：`architecture_plan.pattern_bindings[*].llm_advisory`、`architecture_plan.warnings`、`decomposition_result.warnings`、`subsystem_slots[*].score_breakdown`。
- 不变量：规则优先生成系统骨架、页签、子系统槽位、共享信号约束与规划顺序；LLM 不允许删除必需页或显式子系统，不允许引入候选集外模板 ID，多 owner shared signal 只进入 warning/advisory。

### `architecture_review`

- 输入：`architecture_plan`、`decomposition_result`、`enable_hitl_architecture_review`、review 状态字段
- 输出：`review_required`、`review_request`、`review_response`、`review_history`、`review_enabled`、`review_status`、`review_id`、`hitl_stage`、`current_step`
- 路由：
  - 架构对象为空：`not_required -> subsystem_planning`
  - `review_enabled=True` 且 `review_status=pending`：`interrupt(request)` 后回到本节点
  - 已回答或有 decision：进入 `architecture_feedback_apply`
  - 不启用 HITL：走 `assumed` 旁路到 `subsystem_planning`
- 不变量：只展示骨架摘要并收集反馈，不直接修改结构产物。

### `architecture_feedback_apply`

- 输入：`review_response`、`review_request`、`requirement_spec`
- 输出：按 decision 更新 `requirement_spec`、`architecture_feedback_patch`、`review_history`、`review_status`、`hitl_stage`、`current_step`，必要时写 `final_output["review_abort"]`
- 路由：
  - `approve` 或空 decision：`subsystem_planning`
  - `feedback / clarify`：回写需求侧后回到 `architecture_planning`
  - `reject`：`END`
- 不变量：`approve` 不生成 patch；`feedback/clarify` 只回写需求侧。

### `subsystem_planning`

- 输入：`requirement_spec`、`decomposition_result`、`architecture_plan`、`retrieval_bundle`
- 输出：`subsystem_plan_map`、`current_step=subsystem_planned`
- 下游：`global_assembly`
- 可选增强：`LLM_ENHANCEMENT_ENABLED=True` 且 `SUBSYSTEM_USE_LLM_ADAPTER=True` 时，可在模板复用前调用接口适配器，采用通过本地校验的 `port_binding_patch`。
- 写回位置：每个子系统 `template_binding.llm_advisory`，记录 enabled、adopted、patch_count、rejected_patch_count、risk_flags、fallback 等诊断。
- 不变量：当前按 `planning_order` 串行执行，不使用 `Send`；只消费显式 `interface_bindings`，不再从 compat-only `imports / exports` 自动合成绑定；LLM patch 必须匹配当前 `subsystem_id / template_id`、合法端口范围和合法 `binding_kind`，采用后仍重新执行 `_analyze_template_interface()`。

### `global_assembly`

- 输入：`requirement_spec`、`architecture_plan`、`subsystem_plan_map`、`retrieval_bundle`
- 输出：`assembled_graph_ir`、`current_step=global_assembly_completed`
- 下游：`coding`
- 不变量：正式输入是 bundle-only；不回填顶层 `execution_plan`，`assembled_graph_ir` 也不携带 `source_execution_plan`。

### `coding`

- 输入：`assembled_graph_ir`、`retrieval_bundle`
- 输出：`compiled_artifact`、`current_step=coding_completed`
- 下游：`verification`
- 不变量：正式产物是 `compiled_artifact`；仓库级 `compile_graph(...) / generate_json(...)` compat API 已删除；不回填顶层 `generated_code`。

### `verification`

- 输入：`assembled_graph_ir`、`compiled_artifact`、`requirement_spec`、`architecture_plan`、`subsystem_plan_map`
- 输出：`verification_report`、`final_output`、`current_step=verification_completed`
- 下游：`repair_router`
- 不变量：同时检查 planning / assembly / compile 三层；`repair_payload` 是 repair 正式输入。

### `repair_router`

- 输入：`verification_report`、`retry_budget`、`retry_counts_by_scope`
- 输出：规范化后的 retry 字段、`route_decision`；非 `accept` 时写 `current_step=repair_router_completed`
- 路由：
  - `verification_report.status=passed`：`accept -> END`
  - `repair_scope=planning` 且预算未耗尽：`planning_repair -> repair_agent`
  - `repair_scope=assembly` 且预算未耗尽：`assembly_repair -> repair_agent`
  - `repair_scope=compile` 且预算未耗尽：`compile_repair -> repair_agent`
  - `enable_repair_agent=False` 且原本需要 repair：`reject -> END`
  - scope 不支持或预算耗尽：`reject -> END`
- 不变量：主预算按 scope 计数，不按 `retry_count` 决策。

### `repair_agent`

- 输入：`route_decision`、`verification_report`、retry 字段、以及 repair scope 对应的上游主对象
- 输出：`repair_context`、`repair_history`、更新后的 `route_decision`、retry 字段、被修补主对象、`current_step`
- 回跳：
  - `planning`：修补 `architecture_plan / decomposition_result`，回到 `subsystem_planning`
  - `assembly`：修补 `subsystem_plan_map`，回到 `global_assembly`
  - `compile`：修补 `assembled_graph_ir`，回到 `coding`
  - 无可修补项或不支持规则：`reject -> END`
- 不变量：只修补下一跳真正会消费的上游对象；repair 后显式失效对应下游派生产物，避免旧结果覆盖 patch。

## 5. Review / HITL 契约

合法 review stage：

```text
none, clarification_review, architecture_review, repair_review
```

当前正式主链只使用：

```text
none, clarification_review, architecture_review
```

合法 decision：

```text
approve, feedback, clarify, reject
```

合法 status：

```text
none, not_required, pending, interrupted, assumed, answered, applied, rejected
```

注意：

- `interrupted` 主要由 trace/API 状态归纳或外部投影使用，review 节点常规回写多为 `pending / assumed / answered / applied / rejected / not_required`。
- `review_request` 必须包含 `review_id / stage / question / options / context_summary / created_at`。
- `review_response` 必须包含 `decision / answers / feedback / updated_constraints / review_id`。
- review apply 节点只能回写需求侧允许字段，不能直接改检索、规划、装配、编译、验收产物。
- `enable_hitl_*` 开关只有在 `checkpointer + thread_id` 同时存在时才会由入口置为 true。

## 6. 核心对象字段

| 对象 | 关键字段 |
| --- | --- |
| `requirement_spec` | `schema_version`、`system_type`、`scenario_summary`、`subsystems`、`signals`、`required_pages`、`global_modes`、`ambiguities`、`assumptions`、`acceptance_criteria`、`confidence`、`warnings`；可选 `engineering.points/control_loops/interlocks/communication/naming_convention/missing_required_fields/target_profile/retrieval_hints` |
| `retrieval_bundle` | `atomic_modules`、`subflow_templates`、`system_patterns`、`style_guides`、`metadata`；`metadata` 可含 `llm_rewrite`、`reranker`、`top_*`、`template_query_variants`、`pattern_query_variants` 诊断 |
| `decomposition_result` | `pages`、`subsystem_descriptors`、`shared_signal_registry`、`template_needs`、`planning_order`、`warnings` |
| `architecture_plan` | `goal`、`pages`、`subsystem_slots`、`shared_signal_registry`、`global_constraints`、`naming_strategy`、`layout_strategy`、`pattern_bindings`、`warnings`；`pattern_bindings[*].llm_advisory` 可选 |
| `subsystem_plan_map[*]` | `subsystem_id`、`page_id`、`implementation_mode`、`template_binding`、`template_interface_bindings`、`node_instances`、`edges`、`imported_signals`、`exported_signals`、`unresolved_items`；`template_binding.llm_advisory` 可选 |
| `assembled_graph_ir` | `graph_ir_version`、`goal`、`pages`、`subflow_definitions`、`node_instances`、`edges`、`signal_registry`、`layout_hints`、`unresolved_items`；`subflow_definitions[*].internal_flow_objects` 携带模板 body |
| `compiled_artifact` | `json_text`、`flow_objects`、`id_map`、`layout_map`、`compile_report`；`compile_report` 含 body 与 strict compile 诊断 |
| `verification_report` | `status`、`repair_scope`、`issue_summary`、`issues[*].repair_payload`、`warnings`、`metrics` |
| `route_decision` | `decision`、`repair_scope`、`next_node`、`reason`、`issue_ids`、`retry_exhausted`、`retry_count_for_scope`、`retry_budget_for_scope` |
| `repair_context` | `repair_round`、`repair_scope`、`issue_ids`、`target_ids`、`target_state_keys`、`repair_strategy`、`patch_instructions`、`resume_node` |

## 7. API 投影 DTO 边界

API 不返回完整 `WorkflowState`，只返回投影。

| DTO | 来源 | 关键字段 |
| --- | --- | --- |
| `ThreadOverview` | `build_thread_overview` | `thread_id`、`title`、`latest_attempt_id`、`latest_status`、`latest_current_step`、`latest_review` |
| `AttemptListResponse` | `build_attempt_list_item` | `attempt_id`、`status`、`current_step`、`started_at`、`finished_at`、`verification_status`、`final_route_decision` |
| `AttemptDetail` | `build_attempt_detail` | `status`、`current_step`、`user_query`、`review`、`progress`、`diagnostics`、`trace_files`、`subtasks` |
| `AttemptResult` | `build_attempt_result` | `result.json_text`、`result.compile_report`、`result.verification_report`、`trace_files` |
| `TraceProjection` | `build_trace_projection` | `summary`、`review`、`diagnostics`、`trace_files` |
| `StateHistoryProjection` | `build_state_history_projection` | `created_at`、`next`、`metadata`、瘦身 state |
| `WorkflowHealth` | `WorkflowService.health` | LLM/embedding provider、checkpointer、worker、trace、Chroma readiness |

attempt 状态推断：

- `__interrupt__` 或 `review_status=pending` 且有 review stage：`interrupted`
- `route_decision.decision=reject` 或 `final_output.review_abort`：`rejected`
- `route_decision.decision=accept` 或 `verification_report.status=passed`：`completed`
- 否则使用 repository fallback 状态

前端不得依赖：

- 完整 `WorkflowState`
- 完整节点 IO trace
- trace 文件物理路径格式
- 固定节点数量或百分比进度
- `subtasks` 当前为空这个实现细节

## 8. 已移除 / 不应复活的兼容口径

- 正式 `WorkflowState` 不包含 `retrieval_context`、`execution_plan`、`generated_code`、`execution_result`、`validation_result`、`next_step`。
- `assembled_graph_ir.source_execution_plan` 已退出正式 IR。
- `RetrievalAgent.retrieve()` 仓库级 compat API 已删除；正式调用使用 `retrieve_bundle(...)` / `__call__`。
- `CodingAgent.compile_graph(...)`、`CodingAgent.generate_json(...)` 仓库级 compat API 已删除；正式调用使用 `compile_graph_from_bundle(...)` / `__call__`。
- `utils.retrieval_bundle_utils` 与 `utils.context_formatter` 当前只保留 formal bundle-only helper。
- `SubsystemPlanner` 不再从 compat-only `imports / exports` 自动合成 signal bindings。
- `Send` 只在 POC/合同测试层出现，正式 `workflow.py` / `workflow_trace.py` 不应出现 `Send(`。

## 9. 一句话主线

```text
自然语言需求
-> analysis_result / requirement_spec
-> 条件式 clarification review
-> retrieval_bundle
-> architecture_plan / decomposition_result
-> architecture review
-> subsystem_plan_map
-> assembled_graph_ir
-> compiled_artifact
-> verification_report
-> route_decision
-> 必要时 deterministic repair 并回跳
```

## 10. 子流程 body 与 strict compile

- `global_assembly` 会从命中的 `subflow_templates` 透传 `internal_flow_objects` 到 `SubflowDefinitionIR`。
- `coding` 编译 subflow 定义时会展开 body 节点；同一真实 definition 只展开一次，多个 `subflow:<id>` 实例复用同一 definition。
- body 编译会重映射内部节点 `id`，将 body 节点 `z` 指向真实 subflow id，并重写 body 内部 `wires` 与 subflow `in/out.wires`。
- `compile_report` 字段包括 `body_node_count`、`dropped_node_count`、`missing_template_count`、`unresolved_placeholder_count`、`body_expansion_errors`。
- `VerifierAgent` 会将节点跳过、模板缺失、残留占位符、body 展开错误、subflow body 缺失和实例定义缺失升级为 compile scope error。

## 11. 可选 LLM 增强边界

- 已落地且默认关闭：`analysis` 工程需求编译、`retrieval` 查询改写、`retrieval` Cross-Encoder rerank、`architecture_planning` 架构 advisor、`subsystem_planning` 接口适配。
- 总开关：`LLM_ENHANCEMENT_ENABLED`；分节点开关：`ANALYSIS_USE_ENGINEERING_COMPILER`、`RETRIEVAL_USE_LLM_REWRITE`、`RETRIEVAL_USE_CROSS_ENCODER_RERANK`、`ARCHITECTURE_USE_LLM_ADVISOR`、`SUBSYSTEM_USE_LLM_ADAPTER`。
- LLM 输出只能作为结构化 patch/advisory，经本地规则过滤后写入正式对象；不能直接写 `compiled_artifact`、`flow_objects` 或最终 JSON。
- 默认关闭时，现有 deterministic 行为和正式节点输出结构保持不变；启用后也必须保留 fallback 诊断。
- DeepSeek 真实调用使用 JSON mode 回退路径；诊断中的 `structured_output_skipped_for_deepseek_json_mode` 表示跳过不兼容 tool-calling，不等同于节点 fallback。
- 真实 smoke 记录：`outputs/llm_enhancement_smoke_v2/comparison.json` 验证 Analysis 工程补丁、Retrieval rewrite、SiliconFlow reranker、Architecture/Subsystem advisory 均可真实调用，最终 `verification.status=passed`、`route_decision=accept`；当前仍有非阻断 quote 引用 warning。
