# 工作流节点输入输出说明

> 最后核对时间：2026-04-23
> 核对范围：`workflow.py`、`workflow_trace.py`、`agents/*.py`、`utils/phase3_*.py`、`utils/signal_semantics.py`、`utils/retrieval_bundle_utils.py`、`utils/graph_ir.py`、`utils/workflow_runtime.py`、`utils/trace_index.py`、`utils/review_index.py`、`app/*.py`、`app/api/*.py`、`app/repositories/*.py`、`app/services/*.py`、`scripts/run_phase5_query_suite.py`、`scripts/run_phase6_real_query_suite.py`、`scripts/run_phase6_retrieval_eval.py`、`scripts/run_phase8_hitl_smoke.py`、`tests/test_workflow_api.py`、`tests/test_phase3_*.py`、`tests/test_phase4_*.py`、`tests/test_phase5_workflow_query_suite.py`、`tests/test_phase6_*.py`、`tests/test_phase7_*.py`、`tests/test_phase8_*.py`。
> 若与旧设计稿、历史阶段文档或早期实现不一致，以当前代码为准。

## 1. 当前正式主链

当前正式主链已经接入条件式澄清、架构评审和 Repair 闭环；`Send` 并行与 `repair_review` 仍未入主链。

```text
analysis
  -> ambiguity_router
ambiguity_router
  -> clarification_review | retrieval
clarification_review
  -> clarification_review | clarification_apply | retrieval
clarification_apply
  -> retrieval | END(reject)
retrieval
  -> architecture_planning
architecture_planning
  -> architecture_review
architecture_review
  -> architecture_review | architecture_feedback_apply | subsystem_planning
architecture_feedback_apply
  -> architecture_planning(feedback/clarify) | subsystem_planning(approve) | END(reject)
subsystem_planning
  -> global_assembly
  -> coding
  -> verification
  -> repair_router
repair_router
  -> END(accept/reject) | repair_agent
repair_agent
  -> subsystem_planning | global_assembly | coding | END
```

对应常量：

```python
PHASE3_NODE_ORDER = [
    "analysis",
    "retrieval",
    "architecture_planning",
    "subsystem_planning",
    "global_assembly",
    "coding",
    "verification",
]

PHASE8_REVIEW_NODE_ORDER = [
    "ambiguity_router",
    "clarification_review",
    "clarification_apply",
    "architecture_review",
    "architecture_feedback_apply",
]

REPAIR_ROUTER_NODE = "repair_router"
REPAIR_AGENT_NODE = "repair_agent"
PHASE4_RECURSION_LIMIT = 40
```

## 2. 入口与运行时

### 2.1 正式工作流入口

- `workflow.create_workflow(checkpointer: Any | None = None)`：创建主图，不运行
- `workflow.run_workflow(user_query: str, *, thread_id: str | None = None, checkpointer: Any | None = None, runtime_metadata: dict | None = None, enable_hitl_clarification: bool = False, enable_hitl_architecture_review: bool = False)`：运行正式主链
- `workflow_trace.run_workflow(...)`：运行同一条主链，并额外记录 trace

### 2.2 配套评测与冒烟脚本

- `scripts/run_phase5_query_suite.py`：固定 7 条 deterministic workflow case
- `scripts/run_phase6_real_query_suite.py`：固定 case 源 [AHU程序/phase6_real_query_cases.json](AHU程序/phase6_real_query_cases.json) 的真实 workflow suite；当前 case 文件共 12 条 case
- `scripts/run_phase6_retrieval_eval.py`：基于同一 case 源的检索层评估
- `scripts/run_phase8_hitl_smoke.py`：最小 HITL smoke CLI；内部构建的是独立 smoke graph，用于 pause/resume、thread continuity 与 state history 冒烟验证，不是正式主链入口

两条运行入口都会带：

```python
{"recursion_limit": 40}
```

### 2.3 `thread_id` / `checkpointer` / HITL 规则

- 未传 `checkpointer` 时，`thread_id` 只进入 `metadata` 与 trace 索引；不会建立 LangGraph persistence
- 传入 `checkpointer` 时，必须同时提供非空 `thread_id`；否则会抛 `ValueError`
- `enable_hitl_clarification=True` 或 `enable_hitl_architecture_review=True` 只有在同时满足 `checkpointer + thread_id` 时才会真正触发 `interrupt`
- 不满足上述条件时，review 节点仍会运行，但走 `assumed` 旁路，不会暂停

### 2.4 初始状态

`workflow.build_initial_state(user_query)` 会创建统一初始状态：

```python
{
    "user_query": str,
    "analysis_result": {},
    "requirement_spec": {},
    "retrieval_bundle": {},
    "decomposition_result": {},
    "architecture_plan": {},
    "subsystem_plan_map": {},
    "assembled_graph_ir": {},
    "compiled_artifact": {},
    "verification_report": {},
    "final_output": {},
    "debug_history": [],
    "repair_context": {},
    "repair_history": [],
    "route_decision": {},
    "retry_budget": {"planning": 2, "assembly": 2, "compile": 2},
    "retry_counts_by_scope": {"planning": 0, "assembly": 0, "compile": 0},
    "retry_count": 0,
    "hitl_stage": "none",
    "review_request": {
        "review_id": "",
        "stage": "none",
        "question": "",
        "options": [],
        "context_summary": "",
        "created_at": "",
    },
    "review_response": {
        "decision": "",
        "answers": [],
        "feedback": "",
        "updated_constraints": {},
        "review_id": "",
    },
    "review_history": [],
    "review_enabled": False,
    "review_required": False,
    "review_status": "none",
    "review_id": "",
    "clarification_round": 0,
    "architecture_feedback_patch": {},
    "enable_hitl_clarification": False,
    "enable_hitl_architecture_review": False,
    "current_step": "start",
}
```

### 2.5 Workflow API V1 入口

API 服务层是正式主链外的一层投影与运行编排，不新增 `WorkflowState` 字段，也不改变 LangGraph 拓扑。

服务入口：

- `app.main:create_app(service: WorkflowService | None = None)`
- `app.main:app`

核心 API：

- `POST /api/workflow/runs`
- `GET /api/workflow/threads/{thread_id}`
- `GET /api/workflow/threads/{thread_id}/attempts`
- `GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}`
- `GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/result`
- `GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/trace`
- `GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/state-history`
- `POST /api/workflow/threads/{thread_id}/resume`
- `GET /api/workflow/health`

API 主键规则：

- `thread_id` 是 LangGraph checkpoint 恢复指针
- `attempt_id` 是 API / trace / 前端展示层的一次运行主键
- `review_id` 是当前 review 卡片主键
- 同一 `thread_id` 同一时刻只允许一个 active attempt
- active 状态为 `queued / running / interrupted`
- 终态 `completed / rejected / failed` 不允许再次 `resume`
- `POST /resume` 必须同时匹配当前 active interrupted attempt 的 `attempt_id` 与 `review_id`

API 对外状态机：

```text
queued -> running
running -> interrupted
running -> completed
running -> rejected
running -> failed
interrupted -> running
```

统一错误结构：

```json
{
  "error": {
    "code": "conflict",
    "message": "当前 thread 已存在 active attempt，请等待完成或恢复当前 review。",
    "details": {}
  }
}
```

## 3. 顶层状态字段

### 3.1 主链主字段

| 字段 | 生产节点 | 主要消费者 | 说明 |
| --- | --- | --- | --- |
| `user_query` | 入口 | `analysis`、`retrieval` | 用户自然语言需求 |
| `analysis_result` | `analysis` | `ambiguity_router`、`retrieval` | Analysis 原始结果；包含 `clarification_signals` |
| `requirement_spec` | `analysis` / review apply | `architecture_planning`、`subsystem_planning`、`global_assembly`、`verification` | 正式结构化需求入口 |
| `retrieval_bundle` | `retrieval` | `architecture_planning`、`subsystem_planning`、`global_assembly`、`coding` | 正式检索主输出 |
| `decomposition_result` | `architecture_planning` | `architecture_review`、`subsystem_planning` | 系统拆解结果 |
| `architecture_plan` | `architecture_planning` | `architecture_review`、`subsystem_planning`、`global_assembly`、`verification` | 系统级骨架 |
| `subsystem_plan_map` | `subsystem_planning` | `global_assembly`、`verification` | 各子系统局部 IR |
| `assembled_graph_ir` | `global_assembly` | `coding`、`verification` | 当前真实编译输入 |
| `compiled_artifact` | `coding` | `verification` | 当前真实编译产物 |
| `verification_report` | `verification` | `repair_router`、外部调用方 | 当前真实验收结果 |
| `repair_context` | `repair_agent` | repair 回跳、trace | 最近一轮 repair 的上下文 |
| `repair_history` | `repair_agent` | trace、审计 | repair / reject 结构化历史 |
| `route_decision` | `repair_router` / `repair_agent` | repair 分支、trace | 稳定路由决策 |
| `retry_budget` | 初始状态 / `repair_router` / `repair_agent` | `repair_router`、`repair_agent` | 各 scope 预算上限；`repair_router` 会先规范化再回写 |
| `retry_counts_by_scope` | 初始状态 / `repair_router` / `repair_agent` | `repair_router`、trace | 各 scope 已消耗计数；`repair_router` 会先规范化再回写 |
| `retry_count` | 初始状态 / `repair_router` / `repair_agent` | trace | 聚合影子值，等于 `sum(retry_counts_by_scope.values())`；主路由不直接依赖它 |
| `final_output` | `verification` / review apply / trace | 外部调用方 | 最终对外摘要；trace 文件总会落盘，但只有 `workflow_trace.run_workflow()` 成功返回结果对象时才会追加 `workflow_trace` |

### 3.2 Review / HITL 字段

| 字段 | 生产节点 | 说明 |
| --- | --- | --- |
| `hitl_stage` | `ambiguity_router` / review 节点 | 当前 review 所在阶段，典型值为 `none`、`clarification_review`、`architecture_review` |
| `review_request` | `clarification_review` / `architecture_review` | 冻结后的 review 请求 |
| `review_response` | review 节点 / 恢复命令 | review 回复载荷 |
| `review_history` | review 与 apply 节点 | review 生命周期审计记录 |
| `review_enabled` | review 节点 | 当前 review 是否允许真正 `interrupt` |
| `review_required` | `ambiguity_router` / `architecture_review` | 是否需要 review |
| `review_status` | router / review / apply | 主链常规回写值为 `none / pending / assumed / answered / applied / rejected / not_required`；`interrupted` 主要由 trace 摘要归纳 |
| `review_id` | review 节点 | 当前 review 的稳定 ID |
| `clarification_round` | `clarification_apply` | 澄清回写轮次 |
| `architecture_feedback_patch` | `architecture_feedback_apply` | 架构反馈回写到需求侧的补丁摘要 |
| `enable_hitl_clarification` | 入口 | clarification review 的 HITL 开关 |
| `enable_hitl_architecture_review` | 入口 | architecture review 的 HITL 开关 |

### 3.3 Compat / Legacy 对象

| 对象 | 来源 | 当前定位 |
| --- | --- | --- |
| `build_bundle_doc_map(...)` / `build_bundle_allowed_module_types(...)` | `utils.retrieval_bundle_utils` | formal bundle-only helper，正式节点与正式 planner/compiler helper 使用 |
| `GlobalAssembler.assemble(..., retrieval_bundle)` | `agents.global_assembler` | 正式 assembler 入口，已收口为 bundle-only |

补充：

- `retrieval_context`、`execution_plan`、`generated_code` 都已从正式 `WorkflowState` 移出；其中 `execution_plan` 对应的 runtime compat helper 也已删除
- `utils.retrieval_bundle_utils` 当前只保留 formal bundle-only helper；仓内已不再保留 runtime legacy helper
- `utils/context_formatter.py` 当前只保留 formal bundle-only formatter
- 仓库级 `RetrievalAgent.retrieve()`、`CodingAgent.compile_graph(...)`、`CodingAgent.generate_json(...)` 已删除；formal 调用统一使用 `retrieve_bundle(...)`、`compile_graph_from_bundle(...)` 与 `compiled_artifact`
- `SubsystemPlanner` 已不再从 `imports / exports` 合成绑定；shared-signal registry 相关正式字段统一为 `candidate_exporters`

### 3.4 预留 / 历史字段

`debug_history` 当前不参与主链决策；`execution_result`、`validation_result`、`next_step` 已从正式 `WorkflowState` 移出。

## 4. 节点间主线

```text
user_query
  -> analysis_result + requirement_spec
  -> review_required / review_request / review_response / clarification_round / architecture_feedback_patch
  -> retrieval_bundle
  -> decomposition_result + architecture_plan
  -> subsystem_plan_map
  -> assembled_graph_ir
  -> compiled_artifact
  -> verification_report + final_output
  -> route_decision
  -> repair_context + repair_history
```

主链传播逻辑：

1. `analysis` 产出 `analysis_result`，并投影出 `requirement_spec`
2. `ambiguity_router` 基于 `clarification_signals` 决定是否进入前置澄清
3. `clarification_review / clarification_apply` 只允许回写需求侧输入，不直接修改检索和规划产物
4. `retrieval` 基于 `user_query + analysis_result` 产出 `retrieval_bundle`
5. `architecture_planning` 基于 `requirement_spec + retrieval_bundle` 产出系统级骨架
6. `architecture_review / architecture_feedback_apply` 只允许把反馈回写到需求侧，再决定是否重跑 `architecture_planning`
7. `subsystem_planning` 生成各子系统局部 IR
8. `global_assembly` 组装成全局 `assembled_graph_ir`
9. `coding` 仅消费 `assembled_graph_ir` 与检索文档，确定性编译
10. `verification` 同时检查规划层、装配层和编译层
11. `repair_router` 负责 scope 路由与预算裁决
12. `repair_agent` 只修补下一跳真正会消费的上游对象，然后回跳

## 5. 节点详解

### 5.1 `analysis`

输入：

```python
{"user_query": str}
```

输出：

```python
{
    "analysis_result": {
        "retrieval_plan": {...},
        "scenario_analysis": {...},
        "metadata": {...},
        "clarification_signals": {
            "should_clarify": bool,
            "signals": list[dict],
            "signal_count": int,
        },
    },
    "requirement_spec": {...},
    "current_step": "analysis_completed",
}
```

说明：

- `analyze()` 优先走 LLM structured output，失败后回退到普通 LLM + JSON 解析，再失败走本地 fallback
- `build_requirement_spec()` 会把 `scenario_analysis` 投影成正式 `requirement_spec`
- `clarification_signals` 是前置澄清分支的真实上游输入

### 5.2 `ambiguity_router`

输入：

```python
{
    "analysis_result": dict,
    "requirement_spec": dict,
}
```

输出：

```python
{
    "analysis_result": dict,   # 当 clarification_signals 缺失时会补写
    "review_required": bool,
    "hitl_stage": "clarification_review" | "none",
    "review_status": "pending" | "not_required",
    "current_step": "ambiguity_routed",
}
```

说明：

- 若 `analysis_result.clarification_signals` 缺失，会先重新推导并回写 `analysis_result`
- 该节点不改检索、规划和装配对象

### 5.3 `clarification_review`

输入：

```python
{
    "analysis_result": dict,
    "requirement_spec": dict,
    "review_required": bool,
    "review_enabled": bool,
    "review_request": dict,
    "review_history": list,
    "review_id": str,
}
```

输出：

```python
{
    "review_request": {
        "review_id": str,
        "stage": "clarification_review",
        "question": str,
        "options": list[dict],
        "context_summary": str,
        "created_at": str,
    },
    "review_response": dict,
    "review_history": list,
    "review_enabled": bool,
    "review_status": "pending" | "assumed" | "answered" | "not_required",
    "review_id": str,
    "hitl_stage": "clarification_review" | "none",
    "current_step": str,
}
```

说明：

- `review_required=False` 时直接旁路，`current_step = clarification_skipped`
- `review_enabled=False` 时会冻结 request，但走 `assumed`
- `review_enabled=True` 时会 `interrupt(request)`，恢复后把结果规范化到 `review_response`
- 该节点不会改 `retrieval_bundle / architecture_plan / subsystem_plan_map`

### 5.4 `clarification_apply`

输入：

```python
{
    "review_response": dict,
    "analysis_result": dict,
    "requirement_spec": dict,
    "clarification_round": int,
}
```

输出：

```python
{
    "analysis_result": dict,
    "requirement_spec": dict,
    "clarification_round": int,
    "review_history": list,
    "review_enabled": False,
    "review_status": "applied" | "rejected",
    "hitl_stage": "none",
    "final_output": dict,   # reject 时会补 review_abort
    "current_step": "clarification_applied",
}
```

说明：

- 只允许回写需求侧输入
- 会把 `answers / feedback` 追加到 `assumptions`
- `decision == "reject"` 时会写 `final_output["review_abort"]`

### 5.5 `retrieval`

输入：

```python
{
    "user_query": str,
    "analysis_result": dict,
}
```

输出：

```python
{
    "retrieval_bundle": {
        "atomic_modules": list[dict],
        "subflow_templates": list[dict],
        "system_patterns": list[dict],
        "style_guides": list[dict],
        "metadata": {
            "query_text": str,
            "query_variants": list[str],
            "intent": str,
            "detected_operations": list[str],
            "selected_case_pattern_id": str,
            "retrieved_atomic_count": int,
            "retrieved_subflow_count": int,
            "retrieved_pattern_count": int,
            "avg_atomic_score": float,
            "rewrite_used": bool,
            "analysis_used": bool,
            "llm_queries": list[str],
            "llm_category_l1": str,
            "analysis_summary": str,
            "analysis_confidence": float,
            "top_atomic_module_types": list[str],
            "top_atomic_scores": list[float],
            "top_subflow_template_ids": list[str],
            "top_subflow_scores": list[float],
            "top_system_pattern_ids": list[str],
            "top_system_pattern_scores": list[float],
            "query_bundle_version": "phase2-v1",
        },
    },
    "current_step": "retrieval_completed",
}
```

说明：

- Retrieval 正式节点当前只回填 `retrieval_bundle`
- `style_guides` 取自命中的首个 `system_pattern.style_guides`

### 5.6 `architecture_planning`

输入：

```python
{
    "requirement_spec": dict,
    "retrieval_bundle": dict,
}
```

输出：

```python
{
    "decomposition_result": {
        "pages": list[dict],
        "subsystem_descriptors": list[{
            "subsystem_id": str,
            "subsystem_type": str,
            "page_id": str,
            "goal": str,
            "implementation_preference": str,
            "interface_bindings": list[dict],
            "imports": list[str],
            "exports": list[str],
            "priority": int,
            "reasoning": str,
        }],
        "shared_signal_registry": list[dict],
        "template_needs": list[dict],
        "planning_order": list[str],
        "warnings": list[str],
    },
    "architecture_plan": {
        "goal": str,
        "pages": list[dict],
        "subsystem_slots": list[{
            "subsystem_id": str,
            "page_id": str,
            "preferred_implementation": str,
            "preferred_template_ids": list[str],
            "score_breakdown": list[dict],
            "selection_reason": str,
            "degrade_reason": str,
            "fallback_mode": str,
            "priority": int,
            "reasoning": str,
        }],
        "shared_signal_registry": list[{
            "signal_name": str,
            "signal_key": str,
            "canonical_signal_key": str,
            "semantic_role": str,
            "owner_subsystem_id": str,
            "allowed_external": bool,
            "required_exporter_count": int,
            "candidate_exporters": list[str],
            "resolution_status": str,
            "resolution_evidence": list[str],
            "consumers": list[str],
            "source_reason": str,
        }],
        "global_constraints": list[dict],
        "naming_strategy": dict,
        "layout_strategy": dict,
        "pattern_bindings": list[dict],
        "warnings": list[str],
    },
    "current_step": "architecture_planned",
}
```

说明：

- 当 `requirement_spec.subsystems` 为空时会退化成单一 `generic_control`
- 共享信号 registry 只针对真正的 `shared_signal`；`external_*` 不进入 owner/exporter 推断链
- formal 契约当前应优先依赖 `subsystem_descriptors[*].interface_bindings` 与 `shared_signal_registry[*].candidate_exporters`
- `imports / exports` 当前仍作为描述符镜像字段保留，但正式消费方不再依赖它们推导接口绑定
- `exporter_candidates` 已从生产代码移除；formal 契约统一使用 `candidate_exporters`

### 5.7 `architecture_review`

输入：

```python
{
    "architecture_plan": dict,
    "decomposition_result": dict,
    "review_request": dict,
    "review_history": list,
    "review_id": str,
}
```

输出：

```python
{
    "review_required": bool,
    "review_request": {
        "review_id": str,
        "stage": "architecture_review",
        "question": str,
        "options": list[dict],
        "context_summary": str,
        "created_at": str,
    },
    "review_response": dict,
    "review_history": list,
    "review_enabled": bool,
    "review_status": "pending" | "assumed" | "answered" | "not_required",
    "review_id": str,
    "hitl_stage": "architecture_review" | "none",
    "current_step": str,
}
```

说明：

- 若 `architecture_plan` 和 `decomposition_result` 都为空，则 `not_required`
- 命中 HITL 时会暂停；否则走 `assumed`
- 只展示骨架摘要并收集反馈，不直接改结构产物

### 5.8 `architecture_feedback_apply`

输入：

```python
{
    "review_response": dict,
    "review_request": dict,
    "requirement_spec": dict,
}
```

输出：

```python
{
    # approve:
    "review_enabled": False,
    "review_status": "applied",
    "hitl_stage": "none",
    "current_step": "architecture_feedback_approved",

    # feedback / clarify:
    "requirement_spec": dict,
    "architecture_feedback_patch": {
        "review_id": str,
        "decision": str,
        "answers": list,
        "feedback": str,
        "updated_constraints": dict,
    },
    "review_request": {"stage": "none", ...},
    "review_response": {"decision": "", ...},
    "review_id": "",
    "review_enabled": False,
    "review_status": "applied",
    "hitl_stage": "none",
    "current_step": "architecture_feedback_applied",

    # reject:
    "final_output": dict,   # 会补 review_abort
    "review_enabled": False,
    "review_status": "rejected",
    "hitl_stage": "none",
    "current_step": "architecture_feedback_rejected",
}
```

说明：

- `approve`：不改 `requirement_spec`，直接进入 `subsystem_planning`
- `feedback / clarify`：只回写需求侧，再回到 `architecture_planning`
- `reject`：写 `final_output["review_abort"]` 并终止

### 5.9 `subsystem_planning`

输入：

```python
{
    "requirement_spec": dict,
    "decomposition_result": dict,
    "architecture_plan": dict,
    "retrieval_bundle": dict,
}
```

输出：

```python
{
    "subsystem_plan_map": {
        "<subsystem_id>": {
            "subsystem_id": str,
            "page_id": str,
            "implementation_mode": "reuse_template" | "atomic_assembly",
            "template_binding": {
                "template_id": str,
                "selection_reason": str,
                "score_breakdown": list[dict],
                "reasoning": str,
                # 以下字段按路径可选：
                "degraded": bool,
                "degrade_reason": str,
                "degrade_from": str,
                "degrade_issue_types": list[str],
            },
            "selection_reason": str,
            "degrade_reason": str,
            "template_interface_bindings": list[dict],
            "node_instances": list[dict],
            "edges": list[dict],
            "imported_signals": list[dict],
            "exported_signals": list[dict],
            "constraints": list[dict],
            "unresolved_items": list[dict],
            "reasoning": str,
        }
    },
    "current_step": "subsystem_planned",
}
```

说明：

- 当前实现是串行规划，不使用 `Send`
- `imported_signals / exported_signals` 会保留 `binding_kind / allowed_external / canonical_signal_key / candidate_exporters / resolution_status / resolution_evidence`
- `SubsystemPlanner` 现在只消费上游显式提供的 `interface_bindings`；若仓外调用方仍只提供 compat-only 描述，将不再自动合成绑定

### 5.10 `global_assembly`

输入：

```python
{
    "requirement_spec": dict,
    "architecture_plan": dict,
    "subsystem_plan_map": dict,
    "retrieval_bundle": dict,
}
```

输出：

```python
{
    "assembled_graph_ir": {
        "graph_ir_version": "2.0",
        "goal": str,
        "pages": list[dict],
        "subflow_definitions": list[dict],
        "node_instances": list[dict],
        "edges": list[dict],
        "signal_registry": list[dict],
        "layout_hints": dict,
        "unresolved_items": list[dict],
    },
    "current_step": "global_assembly_completed",
}
```

说明：

- 当前正式节点只读取 `retrieval_bundle`
- `GlobalAssembler.assemble()` 已收口为 `retrieval_bundle` 正式签名
- 当前不再写回顶层 `execution_plan`
- `GlobalAssembler` 已不再继承 legacy `AssemblyAgent`，共享装配 helper 由 `agents/assembly_shared.py` 提供，且该 shared 层已不再保留 `_build_compat_doc_map(...)`
- `requirement_spec` 会被用于 external 信号判定和目标回退

`assembled_graph_ir.unresolved_items` 当前至少包括两类结构化问题：

1. planning unresolved item

```python
{
    "type": str,
    "scope": "planning",
    "severity": "error" | "warning",
    "signal_name": str,
    "signal_key": str,
    "canonical_signal_key": str,
    "binding_kind": str,
    "allowed_external": bool,
    "owner_subsystem_id": str,
    "candidate_exporters": list[str],
    "consumer_subsystem_ids": list[str],
    "resolution_status": str,
    "resolution_hint": str,
    "resolution_evidence": list[str],
    "message": str,
    "suggested_fix": str,
}
```

2. assembly unresolved item

```python
{
    "type": str,
    "scope": "assembly",
    "severity": "error" | "warning",
    "subsystem_id": str,
    "edge_locator": {
        "subsystem_id": str,
        "edge_id": str,
        "edge_ids": list[str],
        "from_node": str,
        "to_node": str,
    },
    "edge_ids": list[str],
    "from_node": str,
    "to_node": str,
    "reason": str,
    "message": str,
    "suggested_fix": str,
}
```

### 5.11 `coding`

输入：

```python
{
    "assembled_graph_ir": dict,
    "retrieval_bundle": dict,
}
```

输出：

```python
{
    "compiled_artifact": {
        "json_text": str,
        "flow_objects": list[dict],
        "id_map": dict[str, str],
        "layout_map": dict[str, {"x": int, "y": int}],
        "compile_report": {
            "node_count": int,
            "subflow_count": int,
            "page_count": int,
            "warnings": list[str],
        },
    },
    "current_step": "coding_completed",
}
```

说明：

- 真实产物是 `compiled_artifact`
- 仓库级 `CodingAgent.generate_json()` 已删除；需要 JSON 文本时应读取 `compiled_artifact["json_text"]`
- 正式顶层状态不再回填 `generated_code`

### 5.12 `verification`

输入：

```python
{
    "assembled_graph_ir": dict,
    "compiled_artifact": dict,
    "requirement_spec": dict,
    "architecture_plan": dict,
    "subsystem_plan_map": dict,
}
```

输出：

```python
{
    "verification_report": {
        "status": "passed" | "retryable_error",
        "repair_scope": "none" | "planning" | "assembly" | "compile",
        "issue_summary": str,
        "issues": list[{
            "issue_id": str,
            "severity": str,
            "scope": str,
            "target_id": str,
            "rule_id": str,
            "message": str,
            "suggested_fix": str,
            "repair_payload": dict,
        }],
        "warnings": list[str],
        "metrics": {
            "missing_required_inputs": int,
            "isolated_nodes": int,
            "invalid_port_refs": int,
        },
    },
    "final_output": {
        "json_text": str,
        "compile_report": dict,
        "verification_report": dict,
    },
    "current_step": "verification_completed",
}
```

说明：

- `VerifierAgent` 会同时检查 planning、assembly、compile 三层
- `repair_payload` 是 Repair 的正式输入，不是展示字段

### 5.13 `repair_router`

输入：

```python
{
    "verification_report": dict,
    "retry_budget": {"planning": int, "assembly": int, "compile": int},
    "retry_counts_by_scope": {"planning": int, "assembly": int, "compile": int},
}
```

说明：

- 该节点会先规范化并回写 `retry_budget`、`retry_counts_by_scope`、`retry_count`
- 仅在 `decision != "accept"` 时写入 `current_step = "repair_router_completed"`；`accept` 分支保留上一步 `current_step`

输出：

```python
{
    "route_decision": {
        "decision": "accept" | "planning_repair" | "assembly_repair" | "compile_repair" | "reject",
        "repair_scope": str,
        "next_node": "END" | "repair_agent",
        "reason": str,
        "issue_ids": list[str],
        "retry_exhausted": bool,
        "retry_count_for_scope": int,
        "retry_budget_for_scope": int,
    },
    "retry_budget": dict[str, int],
    "retry_counts_by_scope": dict[str, int],
    "retry_count": int,
    "current_step": "repair_router_completed",   # 仅 decision != accept 时写入
}
```

### 5.14 `repair_agent`

输入：

```python
{
    "route_decision": dict,
    "verification_report": dict,
    "retry_budget": dict,
    "retry_counts_by_scope": dict,
    "architecture_plan": dict,
    "decomposition_result": dict,
    "subsystem_plan_map": dict,
    "assembled_graph_ir": dict,
    "compiled_artifact": dict,
}
```

输出：

成功修补：

```python
{
    "repair_context": {
        "repair_round": int,
        "repair_scope": str,
        "issue_ids": list[str],
        "target_ids": list[str],
        "target_state_keys": list[str],
        "repair_strategy": str,
        "patch_instructions": list[str],
        "resume_node": "subsystem_planning" | "global_assembly" | "coding",
    },
    "repair_history": list[dict],
    "route_decision": {
        "decision": "*_repair",
        "repair_scope": str,
        "next_node": "subsystem_planning" | "global_assembly" | "coding",
        "reason": "repair_patch_applied",
        "issue_ids": list[str],
        "retry_exhausted": False,
        "retry_count_for_scope": int,
        "retry_budget_for_scope": int,
    },
    "retry_counts_by_scope": dict[str, int],
    "retry_count": int,
    "current_step": "repair_completed",
}
```

拒绝：

```python
{
    "repair_context": {..., "repair_strategy": "reject", "resume_node": ""},
    "repair_history": [..., {"result": "rejected", "next_node": "END"}],
    "route_decision": {
        "decision": "reject",
        "repair_scope": str,
        "next_node": "END",
        "reason": str,
        "issue_ids": list[str],
    },
    "current_step": "repair_rejected",
}
```

说明：

- 当前支持的自动修复规则仍是白名单子集
- `prepare_repair_patch()` 与 `apply_repair_patch()` 已分离
- repair 后会按 scope 显式失效下游派生产物，避免旧结果覆盖 patch

## 6. 核心对象关系

### 6.1 `requirement_spec` -> `architecture_plan`

解决“系统骨架长什么样”，而不是节点级连线。

### 6.2 `architecture_plan` -> `subsystem_plan_map`

解决“每个子系统内部怎么落图”。

### 6.3 `subsystem_plan_map` -> `assembled_graph_ir`

解决“把多个子系统拼成整个项目图”。

### 6.4 `assembled_graph_ir` -> `compiled_artifact`

解决“把强结构 IR 编译成平台 JSON”。

## 7. Phase 脚本与正式产物

### 7.1 `phase6_real_query_suite`

suite 级摘要当前至少包括：

- `generated_at` / `schema_version` / `case_owner` / `case_file`
- `case_count` / `passed_count` / `failed_count`
- `repair_then_passed_count` / `rejected_count` / `unexpected_exception_count`
- `drift_case_count` / `rebaseline_candidate_count`
- `failure_bucket_counts`
- `expectation_diagnosis_counts` / `expectation_diagnosis_case_ids`
- `failed_case_ids` / `drift_case_ids` / `rebaseline_candidate_case_ids`
- `golden_case_count` / `golden_case_pass_rate` / `golden_threshold_met`
- `run_dir` / `summary_json` / `summary_md`
- `all_passed` / `retention`

每条 case 当前至少包括：

- `verification_status`
- `repair_scope`
- `route_decision`
- `failure_bucket`
- `actual_end_state`
- `selected_case_pattern_id`
- `retrieved_*_count`
- `top_subflow_template_ids`
- `top_system_pattern_ids`
- `subsystem_ids`
- `unresolved_item_types`
- `repair_round_count`
- `trace_dir` / `trace_summary_json` / `trace_summary_md` / `final_state_json`
- `repair_reject_category`
- `verification_issue_summary`
- `error_type` / `error_message`

### 7.2 `phase6_retrieval_eval`

suite 级摘要当前至少包括：

- `generated_at` / `schema_version` / `case_owner` / `case_file`
- `case_count` / `meets_expectation_count`
- `diagnosis_counts` / `diagnosis_case_ids`
- `template_asset_gap_case_ids` / `pattern_asset_gap_case_ids`
- `golden_case_count`
- `golden_template_hit_rate_top5`
- `golden_pattern_hit_rate_top3`
- `ready_for_c` / `ready_for_c_reason` / `ready_for_c_blockers`
- `preferred_next_step`
- `run_dir` / `summary_json` / `summary_md`
- `all_passed` / `retention`

每条 case 当前至少包括：

- `target_template_hit_top1/top3/top5`
- `target_pattern_hit_top1/top3`
- `obvious_false_positive_in_top5`
- `retrieval_diagnosis`
- `template_assets_exist` / `pattern_assets_exist`
- `missing_template_roles` / `missing_pattern_ids`
- `default_top_*_ids`
- `default_top_subflow_template_roles`
- `default_top_*_scores`
- `variant_top_*_ids`
- `variant_query_variants`
- `variant_improved_*`
- `meets_expectation`
- `asset_gap_root_cause`
- `asset_gap_source_flow_paths` / `asset_gap_source_pattern_page_keys`
- `missing_template_role_diagnostics`
- `error_type` / `error_message`

### 7.3 `run_phase8_hitl_smoke.py`

这是 stdout-only 的 HITL smoke CLI，不产出 phase 摘要文件。它主要验证：

- `interrupt`
- `Command(resume=...)`
- 同一 `thread_id` 的连续恢复
- `get_state()` / `get_state_history()`
- `architecture_review` 在 feedback 后重规划，再次 approve 的闭环

## 8. `workflow_trace.py` 的附加行为

`workflow_trace.py` 与正式主链共用同一套拓扑和节点实现，只额外做：

1. 包装每个节点，记录输入、输出、耗时、变更字段
2. 落盘 `workflow_node_io_record.json`、`workflow_node_io_record.md`、`final_state.json`
3. 在成功返回结果对象时回写 `final_output["workflow_trace"]`

当前回写结构是：

```python
final_output["workflow_trace"] = {
    "thread_id": str,
    "attempt_id": str,
    "trace_dir": str,
    "summary_json": str,
    "summary_md": str,
    "final_state_json": str,
    "review_records_json": str,        # 存在 review_history 时为非空路径
    "approval_record_json": str,       # 存在 review_history 时为非空路径
    "thread_index_json": str,          # 仅 thread_id 非空时
    "attempt_index_json": str,         # 仅 thread_id 非空时
    "review_attempt_index_json": str,  # thread_id 非空时键会出现；存在 review_history 时为非空路径
    "review_thread_index_json": str,   # thread_id 非空时键会出现；存在 review_history 时为非空路径
    "review_record_jsons": list[str],  # 仅 thread_id 非空且存在 review_history 时
}
```

索引与 review 文件的生成条件：

- `review_records.json`、`approval_record.json`：只要存在 `review_history`
- `outputs/_trace_index/threads/*.json`、`attempts/*.json`：要求 `thread_id` 非空
- `outputs/_trace_index/review_threads/*.json`、`review_attempts/*.json`、`reviews/*.json`：同时要求 `thread_id` 非空且存在 `review_history`

当前 trace 摘要稳定暴露的诊断字段包括：

- `workflow_status`
- `failed_node`
- `last_successful_node`
- `selected_case_pattern_id`
- `retrieved_*_count`
- `top_subflow_template_ids`
- `top_system_pattern_ids`
- `reuse_template_subsystem_count`
- `atomic_assembly_subsystem_count`
- `unresolved_item_count` / `unresolved_item_types`
- `planning_unresolved_by_type`
- `ambiguous_signal_count`
- `verification_status` / `verification_repair_scope` / `verification_issue_summary`
- `verification_error_count` / `verification_warning_count`
- `verification_metrics`
- `repair_round_count`
- `repair_scopes_seen`
- `final_route_decision`
- `retry_exhausted`
- `retry_counts_by_scope`
- `last_repair_issue_ids`
- `last_repair_actions`
- `reject_reason`
- `repair_reject_category`
- `hitl_stage` / `review_enabled` / `review_required` / `review_status` / `review_id`
- `approval_record_json`
- `failure_bucket`
- `acceptance_summary`

## 8A. API 投影 DTO

API 返回投影 DTO，不直接返回完整 `WorkflowState`。

### Attempt Detail

`GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}` 当前稳定返回：

```json
{
  "thread_id": "wf_...",
  "attempt_id": "...",
  "status": "running | interrupted | completed | rejected | failed | queued",
  "current_step": "architecture_review_prepared",
  "workflow_status": "",
  "user_query": "...",
  "review": {
    "review_id": "...",
    "stage": "clarification_review | architecture_review | none",
    "status": "pending | assumed | answered | applied | rejected | not_required | none",
    "question": "...",
    "options": [],
    "context_summary": "..."
  },
  "progress": {
    "current_step_label": "...",
    "last_successful_node": "",
    "node_count": 0
  },
  "diagnostics": {
    "verification_status": "",
    "verification_issue_summary": "",
    "repair_round_count": 0,
    "retry_counts_by_scope": {},
    "final_route_decision": ""
  },
  "trace_files": {},
  "subtasks": []
}
```

### Attempt Result

`GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/result` 当前稳定返回：

```json
{
  "thread_id": "wf_...",
  "attempt_id": "...",
  "status": "completed",
  "result": {
    "json_text": "{...}",
    "compile_report": {},
    "verification_report": {}
  },
  "trace_files": {}
}
```

### Trace Summary

`GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/trace` 返回 trace 摘要投影：

```json
{
  "thread_id": "wf_...",
  "attempt_id": "...",
  "status": "interrupted",
  "current_step": "...",
  "summary": {
    "workflow_status": "interrupted",
    "node_count": 0,
    "last_successful_node": ""
  },
  "review": {},
  "diagnostics": {},
  "trace_files": {}
}
```

### State History

`GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/state-history?limit=10` 仅供调试页使用，返回 `get_state_history()` 的瘦身投影：

```json
{
  "thread_id": "wf_...",
  "attempt_id": "...",
  "items": [
    {
      "created_at": "...",
      "next": [],
      "metadata": {},
      "state": {
        "current_step": "...",
        "review_status": "",
        "review_id": "",
        "hitl_stage": "",
        "verification_status": "",
        "route_decision": ""
      }
    }
  ]
}
```

## 9. 正式字段优先级

```text
正式需求入口: requirement_spec > analysis_result
正式检索入口: retrieval_bundle
正式编译输入: assembled_graph_ir
正式编译产物: compiled_artifact
正式验收结果: verification_report
最终对外摘要: final_output
```

## 10. 当前边界

当前仍未进入正式主链的能力：

- `Send` 并行子系统派发
- reducer fan-in
- `repair_review`
- 完整 `internal_flow_objects` body 编译
- 超出当前 deterministic Repair 白名单的自动修复类型

API 服务层仍未完成的能力：

- 生产级队列 worker
- PostgreSQL 或等价生产 checkpointer
- 持久化数据库元数据仓储
- SSE / WebSocket 事件流
- 多实例部署下的 active attempt 分布式锁

## 11. 一句话总结

当前正式工作流不是“LLM 直接输出 JSON”，而是：

```text
自然语言需求
-> analysis_result / requirement_spec
-> 条件式 clarification review
-> retrieval_bundle
-> architecture_plan / decomposition_result
-> architecture review
-> subsystem_plan_map
-> assembled_graph_ir
-> compiled_artifact
-> verification_report
-> route_decision
-> 必要时 deterministic repair 并回跳
```
