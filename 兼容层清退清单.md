# 兼容层清退清单

基于当前正式主链代码、`工作流总结文档.md`、`工作流节点输入输出说明.md` 的交叉审查结果整理。

本文目标不是一次性“全删”，而是把兼容层按风险和依赖拆开，避免在清退过程中误伤当前正式链路。

## 0. 本轮进展

- 已完成正式主链对 `retrieval_context` 的运行时解耦：
  - `RetrievalAgent.__call__` 不再回填 `retrieval_context`
  - `GlobalAssembler.__call__` 只读取 `retrieval_bundle`
  - `CodingAgent.__call__` 只读取 `retrieval_bundle`
  - `WorkflowState` / `build_initial_state()` 已移除 `retrieval_context`
- `build_legacy_retrieval_context(...)` helper 仍保留，供 legacy 调用方和 legacy 测试使用；当前已迁到 `agents/legacy/retrieval_context_utils.py`
- 本轮继续完成：
  - `utils/retrieval_bundle_utils.py` 已拆分 formal bundle-only helper 与 legacy context-only helper
  - `ArchitecturePlanner`、`SubsystemPlanner`、`GlobalAssembler` 的正式 helper 路径已切到 bundle-only helper
  - `CodingAgent.__call__` 已通过 `compile_graph_from_bundle(...)` 走 bundle-only helper
- 本轮新增完成：
  - `utils/retrieval_bundle_utils.py` 中仓内 `0` 调用的 bundle-or-context dispatcher 已删除：
    - `get_atomic_modules(...)`
    - `get_subflow_templates(...)`
    - `get_system_patterns(...)`
    - `get_style_guides(...)`
    - `build_compilable_doc_map(...)`
    - `build_allowed_module_types(...)`
- 本轮新增完成：
  - `utils/retrieval_bundle_utils.py` 中 legacy-only helper 已迁到 `agents/legacy/retrieval_context_utils.py`
  - `utils/context_formatter.py` 已收口为 formal bundle-only 输入
  - legacy formatter 已迁到 `agents/legacy/context_formatter.py`
  - `agents/assembly_shared.py::_build_compat_doc_map(...)` 已删除，legacy assembly 改为显式依赖 legacy helper
- 本轮继续收口：
  - formal 测试已不再通过 `CodingAgent.compile_graph(...)` 触达兼容入口
  - `tests/test_phase3_global_assembler.py`、`tests/test_phase3_verifier_native_contract.py` 已切到 `compile_graph_from_bundle(...)`
- 本轮继续完成：
  - `RetrievalAgent.retrieve()` 已从仓库级 public API 删除
  - `CodingAgent.compile_graph(...)` / `generate_json(...)` 已从仓库级 public API 删除
  - 仅服务这 3 个 API 的 `agents/legacy/retrieval_compat.py`、`agents/legacy/coding_compat.py` 已同步删除
- 本轮新增完成：
  - `WorkflowState` / `build_initial_state()` 已移除 `execution_plan`、`generated_code`
  - `GlobalAssembler.__call__` 不再写回顶层 `execution_plan`
  - `CodingAgent.__call__` 不再同步顶层 `generated_code`
  - `RepairAgent` 失效清理不再维护这两个顶层 compat 字段
- 本轮继续完成：
  - `VerifierAgent` 已不再依赖 `source_execution_plan` 做正式 planning 验收
  - `AssembledGraphIR.source_execution_plan` 已从正式 IR schema 与 assembler 写回路径中移除
- 本轮继续收口：
  - `build_legacy_execution_plan(...)` 已下沉到 `utils/legacy_execution_plan.py`
  - `utils.phase3_adapters` 已不再 re-export 该 helper；legacy 用途统一收口到 canonical helper
- 本轮继续完成：
  - `WorkflowState` / `build_initial_state()` 已移除 `execution_result`、`validation_result`、`next_step`
- 本轮继续完成：
  - `validation_agent.py`、`debugging_agent.py`、`retrieval_agent_old.py` 的真实实现已迁移到 `agents/legacy/`
  - 原路径仅保留兼容 import wrapper
- 本轮继续完成：
  - `planning_agent.py` 的真实实现已迁移到 `agents/legacy/planning_agent.py`
  - 原路径仅保留兼容 import wrapper，并保留 `LLMManager` / `PlanIR` 等模块级兼容导出
- 本轮继续完成：
  - `assembly_agent.py` 的真实实现已迁移到 `agents/legacy/assembly_agent.py`
  - `GlobalAssembler` 不再继承 legacy `AssemblyAgent`
  - 共享装配 helper 已抽到 `agents/assembly_shared.py`
- 本轮继续完成：
  - `tests/test_phase2_planning_bundle.py`
    的真实测试实现已下沉到 `tests/legacy/`
  - 原路径仅保留兼容入口，继续兼容现有显式回归命令
- 本轮新增完成：
  - 仅覆盖已删除 public compat API 的 legacy 测试入口与真实测试实现已同步删除：
    - `tests/test_phase1_workflow.py`
    - `tests/test_phase2_bundle_consumers.py`
    - `tests/test_phase7_coding_determinism.py`
    - `tests/legacy/phase1_workflow_compat.py`
    - `tests/legacy/phase2_bundle_consumers_compat.py`
    - `tests/legacy/phase2_retrieval_agent_compat.py`
    - `tests/legacy/phase7_coding_determinism_compat.py`
- 本轮继续完成：
  - `tests/test_legacy_execution_plan.py` 的真实测试实现已下沉到 `tests/legacy/`
  - 原路径仅保留兼容入口，继续兼容现有显式回归命令
- 本轮继续完成：
  - `tests/test_phase2_retrieval_bundle.py` 已拆分为 formal contract + legacy compat 两部分实现
  - 旧路径仅保留兼容入口，继续兼容现有显式回归命令
- 本轮继续完成：
  - `tests/test_phase2_retrieval_agent.py` 已拆分为 formal contract + legacy compat 两部分实现
  - 旧路径仅保留兼容入口，继续兼容现有显式回归命令
- 本轮继续完成：
  - `tests/test_phase6_retrieval_eval_contract.py` 已拆分为 formal contract + legacy compat 两部分实现
  - 旧路径仅保留兼容入口，继续兼容现有显式回归命令
- 本轮继续完成：
  - `tests/test_legacy_agent_imports.py` 的真实测试实现已下沉到 `tests/legacy/`
  - 原路径仅保留兼容入口，继续兼容现有显式回归命令
- 本轮继续完成：
  - `tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py`
    的 formal 测试桩已不再手动注入 `retrieval_context`
  - 当前非 legacy 测试中仅剩对“结果不应包含 `retrieval_context`”的负向断言
- 本轮继续完成：
  - `tests/test_phase3_workflow.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase4_workflow_repair_loop.py`
    以及 `tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py`
    已补充 formal 路径负向断言，明确结果状态中不应回流 `retrieval_context`、`execution_plan`、`generated_code`
- 本轮审查结论：
  - `tests/` 根目录当前未发现新的“纯 compat 且尚未下沉”的真实测试文件
  - 现存带 compat 属性的根目录文件，已是 compat 入口，或属于 formal/contract 测试里的必要负向约束
- 仍保留的 compat 投影：
  - `build_legacy_execution_plan(...)`

## 1. 清退目标

- 让正式主链只依赖 `requirement_spec`、`retrieval_bundle`、`architecture_plan`、`subsystem_plan_map`、`assembled_graph_ir`、`compiled_artifact`、`verification_report` 这组正式契约。
- 将 `retrieval_context`、`execution_plan`、`generated_code` 及其历史调用方，降级为明确的 legacy 资产，最终退出正式运行时。
- 将“旧主链测试”和“正式主链测试”分层，避免 compat 测试继续绑定正式入口设计。

## 2. 当前兼容层盘点

| 兼容对象 | 当前位置 | 当前状态 | 建议分类 |
| --- | --- | --- | --- |
| `retrieval_context` | `agents/legacy/retrieval_context_utils.py`、legacy 模块 / legacy 测试 | 已从正式主链状态与正式节点运行时移除；legacy helper 已迁出 formal utils | 本轮已完成 |
| `execution_plan` | `utils/legacy_execution_plan.py`、legacy 测试 / 文档 | 已从正式顶层状态移除，仅保留 legacy projection builder | 本轮已完成（运行时状态层） |
| `generated_code` | legacy 模块 / legacy 测试 / 文档 | 已从正式顶层状态移除；真实产物统一为 `compiled_artifact["json_text"]` | 本轮已完成（运行时状态层） |
| `execution_result` / `validation_result` | `agents/validation_agent.py`、`agents/debugging_agent.py` | 已从正式主链状态移除，主要服务旧占位实现 | 本轮已完成（运行时状态层） |
| `next_step` | legacy 文档 / 历史说明 | 已从正式主链状态移除，当前主链无消费者 | 本轮已完成（运行时状态层） |
| `utils/retrieval_bundle_utils.py` 双形态 helper | `utils/retrieval_bundle_utils.py` | 已收口为 formal bundle-only helper；legacy retrieval_context helper 已迁到 `agents/legacy/retrieval_context_utils.py` | 本轮已完成（helper 分层 + legacy helper 迁移） |
| `utils/context_formatter.py` `bundle_or_context` 兼容签名 | `utils/context_formatter.py`、`agents/legacy/context_formatter.py` | formal formatter 已收口为 bundle-only；legacy formatter 已迁到 `agents/legacy/context_formatter.py` | 本轮已完成（formatter 分层） |
| `agents/assembly_shared.py::_build_compat_doc_map(...)` | `agents/assembly_shared.py` | compat doc-map helper 已删除；formal shared helper 只保留 bundle-only doc map | 本轮已完成（shared helper 收口） |
| `RetrievalAgent.retrieve()` legacy 视图 | 已删除 | 仓库级 public compat API 已删除；正式主链只保留 `retrieve_bundle()` / `__call__` | 本轮已完成删除 |
| `debug_history` | `workflow.py`、Phase 8 若干测试桩 | 正式主链不决策，但测试桩仍在写入 | 暂时保留，后移除 |
| `retry_count` 聚合影子字段 | `workflow.py`、`agents/repair_router.py`、`agents/repair_agent.py`、Phase 4 测试 | 正式 repair 主链仍会规范化并回写，当前仍有真实状态契约与测试断言 | 暂不清退，仅维持 shadow 聚合语义 |
| `agents/planning_agent.py` | legacy 兼容包装 | 真实实现已迁移到 `agents/legacy/planning_agent.py`；仍有 compat 测试覆盖 | 本轮已完成（目录收口） |
| `agents/assembly_agent.py` | legacy 兼容包装 | 真实实现已迁移到 `agents/legacy/assembly_agent.py`；正式共享 helper 已抽到 `agents/assembly_shared.py` | 本轮已完成（目录收口 + 基类解耦） |
| `agents/coding_agent.py` shared compile surface | `agents/coding_agent.py` | 正式 `__call__` 与 formal 测试已走 `compile_graph_from_bundle(...)`；仓库级 `compile_graph(...)` / `generate_json(...)` compat API 已删除 | 本轮已完成删除 |
| `workflow.py::populate_phase3_workflow(...)` | `workflow.py` | 仓内已无真实调用，仅剩 dead shim | 本轮已删除 |
| `SubsystemPlanner.imports / exports` 回退 | `agents/subsystem_planner.py` | 旧逻辑已移除；正式 planner 只消费 `interface_bindings` | 本轮已完成 Batch 3 联动清退 |
| `exporter_candidates` 别名 | `agents/architecture_planner.py`、`agents/global_assembler.py`、`agents/subsystem_planner.py` | 旧别名已从生产代码移除，registry 统一收口到 `candidate_exporters` | 本轮已完成 Batch 3 联动清退 |
| `RepairAgent` 读取描述符 `imports / exports` | `agents/repair_agent.py` | planning repair 已只依据 `interface_bindings` / `exported_signals` / registry 推断 exporter | 本轮已完成 Batch 3 联动清退 |
| `agents/validation_agent.py` | legacy 兼容包装 | 真实实现已迁移到 `agents/legacy/validation_agent.py` | 本轮已完成（目录收口） |
| `agents/debugging_agent.py` | legacy 兼容包装 | 真实实现已迁移到 `agents/legacy/debugging_agent.py` | 本轮已完成（目录收口） |
| `agents/retrieval_agent_old.py` | legacy 兼容包装 | 真实实现已迁移到 `agents/legacy/retrieval_agent_old.py` | 本轮已完成（目录收口） |

## 3. 建议分组

### 3.1 可立即清出正式主链

这些项不应再继续出现在正式入口 `WorkflowState` 或正式节点状态初始化里：

- `execution_result`
- `validation_result`
- `next_step`

当前状态：

- 已完成

这些字段当前的主要问题不是“会影响逻辑”，而是会持续暗示还有一条旧链在被正式支持。

同时建议把下列模块直接迁移到 `legacy/` 或 `agents/legacy/`：

- `agents/validation_agent.py`
- `agents/debugging_agent.py`
- `agents/retrieval_agent_old.py`

当前状态：

- 已完成；旧路径现为兼容 wrapper

### 3.2 需要分阶段迁移

这部分仍有真实运行时依赖，不能直接删除：

- `retrieval_context`
- `build_legacy_retrieval_context`
- `build_legacy_execution_plan`
- `retry_count`

补充判断：

- `retry_count` 虽然本质上是 `retry_counts_by_scope` 的聚合影子，但当前正式 repair 主链仍会在 `repair_router` / `repair_agent` 中规范化并回写，不能按“纯 legacy 字段”处理。

### 3.3 暂时保留但要收口

- `debug_history`
- `agents/coding_agent.py` shared compile surface

这些项不一定要先删，但必须明确“只服务 legacy 调用方，不再反向塑造正式主链契约”。

补充：

- `agents/planning_agent.py` 已完成目录收口，后续剩余工作主要是继续下沉 compat 测试，而不是继续保留真实实现于根目录
- `agents/assembly_agent.py` 已完成目录收口；后续剩余工作主要是继续把仍直接覆盖 legacy 装配链的 compat 测试下沉到更明确的 legacy 测试区域
- `tests/test_phase2_planning_bundle.py`
  已完成第一步下沉；当前剩余工作主要是继续识别其他仍直接绑定 legacy 资产的测试
- `tests/test_legacy_execution_plan.py` 已完成第一步下沉；`build_legacy_execution_plan(...)` 当前的真实直接运行时消费者仅剩 canonical legacy helper 路径与 legacy 测试
- `tests/test_legacy_agent_imports.py` 已完成第一步下沉；当前只覆盖 wrapper -> legacy re-export 兼容关系，不再占用根目录真实实现位置
- `tests/test_phase2_retrieval_bundle.py` 已完成第一步拆分；正式 `retrieval_bundle` 合同与 legacy `retrieval_context` helper 断言已分离到不同测试模块
- `tests/test_phase2_retrieval_agent.py` 已完成第一步拆分；正式 `retrieve_bundle` / `__call__` 契约与 legacy `retrieve()` 兼容视图断言已分离到不同测试模块
- `tests/test_phase6_retrieval_eval_contract.py` 已完成第一步拆分；正式 Phase 6 retrieval eval 断言与 legacy `build_legacy_retrieval_context(...)` 兼容断言已分离到不同测试模块
- `tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py` 已完成 formal stub 清理；测试桩不再默默提供 `retrieval_context`
- `tests/test_phase3_workflow.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py`
  已补充 formal 负向断言；当前正式测试会显式拒绝 compat 字段回流到结果状态
- `tests/test_phase3_global_assembler.py`、`tests/test_phase3_verifier_native_contract.py`
  已改为显式使用 `compile_graph_from_bundle(...)`；formal 测试不再借道 `compile_graph(...)` compat surface
- `tests/test_phase3_architecture_planner.py` 已补充 formal 合同断言，明确 `subsystem_descriptors[*].interface_bindings` 与 `shared_signal_registry[*].candidate_exporters` 属于正式产物
- `tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase8_architecture_review_contract.py`
  的 formal stub 已切到原生 `interface_bindings` / `candidate_exporters` 形态，不再用 compat-only 描述来喂 repair / review 链路
- `tests/` 根目录本轮复核未发现新的纯 compat 真实测试；下一步重点应转向 formal 测试的历史字段影子，而不是机械搬运文件

### 3.4 已完成的中高风险联动清退

- `SubsystemPlanner` 中由 `imports / exports` 合成 `interface_bindings` 的 compat 回退已删除
- `shared_signal_registry` 上的 `exporter_candidates` 别名兜底已删除
- `RepairAgent` 在 planning repair 中对描述符 `imports / exports` 的回退扫描已删除

清退后，正式 Phase 3/4 主链统一依赖 `interface_bindings`、`candidate_exporters` 与 `exported_signals`；仓外若仍手工构造 compat-only 描述，将不再被正式主链兜底接受。

## 4.1 本轮代码分层判断

- 正式主链仍会走到：
  - `workflow.py` 中的 `retry_budget` / `retry_counts_by_scope` / `retry_count`
  - `RetrievalAgent.__call__` -> `retrieve_bundle(...)`
  - `CodingAgent.__call__` -> `compile_graph_from_bundle(...)`
  - `SubsystemPlanner` / `RepairAgent` 中与 `interface_bindings`、shared-signal repair 相关的正式路径
- 已是 legacy-only 或 compat-only：
  - `build_legacy_retrieval_context(...)`
  - `build_legacy_execution_plan(...)`
- 已删除：
  - `RetrievalAgent.retrieve()`
  - `CodingAgent.generate_json(...)`
  - `CodingAgent.compile_graph(...)`
- 本轮新增确认并完成：
  - `workflow.py::populate_phase3_workflow(...)` 仓内无调用，已按 dead shim 删除
- 当前不能按 legacy-only 处理：
  - `retry_count`，因为正式 repair 主链和 Phase 4 合同测试仍以它作为聚合镜像
  - `debug_history`，因为虽不参与正式决策，但 Phase 8 合同测试仍把它当作 pause/resume 观测字段

## 4.2 建议分批计划

### Batch 1: 低风险继续收口

- 将 formal 测试、formal helper、formal 文档全部显式绑定到 `retrieve_bundle(...)` / `compile_graph_from_bundle(...)`
- 删除仓内 `0` 调用的 bundle-or-context dispatcher，避免正式 utils 继续暴露“猜输入形态”的 helper 叙事
- 将 legacy retrieval helper 与 legacy formatter 迁出 formal utils / shared，避免正式层继续承载 legacy context 输入语义
- 验收重点：
  - 正式测试与主链代码不再借用 compat 入口
  - legacy compat 测试继续覆盖旧入口不回归

当前状态：

- 已完成：
  - `utils/retrieval_bundle_utils.py` 中 6 个 bundle-or-context dispatcher 已删除
  - `utils/retrieval_bundle_utils.py` 中 legacy-only helper 已迁到 `agents/legacy/retrieval_context_utils.py`
  - `utils/context_formatter.py` 已收口为 bundle-only；legacy formatter 已迁到 `agents/legacy/context_formatter.py`
  - `agents/assembly_shared.py::_build_compat_doc_map(...)` 已删除

### Batch 2: 删除仓库级 public compat API

- 删除 `RetrievalAgent.retrieve()`
- 删除 `CodingAgent.compile_graph(...)` / `generate_json(...)`
- 删除仅服务这些 API 的 compat helper 与直连 legacy 测试
- 验收重点：
  - 正式调用点保持不变
  - compat API 的调用者范围被压缩到 legacy 测试和 legacy 入口

当前状态：

- 已完成：
  - `RetrievalAgent.retrieve()`、`CodingAgent.compile_graph(...)`、`CodingAgent.generate_json(...)` 已删除
  - `agents/legacy/retrieval_compat.py`、`agents/legacy/coding_compat.py` 已删除
  - 仓内正式代码、formal 测试与正式脚本当前都不再真实调用这些接口
- 当前仍未完成：
  - Batch 2 中其余 root wrapper 删除
  原因是这部分已超出本轮用户明确授权的 API 删除范围

### Batch 3: 中高风险联动清退

- 在确认 `decomposition_result.subsystem_descriptors[*].interface_bindings` 已稳定原生化后，联动移除：
  - `SubsystemPlanner` 的 `imports / exports` 回退
  - `exporter_candidates` 别名
  - `RepairAgent` 对描述符 `imports / exports` 的回退扫描
- 验收重点：
  - Phase 3 planner/assembler/verifier/repair 契约测试全绿
  - repair loop 仍能稳定给出 `planning_repair` / `assembly_repair` / `compile_repair`

当前状态：

- 已完成前置验证的第一步：
  - `ArchitecturePlanner` formal 合同已显式断言原生 `interface_bindings` / `candidate_exporters`
  - Phase 4 / Phase 8 formal stub 已切到原生字段输入
- 本轮已完成：
  - `SubsystemPlanner` 不再从 `imports / exports` synthesize `interface_bindings`
  - `RepairAgent` 不再扫描描述符 `imports / exports`
  - `ArchitecturePlanner` / `GlobalAssembler` / `SubsystemPlanner` 已不再读写 `exporter_candidates`

## 4. 推荐清退顺序

### Phase A: 先冻结正式契约

目标：先在文档和测试层明确“谁才是正式字段”。

建议动作：

- 在正式契约文档中明确标注：正式节点不得再读取 `retrieval_context`、`execution_plan`、`generated_code` 作为主输入。
- 为正式主链补一组负向合同测试：
  - `GlobalAssembler` 没有 `retrieval_context` 也必须工作。
  - `CodingAgent` 没有 `generated_code` 也必须完成主链。
  - `VerifierAgent` 在没有 `source_execution_plan` 时必须按原生 Phase 3 契约验收。

验收标准：

- 正式测试只围绕 `retrieval_bundle -> assembled_graph_ir -> compiled_artifact -> verification_report` 这一链路断言。

### Phase B: 切断正式节点对 legacy 输入的 fallback

目标：让正式节点只接受正式字段。

建议动作：

- `agents/global_assembler.py`
  - 将 `bundle_or_context = state.get("retrieval_bundle") or state.get("retrieval_context", {})`
    改为只读取 `retrieval_bundle`
- `agents/coding_agent.py`
  - 将 `bundle_or_context = state.get("retrieval_bundle") or state.get("retrieval_context", {})`
    改为只读取 `retrieval_bundle`
- `utils/retrieval_bundle_utils.py`
  - 保留 legacy helper，但正式节点不再走 `bundle_or_context` 模式
- `agents/retrieval_agent.py`
  - 继续保留 `retrieve()` legacy 接口可以，但 `__call__` 不再必须回填 `retrieval_context`

验收标准：

- 去掉状态中的 `retrieval_context` 后，正式工作流与 trace 工作流都能跑通。
- `tests/test_phase3_verifier_native_contract.py` 继续通过。

当前状态：

- 已完成

### Phase C: 将 `execution_plan` 从正式运行时降级为纯 legacy 产物

目标：让 `execution_plan` 不再属于正式工作流状态。

建议动作：

- `workflow.py`
  - 从 `WorkflowState` 和 `build_initial_state()` 中移除 `execution_plan`
- `agents/global_assembler.py`
  - 停止在 `__call__` 中写回 `state["execution_plan"]`
- `agents/verifier_agent.py`
  - 删除 `_compat_planning_issues(...)`
  - 统一只围绕原生 `architecture_plan` / `subsystem_plan_map` / `assembled_graph_ir.unresolved_items` 做 planning 验收

当前状态：

- 已完成本阶段的“正式运行时状态层”目标：
  - `WorkflowState` / `build_initial_state()` 已移除 `execution_plan`
  - `GlobalAssembler.__call__` 已停止写回顶层 `execution_plan`
  - 正式工作流与 trace 工作流的结果对象不再要求出现顶层 `execution_plan`
- 已完成本阶段的“compat 投影验收层”目标：
  - `VerifierAgent` 已不再依赖 `source_execution_plan` 做正式 planning 验收
  - `assembled_graph_ir.source_execution_plan` 已从正式 IR schema 与 assembler 写回中移除
- 已完成本阶段的“compat helper 收口”目标：
  - `utils.phase3_adapters` 不再 re-export `build_legacy_execution_plan(...)`
- 尚未完成：
  - `build_legacy_execution_plan(...)` 仍保留，但已下沉到 legacy 模块且不再接入正式运行时

下一步前置条件：

- compat planner / compat assembly 进一步迁移到 legacy 目录
- 对 `execution_plan` / `build_legacy_execution_plan(...)` 的剩余回归测试继续下沉到 legacy 测试集

阶段完成标准：

- 正式主链、trace、repair 回路都不再出现顶层 `execution_plan`
- Verifier 的 planning 结论最终不再依赖 `source_execution_plan`
- 正式 `assembled_graph_ir` 不再包含 `source_execution_plan`

补充：

- 上述三条现已完成；下一步是处理 `build_legacy_execution_plan(...)` 的最终归宿
- 上述三条现已完成；本轮已把 helper 下沉到显式 legacy 模块，下一步是继续下沉相关回归测试与旧 agent 目录

### Phase D: 清退 `generated_code`

目标：对外和内部都统一只看 `compiled_artifact["json_text"]`

建议动作：

- `workflow.py`
  - 从 `WorkflowState` 和 `build_initial_state()` 中移除 `generated_code`
  - CLI 展示改为直接读取 `compiled_artifact["json_text"]`
- `agents/coding_agent.py`
  - 停止同步 `state["generated_code"]`
  - 如需保留旧接口，仅保留 `generate_json(...)` 作为 legacy helper
- `agents/repair_agent.py`
  - 失效清理仅清空 `compiled_artifact`
- 测试中将 `result["generated_code"] == result["compiled_artifact"]["json_text"]` 改为只断言 `compiled_artifact["json_text"]`

当前状态：

- 已完成本阶段目标：
  - 正式状态里不再出现 `generated_code`
  - `CodingAgent.__call__` 不再同步 compat 镜像
  - `RepairAgent` 仅失效正式主对象，不再维护 `generated_code`

阶段完成标准：

- Repair 回路仍能正确清空编译结果并触发重编译

### Phase E: 收尾历史字段与 legacy 目录迁移

目标：把“还需要保留的旧实现”显式隔离。

建议动作：

- 将 Phase 1 / Phase 2 compat 测试迁移到 `tests/legacy/` 或在命名上明确 legacy 属性
- 视测试依赖情况移除 `debug_history`

当前状态：

- 已完成第一步测试分层：
  - `tests/test_legacy_agent_imports.py`
  - `tests/test_legacy_execution_plan.py`
- `tests/test_phase2_planning_bundle.py`
- `tests/test_phase2_retrieval_bundle.py`
- `tests/test_phase2_retrieval_agent.py`
- `tests/test_phase6_retrieval_eval_contract.py`
  已改为兼容入口或混合入口，真实测试实现位于 `tests/legacy/` / `tests/contracts/`
- 现有显式 pytest 路径命令仍保持兼容

验收标准：

- `agents/` 根目录仅保留正式主链节点与其直接依赖
- `tests/` 中正式合同测试与 legacy 回归测试可以清晰区分

## 5. 文件级清退清单

### P0: 文档和合同冻结

- `工作流总结文档.md`
- `工作流节点输入输出说明.md`
- `tests/test_phase3_verifier_native_contract.py`
- 新增正式主链负向测试

### P1: 先清输入 fallback

- `agents/retrieval_agent.py`
- `agents/global_assembler.py`
- `agents/coding_agent.py`
- `utils/retrieval_bundle_utils.py`

### P2: 再清 compat 规划投影

- `workflow.py`
- `workflow_trace.py`
- `agents/global_assembler.py`
- `utils/phase3_adapters.py`
- `utils/graph_ir.py`
- `agents/verifier_agent.py`
- `agents/repair_agent.py`

### P3: 最后清历史状态和旧模块

- `workflow.py`
- `workflow_trace.py`
- `agents/planning_agent.py`
- `agents/assembly_agent.py`
- `agents/validation_agent.py`
- `agents/debugging_agent.py`
- `agents/retrieval_agent_old.py`
- `tests/test_legacy_agent_imports.py`
- `tests/test_legacy_execution_plan.py`
- `tests/test_phase2_planning_bundle.py`

## 6. 推荐先改的最小闭环

如果希望风险最小，建议先做这一组：

1. 正式节点去掉 `retrieval_context` fallback，只认 `retrieval_bundle`
2. `retrieval_agent.__call__` 停止回填 `retrieval_context`
3. 增加一组“正式状态不包含 `retrieval_context` 仍可运行”的合同测试

这一组改动的收益最大，因为它能先把正式主链和 legacy 检索视图彻底拆开，而且不会立刻触碰 verifier / repair 的 compat 逻辑。

## 7. 不建议当前直接动的点

- 不建议第一步就删 `build_legacy_execution_plan(...)`
  - 因为 legacy 测试与历史调用方仍可能需要显式 compat projection builder
- 不建议第一步就删 `agents/assembly_agent.py`
  - 这一条前提已过时：`GlobalAssembler` 已不再继承或复用 legacy `AssemblyAgent`
  - 当前真正不建议直接删除的原因，是 Phase 1 / Phase 2 / Phase 7 compat 测试与历史调用方仍可能通过 wrapper 路径导入它
- 不建议把 Phase 1 / Phase 2 / Phase 7 的 compat 测试直接删除
  - 应先迁移为 legacy 测试，再从正式回归集中剥离

## 8. 最终目标状态

清退完成后，正式主链应收敛为：

```text
user_query
-> analysis_result / requirement_spec
-> retrieval_bundle
-> decomposition_result / architecture_plan
-> subsystem_plan_map
-> assembled_graph_ir
-> compiled_artifact
-> verification_report
-> repair_context / repair_history / final_output
```

届时应满足：

- 正式状态无 `retrieval_context`
- 正式状态无 `execution_plan`
- 正式状态无 `generated_code`
- verifier 不再读取 compat 投影
- repair 不再维护 compat 字段
- legacy 模块真实实现位于显式历史目录；根目录仅在必要时保留兼容 wrapper
