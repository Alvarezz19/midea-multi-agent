这是一个非常关键的环节。编码智能体不仅要生成代码，还要解决**“物理落地”**的问题。

通过分析提供的首个 JSON 文件（`夏季主机负荷计算.json`）和 Schema 定义（`乘.json`），我发现该平台的连线机制与标准 Node-RED **不同**：
*   **Node-RED**: `wires` 记录**下游**（输出流向）。
*   **该平台**: `wires` 记录**上游**（输入来源）。Schema 中的描述“wires数组...用于连接上游模块的输出”证实了这一点。

这意味着编码智能体必须将规划智能体的“A指向B”转换为“B引用A”。

以下是编码智能体的完整实现方案。

---

### 1. 核心任务分解

编码智能体需要完成以下四个步骤：
1.  **ID 实例化 (ID Mapping)**：将 `logic_id`（如 `calc_temp_diff`）转换为平台所需的 7位/8位 UUID（如 `b45d2af`）。
2.  **模板加载 (Template Loading)**：从 `retrieved_docs_full` 中提取对应 `module_type` 的 `template_json`。
3.  **自动布局 (Auto-Layout)**：计算每个模块的 `x` 和 `y` 坐标，确保图纸美观（通常采用从左到右的分层布局）。
4.  **反向连线 (Reverse Wiring)**：根据 `PlanIR` 的连接关系，填充每个模块的 `wires` 字段（记录上游节点 ID）。

### 2. 详细 Python 实现

我们需要编写一个 Python 函数 `generate_code`，它接收 State 中的数据，返回最终的 JSON 字符串。

#### 依赖库
```python
import json
import uuid
import copy
from typing import List, Dict, Any
```

#### 辅助类与函数

我们需要一个简单的算法来进行**拓扑分层**，以便计算 X 坐标。

```python
def generate_short_uuid():
    """生成类似 'b45d2af' 的短ID"""
    return str(uuid.uuid4())[:7]

def topological_layout(nodes: List[Dict], connections: List[Dict]):
    """
    计算节点的 (x, y) 坐标。
    策略：
    1. 构建邻接表。
    2. 计算每个节点的深度（层级）。
    3. 根据层级分配 X，根据层内顺序分配 Y。
    """
    # 1. 构建图结构
    adj_list = {node['logic_id']: [] for node in nodes}
    in_degree = {node['logic_id']: 0 for node in nodes}
    
    for conn in connections:
        src = conn['from_node']
        dst = conn['to_node']
        if src in adj_list and dst in in_degree:
            adj_list[src].append(dst)
            in_degree[dst] += 1
            
    # 2. 计算层级 (BFS)
    levels = {} # logic_id -> level
    queue = [nid for nid, deg in in_degree.items() if deg == 0]
    
    # 初始化 layer 0
    for nid in queue:
        levels[nid] = 0
        
    processed = 0
    while queue:
        u = queue.pop(0)
        processed += 1
        current_level = levels[u]
        
        for v in adj_list[u]:
            # 下游节点的层级 = max(当前层级, 上游层级 + 1)
            # 这里简化处理，直接赋 next level，若有多个父节点取最大值
            levels[v] = max(levels.get(v, 0), current_level + 1)
            in_degree[v] -= 1
            if in_degree[v] == 0:
                queue.append(v)
                
    # 3. 分配坐标
    # 配置
    START_X, START_Y = 100, 100
    X_GAP, Y_GAP = 200, 80   # 模块间的间距
    
    # 按层级分组
    level_groups = {}
    for nid, lvl in levels.items():
        if lvl not in level_groups:
            level_groups[lvl] = []
        level_groups[lvl].append(nid)
        
    coordinates = {}
    
    # 遍历每一层
    max_level = max(levels.values()) if levels else 0
    for lvl in range(max_level + 1):
        group = level_groups.get(lvl, [])
        for index, nid in enumerate(group):
            x = START_X + (lvl * X_GAP)
            # 简单的垂直居中计算，或者直接向下排列
            y = START_Y + (index * Y_GAP)
            coordinates[nid] = {"x": x, "y": y}
            
    return coordinates
```

#### 核心处理逻辑

这是编码智能体的主体代码。

```python
def coding_agent_core(plan_ir: Dict, full_docs: List[Dict]) -> str:
    """
    编码智能体核心逻辑
    :param plan_ir: 规划智能体输出的 PlanIR 字典
    :param full_docs: 检索阶段获取的包含 template_json 的完整文档列表
    :return: 最终的 JSON 字符串
    """
    
    # --- 1. 建立索引 ---
    # 快速查找 template_json
    doc_map = {doc['module_type']: doc for doc in full_docs}
    
    # 建立 logic_id -> real_uuid 的映射
    id_map = {}
    for node in plan_ir['nodes']:
        id_map[node['logic_id']] = generate_short_uuid()
        
    # --- 2. 自动布局 ---
    coords_map = topological_layout(plan_ir['nodes'], plan_ir['connections'])
    
    # --- 3. 预处理连接关系 (反向索引) ---
    # 目标: target_node_id -> { input_port_index: source_info }
    # 你的平台 wires 格式: wires[input_index] = [ {id: source_uuid, port: source_port_index} ]
    reverse_connections = {node['logic_id']: {} for node in plan_ir['nodes']}
    
    for conn in plan_ir['connections']:
        target_logic_id = conn['to_node']
        port_idx = conn['to_port_index']
        source_logic_id = conn['from_node']
        source_port_idx = conn['from_port_index']
        
        # 记录连接信息
        reverse_connections[target_logic_id][port_idx] = {
            "id": id_map[source_logic_id], # 转换为真实的 UUID
            "port": source_port_idx
        }

    # --- 4. 生成模块 JSON ---
    final_modules = []
    
    # 首先添加一个 Tab 页 (容器)，如文件头所示
    flow_id = generate_short_uuid()
    final_modules.append({
        "id": flow_id,
        "type": "tab",
        "label": plan_ir.get("goal", "自动生成流程"),
        "disabled": False,
        "info": ""
    })
    
    for node in plan_ir['nodes']:
        logic_id = node['logic_id']
        m_type = node['module_type']
        
        # A. 获取模板
        if m_type not in doc_map:
            raise ValueError(f"缺少模块 {m_type} 的定义文档")
            
        template = copy.deepcopy(doc_map[m_type].get('template_json', {}))
        
        # B. 替换基础属性
        real_id = id_map[logic_id]
        template['id'] = real_id
        template['z'] = flow_id  # 所属流程ID
        template['x'] = coords_map.get(logic_id, {'x': 0})['x']
        template['y'] = coords_map.get(logic_id, {'y': 0})['y']
        
        # C. 注入参数 (Parameters Injection)
        # 将 Planner 规划的参数覆盖模板中的 {{placeholder}} 或默认值
        # 注意：这里需要处理 {{inputs}} 这种模板语法，或者直接覆盖键值
        planned_params = node.get('parameters', {})
        for key, value in planned_params.items():
            # 特殊处理：如果 Planner 输出的是 inputCount，但 Schema 里是 inputs
            if key == "inputCount" and "inputs" in template:
                template["inputs"] = value
            else:
                template[key] = value
                
        # 如果 parameters 里有 name，覆盖模板的 name
        if "name" not in planned_params and "name" in template:
             # 可以给个默认名字
             template['name'] = node.get('reasoning', m_type)

        # D. 构建 Wires (反向连接)
        # 确定该模块有多少个输入端口
        # 通常由 parameters['inputs'] 或 template['inputs'] 决定，默认为 1 或 0
        input_count = int(template.get('inputs', 0))
        
        # 如果是 inputCount 参数控制的，以参数为准
        if 'inputCount' in planned_params:
            input_count = int(planned_params['inputCount'])
            
        # 构建 wires 数组
        # 格式: [ [src1], [src2] ]，长度等于 input_count
        wires = []
        node_incoming = reverse_connections.get(logic_id, {})
        
        # 对于 swInput 或 constInput，input_count 通常为 0，wires 为 []
        # 对于 subtract, multiply，input_count >= 2
        for i in range(input_count):
            if i in node_incoming:
                # 存在连接
                conn_info = node_incoming[i]
                wires.append([conn_info]) # 注意这里是数组的数组
            else:
                # 悬空端口
                wires.append([])
        
        template['wires'] = wires
        
        # 清理模板中的占位符 (如果有 {{__GEN_ID__}} 这种)
        # 简单遍历所有 value，如果是字符串且包含 {{}} 则尝试清理或保留
        # 这里假设直接覆盖已完成，只做简单清理
        
        final_modules.append(template)
        
    # --- 5. 序列化 ---
    return json.dumps(final_modules, indent=4, ensure_ascii=False)
```

### 3. LangGraph 节点集成

将上述逻辑封装到 LangGraph 的节点中。


### 4. 运行结果模拟

基于你给出的 `PlanIR` 输入，编码智能体将执行以下操作：

1.  **ID 映射**:
    *   `const_4_18` -> `a1b2c3d`
    *   `input_temp_return` -> `e5f6g7h`
    *   ... (其余类似)

2.  **布局计算**:
    *   **Layer 0 (x=100)**: `const_4_18`, `const_3_6`, `input_temp_return`, `input_temp_supply`, `input_flow`
    *   **Layer 1 (x=300)**: `calc_temp_diff` (依赖 Inputs), `calc_part1` (依赖 Const 4.18) -> *Wait, logic check*:
        *   `calc_part1` 依赖 `const_4_18` (L0) 和 `calc_temp_diff` (L1)。所以 `calc_part1` 应该是 Layer 2。
    *   **修正后的层级**:
        *   L0: Inputs & Consts
        *   L1: `calc_temp_diff` (Input - Input)
        *   L2: `calc_part1` (Const * L1_Result)
        *   L3: `calc_part2` (L2_Result * Flow(L0))
        *   L4: `calc_final` (L3_Result / Const(L0))
    *   **坐标**: `calc_final` 将被放置在最右侧 (x=900 左右)。

3.  **Wires 生成 (关键点)**:
    以 `calc_part1` (乘法) 为例：
    *   Plan: `const_4_18 (Port 0)` -> `calc_part1 (Port 0)`
    *   Plan: `calc_temp_diff (Port 0)` -> `calc_part1 (Port 1)`
    *   **生成的 JSON wires**:
        ```json
        "wires": [
            [ {"id": "a1b2c3d", "port": 0} ],  // 对应 Port 0: 连接到 4.18 常量
            [ {"id": "uuid_of_diff", "port": 0} ] // 对应 Port 1: 连接到 温差计算
        ]
        ```

### 5. 最终生成的文件结构

```json
[
    {
        "id": "flow_uuid_123",
        "type": "tab",
        "label": "计算热功率或热量...",
        ...
    },
    {
        "id": "real_uuid_const_4_18",
        "type": "constInput",
        "fixedValue": 4.18,
        "wires": [],
        "x": 100, "y": 100,
        ...
    },
    {
        "id": "real_uuid_diff",
        "type": "subtract",
        "wires": [
            [{"id": "real_uuid_return", "port": 0}],
            [{"id": "real_uuid_supply", "port": 0}]
        ],
        "x": 300, "y": 180,
        ...
    }
    // ... 其他模块
]
```

这个方案完整覆盖了从逻辑图到物理文件的转换，特别是处理了该平台特殊的“上游连线”机制和自动布局需求。