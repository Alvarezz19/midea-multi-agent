# 仓库级彻底清退执行清单

基于当前仓库真实代码、当前测试、当前文档整理。

本文和 `兼容层清退清单.md` 的区别是：

- `兼容层清退清单.md` 侧重“现状盘点 + 风险判断”
- 本文侧重“按什么顺序做、每批改什么、改完怎么验”

## 1. 目标定义

这里的“仓库级彻底清退”不是指“正式主工作流主链已经不再依赖 compat 字段”。

这里的目标更严格，指的是：

- `agents/` 根目录不再对外暴露 legacy / compat 公开入口
- `utils/` 中不再保留 bundle-or-context 双形态 compat helper
- `workflow.py` 不再保留 legacy shim 入口
- `agents/legacy/` 不再保留仍被仓内正式入口引用的 compat 实现
- `tests/` 根目录不再保留 legacy 测试入口壳
- `tests/legacy/` 不再作为仓内持续维护的回归套件存在

## 2. 当前剩余残留

### 2.1 已完成删除的正式模块 compat 入口

- `agents/coding_agent.py`
  - `compile_graph(...)`
  - `generate_json(...)`
- `agents/retrieval_agent.py`
  - `retrieve(...)`

当前状态：

- 上述 3 个仓库级 public compat API 已删除
- 正式入口统一收口到：
  - `CodingAgent.compile_graph_from_bundle(...)`
  - `RetrievalAgent.retrieve_bundle(...)`
  - `compiled_artifact["json_text"]`

### 2.2 仍保留在 utils / shared 层的 legacy / compat helper

- `utils/legacy_execution_plan.py`
  - `build_legacy_execution_plan(...)`

### 2.3 仍保留的 wrapper / shim

- `agents/assembly_agent.py`
- `agents/planning_agent.py`
- `agents/validation_agent.py`
- `agents/debugging_agent.py`
- `agents/retrieval_agent_old.py`

### 2.4 仍保留的 legacy 实现目录

- `agents/legacy/`

重点包括：

- `agents/legacy/context_formatter.py`
- `agents/legacy/retrieval_context_utils.py`
- `agents/legacy/assembly_agent.py`
- `agents/legacy/planning_agent.py`
- `agents/legacy/validation_agent.py`
- `agents/legacy/debugging_agent.py`
- `agents/legacy/retrieval_agent_old.py`

### 2.5 仍保留的 legacy 测试入口与套件

- `tests/test_phase2_planning_bundle.py`
- `tests/test_phase2_retrieval_agent.py`
- `tests/test_phase2_retrieval_bundle.py`
- `tests/test_phase6_retrieval_eval_contract.py`
- `tests/test_legacy_agent_imports.py`
- `tests/test_legacy_execution_plan.py`
- `tests/legacy/`

已随 Batch 2 删除：

- `tests/test_phase1_workflow.py`
- `tests/test_phase2_bundle_consumers.py`
- `tests/test_phase7_coding_determinism.py`

## 3. 不在本轮清退范围内的正式字段

以下字段属于正式主链仍真实依赖的状态契约，不应误删：

- `review_*`
- `hitl_stage`
- `repair_context`
- `repair_history`
- `route_decision`
- `retry_budget`
- `retry_counts_by_scope`
- `retry_count`
- `current_step`
- `final_output`
- `clarification_round`
- `architecture_feedback_patch`

## 4. 执行策略

整体分成 4 个批次：

- Batch 0：先做调用面审计与冻结
- Batch 1：清 utils / shared / workflow shim
- Batch 2：清正式模块上残留的 compat API 与 wrapper
- Batch 3：清 legacy 实现与 legacy 测试

原则：

- 先切断仓内正式引用
- 再删除公开 compat surface
- 最后删除 legacy 实现与 legacy 测试

## 4.1 当前能力与上下文评估

基于当前仓库代码、当前测试分层、当前已知主链事实，我现在的把握是：

- 已高质量完成 `Batch 0`
- 已高质量完成 `Batch 1`
- 可以高质量完成 `Batch 2` 的仓内 readiness 审计与 legacy-only public surface 标注
- 不能在不额外确认外部调用面的前提下，直接高质量承诺完成 `Batch 2` 的删除动作
- 也不建议在当前上下文下直接推进 `Batch 3`

原因：

- `Batch 0` 与 `Batch 1` 都属于仓内 helper / shim 收口，当前上下文足以直接覆盖
- `Batch 2` 的仓内调用面已经足够审计，但删除动作会触及仍暴露在正式模块上的 public compat API：
  - `CodingAgent.compile_graph(...)`
  - `CodingAgent.generate_json(...)`
  - `RetrievalAgent.retrieve(...)`
  这些接口当前仓内只剩 legacy 测试在用，但是否存在仓外调用，当前上下文无法证明
- `Batch 3` 涉及整批删除 `agents/legacy/` 与 `tests/legacy/`，本质上是“停止维护整个 legacy 面”，需要先完成前两批的彻底切断

## 4.2 本轮已推进结论

- `Batch 0` 已完成仓内调用面审计
- `workflow.py::populate_phase3_workflow(...)` 已确认仓内无调用，并已删除
- `utils/retrieval_bundle_utils.py` 中仓内 `0` 调用的 bundle-or-context 双分发 helper 已删除：
  - `get_atomic_modules(...)`
  - `get_subflow_templates(...)`
  - `get_system_patterns(...)`
  - `get_style_guides(...)`
  - `build_compilable_doc_map(...)`
  - `build_allowed_module_types(...)`
- `utils/retrieval_bundle_utils.py` 中 legacy-only helper 已迁出正式 utils，统一收口到：
  - `agents/legacy/retrieval_context_utils.py`
- `utils/context_formatter.py` 已收口为 bundle-only 输入；legacy formatter 已迁到：
  - `agents/legacy/context_formatter.py`
- `agents/assembly_shared.py::_build_compat_doc_map(...)` 已删除；legacy assembly 已改为显式依赖 legacy helper
- `CodingAgent.compile_graph(...)` / `generate_json(...)` / `RetrievalAgent.retrieve(...)` 已删除
- 仅覆盖上述 3 个 API 的 compat helper 与 legacy 测试已同步删除：
  - `agents/legacy/coding_compat.py`
  - `agents/legacy/retrieval_compat.py`
  - `tests/legacy/phase1_workflow_compat.py`
  - `tests/legacy/phase2_bundle_consumers_compat.py`
  - `tests/legacy/phase2_retrieval_agent_compat.py`
  - `tests/legacy/phase7_coding_determinism_compat.py`
  - `tests/test_phase1_workflow.py`
  - `tests/test_phase2_bundle_consumers.py`
  - `tests/test_phase7_coding_determinism.py`

## 5. Batch 0：调用面审计与冻结

### 5.1 目标

确认哪些 compat 入口还可能被仓内正式代码、脚本、文档命令或仓外调用使用。

### 5.2 可立即执行动作

- 搜索 `compile_graph(...)` / `generate_json(...)` / `retrieve(...)` 的仓内调用
- 搜索 `build_legacy_*` 系列 helper 的仓内调用
- 搜索 `tests/` 根目录哪些文件只是 legacy 入口壳
- 搜索 `workflow.py.populate_phase3_workflow(...)` 是否有真实调用

### 5.3 建议命令

```powershell
conda activate midea
rg -n "compile_graph\(|generate_json\(|retrieve\(" agents utils workflow.py tests
rg -n "build_legacy_|get_legacy_|legacy_execution_plan" agents utils workflow.py tests
rg -n "populate_phase3_workflow\(" agents utils workflow.py tests
rg -n "from tests\.legacy\.|tests/legacy" tests
```

### 5.4 产出要求

- 形成一份“仍有调用 / 无调用”的结果表
- 对下列 3 个入口给出明确结论：
  - `CodingAgent.compile_graph(...)`
  - `CodingAgent.generate_json(...)`
  - `RetrievalAgent.retrieve(...)`

### 5.5 通过标准

- 确认正式主链不调用上述 compat API
- 确认剩余命中仅来自：
  - legacy 测试
  - legacy wrapper
  - legacy helper
  - 文档说明

### 5.6 本轮审计结果

- `CodingAgent.compile_graph(...)`
  - 当前已删除；删除前仓内命中仅来自 `tests/legacy/` 兼容测试与方法定义本身
- `CodingAgent.generate_json(...)`
  - 当前已删除；删除前仓内未发现除方法定义本身外的真实调用
- `RetrievalAgent.retrieve(...)`
  - 当前已删除；删除前仓内命中仅来自 `tests/legacy/phase2_retrieval_agent_compat.py`
- `populate_phase3_workflow(...)`
  - 仓内仅命中定义本身，已确认是 dead shim
- `build_legacy_retrieval_context(...)`
  - 当前仍被 `tests/legacy/` 使用
- `build_legacy_doc_map(...)`
  - 当前仍被 `agents/legacy/assembly_agent.py`、`agents/legacy/context_formatter.py`、`tests/legacy/` 使用
- `build_legacy_allowed_module_types(...)`
  - 当前仍被 `agents/legacy/planning_agent.py` 使用
- `utils/context_formatter.py`
  - 当前正式实现已 bundle-only；legacy planner 仅在 bundle 输入路径复用
- `agents/assembly_shared.py::_build_compat_doc_map(...)`
  - 仓内仅命中定义本身，已在 Batch 1 删除

## 6. Batch 1：先清 utils / shared / workflow shim

### 6.1 目标

先把“正式基础设施层”收口为 bundle-only，避免后续删 agent API 时还被底层 compat helper 托底。

### 6.2 文件范围

- `utils/retrieval_bundle_utils.py`
- `utils/context_formatter.py`
- `agents/assembly_shared.py`
- `workflow.py`

### 6.3 具体动作

- 在 `utils/retrieval_bundle_utils.py` 中移除 bundle-or-context 双分发 helper：
  - `get_atomic_modules(...)`
  - `get_subflow_templates(...)`
  - `get_system_patterns(...)`
  - `get_style_guides(...)`
  - `build_compilable_doc_map(...)`
  - `build_allowed_module_types(...)`
  当前状态：
  - 已完成；仓内搜索命中仅剩当前执行文档与旧阶段设计稿，不再命中正式代码、legacy 实现或测试
- 将 legacy-only helper 移出正式 utils，或在确认无调用后直接删除：
  - `get_legacy_atomic_modules(...)`
  - `get_legacy_subflow_templates(...)`
  - `build_legacy_retrieval_context(...)`
  - `build_legacy_doc_map(...)`
  - `build_legacy_allowed_module_types(...)`
  当前状态：
  - 已完成；legacy-only helper 已迁到 `agents/legacy/retrieval_context_utils.py`
- 将 `utils/context_formatter.py` 收口为 bundle-only 输入
  当前状态：
  - 已完成；legacy formatter 已迁到 `agents/legacy/context_formatter.py`
- 删除 `agents/assembly_shared.py` 中的 `_build_compat_doc_map(...)`
  当前状态：
  - 已完成；legacy assembly 已改为显式依赖 `agents/legacy/retrieval_context_utils.py`
- `workflow.py` 中的 `populate_phase3_workflow(...)` 已于本轮删除，Batch 1 剩余部分聚焦 helper 收口

### 6.4 前置条件

- Batch 0 已确认上述 helper 不再被正式代码使用
- 若仍被 legacy 测试使用，需要先同步迁移测试或一并删除 legacy 测试入口

### 6.5 验收命令

```powershell
conda activate midea
pytest tests\test_phase3_global_assembler.py -q
pytest tests\test_phase3_verifier_native_contract.py -q
pytest tests\test_phase3_workflow.py -q
pytest tests\test_phase4_workflow_repair_loop.py -q
python -m compileall agents utils tests
```

### 6.6 通过标准

- 正式主链相关测试保持通过
- `rg` 搜索中不再出现正式代码调用 legacy retrieval helper
- `rg` 搜索中不再出现仓内代码、测试对 `get_atomic_modules(...)`、`build_compilable_doc_map(...)` 等双分发 helper 的真实调用
- `utils/` 与 `agents/assembly_shared.py` 中不再保留 legacy context 输入兼容签名

## 7. Batch 2：清正式模块上的 compat API 与 wrapper

### 7.1 目标

把 still-public 的 compat surface 从正式模块上移除。

### 7.2 文件范围

- `agents/coding_agent.py`
- `agents/retrieval_agent.py`
- `agents/assembly_agent.py`
- `agents/planning_agent.py`
- `agents/validation_agent.py`
- `agents/debugging_agent.py`
- `agents/retrieval_agent_old.py`

### 7.3 具体动作

- 删除 `CodingAgent.compile_graph(...)`
- 删除 `CodingAgent.generate_json(...)`
- 删除 `RetrievalAgent.retrieve(...)`
- 删除所有根目录 wrapper：
  - `agents/assembly_agent.py`
  - `agents/planning_agent.py`
  - `agents/validation_agent.py`
  - `agents/debugging_agent.py`
  - `agents/retrieval_agent_old.py`

### 7.4 前置条件

- Batch 0 已确认仓内正式代码和仓内脚本不再依赖这些入口
- 如存在仓外调用方，需先完成调用迁移公告或一起修改仓内示例 / 文档

### 7.4.1 当前 readiness 结论

- 仓内正式代码、formal 测试、正式脚本当前都不再依赖：
  - `CodingAgent.compile_graph(...)`
  - `CodingAgent.generate_json(...)`
  - `RetrievalAgent.retrieve(...)`
- 代码层现状已进一步收口：
  - 这 3 个 public compat API 已删除
  - 仅服务这 3 个 API 的 compat helper 已同步删除：
    - `agents/legacy/coding_compat.py`
    - `agents/legacy/retrieval_compat.py`
  - 仅覆盖这 3 个 API 的 legacy 测试已同步删除
- 因此，当前上下文足以支持：
  - 完成 Batch 2 中“删除 3 个 public compat API”这一子项
- 当前上下文仍不足以支持：
  - 在未再次确认仓外调用面与 wrapper 迁移策略前，继续扩大到所有 Batch 2 wrapper 删除

### 7.5 验收命令

```powershell
conda activate midea
rg -n "compile_graph\(|generate_json\(|retrieve\(" agents utils workflow.py tests
pytest tests\test_phase3_architecture_planner.py -q
pytest tests\test_phase3_subsystem_planner.py -q
pytest tests\test_phase4_repair_agent.py -q
pytest tests\test_phase8_architecture_review_contract.py -q
python -m compileall agents utils tests
```

### 7.6 通过标准

- 上述 `rg` 命中只剩文档或历史记录，不再命中正式生产代码
- 正式 Phase 3 / Phase 4 / Phase 8 核心测试保持通过

## 8. Batch 3：清 legacy 实现与 legacy 测试

### 8.1 目标

删除剩余 legacy 目录和 legacy 测试入口，完成仓库级清退。

### 8.2 文件范围

- `agents/legacy/`
- `utils/legacy_execution_plan.py`
- `tests/legacy/`
- 根目录 legacy 测试入口壳

### 8.3 具体动作

- 删除 `agents/legacy/coding_compat.py`
- 删除 `agents/legacy/retrieval_compat.py`
- 删除 `agents/legacy/` 其余 legacy agent 实现
- 删除 `utils/legacy_execution_plan.py`
- 删除 `tests/legacy/`
- 删除以下 legacy 测试入口壳：
  - `tests/test_phase1_workflow.py`
  - `tests/test_phase2_bundle_consumers.py`
  - `tests/test_phase2_planning_bundle.py`
  - `tests/test_phase2_retrieval_agent.py`
  - `tests/test_phase2_retrieval_bundle.py`
  - `tests/test_phase6_retrieval_eval_contract.py`
  - `tests/test_phase7_coding_determinism.py`
  - `tests/test_legacy_agent_imports.py`
  - `tests/test_legacy_execution_plan.py`

### 8.4 前置条件

- Batch 1 和 Batch 2 已完成
- 已确认仓库内不再存在任何 legacy helper 或 wrapper 的真实调用

### 8.5 验收命令

```powershell
conda activate midea
rg -n "legacy|compat|build_legacy_|get_legacy_|retrieval_context|execution_plan|generated_code|execution_result|validation_result|next_step" agents utils workflow.py tests
pytest tests\test_phase3_global_assembler.py -q
pytest tests\test_phase3_verifier_native_contract.py -q
pytest tests\test_phase4_workflow_repair_loop.py -q
pytest tests\test_phase8_clarification_contract.py -q
pytest tests\test_phase8_architecture_review_contract.py -q
python -m compileall agents utils tests
```

### 8.6 通过标准

- `agents/`、`utils/`、`tests/` 根目录中不再保留 legacy / compat 生产代码与测试入口
- 搜索命中仅允许出现在：
  - 历史演进文档
  - 已明确标记为历史记录的说明文档

## 9. 推荐执行顺序

建议严格按下面顺序推进：

1. 先完成 Batch 0，冻结调用面
2. 再做 Batch 1，收口基础 helper
3. 然后做 Batch 2，删除正式模块公开 compat API
4. 最后做 Batch 3，整体删除 legacy 目录与 legacy 测试

不要反过来做。

如果先删 Batch 2 或 Batch 3，通常会遇到两个问题：

- 正式层虽然不走 compat，但底层 helper 还在暗中托底，导致“删不干净”
- legacy 测试 / wrapper 仍在引用旧入口，导致改动面扩大、判断失真

## 10. 每批完成后必须同步的文档

- `兼容层清退清单.md`
- `工作流总结文档.md`
- `工作流节点输入输出说明.md`

建议同步规则：

- Batch 1 完成后：更新 helper / workflow shim 的描述
- Batch 2 完成后：更新 agent API 与 wrapper 描述
- Batch 3 完成后：更新“仓库已不再保留 legacy 路径”的最终状态

## 11. 最终验收标准

完成“仓库级彻底清退”后，应满足：

- 正式主链仍保持当前正式行为不变
- 正式主链不再依赖任何 compat helper 或 compat API
- `agents/` 根目录只保留正式节点与正式依赖
- `utils/` 不再保留 retrieval bundle/context 双形态兼容
- `workflow.py` 不再保留 legacy shim
- `agents/legacy/`、`tests/legacy/` 已整体移除
- 根目录不再存在 legacy 测试入口壳
- 文档不再描述“仓内仍保留 compat API / legacy wrapper”

## 12. 当前建议的下一步

`Batch 0` 和 `Batch 1` 现已完成。下一轮如果继续按风险最小推进，建议直接进入 Batch 2 前置确认，优先确认这 3 个入口的真实调用面：

- `CodingAgent.compile_graph(...)`
- `CodingAgent.generate_json(...)`
- `RetrievalAgent.retrieve(...)`

只有这一步确认完，后面的删改才能真正收口，而不是继续停留在“主链已清、仓库未清”的半完成状态。
