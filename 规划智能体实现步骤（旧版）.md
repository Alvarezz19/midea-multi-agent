以下是**规划智能体（Planning Agent）**的完整实现方案。

### 1. 系统架构与数据流

在 LangGraph 中，规划智能体的节点主要负责：
1.  **接收**：用户 Query + 原始检索上下文 (`retrieval_context`)。
2.  **清洗**：调用 `format_docs_for_planner` 生成 `slim_context`。
3.  **推理**：LLM根据 `slim_context` 生成逻辑拓扑。
4.  **输出**：结构化的中间表示 (`PlanIR`) 写入 State。

### 2. 详细实现步骤

#### 第一步：定义数据结构 (Pydantic Model)

为了保证规划智能体输出的数据绝对可被代码处理，我们使用 Pydantic 定义**中间表示（IR）**的结构。这是连接规划与编码的协议。

```python
from typing import List, Dict, Any, Optional
from langchain_core.pydantic_v1 import BaseModel, Field

# 定义单个节点（积木块）
class PlanNode(BaseModel):
    logic_id: str = Field(..., description="唯一的逻辑ID，建议使用有意义的名称，如 'temp_diff_calc'")
    module_type: str = Field(..., description="对应检索结果中的 module_type")
    # 参数字典：规划智能体在这里决定参数值（如 inputs=3, fixedValue=4.18）
    parameters: Dict[str, Any] = Field(default_factory=dict, description="需要配置的参数键值对")
    reasoning: str = Field(..., description="选择该模块的简短理由")

# 定义连接线（逻辑流）
class PlanConnection(BaseModel):
    from_node: str = Field(..., description="上游节点的 logic_id")
    from_port_index: int = Field(0, description="上游节点的输出端口索引，通常为0")
    to_node: str = Field(..., description="下游节点的 logic_id")
    to_port_index: int = Field(..., description="下游节点的输入端口索引")

# 定义完整的规划图
class PlanIR(BaseModel):
    goal: str = Field(..., description="对当前计算任务的简述")
    nodes: List[PlanNode]
    connections: List[PlanConnection]
    
    # 简单的校验逻辑（可选）
    def validate_ids(self):
        node_ids = {n.logic_id for n in self.nodes}
        for conn in self.connections:
            if conn.from_node not in node_ids or conn.to_node not in node_ids:
                raise ValueError(f"连接中引用了不存在的节点ID: {conn.from_node} -> {conn.to_node}")
```

#### 第二步：实现上下文清洗函数 (`format_docs_for_planner`)

这个函数是“减脂”的关键。它将原始的复杂 JSON 转换为 LLM 易读的 Markdown 或简化 JSON 字符串。



#### 第三步：编写 Prompt 模板

Prompt 需要明确告诉 LLM：你是一个逻辑工程师，不要关心坐标，只关心逻辑正确性。

```python
from langchain_core.prompts import ChatPromptTemplate

PLANNER_SYSTEM_PROMPT = """
你是一个工业自动化控制系统的逻辑规划专家。你的任务是根据用户的自然语言需求，使用提供的模块（积木）设计一个逻辑控制图。

### 你的任务：
1. 分析用户的计算公式或控制逻辑。
2. 从【可用模块列表】中选择合适的模块。**严禁虚构模块**，只能使用列表中的 module_type。
3. 规划每个模块的参数（如乘法模块的输入数量、常量的数值）。
4. 定义模块之间的连接关系（拓扑结构）。

### 规则：
- **逻辑ID**：为每个模块起一个唯一的、有意义的 ID（如 `input_temp`, `calc_diff`）。
- **端口索引**：输入输出端口必须使用 0-based 索引（如 0 代表第一个端口）。
- **常量处理**：如果公式中有常数（如 4.18），请检查模块是否支持直接设置参数（如 fixedValue），如果不支持，则需要实例化一个 `constInput` 模块。
- **参数配置**：确保 `parameters` 字段中的键名与模块定义中的参数名完全一致。

### 可用模块列表：
{slim_context}
"""

prompt_template = ChatPromptTemplate.from_messages([
    ("system", PLANNER_SYSTEM_PROMPT),
    ("user", "用户需求：{user_query}")
])
```

#### 第四步：构建 LangGraph 节点

这里我们将以上所有部分组装成一个 Graph Node。



### 3. 示例运行效果

假设用户输入：“计算公式是 4.18 * (回水 - 供水) * 流量 / 3.6”

**`format_docs_for_planner` 生成的 `slim_context` (传给 LLM 的样子):**
```json
[
  {
    "Module Type": "multiply",
    "Description": "乘法运算...",
    "Configurable Parameters": {
      "inputs": "integer, default: 2. 输入数量",
      "fixedValue": "number, default: 0. 单输入时的系数"
    },
    "Ports": {"inputs": ["[0] 乘数1", "[1] 乘数2"], "outputs": ["[0] 乘积"]}
  },
  {
    "Module Type": "subtract",
    "Ports": {"inputs": ["[0] 被减数", "[1] 减数"], "outputs": ["[0] 差"]}
  },
  ...
]
```

**LLM 生成的 `PlanIR` (存入 State 的样子):**
```json
{
  "goal": "主机负荷计算",
  "nodes": [
    {
      "logic_id": "input_return",
      "module_type": "hwInput",
      "parameters": {"name": "冷冻回水温度"},
      "reasoning": "获取回水温度"
    },
    {
      "logic_id": "input_supply",
      "module_type": "hwInput",
      "parameters": {"name": "冷冻供水温度"},
      "reasoning": "获取供水温度"
    },
    {
      "logic_id": "calc_delta_t",
      "module_type": "subtract",
      "parameters": {},
      "reasoning": "计算温差 (回水-供水)"
    },
    {
      "logic_id": "input_flow",
      "module_type": "hwInput",
      "parameters": {"name": "冷冻水流量"},
      "reasoning": "获取流量"
    },
    {
      "logic_id": "calc_energy_joule",
      "module_type": "multiply",
      "parameters": {
        "inputs": 3, 
        "name": "计算能量"
      },
      "reasoning": "计算 4.18 * 温差 * 流量。这里假设第一个输入连接常量模块，或者如果乘法模块支持直接写系数则省略常量模块。根据Schema，multiply需要输入端口，所以我们需要一个常量模块。"
    },
    {
      "logic_id": "const_cp",
      "module_type": "constInput",
      "parameters": {"fixedValue": 4.18},
      "reasoning": "水的比热容"
    },
     {
      "logic_id": "const_unit_conv",
      "module_type": "constInput",
      "parameters": {"fixedValue": 3.6},
      "reasoning": "单位换算系数"
    },
    {
      "logic_id": "calc_final_kw",
      "module_type": "divide",
      "parameters": {},
      "reasoning": "除以3.6得到KW"
    }
  ],
  "connections": [
    {"from_node": "input_return", "from_port_index": 0, "to_node": "calc_delta_t", "to_port_index": 0},
    {"from_node": "input_supply", "from_port_index": 0, "to_node": "calc_delta_t", "to_port_index": 1},
    {"from_node": "const_cp", "from_port_index": 0, "to_node": "calc_energy_joule", "to_port_index": 0},
    {"from_node": "calc_delta_t", "from_port_index": 0, "to_node": "calc_energy_joule", "to_port_index": 1},
    {"from_node": "input_flow", "from_port_index": 0, "to_node": "calc_energy_joule", "to_port_index": 2},
    {"from_node": "calc_energy_joule", "from_port_index": 0, "to_node": "calc_final_kw", "to_port_index": 0},
    {"from_node": "const_unit_conv", "from_port_index": 0, "to_node": "calc_final_kw", "to_port_index": 1}
  ]
}
```

### 4. 关键点总结

1.  **视图分离**：通过 `format_docs_for_planner`，我们成功地屏蔽了 `template_json` 的噪音，让 LLM 专注于逻辑组合。
2.  **结构化输出**：使用 Pydantic 强制 LLM 生成符合 Schema 的 JSON，避免了后续解析错误。
3.  **逻辑抽象**：引入 `logic_id`，将“逻辑设计”与“物理生成（UUID/坐标）”解耦，这是系统稳定性的基石。