# 第六阶段当前状态交接

> 更新时间：2026-04-17  
> 适用范围：作为第六阶段下一轮开工的直接承接文档。  
> 说明：本文件优先反映当前工作区已落地的正式结果；若与 `第六阶段实施清单.md` 中较早的阶段性描述冲突，以本文件和最新正式产物为准。

---

## 1. 当前正式结论

- `phase6_real_query_suite` 当前正式结果为 `12/12 passed`
- `golden_case_pass_rate=1.0`
- `drift_case_count=0`
- `rebaseline_candidate_count=0`
- `phase6_retrieval_eval` 当前正式结果为 `healthy=12`，其余诊断项均为 `0`
- `golden_template_hit_rate_top5=1.0`
- `golden_pattern_hit_rate_top3=1.0`
- 之前聚合出的 3 条 `exhaust_fan_control` 单点 `asset_gap` 已在本轮通过最小资产侧闭环清零
- 工作包 C 已正式实现，并完成正式 suite / retrieval eval 回归
- 已完成工作包 D gate 判断：当前没有明确值得扩成 deterministic patch 的高频失败桶，不进入工作包 D
- 工作包 F 已正式完成，Phase 7 `Send / HITL` 准入资料已补齐
- 当前正式建议为：第六阶段可以按“质量收口 + 准入包完成”视角收尾；若后续继续推进，优先转入 Phase 7 的最小工程改造，而不是回头重复 A/B/C 或强行扩 D

结论口径：

- 第六阶段 A/B 侧围绕排风机资产缺口的诊断收口已经完成，不需要再重复做上一轮已经完成的基础代码、正式运行、基线重定和文档同步
- 工作包 C 已基于正式产物完成，不需要再回到“是否放开 C”的判断
- 工作包 D 当前不是“待开工”，而是“已完成 gate 判断且本轮不进入”
- 工作包 F 已从“待补文档”变成正式阶段产物，不需要下轮再从零摸底 `thread_id / reducer / checkpointer`

---

## 2. 已完成内容

### 2.1 上一轮已完成并确认不需要重做的内容

- phase6 共用 case 源与 case 校验
- `phase6_real_query_suite` 正式脚本、输出目录和 retained 机制
- `phase6_retrieval_eval` 正式脚本与摘要输出
- retrieval / workflow trace 诊断字段补齐
- 对应 contract test 与回归测试
- A/B 诊断收口的第一轮结论
- 正式 case 基线重定
- 上一轮配套文档同步

### 2.2 本轮新增完成的内容

- 完成工作包 C 的正式实现与回归
- 完成工作包 D gate 判断，并确认当前不进入 D
- 完成工作包 F：新增副作用幂等性清单、`thread_id` 与恢复契约、`Send / HITL` 准入评审表
- `ArchitecturePlanner` 已补强模板接口覆盖度评分，并对 pattern 缺页增加负分
- `SubsystemPlanner` 已把模板接口明显不匹配的路径改为尽早降级到 `atomic_assembly`，并显式记录 `degrade_reason`
- `tests/test_phase3_workflow.py` 的真实多子系统回归矩阵已扩大到风阀 / CO2、送排风联动和三子系统联动
- 已重新正式运行 `phase6_real_query_suite`，结果仍为 `12/12 passed`
- 已重新正式运行 `phase6_retrieval_eval`，结果仍为 `healthy=12`
- 围绕剩余 3 条 `exhaust_fan_control` 单点 `asset_gap` 做了最小资产侧闭环
- 在 source flow 存在“排风机页复用送风机标准控制子流程实例”的情况下，正式补出 `exhaust_fan_control` alias 模板资产
- 正式 pattern library 已刷新，`subflow_template_count` 已从旧状态增加到 `8`
- `phase6_retrieval_eval` 已重新正式运行并收敛到 `healthy=12`
- 对两个关键排风机场景做了最小 workflow recheck，结果均稳定 direct pass

### 2.3 本轮具体改动点

- `agents/architecture_planner.py`
  - 增加模板 `imports / exports / 端口覆盖度` 加权
  - 增加 system pattern 的子系统缺页 / mode 缺页负分
- `agents/subsystem_planner.py`
  - 增加模板接口分析
  - 当模板输入/输出口覆盖不足时，直接降级到 `atomic_assembly`
  - 将 mismatch 信息收口到 `degrade_reason` 与 `template_binding` 元数据
- `tests/test_phase3_architecture_planner.py`
  - 增加模板接口覆盖度排序回归测试
- `tests/test_phase3_subsystem_planner.py`
  - 增加模板输入/输出接口不足时的早降级回归测试
- `tests/test_phase3_workflow.py`
  - 扩大真实多子系统回归矩阵
- `utils/ahu_knowledge_builder.py`
  - 增加排风机页复用送风机模板时的 alias template 抽取逻辑
- `tests/test_phase2_ahu_knowledge_builder.py`
  - 增加排风机 alias 资产回归测试
- `tests/test_phase6_retrieval_eval_contract.py`
  - 调整 contract test，使其不再绑定旧的实时资产状态
- 新增 `tests/test_phase6_readiness_contract.py`
  - 对 Phase 6 readiness 文档覆盖面与“主链仍未接 checkpointer / thread_id、POC 已接入”这一事实加轻量护栏
- `AHU程序/pattern_library/subflow_templates.json`
  - 已包含 `exhaust_fan_control` alias 模板资产
- `AHU程序/pattern_library/manifest.json`
  - 已反映新的模板计数
- 新增 `docs/ahu/Phase6_副作用幂等性清单.md`
- 新增 `docs/ahu/Phase6_thread_id与恢复契约.md`
- 新增 `docs/ahu/Phase6_Send_HITL准入评审表.md`

### 2.4 当前已验证稳定的结果

- 正式 `phase6_real_query_suite` 仍保持 `12/12 passed`
- 正式 `phase6_retrieval_eval` 已达到 `healthy=12`
- `tests.test_phase3_workflow` 已在扩大后的真实多子系统矩阵下通过
- `tests.test_phase3_architecture_planner`、`tests.test_phase3_subsystem_planner` 已覆盖工作包 C 新增逻辑并通过
- `tests.test_phase6_readiness_contract` 已覆盖工作包 F 的文档与代码边界事实
- 6 条 golden case 的检索命中率没有回退
- 3 条排风机关联 case 不再出现 `asset_gap`
- 针对 `reject_exhaust_fan_only` 与 `reject_supply_and_exhaust_fan` 的补验均为 `passed`

---

## 3. 未完成内容

### 3.1 仍未开始的正式工作包

- 工作包 D：未正式实现，且当前 gate 判断为“不进入”
- 工作包 F：已正式实现

### 3.2 本轮仍然保持不动的正式边界

- 不改 `workflow.py` / `workflow_trace.py` 正式主链拓扑
- 不把 `Send`、`interrupt`、`checkpointer`、`persistence` 正式接入主链
- 不提前做完整 `Send fan-out / fan-in` 改造
- 不做与第六阶段当前目标无关的扩面重构

### 3.3 当前真正还剩下的事项

- 若未来出现新的稳定失败桶，再重新评估是否重开工作包 D
- 若进入 Phase 7，应以现有准入包为起点做最小工程改造，而不是重新做摸底文档

---

## 4. 稳定项与波动项

### 4.1 稳定项

- `phase6_real_query_suite` 正式摘要是稳定绿态
- `phase6_retrieval_eval` 正式摘要是稳定绿态
- 工作包 C 相关代码回归与 Phase 3 多子系统回归已通过
- 工作包 F 文档与 readiness contract 已落地
- 排风机 alias 资产已经进入正式 pattern library
- 关键排风机场景的 targeted recheck 通过

### 4.2 有历史波动但本轮未判为回归的项

- `tests.test_phase3_workflow` 中有一条真实检索集成用例历史上曾出现过一次波动，曾单独复跑通过
- 该项更像检索 / 相似度分数波动，不宜在未复现前误判为确定性回归
- 因此本轮没有重跑整套 `tests.test_phase3_workflow`，而是采用关键排风机场景的最小补验来确认本轮改动未打坏现有行为
- 截至 2026-04-17 的最新结论：未复现；当前仍按“历史单次波动、未形成稳定失败桶”处理，不重开第六阶段工作包 D

---

## 5. 下次开工建议

### 5.1 建议的读取顺序

1. 先读本文件，快速恢复阶段边界和当前结论
2. 再读 `docs/ahu/第六阶段实施清单.md`，仅用于回看原任务拆包背景
3. 再读 `docs/ahu/Phase6_副作用幂等性清单.md`、`docs/ahu/Phase6_thread_id与恢复契约.md`、`docs/ahu/Phase6_Send_HITL准入评审表.md`
4. 最后读最新正式摘要与 retained 产物，确认当前工作区没有被新的运行覆盖

### 5.2 建议的判断原则

- 先看正式摘要，不要先重跑全量脚本
- 如果只是承接现状，优先复用 retained/golden 与最新 summary
- 只有在正式产物与代码状态不一致时，才做最小必要验证
- 不要再把“是否进入工作包 C”或“是否先做 F”当作未决问题；当前真正的选择点已经变成“Phase 7 先做哪块最小工程改造”

---

## 6. 下次可直接承接的输入产物

### 6.1 核心说明文件

- `docs/ahu/第六阶段当前状态交接.md`
- `docs/ahu/第六阶段实施清单.md`
- `AHU程序/phase6_real_query_cases.json`
- `docs/ahu/Phase6_副作用幂等性清单.md`
- `docs/ahu/Phase6_thread_id与恢复契约.md`
- `docs/ahu/Phase6_Send_HITL准入评审表.md`

### 6.2 当前正式 real query suite 产物

- `outputs/phase6_real_query_suite/suite_20260417_103517_951405/phase6_real_query_suite_summary.json`
- `outputs/phase6_real_query_suite/_retained/latest_green_run.json`
- `outputs/phase6_real_query_suite/_retained/golden/golden_supply_fan_standard_v1.json`
- `outputs/phase6_real_query_suite/_retained/golden/golden_fan_chw_coupled.json`
- `outputs/phase6_real_query_suite/_retained/golden/golden_fan_dx_coupled.json`

固定 3 条真实 trace 证据路径：

- `outputs/phase6_real_query_suite/suite_20260417_103517_951405/workflow_traces/workflow_trace_20260417_103540_891655`
- `outputs/phase6_real_query_suite/suite_20260417_103517_951405/workflow_traces/workflow_trace_20260417_103600_366901`
- `outputs/phase6_real_query_suite/suite_20260417_103517_951405/workflow_traces/workflow_trace_20260417_103620_265872`

### 6.3 当前正式 retrieval eval 产物

- `outputs/phase6_retrieval_eval/eval_20260417_103315_704147/phase6_retrieval_eval_summary.json`
- `outputs/phase6_retrieval_eval/_retained/latest_green_run.json`

### 6.4 本轮补验产物

- `outputs/phase6_real_query_suite_rechecks/recheck_20260416_223302_896837/phase6_targeted_recheck_summary.json`

### 6.5 当前正式资产库产物

- `AHU程序/pattern_library/subflow_templates.json`
- `AHU程序/pattern_library/system_patterns.json`
- `AHU程序/pattern_library/manifest.json`
- `AHU程序/flows_20260206160555.json`

---

## 7. 一句话交接结论

第六阶段到当前为止，A/B/E、工作包 C 与工作包 F 已正式收口，当前没有新的高频失败桶推动进入 D；排风机 `exhaust_fan_control` 资产缺口已闭环，规划层已补齐接口覆盖评分与模板 mismatch 早降级，正式检索与正式真实 query 结果仍为绿色；后续若继续推进，重点应转向 Phase 7 的最小工程改造，而不是重复做阶段摸底。

