**签收口径**
下面按签收优先级给整改清单。`P0` 是签收阻塞项，未完成前不建议把第二阶段判定为“通过”；`P1` 是强烈建议同轮补齐；`P2` 是文档与资产治理收口。

**P0 阻塞项**
- `空计划不能通过验收`：当前空 `execution_plan` 会一路走到 `verification=passed`。整改点放在 [planning_agent.py](D:/yjsproject/midea/agents/planning_agent.py#L363) 和 [verifier_agent.py](D:/yjsproject/midea/agents/verifier_agent.py#L326)。最小要求是让“规划失败/空计划/零节点编译产物”进入 `retryable_error`，不能再被判定为 `passed`。测试补到 [test_phase1_workflow.py](D:/yjsproject/midea/tests/test_phase1_workflow.py)。
- `修正 compiled wire 目标端口校验`：当前 [verifier_agent.py](D:/yjsproject/midea/agents/verifier_agent.py#L284) 用目标节点 `outputs` 校验 wire 目标 `port`，应改为对照目标节点 `inputs`。新增回归测试：目标节点 `inputs=1`、wire 指向 `port=2` 时，`verify()` 必须返回 `retryable_error`。

**P1 强烈建议本轮补齐**
- `子流程模板主键补拓扑签名`：当前 [ahu_knowledge_builder.py](D:/yjsproject/midea/utils/ahu_knowledge_builder.py#L175) 的 `template_id` 只看名称和 IO，和实施清单 [工作流V2第二阶段实施清单.md](工作流V2第二阶段实施清单.md#L413) 不一致。应把内部 body 的稳定拓扑摘要纳入签名，同时继续排除随机 `id`、坐标、注释。验收标准：同名同 IO 但不同内部拓扑的模板，`template_id` 必须不同；同拓扑仅随机 ID 不同，`template_id` 必须相同。
- `修复 dependency_module_types 抽取`：当前 [ahu_knowledge_builder.py](D:/yjsproject/midea/utils/ahu_knowledge_builder.py#L214) 只收集 `subflow:` 依赖，真实模板里的 `compare/pid/switch/constInput` 都丢了。应基于 `internal_flow_objects` 提取真实依赖类型，并排除 `tab/comment/quote` 这类非功能对象。验收标准：至少有一条真实模板资产产出非空 `dependency_module_types`。
- `让 write_assets_to_chroma 真正可重复构建`：当前 [ahu_knowledge_builder.py](D:/yjsproject/midea/utils/ahu_knowledge_builder.py#L517) 只有 `upsert`，不会删掉过期 ID。应在写入前后对目标 collection 做 stale ID 清理。验收标准：第一次写 `t1,t2`，第二次只写 `t1` 后，collection 内只剩 `t1`。
- `补 Planner 的 Phase 2 行为验收`：实施清单要求显式写出“先参考 `system_patterns`、优先复用 `subflow_templates`、模板不足再退化到 `atomic_modules`” [工作流V2第二阶段实施清单.md](工作流V2第二阶段实施清单.md#L584)。当前 [planning_agent.py](D:/yjsproject/midea/agents/planning_agent.py#L109) 和测试 [test_phase2_bundle_consumers.py](D:/yjsproject/midea/tests/test_phase2_bundle_consumers.py#L128) 只证明了接线，不证明真实规划行为。建议新增 [tests/test_phase2_planning_bundle.py](D:/yjsproject/midea/tests/test_phase2_planning_bundle.py)；测试必须走 `PlanningAgent.plan()` 真路径，用可控 fake LLM，而不是直接 stub `plan/_generate_plan`。

**P2 收口项**
- `文档同步降温`：更新 [工作流总结文档.md](D:/yjsproject/midea/工作流总结文档.md#L23) 和 [工作流演进记录.md](D:/yjsproject/midea/工作流演进记录.md#L8)，把第二阶段状态从“已完成”改成“条件通过/待整改关闭”；同时明确 `system_patterns` 只是 prompt hint 基线，不是更高层规划交付物。
- `资产复现性决策`：当前构建脚本依赖 `AHU程序/flows_*.json`，但这些文件未进入 git 跟踪。要么把它们纳入版本管理，要么在文档里明确它们是外部输入前置条件，否则“干净克隆可复现”仍不成立。

**建议执行顺序**
- 先做 `P0` 两项，再跑全量回归。
- 然后做 `P1` 的 `template_id`、`dependency_module_types`、`write_assets_to_chroma`。
- 最后补 Planner 行为测试和文档同步。

**整改完成后的验收命令**
```powershell
conda activate midea
python -m unittest discover -s tests -p "test_phase2*.py"
python -m unittest discover -s tests -p test_phase1_workflow.py
python -m compileall agents utils scripts tests workflow.py workflow_trace.py
python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_acceptance
python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_acceptance_write --write-chroma --persist-dir outputs/test_tmp/chroma_phase2_acceptance
```

**最终签收标准**
- `通过`：`P0` 全部关闭，新增回归全部通过，现有 `17 + 9` 回归不退化。
- `条件通过`：`P0` 关闭，`P1` 仍有明确遗留且文档已同步。
- `不通过`：任一 `P0` 未关闭。

如果你要，我下一步可以直接把这份清单转成“可执行任务单”，按文件拆成逐项修改方案。
