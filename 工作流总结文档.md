# 工作流总结文档

> 最后核对时间：2026-04-07  
> 核对范围：`workflow.py`、`workflow_trace.py`、`agents/*.py`、`utils/*.py`、`tests/test_phase1_workflow.py`、`tests/test_phase2_*.py`、`tests/test_phase3_*.py`  
> 定位：描述当前代码真实行为；变更时间线与决策见 [工作流演进记录](工作流演进记录.md)。

## 1. 当前结论

- 当前系统是 `Phase 3 首版已完成` 的 LangGraph 工作流，已经从单体 `PlanningAgent` 升级为分层规划主链。
- 当前正式主链为：

```text
user_query
  -> analysis
  -> retrieval
  -> architecture_planning
  -> subsystem_planning
  -> global_assembly
  -> coding
  -> verification
  -> END
```

- 当前阶段判断：`Phase 1 已稳定`、`Phase 2 已通过`、`Phase 3 首版已完成并建立回归基线`。
- 当前未接入：`RepairRouter / RepairAgent`、人工澄清/中断节点、`Send` 并行子系统派发、reducer fan-in、完整 `internal_flow_objects` body 编译、Phase 2 collections 正式写入目标 `chroma_db` 的策略。

## 2. 主链与节点契约

| 节点 | 职责 | 核心输入 | 核心输出 | 当前边界 |
|:---|:---|:---|:---|:---|
| `analysis` | 需求理解与 Phase 3 需求结构化 | `user_query` | `analysis_result`、`requirement_spec` | 保留原有 `retrieval_plan + scenario_analysis`，新增 `requirement_spec` 投影 |
| `retrieval` | 检索原子模块与 Phase 2 资产 | `analysis_result.retrieval_plan` | `retrieval_bundle`、`retrieval_context` | `retrieval_context` 仍是兼容视图 |
| `architecture_planning` | 生成系统骨架与拆解结果 | `requirement_spec`、`retrieval_bundle` | `decomposition_result`、`architecture_plan` | 当前为规则优先的系统级规划层 |
| `subsystem_planning` | 顺序生成子系统局部 IR | `requirement_spec`、`decomposition_result`、`architecture_plan`、`retrieval_bundle` | `subsystem_plan_map` | 首版按 `planning_order` 顺序执行，不使用 `Send` |
| `global_assembly` | 组装多页签全局 Graph IR | `architecture_plan`、`subsystem_plan_map`、`retrieval_bundle` | `assembled_graph_ir`、兼容 `execution_plan` | 回填 `assembled_graph_ir.source_execution_plan` 以兼容当前 `VerifierAgent` |
| `coding` | 确定性编译 | `assembled_graph_ir`、`retrieval_bundle/retrieval_context` | `compiled_artifact`、`generated_code` | 真实输入仍是 `assembled_graph_ir` |
| `verification` | 结构验收 | `assembled_graph_ir`、`compiled_artifact` | `verification_report`、`final_output` | 只验收，不修复 |

- `workflow.py` 与 `workflow_trace.py` 使用同一套 `Phase 3` 线性拓扑。
- `workflow_trace.py` 只增加 trace 记录、耗时统计、异常/成功落盘与 `final_output.workflow_trace` 回写，不改变主链拓扑。
- 当前仍未使用 LangGraph 的条件分支、并行派发、reducer、中断/人工澄清能力。

## 3. 正式入口与运行方式

### Python 入口

```python
from workflow import run_workflow
result = run_workflow(user_query)
```

```python
from workflow_trace import run_workflow
result = run_workflow(user_query)
```

### 命令行

```powershell
conda activate midea
python workflow.py
```

```powershell
conda activate midea
python workflow_trace.py
```

### Trace 版本附加行为

- 记录节点输入/输出、耗时、顶层状态字段变更。
- 成功或失败都落盘到 `outputs/workflow_trace_时间戳/`。
- 成功时把 trace 路径写入 `final_output.workflow_trace`。

## 4. `WorkflowState` 真实字段

| 字段 | 主链是否使用 | 说明 |
|:---|:---:|:---|
| `user_query` | 是 | 用户自然语言需求 |
| `analysis_result` | 是 | Analysis 输出 |
| `requirement_spec` | 是 | Phase 3 结构化需求对象 |
| `retrieval_bundle` | 是 | Retrieval 正式主输出 |
| `retrieval_context` | 是 | `build_legacy_retrieval_context(retrieval_bundle)` 派生的兼容视图 |
| `decomposition_result` | 是 | ArchitecturePlanner 输出的页签/子系统拆解结果 |
| `architecture_plan` | 是 | 系统级骨架、模板优先级、全局命名/布局策略 |
| `subsystem_plan_map` | 是 | 每个子系统的局部 IR |
| `execution_plan` | 是 | 兼容投影；由 `GlobalAssembler` 回填的 legacy execution plan |
| `assembled_graph_ir` | 是 | GlobalAssembler 输出的全局 Graph IR |
| `compiled_artifact` | 是 | Coding 输出的编译结果包 |
| `verification_report` | 是 | Verifier 输出的结构验收报告 |
| `generated_code` | 是 | 兼容字段，等于 `compiled_artifact["json_text"]` |
| `execution_result` | 否 | 历史预留 |
| `validation_result` | 否 | 历史预留 |
| `debug_history` | 否 | 历史预留 |
| `retry_count` | 否 | 为未来 Repair 闭环预留 |
| `current_step` | 是 | 每个节点都会更新 |
| `next_step` | 否 | 当前未使用 |
| `final_output` | 是 | Verifier 写入最终摘要；Trace 入口追加 trace 信息 |

## 5. 核心数据契约

| 对象 | 生产者 | 核心字段 | 契约说明 |
|:---|:---|:---|:---|
| `requirement_spec` | `AnalysisAgent` / `build_requirement_spec()` | `schema_version`、`system_type`、`scenario_summary`、`subsystems`、`signals`、`required_pages`、`global_modes` | Phase 3 结构化需求入口 |
| `retrieval_bundle` | `RetrievalAgent` | `atomic_modules`、`subflow_templates`、`system_patterns`、`style_guides`、`metadata` | Phase 2/3 正式检索契约 |
| `decomposition_result` | `ArchitecturePlanner` | `pages`、`subsystem_descriptors`、`shared_signal_registry`、`template_needs`、`planning_order` | 子系统边界与规划顺序 |
| `architecture_plan` | `ArchitecturePlanner` | `pages`、`subsystem_slots`、`global_constraints`、`naming_strategy`、`layout_strategy`、`pattern_bindings` | 系统骨架层正式输出 |
| `subsystem_plan_map` | `SubsystemPlanner` | `implementation_mode`、`template_binding`、`node_instances`、`edges`、`imported_signals`、`exported_signals` | 子系统局部 IR |
| `execution_plan` | `GlobalAssembler` / `build_legacy_execution_plan()` | `goal`、`nodes[*]`、`connections[*]` | 仅为兼容 `VerifierAgent` 与旧语义投影 |
| `assembled_graph_ir` | `GlobalAssembler` | `pages`、`subflow_definitions`、`node_instances`、`edges`、`signal_registry`、`layout_hints`、`unresolved_items`、`source_execution_plan` | 当前编译器与验收器的真实输入 |
| `compiled_artifact` | `CodingAgent / JSONCompiler` | `json_text`、`flow_objects`、`id_map`、`layout_map`、`compile_report` | 最终真实产物 |
| `verification_report` | `VerifierAgent` | `status`、`repair_scope`、`issue_summary`、`issues`、`warnings`、`metrics` | 当前验收出口；不是修复入口 |

- `system_patterns` 在 Phase 3 中不再只是 prompt hint；`ArchitecturePlanner` 会显式记录 `pattern_bindings`。
- `internal_flow_objects` 当前只进入规划与追踪上下文，不承诺完整编译进最终平台 JSON。
- 当前 AHU 资产构建仍依赖 `AHU程序/flows_*.json` 作为前置输入；若这些源 flow 未进入版本管理，则“干净克隆即可复现”不成立。

## 6. 当前已完成与未完成

### 已完成

- Phase 3 正式主链：`analysis -> retrieval -> architecture_planning -> subsystem_planning -> global_assembly -> coding -> verification -> END`
- `requirement_spec`、`decomposition_result`、`architecture_plan`、`subsystem_plan_map` 正式入链
- `ArchitecturePlanner`、`SubsystemPlanner`、`GlobalAssembler` 已落地
- `GlobalAssembler` 已回填 `execution_plan` 与 `assembled_graph_ir.source_execution_plan`
- `ArchitecturePlanner` 已修正模板匹配排序，优先命中 `template_role == subsystem_type` 的真实模板
- `GlobalAssembler` / `VerifierAgent` 已收紧共享信号验收：缺少真实导出方或存在歧义时不再静默 `passed`
- `CodingAgent` 继续以 `assembled_graph_ir` 为真实输入，无需重写
- `VerifierAgent` 兼容路径保持可用，未因 Phase 3 主链切换而退化
- `workflow.py` / `workflow_trace.py` 共用一套 Phase 3 线性拓扑与初始状态
- 已补齐 `tests/test_phase3_*.py` 自动化回归基线

### 未完成

- `RepairRouter / RepairAgent`
- 人工澄清与中断节点
- `Send` 并行子系统派发与 reducer fan-in
- 完整 `internal_flow_objects` body 编译
- Phase 2 collections 正式写入目标 `chroma_db`
- 真实 AHU 场景下的系统级规划质量验证

### 未接入主链的历史模块

- `agents/planning_agent.py`：保留为旧接口/历史兼容语义，不再是正式主链规划节点。
- `agents/assembly_agent.py`：保留为旧兼容装配路径，正式主链已切到 `GlobalAssembler`。
- `tools/execution_tool.py`、`agents/validation_agent.py`、`agents/debugging_agent.py`：仍偏旧契约，未与当前 Graph IR 主链对齐。

## 7. 验收与测试事实

### 已覆盖文件

- `tests/test_phase1_workflow.py`
- `tests/test_phase2_retrieval_bundle.py`
- `tests/test_phase2_ahu_knowledge_builder.py`
- `tests/test_phase2_bundle_consumers.py`
- `tests/test_phase2_planning_bundle.py`
- `tests/test_phase2_retrieval_agent.py`
- `tests/test_phase3_requirement_spec.py`
- `tests/test_phase3_architecture_planner.py`
- `tests/test_phase3_subsystem_planner.py`
- `tests/test_phase3_global_assembler.py`
- `tests/test_phase3_workflow.py`

### 已覆盖能力

- Phase 1 Graph IR 装配、确定性编译、结构验收
- `retrieval_bundle -> retrieval_context` 兼容视图
- Planning / Assembly / Coding 对 bundle 的消费链路
- AHU 子流程模板与系统模式资产构建
- `requirement_spec` 适配
- `ArchitecturePlanner` 对 `system_patterns` 的显式绑定
- `SubsystemPlanner` 的模板复用与 atomic fallback
- `GlobalAssembler` 的多页签装配、共享信号连接与 `source_execution_plan` 回填
- `workflow.py` / `workflow_trace.py` 的 Phase 3 主链运行
- 真实 `AnalysisAgent + RetrievalAgent + 临时 Chroma + AHU程序/flows_*.json` 的 Phase 3 集成回归
- 共享信号缺少真实导出方、共享信号多导出方歧义的负向验收路径

### 已执行命令

```powershell
conda activate midea
python -m compileall workflow.py workflow_trace.py agents utils tests
python -m unittest discover -s tests -p "test_phase3*.py"
python -m unittest discover -s tests -p "test_phase2*.py"
python -m unittest discover -s tests -p "test_phase1_workflow.py"
```

### 当前结果

- `13` 个 Phase 3 测试通过
- `22` 个 Phase 2 测试通过
- `11` 个 Phase 1 回归测试通过
- `compileall` 通过

### 当前未覆盖

- 在线 LLM 质量
- 真实 AHU 项目上的召回质量
- 真实 AHU 项目上的系统级规划质量
- Repair 闭环与人工澄清流程

## 8. 当前判断与后续优先级

- 当前系统不是完整 V2；它是 `Phase 1 + Phase 2 + Phase 3 首版分层规划` 的稳定基线。
- `CodingAgent` 的真实输入仍然是 `assembled_graph_ir`，不是 `execution_plan`。
- `generated_code` 不是主产物，只是兼容字段。
- `VerifierAgent` 仍只做结构验收，不做自动修复。
- 当前主链已经具备分层规划骨架，但系统级质量仍主要依赖规则投影与资产覆盖率。

| 优先级 | 事项 | 说明 |
|:---|:---|:---|
| P0 | Phase 3 质量迭代 | 提高 `ArchitecturePlanner` / `SubsystemPlanner` 的 AHU 规划质量 |
| P1 | AHU 资产质量与检索质量 | 提高模板覆盖率、system pattern 提取质量与 Chroma 召回质量 |
| P1 | Phase 2 collections 落库策略 | 明确是否以及如何写入目标 `chroma_db` |
| P1 | Phase 4 Repair 闭环 | 统一修复对象与 repair scope |
| P2 | `Send` 并行子系统派发 | 在契约稳定后再引入并行与 fan-in |
