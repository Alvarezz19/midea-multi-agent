# 工作流总结文档

> 最后核对时间：2026-03-31
> 核对范围：`workflow.py`、`workflow_trace.py`、`agents/*.py`、`utils/graph_ir.py`、`tests/test_phase1_workflow.py`
> 说明：本文档面向“后续新对话的预读材料”，优先描述当前代码真实行为，其次再补充设计意图、演进背景与未接入能力。

## 1. 一句话结论

当前正式工作流已经从 V1 的“四节点线性链路”升级为一个 **Phase 1 Graph IR 编译流水线**：

```text
user_query
  -> Analysis Agent
  -> Retrieval Agent
  -> Planning Agent
  -> Assembly Agent
  -> Coding Agent(JSONCompiler)
  -> Verifier Agent
  -> END
```

它的当前目标不是直接“让 LLM 生成最终平台 JSON”，而是先把自然语言需求变成 `execution_plan`，再升级成强结构 `assembled_graph_ir`，最后由确定性编译器输出平台 JSON，并在结束前做一次结构验收。  
按 `AHU程序/LangGraph工作流V2架构设计.md` 第 1 阶段的要求，当前实现已达标。

---

## 2. 先记住这几个最重要的事实

### 2.1 当前正式主链已经是 6 个节点

`workflow.py` 当前实际注册并启用的节点是：

1. `analysis`
2. `retrieval`
3. `planning`
4. `assembly`
5. `coding`
6. `verification`

对应边为：

1. `analysis -> retrieval`
2. `retrieval -> planning`
3. `planning -> assembly`
4. `assembly -> coding`
5. `coding -> verification`
6. `verification -> END`

当前仍然没有条件路由、Repair 闭环或人工澄清节点；这次重构的重点是先把中间产物和验收立住。

### 2.2 当前已经存在强结构中间产物 `assembled_graph_ir`

这是本次 Phase 1 的核心变化。  
现在主链不再让 `CodingAgent` 直接消费 `execution_plan` 生成最终 JSON，而是先由 `AssemblyAgent` 把 `execution_plan + retrieval_context` 装配成 `assembled_graph_ir`。

这意味着：

1. `execution_plan` 仍然是 Planner 产出的弱计划层
2. `assembled_graph_ir` 是编译器和验收器使用的强结构层
3. `compiled_artifact` 是编译后的结果包

### 2.3 `CodingAgent` 现在本质上是确定性编译器

当前 `agents/coding_agent.py` 虽然还叫 `CodingAgent`，但职责已经收窄为 `JSONCompiler`：

1. 输入是 `assembled_graph_ir + retrieval_context`
2. 不再理解自然语言需求
3. 不再直接消费 `analysis_result`
4. 输出 `compiled_artifact`

为了兼容旧接口，它仍会同步写入：

- `generated_code = compiled_artifact["json_text"]`

也就是说，`generated_code` 仍然存在，但它已经被降格为兼容字段。

### 2.4 `VerifierAgent` 已经正式接入主链

这是 Phase 1 的第二个关键变化。  
当前系统在 END 前会做一次轻量、规则优先的结构验收，输出 `verification_report`。

当前 Verifier 的职责不是业务语义修复，而是：

1. 检查 IR 结构是否自洽
2. 检查编译产物是否是合法 JSON
3. 检查 `tab / subflow / 普通节点 / wires / z`、端口索引、必需输入这些关系是否成立
4. 给出结构化错误、警告和统计指标

### 2.5 当前主链已经具备承载 AHU 复杂结构的最小骨架，但还没有进入 V2 的分层规划阶段

现在系统已经能表达：

1. `tab/page`
2. `subflow_definition`
3. `subflow实例`
4. 普通节点实例
5. 端口级边
6. 编译报告
7. 验收报告

但还没有进入 V2 方案中的：

1. 三层检索包 `retrieval_bundle`
2. `ArchitecturePlanner`
3. `SubsystemPlanner`
4. `GlobalAssembler`
5. `RepairRouter / RepairAgent`

因此当前系统应被视为：  
**V2 方向下的 Phase 1 基础设施落地版，而不是完整 V2。**

---

## 3. 与 LangGraph 的关系

当前实现仍然使用 LangGraph `StateGraph`，而且完全遵循官方 Graph API 的基本模式：

1. 定义共享状态 `WorkflowState`
2. 添加节点
3. 添加边
4. `compile()`
5. `invoke(initial_state, config=...)`

当前工作流仍然是单链顺序执行，没有用到：

1. `add_conditional_edges()`
2. `Send` 并行分发
3. reducer 合并子图结果
4. 中断/人工澄清

这些能力对应的是 V2 后续阶段，而不是当前 Phase 1。

参考：

- LangGraph Graph API overview: https://docs.langchain.com/oss/python/langgraph/graph-api
- LangGraph Subgraphs: https://docs.langchain.com/oss/python/langgraph/use-subgraphs

---

## 4. 当前正式入口与运行方式

### 4.1 正式入口

当前正式入口仍然是：

```python
from workflow import run_workflow
result = run_workflow(user_query)
```

`run_workflow()` 的执行顺序：

1. 调 `create_workflow()`
2. `compile()`
3. 初始化 `WorkflowState`
4. `app.invoke(initial_state, config=...)`
5. 返回完整最终状态

### 4.2 调试入口

调试入口是：

```python
from workflow_trace import run_workflow
result = run_workflow(user_query)
```

`workflow_trace.py` 已经同步到 Phase 1 主链，会额外：

1. 记录每个节点输入输出
2. 记录节点耗时
3. 记录顶层状态变更字段
4. 成功或失败都会落盘到 `outputs/workflow_trace_时间戳/`
5. 成功时把 trace 路径写进 `result["final_output"]["workflow_trace"]`

### 4.3 命令行运行方式

在新终端中：

```powershell
conda activate midea
python workflow.py
```

或：

```powershell
conda activate midea
python workflow_trace.py
```

---

## 5. 当前 `WorkflowState` 真实字段

| 字段 | 当前是否在主链使用 | 说明 |
|:---|:---:|:---|
| `user_query` | 是 | 用户自然语言需求 |
| `analysis_result` | 是 | Analysis Agent 输出 |
| `retrieval_context` | 是 | Retrieval Agent 输出 |
| `execution_plan` | 是 | Planning Agent 输出 |
| `assembled_graph_ir` | 是 | Assembly Agent 输出的强结构 IR |
| `compiled_artifact` | 是 | Coding Agent 输出的编译结果包 |
| `verification_report` | 是 | Verifier Agent 输出的验收报告 |
| `generated_code` | 是 | 兼容字段，实际等于 `compiled_artifact.json_text` |
| `execution_result` | 否 | 历史预留，当前主链未使用 |
| `validation_result` | 否 | 历史预留，当前主链未使用 |
| `debug_history` | 否 | 历史预留，当前主链未使用 |
| `retry_count` | 否 | 为未来 Repair 闭环预留 |
| `current_step` | 是 | 每个节点都会更新 |
| `next_step` | 否 | 当前主链未使用 |
| `final_output` | 是 | Verifier 会填入最终摘要；Trace 入口会追加 trace 信息 |

---

## 6. 六个正式节点的真实职责

### 6.1 Analysis Agent

文件：`agents/analysis_agent.py`

职责不变：

1. 读取 `user_query`
2. 生成 `analysis_result`
3. 产出 `retrieval_plan + scenario_analysis + metadata`

它仍然是“需求理解层”，不是检索层，也不是编译层。

### 6.2 Retrieval Agent

文件：`agents/retrieval_agent.py`

职责不变：

1. 消费 `analysis_result.retrieval_plan`
2. 调 ChromaDB 做向量检索
3. 返回完整模块定义 `retrieval_context`

它仍然负责把模块定义完整返给下游，包括：

1. `parameters_schema`
2. `ports_definition`
3. `template_json`
4. `usage_guides`
5. `category`

### 6.3 Planning Agent

文件：`agents/planning_agent.py`

职责基本不变：

1. 输入 `user_query + retrieval_context + analysis_result`
2. 输出 `execution_plan`
3. 计划仍然是 `goal + nodes + connections`

当前它仍是“弱计划层”，还没有分裂成 V2 的系统级/子系统级规划器。

### 6.4 Assembly Agent

文件：`agents/assembly_agent.py`

这是 Phase 1 新增节点，职责是：

1. 消费 `execution_plan + retrieval_context`
2. 把 plan 组装成 `assembled_graph_ir`
3. 建立页面、子流程定义、节点实例、边、信号注册表等中间结构
4. 记录 `unresolved_items`

这是当前从“LLM 计划”走向“工程编译”的关键桥梁。

### 6.5 Coding Agent / JSONCompiler

文件：`agents/coding_agent.py`

当前它的真实职责已经变成：

1. 消费 `assembled_graph_ir`
2. 根据 `retrieval_context` 中的模板做确定性编译
3. 输出 `compiled_artifact`

`compiled_artifact` 目前包含：

1. `json_text`
2. `flow_objects`
3. `id_map`
4. `layout_map`
5. `compile_report`

### 6.6 Verifier Agent

文件：`agents/verifier_agent.py`

当前它做的是 Phase 1 结构验收，而不是 Repair：

1. 检查 IR 中节点作用域是否合法
2. 检查边引用和端口索引是否合法
3. 检查 `compiled_artifact.json_text` 是否可解析
4. 检查 `flow_objects` 中 `tab / subflow / wires / z` 的结构关系
5. 输出 `verification_report`

当前 `verification_report.status` 主要有：

1. `passed`
2. `retryable_error`

但当前主链还不会根据这个报告自动修复。

---

## 7. 当前主要数据契约

### 7.1 `execution_plan`

当前仍由 Planning Agent 输出，关键字段：

1. `goal`
2. `nodes[*].logic_id`
3. `nodes[*].module_type`
4. `nodes[*].parameters`
5. `connections[*].from_node`
6. `connections[*].from_port_index`
7. `connections[*].to_node`
8. `connections[*].to_port_index`

### 7.2 `assembled_graph_ir`

当前由 Assembly Agent 输出，核心对象：

1. `pages`
2. `subflow_definitions`
3. `node_instances`
4. `edges`
5. `signal_registry`
6. `layout_hints`
7. `unresolved_items`

这是当前第一阶段最重要的新契约。

### 7.3 `compiled_artifact`

当前由 Coding Agent 输出，关键字段：

1. `json_text`
2. `flow_objects`
3. `id_map`
4. `layout_map`
5. `compile_report`

它明确表达：

- 最终 JSON 文本
- 编译后的对象列表
- 逻辑 ID 到真实 ID 的映射
- 布局信息
- 编译统计与警告

### 7.4 `verification_report`

当前由 Verifier Agent 输出，关键字段：

1. `status`
2. `repair_scope`
3. `issue_summary`
4. `issues`
5. `warnings`
6. `metrics`

这是当前的“验收出口”，而不是修复入口。

---

## 8. `workflow.py` 与 `workflow_trace.py` 的区别

### 8.1 `workflow.py`

这是正式入口版本：

1. 使用 Phase 1 六节点主链
2. 返回完整状态
3. 不记录节点级 trace 文件

### 8.2 `workflow_trace.py`

这是调试增强版本：

1. 与 `workflow.py` 使用同一条 Phase 1 主链
2. 额外记录节点输入输出和耗时
3. 落盘 trace 结果
4. 在 `final_output.workflow_trace` 中返回 trace 路径

当前二者不再是“两套不同逻辑”，而是“同一条主链 + 是否落 trace”的区别。

---

## 9. 当前未接入主链的能力

### 9.1 Execution Tool

文件：`tools/execution_tool.py`

当前仍然是旧设计残留，更接近“执行 Python 代码的沙箱”，与现在“最终产物是平台 JSON”的事实不匹配。

### 9.2 Validation Agent

文件：`agents/validation_agent.py`

当前没有接入正式主链，而且假设的数据结构更偏旧版 `{nodes, wires}`，尚未适配当前 `compiled_artifact / verification_report` 的 Phase 1 现实。

### 9.3 Debugging Agent

文件：`agents/debugging_agent.py`

当前也没有接入正式主链，而且仍偏向“分析错误并返回修正后的 Python 代码”的旧语义。

### 9.4 结论

当前不要把 `execution / validation / debugging` 看成“已经实现只是没打开开关”，它们仍属于：

1. 历史方案残留
2. 未来 Repair 闭环的候选基础
3. 尚未与当前 Graph IR 主链对齐的模块

---

## 10. 当前验收与测试事实

本次 Phase 1 已经补了离线验收测试：

文件：`tests/test_phase1_workflow.py`

当前覆盖：

1. Graph IR 装配
2. 确定性编译
3. `Assembly -> Compiler` 的子流程定义/实例关系
4. 必需输入缺失的结构验收
5. 坏边引用下的编译容错与验收
6. 带 stub 的 LangGraph 端到端运行
7. `workflow_trace` 的失败落盘

已执行的命令：

```powershell
conda activate midea
python -m compileall workflow.py workflow_trace.py agents utils tests\test_phase1_workflow.py
```

```powershell
conda activate midea
python -m unittest discover -s tests -p test_phase1_workflow.py
```

结果：

1. 语法编译通过
2. 9 个 Phase 1 离线验收测试全部通过

说明：

- 当前验收重点是 `assembly -> coding -> verification` 这条新主干
- 集成测试中对 `analysis / retrieval / planning` 使用了 stub，以避免依赖在线 LLM 与本地 ChromaDB 环境

---

## 11. 当前最容易误解的点

### 11.1 误解一：现在已经是完整 V2

不是。  
当前只是把 V2 的第一阶段落地了：先立 Graph IR、Compiler、Verifier。

### 11.2 误解二：`CodingAgent` 还在直接把 plan 变成 JSON

不是。  
当前它已经先消费 `assembled_graph_ir`，再编译成 JSON。

### 11.3 误解三：`generated_code` 还是核心输出字段

不完全是。  
它现在是兼容字段，核心真实产物已经变成 `compiled_artifact`。

### 11.4 误解四：`VerifierAgent` 已经具备自动修复能力

不是。  
当前它只负责规则优先验收，还没有 RepairRouter 和 RepairAgent 闭环。

### 11.5 误解五：当前系统已经能直接规划复杂 AHU 多子系统工程

还不能完全做到。  
当前它已经具备承载复杂结构的最小编译骨架，但还没有进入三层检索和分层规划阶段。

---

## 12. 如果后续继续演进，优先关注哪里

### 12.1 Phase 2：扩展检索层次

优先看：

1. `agents/retrieval_agent.py`
2. `schemas/`
3. `chroma_db/`
4. `AHU程序/LangGraph工作流V2架构设计.md`

目标是把当前 `retrieval_context` 扩展成三层 `retrieval_bundle`。

### 12.2 Phase 3：拆 Planning

优先看：

1. `agents/planning_agent.py`
2. `agents/assembly_agent.py`
3. `utils/graph_ir.py`

目标是把当前单体 Planner 拆成：

1. `ArchitecturePlanner`
2. `SubsystemPlanner`
3. `GlobalAssembler`

### 12.3 Phase 4：接入 Repair 闭环

优先看：

1. `agents/verifier_agent.py`
2. `workflow.py`
3. `workflow_trace.py`
4. `agents/validation_agent.py`
5. `agents/debugging_agent.py`

但前提是先统一“修复对象”到底是：

1. `assembled_graph_ir`
2. `compiled_artifact`
3. 还是更上游的规划层

---

## 13. 推荐的新对话预读顺序

1. 本文档
2. `工作流演进记录.md`
3. `workflow.py`
4. `utils/graph_ir.py`
5. `agents/assembly_agent.py`
6. `agents/coding_agent.py`
7. `agents/verifier_agent.py`
8. `tests/test_phase1_workflow.py`

如果要排查节点级执行过程，再看：

9. `workflow_trace.py`

---

## 14. 最终总结

当前项目最准确的画像已经不是：

“一个四节点的 JSON 生成流水线”

而是：

**一个已经完成第一阶段重构的 LangGraph 工程编译式工作流：先做需求理解与模块检索，再做弱计划，接着装配成 Graph IR，之后确定性编译为平台 JSON，并在结束前做结构验收。**

它当前最明确的优点是：

1. 主链职责分层更清楚
2. 编译边界更清晰
3. 中间产物更强结构
4. 已经具备最小可重复验收能力

它当前最明确的限制也同样清楚：

1. 还没有三层检索
2. 还没有分层规划
3. 还没有 Repair 闭环
4. `generated_code` 等旧命名仍保留

因此，当前系统应被视为：

**V2 路线下已经完成 Phase 1 落地、满足第 1 阶段验收承诺、并通过离线验收的工作流基础版本。**
