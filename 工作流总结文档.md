# 工作流总结文档

> 最后核对时间：2026-04-15  
> 核对范围：`workflow.py`、`workflow_trace.py`、`agents/*.py`、`utils/*.py`、`scripts/build_phase2_retrieval_indexes.py`、`scripts/poc_phase4_hitl.py`、`scripts/run_phase4_query_smoke.py`、`scripts/run_phase5_query_suite.py`、`requirements.txt`、`tests/test_phase1_workflow.py`、`tests/test_phase2_*.py`、`tests/test_phase3_*.py`、`tests/test_phase4_*.py`、`tests/test_phase5_workflow_query_suite.py`、`tests/test_runtime_versions.py`  
> 定位：描述当前代码真实行为；变更时间线与决策见 [工作流演进记录](工作流演进记录.md)。

## 1. 当前结论

- 当前系统是 `Phase 5 前四批已完成` 的 LangGraph 工作流，正式主链保持为分层规划 + Repair 闭环主链，第四阶段主链拓扑没有变化。
- 前三阶段遗留问题整改已收口；第四阶段已完成 Repair 闭环与第一轮整改；第五阶段前三批已补齐共享信号归属前移、模板选择解释、planning/assembly/compile 结构化 `repair_payload`、`ambiguous_shared_signal` 的“有限自动修复 + 稳定 reject”闭环；第四批已补齐 Phase 5 deterministic query suite 与 trace 摘要扩展。
- 当前正式主链为：

```text
user_query
  -> analysis
  -> retrieval
  -> architecture_planning
  -> subsystem_planning
  -> global_assembly
  -> coding
  -> verification
  -> repair_router
  -> (accept -> END) / (repair_agent -> subsystem_planning | global_assembly | coding)
```

- 当前阶段判断：`Phase 1 已稳定`、`Phase 2 已通过并冻结正式资产链口径`、`Phase 3 首版已完成并建立回归基线`、`Phase 4 首版 Repair 闭环已完成并完成第一轮问题整改`、`Phase 5 前四批已完成并建立 deterministic query suite 与 Phase 5 trace 口径`。
- 共享接口当前先统一归类为 `shared_signal / external_input / external_parameter / external_command / subsystem_output`，后续 planner / assembler / verifier / repair 共用这一层语义。
- 当前未接入正式主链：人工澄清/中断节点、`Send` 并行子系统派发、reducer fan-in、完整 `internal_flow_objects` body 编译。

## 2. 主链与节点契约

| 节点 | 职责 | 核心输入 | 核心输出 | 当前边界 |
|:---|:---|:---|:---|:---|
| `analysis` | 需求理解与结构化 | `user_query` | `analysis_result`、`requirement_spec` | `analysis_result` 保持 compat；`requirement_spec` 是正式规划入口 |
| `retrieval` | 检索原子模块与 AHU 资产 | `analysis_result.retrieval_plan` | `retrieval_bundle`、兼容 `retrieval_context` | `retrieval_bundle` 是正式主输出 |
| `architecture_planning` | 系统骨架、页签、共享信号约束 | `requirement_spec`、`retrieval_bundle` | `decomposition_result`、`architecture_plan` | 规则优先的系统级规划层 |
| `subsystem_planning` | 逐个子系统生成局部 IR | `requirement_spec`、`decomposition_result`、`architecture_plan`、`retrieval_bundle` | `subsystem_plan_map` | 首版按 `planning_order` 顺序执行，不使用 `Send` |
| `global_assembly` | 组装多页签全局 Graph IR | `architecture_plan`、`subsystem_plan_map`、`retrieval_bundle` | `assembled_graph_ir`、兼容 `execution_plan` | `execution_plan` 只是 compat 投影 |
| `coding` | 确定性编译 | `assembled_graph_ir`、`retrieval_bundle/retrieval_context` | `compiled_artifact`、`generated_code` | 真实输入仍是 `assembled_graph_ir` |
| `verification` | 结构验收 | `assembled_graph_ir`、`compiled_artifact` | `verification_report`、`final_output` | 只验收，不直接修复 |
| `repair_router` | 条件路由与预算裁决 | `verification_report`、`retry_budget`、`retry_counts_by_scope` | `route_decision` | 按 scope 预算决定 `accept / *_repair / reject` |
| `repair_agent` | 规则优先局部修补 | `route_decision`、`verification_report`、对应上游主对象 | `repair_context`、`repair_history`、被修补主对象 | 只修补真正会被下一跳消费的上游对象 |

- `workflow.py` 与 `workflow_trace.py` 共用同一套 Phase 4 拓扑注册函数；Phase 5 没有改变主链拓扑，只继续扩展契约、回归和 trace 观测。
- Phase 4 loop 运行时带 `recursion_limit=40` 作为兜底保险丝。
- `workflow_trace.py` 只附加 trace 记录、摘要落盘和 `final_output.workflow_trace` 回写，不改变主链业务语义。

## 3. 正式入口与 Trace 行为

### Python 入口

```python
from workflow import run_workflow
result = run_workflow(user_query)
```

```python
from workflow_trace import run_workflow
result = run_workflow(user_query)
```

### 命令行

```powershell
conda activate midea
python workflow.py
```

```powershell
conda activate midea
python workflow_trace.py
```

### Trace 版本附加行为

- 记录节点输入/输出、耗时、顶层状态字段变更。
- 成功或失败都会落盘到 `outputs/workflow_trace_时间戳_微秒/`，避免 query suite 连续运行时 trace 目录互相覆盖。
- `workflow_node_io_record.json` 与 `workflow_node_io_record.md` 现在包含更短的诊断字段，包括：
  - `workflow_status`
  - `failed_node`
  - `last_successful_node`
  - `selected_case_pattern_id`
  - `retrieved_*_count`
  - `reuse_template_subsystem_count`
  - `atomic_assembly_subsystem_count`
  - `unresolved_item_count` / `unresolved_item_types`
  - `planning_unresolved_by_type`
  - `ambiguous_signal_count`
  - `verification_status` / `verification_repair_scope` / `verification_issue_summary`
  - `verification_error_count` / `verification_warning_count`
  - `verification_metrics`
  - `repair_round_count`
  - `repair_scopes_seen`
  - `final_route_decision`
  - `retry_exhausted`
  - `retry_counts_by_scope`
  - `last_repair_issue_ids`
  - `last_repair_actions`
  - `reject_reason`
  - `repair_reject_category`
  - `acceptance_summary`

## 4. `WorkflowState` 真实字段

| 字段 | 主链是否使用 | 说明 |
|:---|:---:|:---|
| `user_query` | 是 | 用户自然语言需求 |
| `analysis_result` | 是 | Analysis 输出 |
| `requirement_spec` | 是 | Phase 3 正式结构化需求对象 |
| `retrieval_bundle` | 是 | Retrieval 正式主输出 |
| `retrieval_context` | 是 | `build_legacy_retrieval_context(retrieval_bundle)` 派生的 compat 视图 |
| `decomposition_result` | 是 | ArchitecturePlanner 输出的页签/子系统拆解结果 |
| `architecture_plan` | 是 | 系统级骨架、模板偏好、全局命名/布局策略 |
| `subsystem_plan_map` | 是 | 每个子系统的局部 IR |
| `execution_plan` | 是 | Compat 投影；由 `GlobalAssembler` 回填 |
| `assembled_graph_ir` | 是 | GlobalAssembler 输出的全局 Graph IR |
| `compiled_artifact` | 是 | Coding 输出的编译结果包 |
| `verification_report` | 是 | Verifier 输出的结构验收报告 |
| `generated_code` | 是 | Compat 字段，等于 `compiled_artifact["json_text"]` |
| `execution_result` | 否 | 历史预留 |
| `validation_result` | 否 | 历史预留 |
| `debug_history` | 否 | 历史预留 |
| `repair_context` | 是 | 最近一轮 repair 的目标、策略、回跳节点 |
| `repair_history` | 是 | 每次 repair / reject 的结构化记录 |
| `route_decision` | 是 | `repair_router` 或 `repair_agent` 写入的稳定路由决策 |
| `retry_budget` | 是 | 按 `planning / assembly / compile` 的预算上限 |
| `retry_counts_by_scope` | 是 | 各 repair scope 的实际重试计数 |
| `retry_count` | 是 | 聚合统计值，等于 `sum(retry_counts_by_scope.values())` |
| `current_step` | 是 | 每个节点都会更新 |
| `next_step` | 否 | 当前未使用 |
| `final_output` | 是 | Verifier 写入最终摘要；Trace 入口追加 trace 信息 |

## 5. 核心数据契约

| 对象 | 生产者 | 核心字段 | 契约说明 |
|:---|:---|:---|:---|
| `requirement_spec` | `AnalysisAgent` | `schema_version`、`system_type`、`scenario_summary`、`subsystems`、`signals`、`required_pages`、`global_modes` | Phase 3 正式需求入口 |
| `retrieval_bundle` | `RetrievalAgent` | `atomic_modules`、`subflow_templates`、`system_patterns`、`style_guides`、`metadata` | Phase 2/3 正式检索契约 |
| `decomposition_result` | `ArchitecturePlanner` | `pages`、`subsystem_descriptors[*].interface_bindings`、`shared_signal_registry`、`template_needs`、`planning_order` | 子系统边界、接口语义与规划顺序 |
| `architecture_plan` | `ArchitecturePlanner` | `pages`、`subsystem_slots`、`global_constraints`、`naming_strategy`、`layout_strategy`、`pattern_bindings` | 系统骨架层正式输出 |
| `subsystem_plan_map` | `SubsystemPlanner` | `implementation_mode`、`template_binding`、`template_interface_bindings`、`node_instances`、`edges`、`imported_signals`、`exported_signals` | 子系统局部 IR；`imported/exported_signals` 带 `binding_kind / allowed_external / canonical_signal_key` |
| `execution_plan` | `GlobalAssembler` | `goal`、`nodes[*]`、`connections[*]` | 仅为 compat `execution_plan` 语义保留 |
| `assembled_graph_ir` | `GlobalAssembler` | `pages`、`subflow_definitions`、`node_instances`、`edges`、`signal_registry`、`layout_hints`、`unresolved_items`、`source_execution_plan` | 当前编译器与验收器的真实输入 |
| `compiled_artifact` | `CodingAgent / JSONCompiler` | `json_text`、`flow_objects`、`id_map`、`layout_map`、`compile_report` | 最终真实产物 |
| `verification_report` | `VerifierAgent` | `status`、`repair_scope`、`issue_summary`、`issues[*].repair_payload`、`warnings`、`metrics` | 当前验收出口；由 `RepairRouter` / `RepairAgent` 消费 |
| `route_decision` | `RepairRouter / RepairAgent` | `decision`、`repair_scope`、`next_node`、`reason`、`issue_ids`、`retry_*` | Phase 4 稳定路由契约 |
| `repair_context` | `RepairAgent` | `repair_round`、`repair_scope`、`target_ids`、`target_state_keys`、`repair_strategy`、`resume_node` | 最近一次 repair 的可回放上下文 |

## 6. Phase 2 正式资产链与环境基线

### 正式资产链口径

- 原子模块来自 `schemas/*.json`，写入 `kong_modules_v1`
- AHU 子流程模板与 system pattern 来自 `AHU程序/flows_*.json`
- 正式 Chroma 默认落库目录是 `outputs/chroma_db`
- 当前环境的活动 `CHROMA_PERSIST_DIR` 受 `.env` 覆盖为 `./chroma_db`；2026-04-11 的 Phase 4 baseline 已在该目录补齐 `ahu_subflow_templates_v1` 与 `ahu_system_patterns_v1`
- 规范化导出目录默认是 `AHU程序/pattern_library`
- manifest 会记录 `build_command`、`persist_dir`、`collection_names`、`collection_owner`、源 flow 摘要
- `pattern_library` 是可重建缓存，不是唯一事实源

### 当前验证环境

| 组件 | 已验证版本 |
|:---|:---|
| Python | `3.12.12` |
| `langgraph` | `1.0.6` |
| `langchain` | `1.2.6` |
| `chromadb` | `1.4.1` |
| `langchain-openai` | `1.1.7` |
| `openai` | `2.15.0` |
| `pydantic` | `2.12.4` |
| `sentence-transformers` | `5.2.0` |
| `torch` | `2.10.0` |
| `transformers` | `4.57.6` |
| `python-dotenv` | `1.2.1` |
| `langsmith` | `0.6.0` |
| `colorama` | `0.4.6` |

- `requirements.txt` 已按当前基线收紧下界。
- 环境验收新增 `tests/test_runtime_versions.py` 与 `python -m pip check`。
- 若未来引入 LangGraph 的 `interrupt` / persistence / HITL，需要从当前版本基线重新验证并补 checkpointer 相关依赖。

## 7. 当前已完成、未完成与遗留模块状态

### 已完成

- Phase 4 正式主链：`analysis -> retrieval -> architecture_planning -> subsystem_planning -> global_assembly -> coding -> verification -> repair_router -> (...)`
- `requirement_spec`、`decomposition_result`、`architecture_plan`、`subsystem_plan_map` 正式入链
- `ArchitecturePlanner`、`SubsystemPlanner`、`GlobalAssembler` 已落地
- `utils/signal_semantics.py` 与 `InterfaceBindingSpec` 已落地，模板接口先统一分类再进入规划链
- `GlobalAssembler` 已回填 `execution_plan` 与 `assembled_graph_ir.source_execution_plan`
- `VerifierAgent` 可直接消费 Phase 3 原生产物，不再对 legacy `execution_plan` 有硬依赖
- `RepairRouter` 已按 scope 预算实现 `accept / *_repair / reject`
- `ArchitecturePlanner / SubsystemPlanner / GlobalAssembler` 已按接口语义层收口：只有 `shared_signal` 进入 owner/exporter 约束链，external 类接口直接走外部占位源语义
- `ArchitecturePlanner` 已为共享信号产出 `candidate_exporters / resolution_status / resolution_evidence`
- `subsystem_slots` / `subsystem_plan_map` 已补齐 `selection_reason / degrade_reason`
- `GlobalAssembler` 不再猜测 shared signal owner，而是消费 architecture/shared-signal policy
- `GlobalAssembler` 已为 assembly unresolved item 补齐 `edge_locator / edge_ids / from_node / to_node / reason`
- `VerifierAgent` 已为 planning / assembly / compile 三类问题生成结构化 `repair_payload`
- `RepairAgent` 已支持 `ambiguous_shared_signal` 在“可收敛为唯一 exporter”时自动收敛，在“无法收敛”时稳定 reject 并写入 `ambiguous_shared_signal_unresolved`
- 真实多子系统 AHU 样本已进入 Phase 3 集成回归
- 共享信号缺少真实导出方或存在歧义时，不再静默 `passed`
- Phase 2 正式 `chroma_db` 落库策略、命令、manifest 与烟测路径已冻结
- README、核心摘要文档、遗留模块状态和环境口径已同步
- Trace 摘要已补齐 Phase 5 诊断字段：`planning_unresolved_by_type`、`ambiguous_signal_count`、`repair_reject_category`
- `AHU程序/Phase4_Send并行预留设计.md` 已落盘
- `scripts/poc_phase4_hitl.py` 与 `AHU程序/Phase4_HITL_POC记录.md` 已落盘
- `outputs/phase4_baseline/` 已生成固定 3 条查询的 baseline 摘要与 trace 目录
- `scripts/run_phase4_query_smoke.py` 已落盘，并通过固定 2 条查询的 Phase 4 smoke
- `scripts/run_phase5_query_suite.py` 已落盘，并通过固定 6 条 deterministic case：成功通过、稳定 reject、planning repair 成功、assembly repair 成功、compile repair 成功、budget exhausted
- `tests/test_phase5_workflow_query_suite.py` 已补齐，并额外覆盖多轮 repair 场景

### 未完成

- 人工澄清与中断节点
- `Send` 并行子系统派发与 reducer fan-in（当前只有设计预留，未入主链）
- 完整 `internal_flow_objects` body 编译
- 在线 LLM 质量与真实项目级规划质量验收
- Repair 支持问题类型仍是 V1 子集，未覆盖所有 verifier failure 类型
- 真实 `workflow_trace.run_workflow(...)` 的 3 条 Phase 5 交付证据 trace 仍需按人工复核口径保留

### 未接入主链的历史模块

| 模块 | 当前定位 |
|:---|:---|
| `agents/planning_agent.py` | 旧主链 / compat planner，仍有测试覆盖 |
| `agents/assembly_agent.py` | 旧装配节点，但基础能力仍被 `GlobalAssembler` 复用 |
| `agents/validation_agent.py` | 未接入主链的旧契约占位实现 |
| `agents/debugging_agent.py` | 未接入主链的旧修复闭环占位实现 |
| `agents/retrieval_agent_old.py` | 历史保留实现 |

## 8. 验收与测试事实

### 已覆盖文件

- `tests/test_phase1_workflow.py`
- `tests/test_phase2_retrieval_bundle.py`
- `tests/test_phase2_ahu_knowledge_builder.py`
- `tests/test_phase2_bundle_consumers.py`
- `tests/test_phase2_planning_bundle.py`
- `tests/test_phase2_retrieval_agent.py`
- `tests/test_phase2_production_chroma_smoke.py`
- `tests/test_phase3_requirement_spec.py`
- `tests/test_phase3_architecture_planner.py`
- `tests/test_phase3_subsystem_planner.py`
- `tests/test_phase3_global_assembler.py`
- `tests/test_phase3_workflow.py`
- `tests/test_phase3_verifier_native_contract.py`
- `tests/test_phase4_state_contract.py`
- `tests/test_phase4_repair_router.py`
- `tests/test_phase4_repair_agent.py`
- `tests/test_phase4_workflow_repair_loop.py`
- `tests/test_phase4_trace_summary.py`
- `tests/test_phase4_signal_semantics.py`
- `tests/test_phase5_workflow_query_suite.py`
- `tests/test_runtime_versions.py`

### 已覆盖能力

- Phase 1 Graph IR 装配、确定性编译、结构验收
- `retrieval_bundle -> retrieval_context` compat 视图
- Planning / Assembly / Coding 对 bundle 的 compat 消费链路
- AHU 子流程模板与 system pattern 资产构建
- `requirement_spec` 适配
- `ArchitecturePlanner` 对 `system_patterns` 的显式绑定
- 模板接口到 `InterfaceBindingSpec` 的统一归类，以及 `shared_signal / external_* / subsystem_output` 语义分流
- `SubsystemPlanner` 的模板复用与 atomic fallback
- `GlobalAssembler` 的多页签装配、共享信号连接、external 占位源注入与 `source_execution_plan` 回填
- `VerifierAgent` 对 Phase 3 原生产物与 `unresolved_items` 的原生验收，以及 planning / assembly / compile 结构化 `repair_payload`
- `RepairRouter` 的 scope 路由、预算裁决与拒绝条件
- `RepairAgent` 对 `synthetic_shared_signal_source`、`shared_signal_owner_mismatch`、`ambiguous_shared_signal`、`missing_local_edge_endpoint`、`compile.wire.port.range` 的 deterministic patch，以及基于 `repair_payload` 的 external 重分类
- `workflow.py` / `workflow_trace.py` 的 Phase 4 主链运行
- Trace 摘要对 repair 轮次、最终路由、scope 计数、planning unresolved 分类、歧义信号数量与 reject 类别的观测
- 真实 `AnalysisAgent + RetrievalAgent + 临时 Chroma + AHU程序/flows_*.json` 的 Phase 3 集成回归
- 真实多子系统 AHU 样本的 Phase 3 端到端回归
- 固定 2 条真实 workflow query 的 Phase 4 smoke
- 固定 6 条 deterministic workflow case 的 Phase 5 query suite
- 运行时版本基线检查与 `pip check`
- 独立 `interrupt / Command(resume=...) / get_state / get_state_history` HITL POC

### 当前验收命令

```powershell
conda activate midea
python -m unittest tests.test_phase3_verifier_native_contract
python -m unittest tests.test_phase4_state_contract
python -m unittest tests.test_phase4_repair_router
python -m unittest tests.test_phase4_repair_agent
python -m unittest tests.test_phase4_workflow_repair_loop
python -m unittest tests.test_phase4_trace_summary
python -m unittest tests.test_phase4_signal_semantics
python -m unittest tests.test_phase5_workflow_query_suite
python -m unittest discover -s tests -p "test_phase3*.py"
python -m unittest discover -s tests -p "test_phase2*.py"
python -m unittest discover -s tests -p "test_phase1_workflow.py"
python -m unittest tests.test_runtime_versions
python scripts/run_phase4_query_smoke.py
python scripts/run_phase5_query_suite.py
python -m compileall workflow.py workflow_trace.py agents utils tests scripts
python -m pip check
```

### 当前结果

- 2026-04-15 增量验证：`tests.test_phase4_trace_summary` 的 `5` 个测试通过；`tests.test_phase5_workflow_query_suite` 的 `2` 个测试通过；`tests.test_phase4_workflow_repair_loop` 的 `6` 个测试通过；`tests.test_phase1_workflow` 的 `11` 个测试通过；`compileall` 通过
- 2026-04-15 `scripts/run_phase5_query_suite.py` 固定 `6` 条 deterministic case 全部通过，并落盘到 `outputs/phase5_query_suite/suite_20260415_131312_103038/`
- `29` 个 Phase 4 状态契约 / router / repair agent / repair loop / trace 摘要 / signal semantics 测试通过（2026-04-14 基线）
- `25` 个 Phase 3 测试通过
- `23` 个 Phase 2 测试通过
- `11` 个 Phase 1 回归测试通过
- `2` 个运行时版本 / 依赖一致性测试通过
- `compileall` 通过
- `pip check` 通过
- `scripts/poc_phase4_hitl.py --thread-id phase4-hitl-ci --resume approve` 本地通过
- Phase 4 baseline 已落盘到 `outputs/phase4_baseline/baseline_20260411_164711/`
- baseline 固定 3 条查询均为 `verification_status=passed` / `route_decision=accept`
- `scripts/run_phase4_query_smoke.py` 固定 2 条查询均为 `verification_status=passed` / `route_decision=accept`
- smoke 摘要已落盘到 `outputs/phase4_smoke/smoke_20260414_214748/phase4_smoke_summary.md`
- `scripts/run_phase5_query_suite.py` 固定 6 条 deterministic case 覆盖成功、稳定 reject、planning repair、assembly repair、compile repair、budget exhausted；额外单测覆盖多轮 repair
- baseline 生成时曾出现一次 `phase3_tests` 子进程失败，随后的立即复跑通过；当前原因未定，已记录在 baseline 摘要

### 当前未覆盖

- 在线 LLM 质量
- 真实 AHU 项目上的召回质量
- 真实 AHU 项目上的系统级规划质量
- 人工澄清正式链路
- `Send` 并行与 reducer fan-in 的真实运行态

## 9. 当前判断与后续优先级

- 当前系统不是完整 V2；它是 `Phase 1 + Phase 2 + Phase 3 首版分层规划 + Phase 4 Repair 闭环 + Phase 5 前四批质量收口` 的稳定基线。
- `CodingAgent` 的真实输入仍然是 `assembled_graph_ir`，不是 `execution_plan`。
- `generated_code` 不是主产物，只是 compat 字段。
- `VerifierAgent` 仍只做结构验收；自动修复发生在 `repair_router -> repair_agent` 之后。
- 当前文档、依赖、query suite 与 trace 观测口径已经收口，后续迭代可以把重点放回真实 AHU 项目质量、资产召回质量与并行化落地。

| 优先级 | 事项 | 说明 |
|:---|:---|:---|
| P0 | Phase 3 / Phase 4 质量迭代 | 提高 `ArchitecturePlanner` / `SubsystemPlanner` 规划质量与 Repair 命中率 |
| P1 | AHU 资产质量与检索质量 | 提高模板覆盖率、system pattern 提取质量与 Chroma 召回质量 |
| P1 | Phase 4 Repair 扩面 | 把更多 verifier failure 类型纳入 deterministic repair 或稳定 reject |
| P1 | LangGraph 新能力版本再验证 | 若把 HITL / interrupt / persistence / `Send` 接入正式主链，从当前验证版本重新起步 |
| P2 | `Send` 并行子系统派发 | 当前已有设计预留，待主契约稳定后再引入并行与 fan-in |
