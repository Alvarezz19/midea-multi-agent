# 工作流总结文档

> 最后核对时间：2026-04-20
> 核对范围：`workflow.py`、`workflow_trace.py`、`agents/*.py`、`utils/*.py`、`scripts/build_phase2_retrieval_indexes.py`、`scripts/poc_phase4_hitl.py`、`scripts/poc_phase7_send_parallel.py`、`scripts/run_phase4_query_smoke.py`、`scripts/run_phase5_query_suite.py`、`scripts/run_phase6_real_query_suite.py`、`scripts/run_phase6_retrieval_eval.py`、`scripts/run_phase8_hitl_smoke.py`、`scripts/phase6_shared.py`、`requirements.txt`、`tests/test_phase1_workflow.py`、`tests/test_phase2_*.py`、`tests/test_phase3_*.py`、`tests/test_phase4_*.py`、`tests/test_phase5_workflow_query_suite.py`、`tests/test_phase6_*.py`、`tests/test_phase7_*.py`、`tests/test_phase8_*.py`。
> 定位：描述当前代码真实行为；变更时间线与阶段决策见 [工作流演进记录](工作流演进记录.md)。

## 1. 当前结论

- 当前系统是 `Phase 1 + Phase 2 + Phase 3 分层规划 + Phase 4 Repair 闭环 + Phase 5 契约守门 + Phase 6 真实质量收口 + Phase 8 前置澄清/架构评审/HITL 挂载` 的稳定基线
- 当前正式 `WorkflowState` 已不再回填 `retrieval_context`、`execution_plan`、`generated_code`、`execution_result`、`validation_result`、`next_step`
- 当前正式主链已经接入：
  - `ambiguity_router -> clarification_review -> clarification_apply`
  - `architecture_review -> architecture_feedback_apply`
  - `repair_router -> repair_agent`
- 当前未接入正式主链：
  - `Send` 并行子系统派发
  - reducer fan-in
  - `repair_review`
  - 完整 `internal_flow_objects` body 编译

当前正式主链：

```text
user_query
  -> analysis
  -> ambiguity_router
ambiguity_router
  -> clarification_review | retrieval
clarification_review
  -> clarification_review | clarification_apply | retrieval
clarification_apply
  -> retrieval | END(reject)
retrieval
  -> architecture_planning
architecture_planning
  -> architecture_review
architecture_review
  -> architecture_review | architecture_feedback_apply | subsystem_planning
architecture_feedback_apply
  -> architecture_planning(feedback/clarify) | subsystem_planning(approve) | END(reject)
subsystem_planning
  -> global_assembly
  -> coding
  -> verification
  -> repair_router
repair_router
  -> END(accept/reject) | repair_agent
repair_agent
  -> subsystem_planning | global_assembly | coding | END
```

## 2. 主链与节点契约

| 节点 | 职责 | 核心输入 | 核心输出 | 当前边界 |
|:---|:---|:---|:---|:---|
| `analysis` | 需求理解与结构化 | `user_query` | `analysis_result`、`requirement_spec` | `analysis_result` 是 Analysis 原始结果与检索/澄清上游输入；正式规划入口是 `requirement_spec` |
| `ambiguity_router` | 早期歧义判定 | `analysis_result`、`requirement_spec` | `analysis_result`、`review_required`、`hitl_stage`、`review_status` | 必要时会补写 `analysis_result.clarification_signals`；不改检索与规划产物 |
| `clarification_review` | 冻结澄清请求并按条件暂停 | `analysis_result`、`requirement_spec`、review 状态字段 | `review_request`、`review_history`、`review_response` | 只生成澄清问题和 pause/resume 审计，不直接改检索/规划产物 |
| `clarification_apply` | 把澄清结果回写到需求侧 | `review_response`、`requirement_spec`、`analysis_result` | 更新后的 `analysis_result`、`requirement_spec`、`clarification_round` | 只改需求侧输入；reject 时写 `final_output.review_abort` |
| `retrieval` | 检索原子模块与 AHU 资产 | `user_query`、`analysis_result` | `retrieval_bundle` | 正式节点只回填 `retrieval_bundle`；`RetrievalAgent.retrieve()` 仍保留 legacy `retrieval_context` 兼容接口，但 compat 投影逻辑已下沉到 `agents/legacy/retrieval_compat.py` |
| `architecture_planning` | 系统骨架、页签、共享信号约束 | `requirement_spec`、`retrieval_bundle` | `decomposition_result`、`architecture_plan` | 规则优先的系统级规划层 |
| `architecture_review` | 结构骨架主评审点 | `architecture_plan`、`decomposition_result`、review 状态字段 | `review_request`、`review_history`、`review_response` | 只展示骨架摘要并收集反馈，不直接改结构产物 |
| `architecture_feedback_apply` | 消费架构评审结果并决定继续、回写或终止 | `review_response`、`requirement_spec` | 条件式更新 `requirement_spec`、`architecture_feedback_patch`、`final_output.review_abort` | `approve` 不生成 patch；`feedback/clarify` 才回写需求并生成 patch；`reject` 只写终止摘要 |
| `subsystem_planning` | 逐个子系统生成局部 IR | `requirement_spec`、`decomposition_result`、`architecture_plan`、`retrieval_bundle` | `subsystem_plan_map` | 当前按 `planning_order` 串行执行，不使用 `Send` |
| `global_assembly` | 组装多页签全局 Graph IR | `requirement_spec`、`architecture_plan`、`subsystem_plan_map`、`retrieval_bundle` | `assembled_graph_ir` | 顶层 `execution_plan` 已移除；正式 IR 也已不再携带 `source_execution_plan` |
| `coding` | 确定性编译 | `assembled_graph_ir`、`retrieval_bundle` | `compiled_artifact` | 正式顶层 `generated_code` 已移除；`generate_json()` 只剩 legacy wrapper 语义，兼容实现已下沉到 `agents/legacy/coding_compat.py` |
| `verification` | 结构验收 | `assembled_graph_ir`、`compiled_artifact`、`requirement_spec`、`architecture_plan`、`subsystem_plan_map` | `verification_report`、`final_output` | 同时检查 planning / assembly / compile 三层 |
| `repair_router` | 条件路由与预算裁决 | `verification_report`、`retry_budget`、`retry_counts_by_scope` | `route_decision` | 按 scope 预算决定 `accept / *_repair / reject` |
| `repair_agent` | 规则优先局部修补 | `route_decision`、`verification_report`、对应上游主对象 | `repair_context`、`repair_history`、被修补主对象 | 只修补下一跳真正会消费的上游对象 |

补充说明：

- `workflow.py` 与 `workflow_trace.py` 共用同一套拓扑注册函数
- Phase 4 loop 仍使用 `recursion_limit=40` 作为保险丝
- 当前正式主链已经接入 Phase 8 的 clarification review 与 architecture review；`Send` 试点仍停留在独立 POC / 合同测试层

## 3. 正式入口与 Trace 行为

### Python 入口

```python
from workflow import run_workflow
result = run_workflow(user_query)
```

```python
from workflow import run_workflow
result = run_workflow(user_query, thread_id="phase8-demo", checkpointer=cp)
```

```python
from workflow_trace import run_workflow
result = run_workflow(user_query)
```

### 命令行

```powershell
conda activate midea
python workflow.py
```

```powershell
conda activate midea
python workflow_trace.py
```

```powershell
conda activate midea
python scripts/run_phase8_hitl_smoke.py --thread-id phase8-smoke --mode clarification --resume clarify
```

### `thread_id` / `checkpointer` / HITL

- 未传 `checkpointer` 时，`thread_id` 只进入 `metadata` 与 trace 索引；不支持 LangGraph resume
- 传入 `checkpointer` 时，必须提供非空 `thread_id`；否则会直接抛 `ValueError`
- `enable_hitl_clarification` 与 `enable_hitl_architecture_review` 只有在同时满足 `checkpointer + thread_id` 时才会真正触发 `interrupt`
- 不满足上述条件时，review 节点会走 `assumed` 旁路，不会暂停

### Trace 版本附加行为

- 记录每个节点的输入、输出、耗时、顶层字段变更
- 成功、失败、`interrupt` 都会落盘
- 传入 `thread_id` 时，会建立 attempt 级与 thread 级 trace 索引
- 若存在 `review_history`，会额外落盘 review 审计文件；否则 review 相关路径字段会保持空字符串或缺省

当前 `final_output.workflow_trace` 至少可能包含：

- `thread_id`
- `attempt_id`
- `trace_dir`
- `summary_json`
- `summary_md`
- `final_state_json`
- `review_records_json`
- `approval_record_json`
- `thread_index_json`
- `attempt_index_json`
- `review_attempt_index_json`
- `review_thread_index_json`
- `review_record_jsons`

其中：

- `thread_index_json` / `attempt_index_json` 要求 `thread_id` 非空
- `review_records_json` / `approval_record_json` 只有在存在 `review_history` 时才会是非空路径
- `review_attempt_index_json` / `review_thread_index_json` 在 `thread_id` 非空时键会出现；只有存在 `review_history` 时值才是非空路径
- `review_record_jsons` 同时要求 `thread_id` 非空且存在 `review_history`

## 4. `WorkflowState` 真实字段

| 字段 | 主链是否使用 | 说明 |
|:---|:---:|:---|
| `user_query` | 是 | 用户自然语言需求 |
| `analysis_result` | 是 | Analysis 输出；包含 `clarification_signals` |
| `requirement_spec` | 是 | 正式结构化需求对象 |
| `retrieval_bundle` | 是 | Retrieval 正式主输出 |
| `decomposition_result` | 是 | 系统拆解结果 |
| `architecture_plan` | 是 | 系统级骨架 |
| `subsystem_plan_map` | 是 | 子系统局部 IR |
| `assembled_graph_ir` | 是 | 真实全局 Graph IR |
| `compiled_artifact` | 是 | 真实编译结果 |
| `verification_report` | 是 | 结构验收结果 |
| `repair_context` | 是 | 最近一轮 repair 上下文 |
| `repair_history` | 是 | repair / reject 审计记录 |
| `route_decision` | 是 | 稳定路由决策 |
| `retry_budget` | 是 | 各 scope 预算上限 |
| `retry_counts_by_scope` | 是 | 各 scope 已消耗计数 |
| `retry_count` | 是 | `retry_counts_by_scope` 的聚合影子值；主路由实际按各 scope 计数判定 |
| `hitl_stage` | 是 | 当前 review 阶段 |
| `review_request` | 是 | 当前冻结的 review 请求 |
| `review_response` | 是 | 当前 review 回复 |
| `review_history` | 是 | review 生命周期记录 |
| `review_enabled` | 是 | 是否允许真正 `interrupt` |
| `review_required` | 是 | 是否需要 review |
| `review_status` | 是 | 当前 review 状态；`interrupted` 主要是 trace 摘要归纳状态，不是 review 节点常规回写值 |
| `review_id` | 是 | 当前 review 稳定 ID |
| `clarification_round` | 是 | 澄清回写轮次 |
| `architecture_feedback_patch` | 是 | 架构反馈回写摘要 |
| `enable_hitl_clarification` | 是 | clarification review HITL 开关 |
| `enable_hitl_architecture_review` | 是 | architecture review HITL 开关 |
| `current_step` | 是 | 绝大多数节点都会更新；`repair_router` 的 `accept` 分支会保留上一步 `current_step` |
| `final_output` | 是 | 最终对外摘要；trace 文件总会落盘，但只有 `workflow_trace.run_workflow()` 成功返回结果对象时才会附加 `workflow_trace` |
| `debug_history` | 否 | 历史预留；部分测试桩仍会写入 |

## 5. 核心数据契约

| 对象 | 生产者 | 当前关键字段 | 契约说明 |
|:---|:---|:---|:---|
| `analysis_result` | `AnalysisAgent` | `retrieval_plan`、`scenario_analysis`、`metadata`、`clarification_signals` | `clarification_signals` 是前置澄清分支的正式上游输入 |
| `requirement_spec` | `AnalysisAgent` / review apply | `schema_version`、`system_type`、`scenario_summary`、`subsystems`、`signals`、`required_pages`、`global_modes`、`ambiguities`、`assumptions`、`acceptance_criteria`、`confidence`、`warnings` | Phase 3 正式需求入口 |
| `retrieval_bundle` | `RetrievalAgent` | `atomic_modules`、`subflow_templates`、`system_patterns`、`style_guides`、`metadata` | `metadata` 里还稳定包含 `rewrite_used`、`analysis_used`、`llm_queries`、`llm_category_l1`、`analysis_summary`、`analysis_confidence` |
| `decomposition_result` | `ArchitecturePlanner` | `pages`、`subsystem_descriptors`、`shared_signal_registry`、`template_needs`、`planning_order`、`warnings` | 子系统边界与规划顺序 |
| `architecture_plan` | `ArchitecturePlanner` | `pages`、`subsystem_slots`、`shared_signal_registry`、`global_constraints`、`naming_strategy`、`layout_strategy`、`pattern_bindings`、`warnings` | `shared_signal_registry` 是 owner/exporter 约束链正式输入 |
| `subsystem_plan_map` | `SubsystemPlanner` | `implementation_mode`、`template_binding`、`selection_reason`、`degrade_reason`、`template_interface_bindings`、`node_instances`、`edges`、`imported_signals`、`exported_signals`、`unresolved_items` | `template_binding` 和 signal bindings 里都带 repair 依赖字段 |
| `assembled_graph_ir` | `GlobalAssembler` | `pages`、`subflow_definitions`、`node_instances`、`edges`、`signal_registry`、`layout_hints`、`unresolved_items` | 当前编译器与 verifier 的真实输入 |
| `compiled_artifact` | `CodingAgent / JSONCompiler` | `json_text`、`flow_objects`、`id_map`、`layout_map`、`compile_report` | 当前真实产物 |
| `verification_report` | `VerifierAgent` | `status`、`repair_scope`、`issue_summary`、`issues[*].repair_payload`、`warnings`、`metrics` | 当前验收出口；由 `RepairRouter` / `RepairAgent` 消费 |
| `route_decision` | `RepairRouter / RepairAgent` | `decision`、`repair_scope`、`next_node`、`reason`、`issue_ids`、`retry_*` | Phase 4 稳定路由契约 |
| `repair_context` | `RepairAgent` | `repair_round`、`repair_scope`、`target_ids`、`target_state_keys`、`repair_strategy`、`resume_node` | 最近一次 repair 的可回放上下文 |

## 6. Phase 2 正式资产链与环境基线

### 正式资产链口径

- 原子模块来自 `schemas/*.json`，写入 `kong_modules_v1`
- AHU 子流程模板与 system pattern 来自 `AHU程序/flows_*.json`
- 正式 Chroma 默认落库目录是 `outputs/chroma_db`
- 当前环境的活动 `CHROMA_PERSIST_DIR` 受 `.env` 覆盖为 `./chroma_db`
- `pattern_library` 是可重建缓存，不是唯一事实源

### 当前验证环境

| 组件 | 已验证版本 |
|:---|:---|
| Python | `3.12.12` |
| `langgraph` | `1.0.6` |
| `langchain` | `1.2.6` |
| `chromadb` | `1.4.1` |
| `langchain-openai` | `1.1.7` |
| `openai` | `2.15.0` |
| `pydantic` | `2.12.4` |
| `sentence-transformers` | `5.2.0` |
| `torch` | `2.10.0` |
| `transformers` | `4.57.6` |
| `python-dotenv` | `1.2.1` |
| `langsmith` | `0.6.0` |
| `colorama` | `0.4.6` |

- `requirements.txt` 已按当前基线收紧下界，并已包含 `langgraph-checkpoint`
- `interrupt / persistence / HITL` 已经接入当前代码；若后续升级相关版本，需要重新验证 runtime、checkpointer 与 review 合同测试

## 7. 当前已完成、未完成与遗留模块状态

### 已完成

- 正式主链已接入条件式澄清、架构评审和 Repair 闭环
- `requirement_spec`、`decomposition_result`、`architecture_plan`、`subsystem_plan_map`、`assembled_graph_ir`、`compiled_artifact` 已形成稳定主契约
- `ArchitecturePlanner / SubsystemPlanner / GlobalAssembler / VerifierAgent / RepairAgent` 已按当前接口语义层收口
- `workflow.py` / `workflow_trace.py` 已支持显式 `thread_id` 与可选 `checkpointer`
- `CodingAgent` 已完成稳定 ID 收口
- Phase 6 正式 case 源、retention 机制、suite 摘要和 retrieval eval 摘要已落地
- Phase 7 runtime / trace-index / coding determinism / repair patch contract 已落地
- Phase 8 clarification / architecture review / persistence smoke / trace review 合同测试已落地

### 未完成

- `Send` 并行子系统派发与 reducer fan-in 的正式主链化
- `repair_review` 的正式主链化
- 完整 `internal_flow_objects` body 编译
- 在线 LLM 质量与真实项目级规划质量验收
- 更大范围的自动修复类型扩展

### 未接入主链的历史模块

| 模块 | 当前定位 |
|:---|:---|
| `agents/planning_agent.py` | legacy 兼容包装；真实实现已迁移到 `agents/legacy/planning_agent.py`，仍有测试覆盖 |
| `agents/assembly_agent.py` | legacy 兼容包装；真实实现已迁移到 `agents/legacy/assembly_agent.py`，正式共享 helper 已抽到 `agents/assembly_shared.py` |
| `agents/validation_agent.py` | legacy 兼容包装；真实实现已迁移到 `agents/legacy/validation_agent.py` |
| `agents/debugging_agent.py` | legacy 兼容包装；真实实现已迁移到 `agents/legacy/debugging_agent.py` |
| `agents/retrieval_agent_old.py` | legacy 兼容包装；真实实现已迁移到 `agents/legacy/retrieval_agent_old.py` |

### 当前仍保留的兼容残留

- `agents/retrieval_agent.py` 仍保留旧构造参数和 `retrieve()` 兼容接口，但 compat 投影逻辑已下沉到 `agents/legacy/retrieval_compat.py`
- `agents/global_assembler.py` 的 `assemble(..., retrieval_bundle)` 已收口为正式 bundle-only 接口；`agents/coding_agent.py` 的正式 `__call__` / `compile_graph_from_bundle(...)` 也只走 bundle-only helper，而 `compile_graph(...)` / `generate_json(...)` 的 compat 逻辑已下沉到 `agents/legacy/coding_compat.py`，当前只保留 API 薄代理供 legacy 调用方与 legacy 测试使用
- `agents/subsystem_planner.py` 仍容忍旧式 `imports / exports` 回退和 `exporter_candidates` 别名；当前 `ArchitecturePlanner` 主产物已经优先输出 `interface_bindings` 与 `candidate_exporters`
- `workflow.py` 顶层仍保留 `debug_history`、`retry_count` 这类 reserved / shadow 字段；其中 `debug_history` 不参与主链决策，`retry_count` 只是聚合影子值

补充：

- formal 合同测试和 Phase 4 / Phase 8 的关键 stub 已开始显式消费 `interface_bindings` / `candidate_exporters`，后续清理 `imports / exports` fallback 与 `exporter_candidates` alias 时将优先以这些原生字段为准

### 已开始分层的 legacy 测试

- `tests/test_legacy_agent_imports.py`
- `tests/test_legacy_execution_plan.py`
- `tests/test_phase1_workflow.py`
- `tests/test_phase2_bundle_consumers.py`
- `tests/test_phase2_planning_bundle.py`
- `tests/test_phase2_retrieval_bundle.py`
- `tests/test_phase2_retrieval_agent.py`
- `tests/test_phase6_retrieval_eval_contract.py`
- `tests/test_phase7_coding_determinism.py`

当前这些文件已仅保留兼容入口或混合入口，真实测试实现位于 `tests/legacy/` / `tests/contracts/`。这样可以继续兼容既有显式回归命令，同时把 legacy 回归与正式合同测试逐步分开。

## 8. 验收与测试事实

### 已覆盖文件

- `tests/test_phase1_workflow.py`
- `tests/test_phase2_*.py`
- `tests/test_phase3_*.py`
- `tests/test_phase4_*.py`
- `tests/test_phase5_workflow_query_suite.py`
- `tests/test_phase6_real_query_suite_contract.py`
- `tests/test_phase6_retrieval_eval_contract.py`
- `tests/test_phase6_readiness_contract.py`
- `tests/test_phase7_*.py`
- `tests/test_phase8_clarification_contract.py`
- `tests/test_phase8_architecture_review_contract.py`
- `tests/test_phase8_persistence_smoke.py`
- `tests/test_phase8_trace_review_contract.py`
- `tests/test_runtime_versions.py`

补充：

- `tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py`
  的 formal stub 已不再注入 `retrieval_context`；当前非 legacy 测试里仅保留“结果不应包含该字段”的负向断言。
- `tests/test_phase3_workflow.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase4_workflow_repair_loop.py`、
  `tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py`
  已补充 formal 负向断言，明确正式路径结果中不应回流 `retrieval_context`、`execution_plan`、`generated_code`。
- `tests/` 根目录本轮复核未发现新的纯 compat 真实测试；当前剩余带 compat 属性的根目录文件已是兼容入口，或承担 formal/contract 负向约束。

### 已覆盖能力

- Phase 1 Graph IR 装配、确定性编译、结构验收
- Phase 2 `retrieval_bundle` 正式契约、legacy `build_legacy_retrieval_context(...)` helper、AHU 子流程模板与 system pattern 资产构建
- Phase 3 分层规划、共享信号语义收口、多页签全局装配、原生 verifier 验收
- Phase 4 repair routing、scope 预算、deterministic patch、trace repair 诊断
- Phase 5 deterministic workflow query suite
- Phase 6 真实 workflow query suite、retrieval eval 与 retained 机制
- Phase 7 runtime / trace index / coding determinism / repair patch / send-reducer 合同测试
- Phase 8 clarification、architecture review、persistence smoke、trace review
- `interrupt / Command(resume=...) / get_state / get_state_history` 的最小 HITL smoke

### 当前验收命令

```powershell
conda activate midea
python -m unittest discover -s tests -p "test_phase1_workflow.py"
python -m unittest discover -s tests -p "test_phase2*.py"
python -m unittest discover -s tests -p "test_phase3*.py"
python -m unittest discover -s tests -p "test_phase4*.py"
python -m unittest tests.test_phase5_workflow_query_suite
python -m unittest tests.test_phase6_real_query_suite_contract
python -m unittest tests.test_phase6_retrieval_eval_contract
python -m unittest tests.test_phase6_readiness_contract
python -m unittest discover -s tests -p "test_phase7*.py"
python -m unittest tests.test_phase8_clarification_contract
python -m unittest tests.test_phase8_architecture_review_contract
python -m unittest tests.test_phase8_persistence_smoke
python -m unittest tests.test_phase8_trace_review_contract
python -m unittest tests.test_runtime_versions
python scripts/run_phase4_query_smoke.py
python scripts/run_phase5_query_suite.py
python scripts/run_phase6_retrieval_eval.py
python scripts/run_phase6_real_query_suite.py
python scripts/run_phase8_hitl_smoke.py --thread-id phase8-smoke --mode clarification --resume clarify
python -m compileall workflow.py workflow_trace.py agents utils tests scripts
python -m pip check
```

### 当前可见结果

- `outputs/phase6_real_query_suite/suite_20260417_103517_951405/phase6_real_query_suite_summary.json` 记录当前正式结果：`case_count=12`、`passed_count=12`、`golden_case_pass_rate=1.0`
- `outputs/phase6_retrieval_eval/eval_20260417_103315_704147/phase6_retrieval_eval_summary.json` 记录当前正式结果：`case_count=12`、`diagnosis_counts.healthy=12`、`golden_template_hit_rate_top5=1.0`、`golden_pattern_hit_rate_top3=1.0`
- 仓库已包含对应的 Phase 4/5/7/8 合同测试与脚本，但本文档不再重复宣称旧的测试数量统计，以免文档再次老化

### 当前未覆盖

- 在线 LLM 质量
- 真实 AHU 项目上的召回质量
- 真实 AHU 项目上的系统级规划质量
- `repair_review` 正式链路
- `Send` 并行与 reducer fan-in 的真实运行态

## 9. 当前判断与后续优先级

- 当前系统不是完整 V2；它是带有前置澄清、架构评审和 Repair 闭环的稳定分层规划基线
- `CodingAgent` 的真实输入仍然是 `assembled_graph_ir`，不是 `execution_plan`
- `generated_code` 已从正式 `WorkflowState` / `build_initial_state()` 移除；当前只在 legacy 调试路径、兼容脚本或兼容测试里出现
- `VerifierAgent` 只做结构验收；自动修复发生在 `repair_router -> repair_agent`
- 当前应优先决定是否推进 `repair_review` 与正式 `Send` 迁移，而不是重复判断 Phase 8 A/B/C/E/F/G 是否“已完成”

| 优先级 | 事项 | 说明 |
|:---|:---|:---|
| P0 | `repair_review` 是否正式入链 | 先确认是否真的需要把兜底审核前推到主链 |
| P1 | Durable execution 边界继续收口 | 持续守住 `analysis`、`retrieval`、`coding`、`repair_agent`、`workflow_trace` 的可恢复边界 |
| P1 | `Send` 并行试验图的迁移条件 | 继续停留在 POC/合同层，未满足条件前不切正式主链 |
| P2 | Repair 扩面 | 只在出现新的稳定失败桶后再扩展自动修复规则 |
补充：

- `build_legacy_execution_plan(...)` 已下沉到 `utils/legacy_execution_plan.py` 作为 canonical legacy helper；`utils.phase3_adapters` 已不再 re-export 它
- `execution_result`、`validation_result`、`next_step` 已从正式 `WorkflowState` 移出
