# 工作流总结文档

> 最后核对时间：2026-04-07
> 核对范围：`workflow.py`、`workflow_trace.py`、`agents/*.py`、`utils/*.py`、`scripts/build_phase2_retrieval_indexes.py`、`tests/test_phase1_workflow.py`、`tests/test_phase2_*.py`
> 定位：描述当前代码真实行为；演进、决策与时间线见 [工作流演进记录](工作流演进记录.md)。

## 1. 当前结论

- 当前系统是 `Phase 1 主链稳定 + Phase 2 验收通过` 的 LangGraph 工作流，不是完整 V2。
- 当前正式主链：

```text
user_query
  -> analysis
  -> retrieval
  -> planning
  -> assembly
  -> coding
  -> verification
  -> END
```

- 当前阶段判断：`Phase 1 已稳定`、`Phase 2 已通过`、下一步进入 `Phase 3 分层规划`。
- 当前未接入：`ArchitecturePlanner`、`SubsystemPlanner`、`GlobalAssembler`、`RepairRouter / RepairAgent`、人工澄清/中断节点、`add_conditional_edges()` / `Send` / reducer、Phase 2 collections 正式写入目标 `chroma_db` 的策略。

## 2. 主链与节点契约

| 节点 | 职责 | 核心输入 | 核心输出 | 当前边界 |
|:---|:---|:---|:---|:---|
| `analysis` | 需求理解 | `user_query` | `analysis_result` | 只做需求理解，不做检索和编译 |
| `retrieval` | 检索原子模块与 Phase 2 资产 | `analysis_result.retrieval_plan` | `retrieval_bundle`、`retrieval_context` | `retrieval_context` 为兼容视图；Windows 控制台走安全输出 |
| `planning` | 生成弱计划 | `user_query`、`analysis_result`、`retrieval_bundle/retrieval_context` | `execution_plan` | 模块白名单优先来自 bundle；仍是单体 Planner |
| `assembly` | 计划装配为强结构 IR | `execution_plan`、`retrieval_bundle/retrieval_context` | `assembled_graph_ir` | 归一化 `template_id / definition_id`；记录 `unresolved_items` |
| `coding` | 确定性编译 | `assembled_graph_ir`、`retrieval_bundle/retrieval_context` | `compiled_artifact`、`generated_code` | 本质是 `JSONCompiler`；`generated_code` 仅兼容 |
| `verification` | 结构验收 | `assembled_graph_ir`、`compiled_artifact` | `verification_report`、`final_output` | 只验收，不修复 |

- `workflow.py` 与 `workflow_trace.py` 使用同一条 `StateGraph` 主链。
- `workflow_trace.py` 只增加 trace 记录、耗时统计、异常/成功落盘与 `final_output.workflow_trace` 回写，不改变主链拓扑。
- 当前未使用 LangGraph 的条件分支、并行分发、reducer、中断/人工澄清能力。

## 3. 正式入口与运行方式

### Python 入口

```python
from workflow import run_workflow
result = run_workflow(user_query)
```

```python
from workflow_trace import run_workflow
result = run_workflow(user_query)
```

### 命令行

```powershell
conda activate midea
python workflow.py
```

```powershell
conda activate midea
python workflow_trace.py
```

```powershell
conda activate midea
python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/pattern_library
```

### Trace 版本附加行为

- 记录节点输入/输出、耗时、顶层状态字段变更。
- 成功或失败都落盘到 `outputs/workflow_trace_时间戳/`。
- 成功时把 trace 路径写入 `final_output.workflow_trace`。

## 4. `WorkflowState` 真实字段

| 字段 | 主链是否使用 | 说明 |
|:---|:---:|:---|
| `user_query` | 是 | 用户自然语言需求 |
| `analysis_result` | 是 | Analysis 输出 |
| `retrieval_bundle` | 是 | Retrieval 正式主输出 |
| `retrieval_context` | 是 | `build_legacy_retrieval_context(retrieval_bundle)` 派生的兼容视图；只保留 `atomic_modules -> relevant_nodes` |
| `execution_plan` | 是 | Planning 输出的弱计划 |
| `assembled_graph_ir` | 是 | Assembly 输出的强结构 IR |
| `compiled_artifact` | 是 | Coding 输出的编译结果包 |
| `verification_report` | 是 | Verifier 输出的结构验收报告 |
| `generated_code` | 是 | 兼容字段，等于 `compiled_artifact["json_text"]` |
| `execution_result` | 否 | 历史预留 |
| `validation_result` | 否 | 历史预留 |
| `debug_history` | 否 | 历史预留 |
| `retry_count` | 否 | 为未来 Repair 闭环预留 |
| `current_step` | 是 | 每个节点都会更新 |
| `next_step` | 否 | 当前未使用 |
| `final_output` | 是 | Verifier 写入最终摘要；Trace 入口追加 trace 信息 |

## 5. 核心数据契约

| 对象 | 生产者 | 核心字段 | 契约说明 |
|:---|:---|:---|:---|
| `retrieval_bundle` | `RetrievalAgent` | `atomic_modules`、`subflow_templates`、`system_patterns`、`style_guides`、`metadata` | Phase 2 正式检索契约；下游优先消费 |
| `retrieval_context` | `RetrievalAgent` | `query`、`relevant_nodes`、`similar_cases`、`metadata` | Phase 2 兼容视图；仅暴露 `atomic_modules` 切片 |
| `execution_plan` | `PlanningAgent` | `goal`、`nodes[*].logic_id`、`nodes[*].module_type`、`nodes[*].parameters`、`connections[*].from_node`、`connections[*].from_port_index`、`connections[*].to_node`、`connections[*].to_port_index` | 弱计划层；不再直接交给 Coding 生成 JSON |
| `assembled_graph_ir` | `AssemblyAgent` | `pages`、`subflow_definitions`、`node_instances`、`edges`、`signal_registry`、`layout_hints`、`unresolved_items` | 当前主链最关键的新中间层；编译器与验收器的真实输入 |
| `compiled_artifact` | `CodingAgent / JSONCompiler` | `json_text`、`flow_objects`、`id_map`、`layout_map`、`compile_report` | 最终真实产物；表达 JSON 文本、对象列表、逻辑 ID 映射、布局与编译报告 |
| `verification_report` | `VerifierAgent` | `status`、`repair_scope`、`issue_summary`、`issues`、`warnings`、`metrics` | 当前验收出口；不是修复入口；`status` 主要为 `passed` / `retryable_error` |

- `system_patterns` 在 Phase 2 里只作为 `PlanningAgent` 的 prompt hint / soft reference，不是新的下游硬契约。
- 当前 AHU 资产构建仍依赖 `AHU程序/flows_*.json` 作为前置输入；若这些源 flow 未进入版本管理，则“干净克隆即可复现”不成立。

## 6. 当前已完成与未完成

### 已完成

- 6 节点正式主链：`analysis -> retrieval -> planning -> assembly -> coding -> verification -> END`
- `assembled_graph_ir`、`compiled_artifact`、`verification_report` 正式入链
- `CodingAgent` 收敛为确定性编译器
- `retrieval_bundle` 正式入链，`retrieval_context` 保留为兼容视图
- `Planning / Assembly / Coding` 已优先消费 bundle
- AHU 子流程模板、系统模式与资产构建链已落地
- 索引脚本直接入口、Windows 控制台安全输出、临时持久化 Chroma 回归已补齐
- 空计划、空 IR、零节点编译产物已被结构验收阻断，不再判定为 `passed`
- compiled wire 的目标端口校验已对照目标节点 `inputs`
- `subflow_template.template_id` 已纳入稳定拓扑签名；`dependency_module_types` 已按真实内部功能对象提取
- `write_assets_to_chroma()` 已支持 stale ID 清理，可重复构建
- `PlanningAgent.plan()` 已补 true-path 的 Phase 2 bundle 行为验收

### 未完成

- `ArchitecturePlanner`、`SubsystemPlanner`、`GlobalAssembler`
- `RepairRouter / RepairAgent`
- 人工澄清与中断节点
- 条件路由、并行分发、reducer 合并
- Phase 2 collections 正式写入目标 `chroma_db`
- 真实 AHU 场景下的系统级规划质量验证

### 未接入主链的历史模块

- `tools/execution_tool.py`：更接近 Python 执行沙箱，不匹配“最终产物是平台 JSON”的现状。
- `agents/validation_agent.py`：仍偏旧版 `{nodes, wires}` 契约，未适配 `compiled_artifact / verification_report`。
- `agents/debugging_agent.py`：仍偏“分析错误并返回修正后的 Python 代码”的旧语义。

## 7. 验收与测试事实

### 已覆盖文件

- `tests/test_phase1_workflow.py`
- `tests/test_phase2_retrieval_bundle.py`
- `tests/test_phase2_ahu_knowledge_builder.py`
- `tests/test_phase2_bundle_consumers.py`
- `tests/test_phase2_planning_bundle.py`
- `tests/test_phase2_retrieval_agent.py`

### 已覆盖能力

- Graph IR 装配
- 确定性编译
- 子流程定义 / 子流程实例关系
- 必需输入缺失与坏边引用的结构验收
- `retrieval_bundle -> retrieval_context` 兼容视图
- Planning / Assembly / Coding 对 bundle 的消费链路
- AHU 子流程模板与 system pattern 资产构建
- 空计划 / 空 IR / 零节点编译产物拦截
- compiled wire 目标端口按目标 `inputs` 校验
- 子流程模板稳定拓扑签名、真实依赖抽取与 Chroma stale 清理
- `PlanningAgent.plan()` 对 Phase 2 bundle 提示链路的 true-path 验收
- `RetrievalAgent` 在临时持久化 Chroma 上的真实检索路径
- Windows 控制台安全输出与脚本直接入口
- 带 stub 的 `workflow.py` / `workflow_trace.py` 主链运行

### 已执行命令

```powershell
conda activate midea
python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_direct_smoke
python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_acceptance
python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_acceptance_write --write-chroma --persist-dir outputs/test_tmp/chroma_phase2_acceptance
python -m unittest discover -s tests -p "test_phase2_retrieval_agent.py"
python -m unittest discover -s tests -p "test_phase2*.py"
python -m unittest discover -s tests -p test_phase1_workflow.py
python -m compileall agents utils scripts tests workflow.py workflow_trace.py
```

### 当前结果

- 直接脚本入口通过；验收构建与写库路径均可生成并写入 `7` 个 `subflow_templates` 与 `1` 个 `system_patterns`
- `test_phase2_retrieval_agent.py` 的 `4` 个测试通过
- `22` 个 Phase 2 测试通过
- `11` 个 Phase 1 回归测试通过
- `compileall` 通过

### 当前未覆盖

- 在线 LLM 质量
- 真实 AHU 项目上的召回质量
- 真实 AHU 项目上的系统级规划质量

## 8. 当前判断与后续优先级

- 当前系统不是完整 V2；它是 V2 路线下 Phase 1 + Phase 2 的稳定基线。
- `CodingAgent` 的真实输入是 `assembled_graph_ir`，不是 `execution_plan`。
- `generated_code` 不是主产物，只是兼容字段。
- `VerifierAgent` 只做结构验收，不做自动修复。
- 当前主链已经能承载复杂结构，但还不能完成分层的 AHU 多子系统规划。

| 优先级 | 事项 | 说明 |
|:---|:---|:---|
| P0 | Phase 3 分层规划 | 拆分 `PlanningAgent` 为 `ArchitecturePlanner + SubsystemPlanner + GlobalAssembler` |
| P1 | AHU 资产质量与检索质量 | 提高模板覆盖率、system pattern 提取质量与 Chroma 召回质量 |
| P1 | Phase 2 collections 落库策略 | 明确是否以及如何写入目标 `chroma_db` |
| P1 | `docs/coding_agent_summary.md` 同步 | 避免继续误解 `CodingAgent` 职责 |
| P2 | Phase 4 Repair 闭环 | 先统一修复对象：`assembled_graph_ir`、`compiled_artifact` 或更上游规划层 |
