# 工作流总结文档

> 最后核对时间：2026-03-27
> 核对范围：`workflow.py`、`workflow_trace.py`、`agents/*.py`、`tools/execution_tool.py`、`utils/*.py`、`docs/*.md`
> 说明：本文档面向“后续新对话的预读材料”，优先描述当前代码真实行为，其次再补充设计意图、历史演进和未接入能力。

## 1. 一句话结论

这个项目当前正式启用的工作流，是一个基于 LangGraph `StateGraph` 的**四节点线性流水线**：

```text
user_query
  -> Analysis Agent
  -> Retrieval Agent
  -> Planning Agent
  -> Coding Agent
  -> END
```

它的目标不是生成 Python 代码并执行，而是把自然语言需求逐步转成 **KONG CUBE 可导入的 JSON 组态**。  
仓库里虽然还保留了 Execution Tool、Validation Agent、Debugging Agent，但它们**没有接入当前正式主链**，而且其设计仍更接近“旧版 Python 代码执行链路”。

---

## 2. 先记住这几个最重要的事实

### 2.1 当前 `workflow.py` 只跑 4 个节点

`workflow.py` 顶部注释仍写着“定义 6 个智能体的协作流程”，但当前实际 `create_workflow()` 里只注册了：

1. `analysis`
2. `retrieval`
3. `planning`
4. `coding`

并且边只有：

1. `analysis -> retrieval`
2. `retrieval -> planning`
3. `planning -> coding`
4. `coding -> END`

没有条件路由，没有循环重试边，也没有 execution / validation / debugging 分支。

### 2.2 当前输出主产物是 JSON，不是 Python 代码

状态字段名叫 `generated_code`，这是历史命名残留。  
当前它实际保存的是**平台 JSON 字符串**，不是 Python 源码。

### 2.3 Analysis 负责“理解”，Retrieval 负责“检索”，Planning 负责“规划”，Coding 负责“落地”

这是当前版本最核心的职责分层：

1. **Analysis Agent**：把自然语言需求拆成 `retrieval_plan` + `scenario_analysis`
2. **Retrieval Agent**：消费 `retrieval_plan`，去 ChromaDB 取模块定义
3. **Planning Agent**：基于检索结果白名单生成 `execution_plan`
4. **Coding Agent**：把 `execution_plan` + `retrieval_context` 落成最终 JSON

### 2.4 真正的“硬约束”来自检索结果，不是来自分析结果

`analysis_result` 主要给 Planning Agent 提供业务语义参考；  
真正约束 Planning Agent 只能用哪些模块的，是 `retrieval_context.relevant_nodes` 中的 `module_type` 白名单。

### 2.5 schema 改了以后，必须同步知识库

`schemas/` 是检索知识库的源数据，向量库存放在 `chroma_db/`。  
如果改了模块 JSON 而没有更新知识库，工作流会继续检索旧版本模块定义，后续规划和编码都会基于旧数据运行。

---

## 3. 与 LangGraph 的关系

当前实现使用的是 LangGraph 的 `StateGraph` 模式，这和官方 Graph API 的基本用法一致：

1. 先定义共享状态 `WorkflowState`
2. 再添加节点
3. 再连边
4. `compile()`
5. `invoke(initial_state, config=...)`

官方文档也强调了这一点：节点读取状态、返回状态更新，图在 `compile()` 之后才可执行。  
本项目当前的实现方式更直接：**各节点拿到 `state` 后直接原地修改，再把整个 `state` 返回**。这在当前线性单链路里是可行的，也比较直观。

参考：

- LangGraph Graph API overview: https://docs.langchain.com/oss/python/langgraph/graph-api

---

## 4. 当前正式工作流的入口与运行方式

### 4.1 入口函数

正式入口是：

```python
from workflow import run_workflow
result = run_workflow(user_query)
```

`run_workflow()` 的做法很简单：

1. 调 `create_workflow()`
2. `compile()`
3. 准备 `initial_state`
4. `app.invoke(initial_state, config=invoke_config)`
5. 返回最终完整状态字典

### 4.2 初始状态

`workflow.py` 中初始化了这些状态字段：

| 字段 | 当前是否主链使用 | 说明 |
|:---|:---:|:---|
| `user_query` | 是 | 用户自然语言需求 |
| `analysis_result` | 是 | Analysis Agent 输出 |
| `retrieval_context` | 是 | Retrieval Agent 输出 |
| `execution_plan` | 是 | Planning Agent 输出 |
| `generated_code` | 是 | 实际是 JSON 字符串 |
| `execution_result` | 否 | 预留给 Execution Tool |
| `validation_result` | 否 | 预留给 Validation Agent |
| `debug_history` | 否 | 预留给 Debugging Agent |
| `retry_count` | 否 | 预留给调试闭环 |
| `current_step` | 部分使用 | 每个节点会更新 |
| `next_step` | 否 | 当前主链未使用 |
| `final_output` | 否 | 当前 `workflow.py` 本身几乎没用它 |

### 4.3 `__main__` 的行为

直接执行：

```powershell
conda activate midea
python workflow.py
```

会做一轮示例调用，并打印：

1. analysis 摘要
2. retrieval 摘要
3. planning 摘要
4. coding 产出的 JSON 预览

然后把 JSON 写到 `generated_flow/`。

---

## 5. 四个正式节点的真实行为

### 5.1 Analysis Agent：把需求变成“检索计划 + 业务场景摘要”

文件：`agents/analysis_agent.py`

### 输入

只读 `user_query`

### 输出

写入 `analysis_result`，结构分三块：

1. `retrieval_plan`
2. `scenario_analysis`
3. `metadata`

### 真正的职责

它不是去检索模块，而是先把用户需求做结构化理解：

1. 为 Retrieval Agent 生成多条查询变体 `queries`
2. 推断一级分类 `category_l1`
3. 推断意图 `intent`
4. 识别数学运算 `detected_operations`
5. 生成给 Planning Agent 用的 `scenario_analysis`

### 关键实现细节

1. 采用 `with_structured_output(AnalysisResult, method="function_calling")` 作为主路径
2. Structured Output 失败时，回退到普通 `llm.invoke()` + JSON 提取解析
3. 再失败时，使用本地 `_fallback_result()`
4. 有一个进程内缓存 `_analyze_cache`
5. 会对模型输出做二次标准化，而不是直接信任原始结果

### 当前约束

1. `intent` 只能落在固定枚举中
2. `detected_operations` 只能是固定枚举
3. `summary` 最长 120 字
4. `ambiguities` / `assumptions` 数量受 `config.py` 限制

### 对下游的意义

1. 给 Retrieval 提供“怎么搜”
2. 给 Planning 提供“从什么业务角度理解需求”

---

### 5.2 Retrieval Agent：纯检索执行器，不再自己做 LLM 分析

文件：`agents/retrieval_agent.py`

### 输入

1. `user_query`
2. `analysis_result`

### 输出

写入 `retrieval_context`

### 当前定位

当前版本的 Retrieval Agent 已经从“检索 + LLM 查询改写混合节点”收缩为：

1. 读取 `analysis_result.retrieval_plan`
2. 再本地标准化一次
3. 调 ChromaDB 做向量检索
4. 把完整模块定义返回给下游

### 为什么它必须返回“完整模块定义”

因为 Planning 和 Coding 不只需要“这个模块相关”，还需要：

1. `parameters_schema`
2. `ports_definition`
3. `template_json`
4. `usage_guides`
5. `category`

其中最关键的是 `template_json`，Coding Agent 会直接依赖它来生成最终平台 JSON。

### 检索主逻辑

如果 `queries` 非空：

1. 走批量多查询
2. 每个变体最多取 `min(top_k, 5)` 条
3. 用 `1 / (1 + distance)` 把 L2 距离转为相似度
4. 过滤低分结果
5. 按 `module_type` 去重
6. 保留分数更高者

如果 `queries` 为空或上游无效：

1. 用原始 `user_query` 做单查询兜底检索

### 重要元数据

`retrieval_context.metadata` 中除了检索统计，还会补充：

1. `rewrite_used`
2. `analysis_used`
3. `llm_queries`
4. `llm_category_l1`
5. `analysis_summary`
6. `analysis_confidence`
7. `intent`
8. `detected_operations`

注意：这些字段名里有些仍带 `llm_` 前缀，但现在更多表示“用了上游分析结果”，不表示 Retrieval 自己又调用了 LLM。

### 初始化依赖

它会初始化：

1. `chromadb.PersistentClient(path=config.CHROMA_PERSIST_DIR)`
2. Embedding 函数
3. 集合 `kong_modules_v1`

---

### 5.3 Planning Agent：在模块白名单内生成 PlanIR

文件：`agents/planning_agent.py`

### 输入

1. `user_query`
2. `retrieval_context`
3. `analysis_result`

### 输出

写入 `execution_plan`

### 本质

Planning Agent 不是直接生成平台 JSON，而是生成一个中间层 IR：`PlanIR`

它由三部分组成：

1. `goal`
2. `nodes`
3. `connections`

其中：

1. `nodes` 是逻辑节点实例
2. `connections` 是逻辑连接关系

### 硬约束与软参考

这是理解 Planning Agent 的关键：

1. `retrieval_context` 是**硬约束**
2. `analysis_result` 是**软参考**

也就是说：

1. `analysis_result` 可以帮助它理解“这是 AHU 还是负荷计算”
2. 但不能赋予它凭空发明模块的权力

### 它如何约束自己

1. 先从 `retrieval_context.relevant_nodes` 提取可用 `module_type` 集合
2. 用 `format_docs_for_planner()` 把检索结果压缩成轻量上下文
3. 用 `_format_analysis_context()` 把 `scenario_analysis` 压成业务提示
4. 用 Structured Output 生成 `PlanIR`
5. 校验连接引用是否合法
6. 校验 `module_type` 是否在白名单内
7. 出错时把上一轮错误反馈给模型再重试

### 当前的一个重要边界

如果 `retrieval_context` 为空，则 `_available_module_types` 也会为空。  
此时 `validate_module_types()` 会被跳过，Planning Agent 仍可能生成一个“形式上完整、但未受模块白名单约束”的计划。

这意味着：

1. 正常情况：规划受检索结果强约束
2. 检索为空时：规划质量会明显下降，且可能出现不可靠 module_type

### 当前 retry 语义

`PLANNING_MAX_RETRIES = 2` 表示**总尝试次数最多 2 次**，不是“失败后还能额外重试 2 次”。

---

### 5.4 Coding Agent：确定性模板落地器

文件：`agents/coding_agent.py`、`agents/coding_utils.py`

### 输入

1. `execution_plan`
2. `retrieval_context`

### 输出

写入 `generated_code`，但内容其实是 JSON 字符串

### 核心思想

它不再做 LLM 推理，只做确定性转换：

1. 读 `execution_plan.nodes`
2. 按 `module_type` 去 `retrieval_context` 找模板
3. 给每个逻辑节点生成真实短 ID
4. 计算节点坐标
5. 生成 `wires`
6. 把参数填进模板
7. 输出最终 JSON 列表

### 生成时的重要步骤

1. 用 `doc_map = {module_type: node_doc}` 建索引
2. 用 `generate_short_uuid()` 生成 7 位短 ID
3. 用 `topological_layout()` 做自动布局
4. 用 `build_reverse_connections()` 把逻辑连接转成平台 `wires`
5. 先创建一个 `tab` 容器节点
6. 再逐个填充模块模板

### 关键辅助函数

#### `resolve_input_count()`

用来解析不同模板的输入端口数量表达方式，兼容：

1. `{{inputs}}`
2. `{{inputCount}}`
3. `{{inputsCount}}`
4. `{{channelsPlusOne}}`
5. 固定数字

这是适配多种 schema 的关键逻辑之一。

#### `fill_template()`

负责：

1. 注入 `id / z / x / y / wires`
2. 注入节点参数
3. 处理 `user_defined_name`
4. 处理 `inputs` 占位符映射
5. 清除未替换的 `{{placeholder}}`

### 当前行为边界

1. 如果 `execution_plan` 引用了一个不在 `doc_map` 的 `module_type`，它会**跳过该节点**，而不是抛异常中断
2. 如果整体生成报错，会返回一个 `disabled=true` 的失败 tab，而不是让整个流程崩溃

这说明它更重视“尽量输出一个结果”，而不是“严格失败即中断”。

---

## 6. 当前工作流里的主要数据契约

### 6.1 `analysis_result`

给 Retrieval 和 Planning 用。

关键字段：

1. `retrieval_plan.queries`
2. `retrieval_plan.category_l1`
3. `retrieval_plan.intent`
4. `retrieval_plan.detected_operations`
5. `scenario_analysis.summary`
6. `scenario_analysis.control_strategy`
7. `scenario_analysis.ambiguities`
8. `scenario_analysis.assumptions`

### 6.2 `retrieval_context`

给 Planning 和 Coding 用。

关键字段：

1. `relevant_nodes[*].module_type`
2. `relevant_nodes[*].parameters_schema`
3. `relevant_nodes[*].ports_definition`
4. `relevant_nodes[*].template_json`
5. `metadata.retrieved_count`
6. `metadata.avg_confidence_score`

### 6.3 `execution_plan`

给 Coding 用。

关键字段：

1. `goal`
2. `nodes[*].logic_id`
3. `nodes[*].module_type`
4. `nodes[*].parameters`
5. `connections[*].from_node`
6. `connections[*].from_port_index`
7. `connections[*].to_node`
8. `connections[*].to_port_index`

### 6.4 `generated_code`

当前是最终 JSON 文本，不是代码 AST，也不是 Python 脚本。

---

## 7. `workflow.py` 与 `workflow_trace.py` 的区别

这两个文件很容易混淆。

### 7.1 `workflow.py`

这是当前最直接、最像“正式入口”的版本：

1. 只有四个启用节点
2. 没有节点输入输出跟踪
3. 没有 trace 文件落盘
4. 返回最终状态

### 7.2 `workflow_trace.py`

这是一个**更适合调试和观察**的增强版编排文件：

1. 仍然只启用了四个正式节点
2. 但会用 `_wrap_node()` 包装节点
3. 记录每个节点的输入、输出、耗时、变化字段
4. 把记录写到 `outputs/workflow_trace_时间戳/`
5. 把 trace 路径塞到 `result["final_output"]["workflow_trace"]`

### 什么时候优先看它

如果后续需要做下面这些事，优先看 `workflow_trace.py`：

1. 排查某个节点到底改了哪些状态字段
2. 比较多次运行的节点输入输出差异
3. 调 prompt 或模块 schema 后观察上下游连锁影响

### 一个现实判断

如果未来要继续扩展 execution / validation / debugging 闭环，`workflow_trace.py` 比 `workflow.py` 更适合作为继续演进的基础。

---

## 8. 仓库里“存在但未接入正式主链”的能力

### 8.1 Execution Tool

文件：`tools/execution_tool.py`

当前定位更像“执行 Python 代码的沙箱”：

1. 输入 `generated_code`
2. 用 `exec()` 执行
3. 捕获 `stdout/stderr`
4. 试图把输出解析成 JSON

### 为什么它和现主链不一致

因为当前 `generated_code` 已经不是 Python 代码，而是最终平台 JSON 字符串。  
所以如果直接把它接到当前主链后面，语义会不匹配。

### 8.2 Validation Agent

文件：`agents/validation_agent.py`

当前问题有两个：

1. 语义验证 LLM 调用还是 TODO / 示例实现
2. 形式化验证假设的 JSON 结构更像 `{nodes, wires}`，和当前 Coding Agent 输出的“节点数组”格式并不完全一致

因此它目前不能视为已经适配当前 JSON 产物。

### 8.3 Debugging Agent

文件：`agents/debugging_agent.py`

它同样延续的是“分析错误并返回修正后的 Python 代码”的旧思路。  
而当前正式工作流的产物是平台 JSON，不是 Python 代码，所以它也没有和现主链对齐。

### 8.4 结论

这三者目前都更适合被看作：

1. 历史方案残留
2. 未来闭环能力的骨架
3. 暂未完成的扩展方向

不要把它们当成“已经接入但只是没打开开关”的成熟能力。

---

## 9. 配置与运行时事实

文件：`config.py`、`utils/model_manager.py`

### 9.1 默认模型与嵌入

默认配置里：

1. `LLM_PROVIDER = deepseek`
2. `EMBEDDING_PROVIDER = bge`

支持的 LLM provider 包括：

1. `deepseek`
2. `openai`
3. `qwen`
4. `glm`
5. `kimi`

### 9.2 当前最关键的两个 agent 温度

1. `ANALYSIS_LLM_TEMPERATURE = 0.2`
2. `PLANNING_LLM_TEMPERATURE = 0.7`

这很符合两者定位：

1. Analysis 更偏结构化分析，温度低
2. Planning 更偏组合式规划，温度高一些

### 9.3 运行时跟踪

代码里有：

```python
os.environ["LANGCHAIN_TRACING_V2"] = "false"
```

但 `create_workflow()` / `run_workflow()` 仍用了 `@traceable` 装饰器。  
可以理解为：代码保留了 LangSmith 追踪钩子，但默认把 V2 tracing 关掉了。

---

## 10. `docs/` 目录到底提供了什么

当前 `docs/` 目录当前最关键的是这 5 份文档：

1. `analysis_agent_summary.md`
2. `new_retrieval_agent_summary.md`
3. `planning_agent_summary.md`
4. `coding_agent_summary.md`
5. `knowledge_base_update_guide.md`

这些文档的价值主要在于：

1. 说明各 agent 的职责边界
2. 记录从旧架构到现架构的变化
3. 明确哪些是硬约束、哪些只是软参考
4. 告诉你 schema 变更后该如何同步知识库

整体上，这些文档和当前代码是**大体一致**的，尤其是四个核心 agent 的职责分工基本对齐。  
不过如果以后出现冲突，应优先相信源码。

---

## 11. `schemas/`、`chroma_db/`、知识库更新链路

### 11.1 `schemas/`

这里是模块定义源头。每个模块 JSON 通常包含：

1. 模块基本信息
2. `parameters_schema`
3. `ports_definition`
4. `template_json`
5. `usage_guides`

### 11.2 `chroma_db/`

这是向量数据库持久化目录。Retrieval Agent 实际从这里检索，不是每次运行都重新读 `schemas/`。

### 11.3 更新方式

已存在的维护入口包括：

1. `init_knowledge_base.py`
2. `update_knowledge_base.py`
3. `auto_sync_schemas.py`
4. `RetrievalAgent.load_knowledge_base()`

### 最重要的操作原则

只要 `schemas/` 改了，就要同步知识库。否则：

1. Analysis 还会产出新的检索计划
2. Retrieval 却会检到旧模块
3. Planning / Coding 会基于旧模块规格继续工作

这会造成非常隐蔽的错位。

---

## 12. 当前版本最容易误解的点

### 12.1 误解一：Retrieval 还在自己做查询改写

不是。  
现在查询改写、意图识别、业务场景理解都已经前移到 Analysis Agent。

### 12.2 误解二：Planning 只看检索结果，不看分析结果

不是。  
Planning 同时看两者，只是：

1. 检索结果是硬约束
2. 分析结果是软参考

### 12.3 误解三：Coding Agent 生成 Python 代码

不是。  
它生成的是平台 JSON 配置。

### 12.4 误解四：仓库里有 Validation / Debugging，就说明闭环已经打通

不是。  
这些能力现在还没有和当前 JSON 主链对齐。

### 12.5 误解五：`generated_code` 就是“最终对外接口”

不完全是。  
它是当前主链最终产物，但字段命名是历史遗留，后续如果继续演进，最好统一成更准确的命名，例如 `generated_flow_json` 或 `generated_config_json`。

---

## 13. 如果以后要改这个工作流，优先关注哪些地方

### 13.1 改需求理解质量

优先看：

1. `agents/analysis_agent.py`
2. `config.py` 中 Analysis 相关参数
3. `docs/analysis_agent_summary.md`

### 13.2 改检索命中质量

优先看：

1. `agents/retrieval_agent.py`
2. `schemas/`
3. `chroma_db/`
4. `docs/new_retrieval_agent_summary.md`
5. `docs/knowledge_base_update_guide.md`

### 13.3 改规划正确性

优先看：

1. `agents/planning_agent.py`
2. `utils/context_formatter.py`
3. `docs/planning_agent_summary.md`

### 13.4 改 JSON 生成质量

优先看：

1. `agents/coding_agent.py`
2. `agents/coding_utils.py`
3. `docs/coding_agent_summary.md`

### 13.5 接入执行/验证/调试闭环

优先看：

1. `workflow_trace.py`
2. `tools/execution_tool.py`
3. `agents/validation_agent.py`
4. `agents/debugging_agent.py`

但要先统一一个前提：  
**后续主产物到底还是 Python 代码，还是继续保持平台 JSON。**  
在这个问题没有明确前，不建议直接把旧版 execution/validation/debugging 骨架硬接回去。

---

## 14. 推荐的新对话预读顺序

如果以后每个新对话都想快速进入状态，建议按下面顺序读：

1. 先读本文档
2. 再看 `workflow.py`
3. 再看 `agents/analysis_agent.py`
4. 再看 `agents/retrieval_agent.py`
5. 再看 `agents/planning_agent.py`
6. 再看 `agents/coding_agent.py`
7. 如果要调试运行过程，再看 `workflow_trace.py`
8. 如果涉及 schema 或模块定义，再看 `docs/knowledge_base_update_guide.md`

如果只想快速掌握“主链是怎么跑起来的”，读完本文档再加 `workflow.py` 基本就够了。  
如果要改质量，必须继续下钻到各 agent 和 `schemas/`。

---

## 15. 最终总结

当前项目已经形成了一条比较清晰的四层流水线：

1. **Analysis**：把自然语言转成结构化理解
2. **Retrieval**：把结构化理解转成模块检索结果
3. **Planning**：把模块检索结果转成逻辑拓扑
4. **Coding**：把逻辑拓扑转成平台 JSON

这条链路当前的最大优点是：

1. 职责分层清楚
2. 检索结果对白名单约束明确
3. 编码阶段是确定性的，不再依赖生成式输出

当前的主要限制也很明确：

1. 正式主链还没有 execution / validation / debugging 闭环
2. 部分状态字段和类注释仍带旧版痕迹
3. `generated_code` 命名不准确
4. 检索为空时，Planning 的白名单约束会弱化
5. 现有 Validation / Debugging 仍未适配“最终产物是平台 JSON”的现实

因此，对这个仓库最准确的理解不是“一个完整闭环的自动代码生成系统”，而是：

**一个已经基本跑通“需求理解 -> 模块检索 -> 逻辑规划 -> JSON 组态生成”的 LangGraph 多智能体工作流系统。**
