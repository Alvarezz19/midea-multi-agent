    {
        "id": "",
        "module_type": "bacipOutput",
        "category": "变量模块/BACIP_IO",
        "name": "BACIP IO(BACIP 输入/输出)",

        // --- 1. 检索与理解层 (供检索智能体使用) ---
        // 这部分内容将被转化为向量 (Vector)，用于匹配用户的自然语言需求
        "description": "BACIP IO模块可以调用控制器所在BACnet IP网络上的其它设备的BACnet点位对象。模块的实例对象，可实现对BACnet IP设备的点对象进行读写。",
        "keywords": [ "BACnet", "BACIP", "IO", "数据传输", "控制", "BACnet/IP", "模拟输入", "模拟输出", "模拟值", "二进制输入", "二进制输出", "二进制值", "多状态输入", "多状态输出", "多状态值", "设备号", "对象地址", "写优先级", "转换", "比例系数", "楼宇自动化", "工业控制", "集成"]
        "usage_guides": [],

        // --- 2. 参数约束层 (供规划智能体/LLM 填充参数使用) ---
        // 这部分定义了各个参数的合法取值，防止 AI 产生幻觉
        "parameters_schema": {
            "user_defined_name": {
            "type": "string",
            "description": "该模块的名称",
            "default": "BACIP IO"
            },
            "bacnetIpDeviceInstance": {
            "type": "integer",
            "description": "设备地址",
            "minimum":0,
            "maximun":4194303,
            "default": 0
            }
            

        },

        // --- 3. 生成模板层 (最终产出的代码) ---
        // 规划智能体将填好的参数代入此模板
        // 注意：id, x, y, z, wires 这些拓扑参数留空或设为占位符，由规划智能体在连线时动态生成

         "template_json": {

        //需要AI填入
        "id":  "{{__GEN_ID__}}",//特殊标记，指示系统生成唯一 ID
        "z": "{{__FLOW_ID__}}",//特殊标记，指示归属的流程 ID
        "name": "{{user_defined_name}}",
        
        // 动态生成
        "x": 0, // 模块在流程图中的x坐标
        "y": 0,// 模块在流程图中的y坐标
        "wires": [],// 模块之间的连线
        "inputs": 0,// 模块的输入数量
        "outputs": 0,// 模块的输出数量
        
        // 以下为默认参数，不需要 AI 操心，直接硬编码
        "type": "bacipOutput",
        "protocol": "ip_output",
        "bacnetIpDeviceInstance": 0,
        "bacnetIpObjectType": "OBJECT_ANALOG_VALUE",
        "bacnetIpObjectInstance": 0,
        "bacnetIpPriority": 16,
        "hwInputConvert": "none",
        "hwInputConvertScale": 1,
        "outOfService": 0,
        "outOfServiceValue": 0

    }
}

