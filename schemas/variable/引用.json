    {
        "id": "",
        "module_type": "quote",
        "category": "变量模块/引用",
        "name": "引用",

        // --- 1. 检索与理解层 (供检索智能体使用) ---
        // 这部分内容将被转化为向量 (Vector)，用于匹配用户的自然语言需求
        "description": "引用模块对实例对象中的某一输出端口的值进行调用。在参数配置中，“标注名称”栏中，输入目标实例对象的关键字/词，则在输入栏中自动列出通过关键字/词索引到的实例对象，从中选择目标实例即可。其中，关键字/词的内容包括实例对象的节点地址，名称，或BACnet的对象名称。",
        "keywords": [  "引用", "节点引用", "数据共享", "数据复用"],
        "usage_guides": [],

        // --- 2. 参数约束层 (供规划智能体/LLM 填充参数使用) ---
        // 这部分定义了各个参数的合法取值，防止 AI 产生幻觉
        "parameters_schema": {
            "user_defined_name": {
            "type": "string",
            "description": "该模块的名称",
            "default": "引用"
            },
            "labelName":{
            "type": "string",
            "description": "引用模块的id、输出口编号和名字",
            "default": "[\"实例对象的节点地址\":\"实例对象的输出端口号\"] \"实例对象名称\""
            }
        },


        "ports_definition": {
            
            "outputs":[
                {
                    "index": 0,
                    "label":"输出",
                    "type":"number",
                    "description":"引用实例对象某一输出端口的值",
                    "condition":"always"

                }
            ]

        },

        // --- 3. 生成模板层 (最终产出的代码) ---
        // 规划智能体将填好的参数代入此模板
        // 注意：id, x, y, z, wires 这些拓扑参数留空或设为占位符，由规划智能体在连线时动态生成

         "template_json": {

        //需要AI填入
        "id":  "{{__GEN_ID__}}",//特殊标记，指示系统生成唯一 ID
        "z": "{{__FLOW_ID__}}",//特殊标记，指示归属的流程 ID
        "name": "{{user_defined_name}}",
        "labelName": "{{labelName}}",
        
        // 动态生成
        "x": 0, // 模块在流程图中的x坐标
        "y": 0,// 模块在流程图中的y坐标
        "wires": [],// 模块之间的连线
        "inputs": 0,// 模块的输入数量
        "outputs": 0,// 模块的输出数量
        
        // 以下为默认参数，不需要 AI 操心，直接硬编码
        "type": "quote",
        "labelNameOld": "",
        "outOfService": 0,
        "outOfServiceValue": 0

    }
}