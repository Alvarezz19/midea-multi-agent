    {
        "id": "",
        "module_type": "systemTime",
        "category": "变量模块/系统时间",
        "name": "系统时间",

        // --- 1. 检索与理解层 (供检索智能体使用) ---
        // 这部分内容将被转化为向量 (Vector)，用于匹配用户的自然语言需求
        "description": "系统时间模块用于管理和提供控制系统中的当前时间信息。该模块可以获取和设置系统的日期和时间，确保所有时间相关的操作和记录都基于准确的时间基准。",
        "keywords": ["系统时间", "时间管理", "日期", "时间","秒","分","小时","日","天","月","年","周天"],
        "usage_guides": [],

        // --- 2. 参数约束层 (供规划智能体/LLM 填充参数使用) ---
        // 这部分定义了各个参数的合法取值，防止 AI 产生幻觉
        "parameters_schema": {
            "user_defined_name": {
            "type": "string",
            "description": "该模块的名称",
            "default": "系统时间"
            },
            "timeUnit": {
            "type": "string",
            "description": "常量的具体值",
            "enum":[
                "second",
                "minute",
                "hour",
                "day",
                "month",
                "year",
                "wday"//周天
            ],
            "default": "minute"
            }

        },

        // --- 3. 生成模板层 (最终产出的代码) ---
        // 规划智能体将填好的参数代入此模板
        // 注意：id, x, y, z, wires 这些拓扑参数留空或设为占位符，由规划智能体在连线时动态生成

         "template_json": {

        //需要AI填入
        "id":  "{{__GEN_ID__}}",//特殊标记，指示系统生成唯一 ID
        "z": "{{__FLOW_ID__}}",//特殊标记，指示归属的流程 ID
        "name": "{{user_defined_name}}",
        
        // 动态生成
        "x": 0, // 模块在流程图中的x坐标
        "y": 0,// 模块在流程图中的y坐标
        "wires": [],// 模块之间的连线
        "inputs": 0,// 模块的输入数量
        "outputs": 0,// 模块的输出数量
        
        // 以下为默认参数，不需要 AI 操心，直接硬编码
        "type": "systemTime",
        "outOfService": 0,
        "outOfServiceValue": 0,
        "timeUnit": "minute"

    }
}

