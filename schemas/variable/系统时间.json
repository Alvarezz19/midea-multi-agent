    {
        "id": "",
        "module_type": "systemTime",
        "category": "变量模块/系统时间",
        "name": "系统时间",

        // --- 1. 检索与理解层 (供检索智能体使用) ---
        // 这部分内容将被转化为向量 (Vector)，用于匹配用户的自然语言需求
        "description": "系统时间模块用于输出设备的系统实时时间",
        "keywords": ["系统时间", "时间管理", "日期", "时间","秒","分","小时","日","天","月","年","周天"],
        "usage_guides": [],

        // --- 2. 参数约束层 (供规划智能体/LLM 填充参数使用) ---
        // 这部分定义了各个参数的合法取值，防止 AI 产生幻觉
        "parameters_schema": {
            "user_defined_name": {
            "type": "string",
            "description": "该模块的名称",
            "default": "系统时间"
            },
            "timeUnit": {
            "type": "string",
            "description": "时间的单位;年year:系统年份;月month:1~12,1代表一月,依次12代表12月;日date:1~31;时hour:0~23;分minute:0~59;秒second:0~59;周天day:1~7,1代表星期一,依次7代表星期天",
            "enum":[
                "second",
                "minute",
                "hour",
                "day",
                "month",
                "year",
                "wday"//周天
            ],
            "default": "minute"
            }

        },

        "ports_definition": {
            "outputs":[
                {
                    "index": 0,
                    "label":"输出",
                    "type":"integer",
                    "description":"根据时间单位,输出对应单位的系统实时时间",
                    "condition":"always"

                }
            ]

        },

        // --- 3. 生成模板层 (最终产出的代码) ---
        // 规划智能体将填好的参数代入此模板
        // 注意：id, x, y, z, wires 这些拓扑参数留空或设为占位符，由规划智能体在连线时动态生成

         "template_json": {

        //需要AI填入
        "id":  "{{__GEN_ID__}}",//特殊标记，指示系统生成唯一 ID
        "z": "{{__FLOW_ID__}}",//特殊标记，指示归属的流程 ID
        "name": "{{user_defined_name}}",
        "timeUnit": "{{timeUnit}}",
        
        // 动态生成
        "x": 0, // 模块在流程图中的x坐标
        "y": 0,// 模块在流程图中的y坐标
        "wires": [],// 模块之间的连线
        "inputs": 0,// 模块的输入数量
        "outputs": 0,// 模块的输出数量
        
        // 以下为默认参数，不需要 AI 操心，直接硬编码
        "type": "systemTime",
        "outOfService": 0,
        "outOfServiceValue": 0

    }
}

