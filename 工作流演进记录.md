# 工作流演进记录

> 用途：记录工作流改动、决策、验收与下一步。
> 当前系统画像、状态字段和当前主契约以 [工作流总结文档](工作流总结文档.md) 为准；本文聚焦“何时改了什么、为什么这样改、做到哪了”。

## 1. 当前进度总览

- 当前状态：`Phase 6 已收口，Phase 7/8 当前正式主链已稳定，仓库级 compat / legacy 清退已完成，Workflow API V1 联调版与前端 V1 驾驶舱已落地；复杂 AHU 子流程 body 编译闭环第一轮、平台 flow schema / 知识合同加载器、Analysis A0 工程需求编译增强、Retrieval 查询改写与 Cross-Encoder rerank、Subsystem LLM interface adapter、Architecture LLM advisor 可选增强已落地`
- 当前主链：`analysis -> ambiguity_router -> (clarification_review? -> clarification_apply? -> retrieval | retrieval) -> architecture_planning -> architecture_review? -> architecture_feedback_apply? -> subsystem_planning -> global_assembly -> coding -> verification -> repair_router -> (accept | repair_agent | reject)`
- 最近一次改动：完成 LLM 节点增强工作包 B/C：`SubsystemPlanner` 新增默认关闭的 LLM 接口适配器，`ArchitecturePlanner` 新增默认关闭的 LLM advisor；二者均只采用结构化 patch/advisory，且必须经过本地白名单、端口、模板 ID、页面和 shared signal 约束校验。
- 当前瓶颈：Analysis / Retrieval / Subsystem / Architecture 的可选增强已具备 fake LLM / fake reranker 测试闭环，但 Verification semantic critic、真实 LLM smoke、真实 AHU 大样本端到端质量、浏览器端到端联调、生产级多进程 worker、PostgreSQL checkpointer、SSE 事件流、`repair_review` 与 `Send / reducer` 主链化仍未完成。
- 当前主要风险：strict compile 与平台 flow 校验已经收紧，但当前原子模块合同只覆盖首批高频类型；新增 LLM 增强均默认关闭，真实启用时仍要通过 trace / 节点 IO 观察 fallback、adopted、rejected_patch、reranker 诊断，并避免 LLM 直接生成最终 flows JSON；同时 `analysis / retrieval / coding / repair_agent / workflow_trace` 的可恢复边界虽然已收口，在正式前推 `repair_review` 或 `Send / reducer` 前，仍要继续守住“review 只回写需求侧输入、repair 只修补下一跳真实消费对象”的边界。
- 补充口径：`system_patterns` 在 Phase 3 中已进入 `ArchitecturePlanner.pattern_bindings`，不再只是 prompt hint。
- 资产复现前提：当前 Phase 2 资产构建依赖 `AHU程序/flows_*.json`；若这些源 flow 未纳入版本管理，则需把它们视为外部输入前置条件。
- 下一优先任务：若继续推进下一轮开发，应优先沿 `docs/生成能力改进/LLM节点增强落地方案.md` 继续做 Verification semantic critic 与真实 LLM smoke；`repair_review` 与 `Send / reducer` 是否正式入链可后置评估。

| 能力 | 状态 | 备注 |
|:---|:---:|:---|
| Graph IR / Compiler / Verifier 主链 | 已稳定 | 2026-03-27 建链，2026-03-31 补齐验收缺口 |
| `retrieval_bundle` 与 bundle consumers | 已完成 | 2026-04-03 入链 |
| AHU 资产构建链 | 已完成 | 2026-04-03 落地 |
| 脚本直接入口 / Windows 安全输出 / 临时 Chroma 回归 | 已完成 | 2026-04-04 补齐 |
| Phase 2 整改清单闭环 | 已完成 | 2026-04-07 关闭 P0/P1 项并补全验收 |
| Phase 2 collections 正式落库 | 已完成 | 2026-04-09 冻结 `outputs/chroma_db` / manifest / smoke 路径 |
| AHU 资产质量与检索质量 | 进行中 | Phase 2 已落地，质量仍需迭代 |
| Phase 3 分层规划 | 已完成首版 | 主链、状态契约、测试基线已落地，质量仍需迭代 |
| README / 环境基线 / Trace 摘要 | 已完成 | 2026-04-09 完成第三批收口 |
| Phase 4 Repair 闭环 | 已完成首版 | 2026-04-11 已接入正式主链 |
| Phase 4 接口语义收口与结构化 repair payload | 已完成 | 2026-04-14 完成第一轮问题整改 |
| Phase 5 共享信号与 Repair 契约收口 | 已完成 | 2026-04-15 前三批已落地，含 ambiguous shared signal 有限修复/稳定 reject |
| Phase 5 query suite 与 trace 摘要 | 已完成 | 固定 7 条 deterministic case 已收口，trace 摘要已覆盖 Phase 5 诊断字段 |
| Phase 6 real query suite | 已完成 | 默认 case 源已冻结为 12 条，suite 脚本与合同测试已落地 |
| Phase 6 retrieval eval | 已完成 | eval 脚本、摘要结构与合同测试已落地 |
| Phase 6 复杂 AHU 规划质量硬化 | 已完成 | 模板接口覆盖度加权、mismatch 早降级、排风机 alias 资产闭环已落地 |
| Phase 6 `Send / HITL` 准入包 | 已完成 | 已形成副作用、`thread_id`、reducer 风险与裁决资料 |
| Phase 4 baseline trace | 已完成 | 固定 3 条查询已落盘到 `outputs/phase4_baseline/` |
| Phase 4 query smoke | 已完成 | 固定 2 条查询已落盘到 `outputs/phase4_smoke/` |
| `Send` 并行预留设计 | 已完成 | 仅文档落盘，未改主链行为 |
| HITL / interrupt / persistence POC | 已完成 | 独立脚本与记录，不入主链 |
| 仓库级 compat / legacy 清退 | 已完成 | 2026-04-21 已完成 Batch 0-3 结案，`agents/legacy/`、`tests/legacy/` 与 root wrapper 均已清退 |
| Workflow API V1 联调版 | 已完成 | 2026-04-23 已完成 REST 轮询、review resume、SQLite checkpointer、attempt 状态机、trace/state-history 接口与联调说明 |
| Workflow Frontend V1 驾驶舱 | 已完成 | 2026-04-23 已完成 Vite/React/TypeScript 前端、API client、轮询状态机、ReviewCard、结果页、Trace 调试页、attempt 历史、fixture、测试与 README |
| RepairAgent 显式启用开关 | 已完成 | 2026-04-24 默认关闭 repair；需显式传 `enable_repair_agent=True` 才进入 repair 路径 |
| 平台 flow schema / 知识合同加载器 | 已完成首版 | 2026-04-25 已完成 `validate_flow_document(...)` 与 `knowledge_contract_loader.py` |
| 复杂 AHU 子流程 body 编译闭环 | 已完成首轮 | 2026-04-25 已打通 template body -> Graph IR -> Coding -> Verifier -> trace |
| Analysis A0 工程需求编译增强 | 已完成首版 | 2026-04-26 默认关闭；fake LLM 测试覆盖补丁合并、澄清信号与 fallback |
| Retrieval 查询改写与 rerank 增强 | 已完成首版 | 2026-04-26 默认关闭；fake LLM / fake reranker 测试覆盖，不改变正式 bundle 结构 |
| Subsystem LLM interface adapter | 已完成首版 | 2026-04-27 默认关闭；fake LLM 测试覆盖端口 patch 采用、拒绝、fallback 和不强制降级 |
| Architecture LLM advisor | 已完成首版 | 2026-04-27 默认关闭；fake LLM 测试覆盖页面补充、模板偏好、非法 patch 拒绝和 shared signal 多 owner warning |
| Subsystem atomic fallback 合同硬化 | 已完成 | 2026-04-26 修复动态输入端口超过 schema maximum 的验收阻塞 |
| `docs/coding_agent_summary.md` 同步 | 已完成 | 已按 Phase 3 真实输入边界重写 |

## 2. 变更索引

| 记录ID | 日期 | 阶段 | 标题 | 状态 | 影响范围 | 测试状态 | 文档同步 |
|:---|:---|:---|:---|:---:|:---|:---:|:---:|
| REC-2026-04-27-LLM-BC | 2026-04-27 | 生成能力改进 | 完成 Subsystem B 与 Architecture C 可选 LLM 增强，并同步节点契约文档 | 已完成 | architecture / subsystem / llm_enhancers / config / tests / docs | 通过 | 已同步 |
| REC-2026-04-26-LLM-A0A | 2026-04-26 | 生成能力改进 | 完成 Analysis A0 与 Retrieval A 可选 LLM 增强，并修复 Subsystem fallback 合同阻塞 | 已完成 | analysis / retrieval / subsystem / config / tests / docs | 通过 | 已同步 |
| REC-2026-04-25-GEN-CLOSE1 | 2026-04-25 | 生成能力改进 | 完成第一轮子流程 body 编译闭环与平台 flow 校验底座 | 已完成 | coding / verifier / graph_ir / knowledge / tests / docs | 已补测试覆盖 | 已同步 |
| REC-2026-04-24-REPAIR-FLAG | 2026-04-24 | Runtime | RepairAgent 改为默认关闭，需显式启用 | 已完成 | workflow / repair / api / frontend / tests / docs | 已补测试覆盖 | 已同步 |
| REC-2026-04-23-FE-V1 | 2026-04-23 | Frontend | 完成 Workflow Frontend V1 工作流驾驶舱 | 已完成 | frontend / app / docs | 通过 | 已同步 |
| REC-2026-04-23-API-V1 | 2026-04-23 | API | 完成 Workflow API V1 联调版与运行契约冻结 | 已完成 | app / requirements / tests / docs | 通过 | 已同步 |
| REC-2026-04-21-CLEANUP | 2026-04-21 | Cleanup | 完成仓库级 compat / legacy 清退结案与历史文档收口 | 已完成 | workflow / agents / utils / tests / docs | 已核对 | 已同步 |
| REC-2026-04-17-P8-EFG | 2026-04-17 | Phase 8 | 完成工作包 E/F/G：persistence smoke、review 索引与文档收口 | 已完成 | workflow_trace / review index / smoke script / tests / docs | 通过 | 已同步 |
| REC-2026-04-17-P8-C | 2026-04-17 | Phase 8 | 完成工作包 C：`architecture_planning` 后主评审节点 | 已完成 | workflow / workflow_trace / architecture review agents / tests / docs | 通过 | 已同步 |
| REC-2026-04-17-P8-AB | 2026-04-17 | Phase 8 | 完成工作包 A/B：第七阶段收口与 `analysis` 后条件式澄清 | 已完成 | workflow / workflow_trace / analysis / clarification agents / tests / docs | 通过 | 已同步 |
| REC-2026-04-17-P7-F | 2026-04-17 | Phase 7 | 完成工作包 F：独立测试图中的 `Send + reducer` 试点 | 已完成 | phase3_contracts / send-poc / tests / docs | 通过 | 已同步 |
| REC-2026-04-17-P7-MIN | 2026-04-17 | Phase 7 | 完成最小工程改造：runtime/trace/stable-id/repair-boundary/docs | 已完成 | workflow / workflow_trace / coding / repair / tests / docs | 通过 | 已同步 |
| REC-2026-04-17-P6 | 2026-04-17 | Phase 6 | 完成第六阶段 A/B/C 状态入档并补齐 Phase 7 准入包 | 已完成 | architecture / subsystem / knowledge_builder / phase6 scripts / tests / docs | 通过 | 已同步 |
| REC-2026-04-15-P5-BATCH4 | 2026-04-15 | Phase 5 | 完成第五阶段第四批：Query Suite、Trace 摘要与文档同步 | 已完成 | workflow_trace / contracts / scripts / tests / docs | 通过 | 已同步 |
| REC-2026-04-14-P4-HARDEN | 2026-04-14 | Phase 4 | 完成第四阶段第一轮整改：接口语义收口、结构化 Repair、Query Smoke | 已完成 | architecture / subsystem / assembly / verifier / repair / tests / scripts / docs | 通过 | 已同步 |
| REC-2026-04-11-P4 | 2026-04-11 | Phase 4 | 完成 Phase 4 首版：Repair 闭环、Trace 增强、并行预留与 HITL POC | 已完成 | workflow / trace / repair / tests / scripts / docs / baseline | 通过 | 已同步 |
| REC-2026-04-09-P3-DOCS-ENV | 2026-04-09 | Phase 3 | 完成第三批收口：文档、环境基线与 Trace 摘要统一 | 已完成 | trace / docs / requirements / runtime tests / legacy module status | 通过 | 已同步 |
| REC-2026-04-08-P3-HARDEN | 2026-04-08 | Phase 3 | 验收加固：真实集成回归、模板匹配修正、共享信号未解析即失败 | 已完成 | architecture / assembly / verifier / phase3 tests / docs | 通过 | 已同步 |
| REC-2026-04-07-P3 | 2026-04-07 | Phase 3 | 接入分层规划主链并建立回归基线 | 已完成 | workflow / analysis / phase3 agents / tests / docs | 通过 | 已同步 |
| REC-2026-04-07-P2-CLOSE | 2026-04-07 | Phase 2 | 关闭第二阶段整改清单并完成验收 | 已完成 | planning / verifier / knowledge_builder / tests / docs | 通过 | 已同步 |
| REC-2026-04-04-P2-HARDEN | 2026-04-04 | Phase 2 | 补齐 Phase 2 验收硬化：脚本入口 / Windows 编码 / 临时 Chroma 回归 | 已完成 | scripts / retrieval / model_manager / console / tests / docs | 通过 | 已同步 |
| REC-2026-04-03-P2 | 2026-04-03 | Phase 2 | 完成 `retrieval_bundle` 主链接入与 AHU 资产构建链落地 | 已完成 | retrieval / planning / assembly / coding / config / scripts / tests / docs | 通过 | 已同步 |
| REC-2026-03-31-P1-FIX | 2026-03-31 | Phase 1 | 补齐 Phase 1 验收缺口并更新回归测试 | 已完成 | assembly / coding / verifier / workflow_trace / tests / docs | 通过 | 已同步 |
| REC-2026-03-27-P1 | 2026-03-27 | Phase 1 | 完成 Graph IR 重构并建立离线验收 | 已完成 | workflow / assembly / coding / verifier / tests | 通过 | 已同步 |

## 3. 记录规范

- 每条记录至少包含：状态、修改人、关联文件、背景、目标、代码/契约变化、影响、验证、风险/回滚、文档同步、后续动作、DoD。
- 当前系统画像、状态字段和主契约不在本文重复展开；统一以 [工作流总结文档](工作流总结文档.md) 为准。

## 4. 详细记录

### [2026-04-27] 完成 Subsystem B 与 Architecture C 可选 LLM 增强，并同步节点契约文档（Record ID: REC-2026-04-27-LLM-BC）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`config.py`、`agents/subsystem_planner.py`、`agents/architecture_planner.py`、`agents/llm_enhancers/subsystem_interface_adapter.py`、`agents/llm_enhancers/architecture_advisor.py`、`tests/test_subsystem_llm_adapter.py`、`tests/test_architecture_llm_advisor.py`、`工作流总结文档.md`、`工作流节点输入输出说明.md`、`工作流演进记录.md`
- 背景：`docs/生成能力改进/LLM节点增强落地方案.md` 的第一批闭环要求在 Analysis A0 与 Retrieval A 之后继续完成 `SubsystemPlanner` LLM interface adapter；随后第二批 C 要求补 `ArchitecturePlanner` LLM advisor。二者都必须保持默认关闭、结构化输出、本地白名单采用和 deterministic fallback，不改变正式 LangGraph 拓扑。
- 目标：
  - 为 `SubsystemPlanner` 增加默认关闭的接口适配器，使 LLM 可建议模板端口与信号绑定，但不能绕过本地 `_analyze_template_interface()`
  - 为 `ArchitecturePlanner` 增加默认关闭的架构 advisor，使 LLM 可建议页面补充、子系统页签/优先级、模板偏好和 shared signal advisory，但不能删除必需页、删除显式子系统或引入候选集外模板
  - 增加 fake LLM 单测覆盖采用、拒绝、fallback、不强制降级和开关关闭路径
  - 将已完成的 LLM 节点增强能力同步到当前工作流总览与节点输入输出契约文档
- 代码/契约变化：
  - `config.py` 新增 `SUBSYSTEM_USE_LLM_ADAPTER`、`SUBSYSTEM_LLM_PROVIDER`、`SUBSYSTEM_LLM_MODEL`、`SUBSYSTEM_LLM_TIMEOUT_S`
  - `config.py` 新增 `ARCHITECTURE_USE_LLM_ADVISOR`、`ARCHITECTURE_LLM_PROVIDER`、`ARCHITECTURE_LLM_MODEL`、`ARCHITECTURE_LLM_TIMEOUT_S`
  - 新增 `agents/llm_enhancers/subsystem_interface_adapter.py`，定义 `PortBindingPatch`、`SubsystemInterfaceAdvice` 与 `SubsystemInterfaceAdapter`
  - `SubsystemInterfaceAdapter` 只输出 `port_binding_patch / signal_aliases / missing_bindings / fallback_required / risk_flags`；本地归一化强制校验 `subsystem_id`、当前 `template_id`、`direction`、端口范围、`binding_kind` 白名单、非空 `signal_name` 和 `confidence >= 0.5`
  - `SubsystemPlanner` 新增可注入 `interface_adapter`；在模板复用路径调用 LLM adapter，采用后的 descriptor 会重新进入 `_analyze_template_interface()`，若仍不匹配则继续走原有 atomic fallback
  - `SubsystemPlanner` 在每个子系统 `template_binding.llm_advisory` 写入 enabled、adopted、patch_count、rejected_patch_count、risk_flags、fallback_required、fallback_reason、fallback_used、elapsed_ms 等诊断
  - 新增 `agents/llm_enhancers/architecture_advisor.py`，定义 `ArchitectureAdvice` 与 `ArchitectureAdvisor`
  - `ArchitectureAdvisor` 只输出 `page_patch / subsystem_patch / template_preferences / shared_signal_patch / warnings / confidence`，不生成 Graph IR、node_instances、wires 或最终 JSON
  - `ArchitecturePlanner` 新增可注入 `architecture_advisor`；在 deterministic draft 后调用 advisor，本地过滤后重新生成 `shared_signal_registry` 并传播 shared signal resolution
  - `ArchitecturePlanner` 只允许采用标准页面补充、已存在子系统的 page/priority/template preference patch、候选模板 ID 内的模板偏好；页面删除、未知模板 ID、未知 subsystem_id、未知 signal、多 owner shared signal 均被拒绝并写入 warning/advisory
  - `ArchitecturePlanner` 在 `architecture_plan.pattern_bindings[*].llm_advisory` 写入 enabled、adopted、patch_count、rejected_patch_count、warnings、fallback_used、fallback_reason、elapsed_ms、confidence 等诊断；相关 warning 同步进入 `architecture_plan.warnings` 与 `decomposition_result.warnings`
- 影响：
  - `architecture_planning` 和 `subsystem_planning` 具备可控 LLM advisory / patch 能力，但默认关闭，正式 deterministic 主链不受影响
  - `subsystem_plan_map[*].template_binding.llm_advisory` 与 `architecture_plan.pattern_bindings[*].llm_advisory` 成为可选诊断字段，不是新的顶层 `WorkflowState` 字段
  - LLM patch 不能直接生成最终 flows JSON，也不能绕过后续 `GlobalAssembler`、`CodingAgent`、`VerifierAgent`
  - `ArchitecturePlanner` 的 shared signal 多 owner 情况仍由 deterministic registry 保持 ambiguous，不因 LLM 建议而强行指定 owner
- 验证：
  - `python -m unittest tests.test_subsystem_llm_adapter`
  - `python -m unittest tests.test_phase3_subsystem_planner`
  - `python -m unittest tests.test_architecture_llm_advisor`
  - `python -m unittest tests.test_phase3_architecture_planner`
  - `python -m unittest tests.test_phase3_workflow`
  - `python -m unittest discover -s tests -p "test_phase3*.py"`
  - `python -m unittest tests.test_analysis_engineering_compiler tests.test_retrieval_llm_enhancer tests.test_retrieval_cross_encoder_rerank tests.test_subsystem_llm_adapter tests.test_architecture_llm_advisor`
  - `python -m unittest tests.test_platform_flow_schema tests.test_coding_subflow_body_expansion`
  - `python -m unittest discover -s tests -p "test_phase4*.py"`
  - `python -m compileall app workflow.py workflow_trace.py agents utils tests scripts`
  - 结果：以上命令均通过
- 风险与回滚：
  - 主要风险：真实 LLM 尚未做在线 smoke；当前只保证 fake LLM 与 deterministic fallback 合同闭环
  - 主要风险：真实 AHU 项目级质量收益仍需通过端到端 trace、compile_report、verification_report 和人工复核验证
  - 回滚：关闭 `LLM_ENHANCEMENT_ENABLED`、`SUBSYSTEM_USE_LLM_ADAPTER`、`ARCHITECTURE_USE_LLM_ADVISOR` 即可恢复 deterministic 基线；必要时回退本记录关联代码文件
- 文档同步：`工作流总结文档.md`、`工作流节点输入输出说明.md`、`工作流演进记录.md` 已同步
- 后续动作：
  - 继续实现 Verification semantic critic，但第一轮不改变 `verification_report.status`
  - 增加 fake LLM workflow smoke，用统一 case 对比 baseline 与 LLM-enhanced 指标
  - 增加真实 LLM 本地 smoke，记录 adopted / rejected / fallback 与最终 compile / verify 指标
  - 评估 `workflow_trace.py` 是否需要把 LLM advisory 聚合为 trace summary 顶层摘要
- DoD：Subsystem interface adapter `[x]`；Architecture advisor `[x]`；fake LLM 测试 `[x]`；默认关闭回归 `[x]`；Phase 3/4 回归 `[x]`；文档同步 `[x]`

### [2026-04-26] 完成 Analysis A0 与 Retrieval A 可选 LLM 增强，并修复 Subsystem fallback 合同阻塞（Record ID: REC-2026-04-26-LLM-A0A）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`config.py`、`.env.example`、`agents/analysis_agent.py`、`agents/retrieval_agent.py`、`agents/subsystem_planner.py`、`agents/llm_enhancers/__init__.py`、`agents/llm_enhancers/analysis_engineering_compiler.py`、`agents/llm_enhancers/retrieval_rewrite.py`、`utils/phase3_adapters.py`、`utils/reranker_manager.py`、`utils/retrieval_rerank.py`、`tests/test_analysis_engineering_compiler.py`、`tests/test_retrieval_llm_enhancer.py`、`tests/test_retrieval_cross_encoder_rerank.py`、`tests/test_phase3_subsystem_planner.py`
- 背景：`docs/生成能力改进/LLM节点增强落地方案.md` 明确建议先做 Analysis A0，再做 Retrieval A 和 Subsystem B。实际推进 Retrieval 前，`tests.test_phase3_workflow` 暴露 `fan_damper` 用例在 `SubsystemPlanner` atomic fallback 中把 `modbusOutput.inputCount` 规划为 3，超过 schema maximum=2，导致 strict compile 失败。该问题不属于 LLM 增强顺序本身，而是 deterministic fallback 合同缺口，必须先修复以稳定后续验收基线。
- 目标：
  - 将 `AnalysisAgent` 从“检索计划 + 场景摘要生成器”增强为可选的 AHU 工程需求编译入口，输出结构化工程补丁并合并到 `requirement_spec.engineering`
  - 将 `RetrievalAgent` 中已有但未真正接线的 `RETRIEVAL_USE_LLM_REWRITE` 改为受控可选能力，并新增 Cross-Encoder rerank 工具层
  - 保持所有增强默认关闭，fake LLM / fake reranker 单测覆盖，不改变正式 `retrieval_bundle` 主结构
  - 修复 `SubsystemPlanner` atomic fallback 生成动态端口参数时未遵守 `parameters_schema.maximum` 的合同问题
- 代码/契约变化：
  - `config.py` / `.env.example` 新增 `LLM_ENHANCEMENT_ENABLED`、`LLM_ENHANCEMENT_PROVIDER`、`LLM_ENHANCEMENT_MODEL`、`LLM_ENHANCEMENT_TEMPERATURE`、`LLM_ENHANCEMENT_TIMEOUT_S`
  - `config.py` / `.env.example` 新增 `ANALYSIS_USE_ENGINEERING_COMPILER`、`ANALYSIS_ENGINEERING_LLM_PROVIDER`、`ANALYSIS_ENGINEERING_LLM_MODEL`、`ANALYSIS_ENGINEERING_LLM_TIMEOUT_S`
  - `config.py` / `.env.example` 新增 `RETRIEVAL_USE_CROSS_ENCODER_RERANK`、`RETRIEVAL_RERANKER_PROVIDER`、`RETRIEVAL_RERANKER_MODEL`、`RETRIEVAL_RERANK_TOP_N`、`RETRIEVAL_RERANK_BATCH_SIZE`
  - 新增 `agents/llm_enhancers/analysis_engineering_compiler.py`，定义 `AhuPointSpec`、`AhuControlLoopSpec`、`AhuInterlockSpec`、`AhuRequirementPatch` 与 `EngineeringRequirementCompiler`
  - `AnalysisAgent.__call__()` 在 `ANALYSIS_USE_ENGINEERING_COMPILER=True` 时调用工程需求编译器；成功后写入 `analysis_result.engineering_analysis`，失败时写 fallback 诊断并保持 deterministic `build_requirement_spec(...)` 结果
  - `utils.phase3_adapters.merge_engineering_requirement_patch(...)` 新增受控合并规则：只追加/归一化 `subsystems / signals / required_pages / global_modes / acceptance_criteria / ambiguities / assumptions`，工程扩展写入 `requirement_spec.engineering`
  - `derive_clarification_signals(...)` 增强工程缺失信号：`missing_equipment_quantity`、`missing_point_schedule`、`missing_communication_address`、`missing_control_target`、`missing_pid_loop_signals` 等 high severity 信号可触发澄清
  - 新增 `agents/llm_enhancers/retrieval_rewrite.py`，定义 `RetrievalRewriteResult` 与 `RetrievalQueryRewriter`，只生成查询变体、模板查询、pattern 查询、归一化术语和风险标记，不选择资产 ID
  - `RetrievalAgent` 新增 `_maybe_rewrite_queries(...)`、`_merge_rewrite_into_plan(...)`、`_maybe_rerank_assets(...)`；LLM 查询改写需要 `LLM_ENHANCEMENT_ENABLED=True` 与 `RETRIEVAL_USE_LLM_REWRITE=True` 同时打开，避免 `.env` 单独打开 rewrite 时误触真实 LLM
  - 新增 `utils/reranker_manager.py`，封装 `sentence-transformers.CrossEncoder` 加载；新增 `utils/retrieval_rerank.py`，只重排已召回候选，不创造 `template_id / pattern_id / module_type`
  - `retrieval_bundle.metadata` 新增可选诊断：`llm_rewrite`、`base_query_variants`、`template_query_variants`、`pattern_query_variants`、`reranker_enabled`、`reranker_model`、`reranker_fallback_used`、`reranker`
  - `SubsystemPlanner` 新增 `_input_parameter_maximum(...)` 与 `_supports_desired_inputs(...)`；atomic fallback 主节点选型优先选择能承载目标输入数的模块，若无候选则按 schema maximum 裁剪动态输入参数，并记录 `atomic_fallback_input_capacity_limited` warning
- 影响：
  - `analysis` 和 `retrieval` 已具备第一批可控 LLM 增强能力，但默认关闭，正式主链 deterministic 行为不受影响
  - `requirement_spec.engineering` 作为扩展字段出现；下游不强依赖该字段，后续 Subsystem / Architecture / Verification LLM 增强可逐步消费
  - `RetrievalAgent` 不再把 `.env` 中的 `RETRIEVAL_USE_LLM_REWRITE` 留作“定义了但不生效”的假开关；真实启用仍需总开关，便于分阶段试跑
  - Cross-Encoder rerank 只影响 `subflow_templates` 与 `system_patterns` 的排序，不改变候选集合，不生成新资产
  - `fan_damper` 的 `air_damper_ctrl` atomic fallback 不再生成违反 `Modbus_IO.inputCount.maximum=2` 的编译产物，`tests.test_phase3_workflow` 恢复通过
- 验证：
  - `python -m unittest tests.test_analysis_engineering_compiler tests.test_phase3_requirement_spec tests.test_phase8_clarification_contract`
  - `python -m unittest tests.test_phase3_subsystem_planner`
  - `python -m unittest tests.test_phase3_workflow`
  - `python -m unittest tests.test_retrieval_llm_enhancer tests.test_retrieval_cross_encoder_rerank`
  - `python -m unittest tests.test_phase2_retrieval_agent tests.test_phase2_retrieval_bundle tests.test_phase2_production_chroma_smoke`
  - `python -m unittest tests.test_analysis_engineering_compiler tests.test_phase3_requirement_spec tests.test_phase3_subsystem_planner tests.test_phase8_clarification_contract`
  - `python -m compileall agents utils tests config.py`
  - 结果：以上命令均通过；新增 fake LLM / fake reranker 测试覆盖 rewrite 采用、fallback、默认关闭、不创造资产 ID、reranker 失败回退与 schema maximum 裁剪
- 风险与回滚：
  - 主要风险：真实 LLM / 真实 Cross-Encoder 模型尚未做在线 smoke；当前只保证接线、fallback 与 fake 单测闭环
  - 主要风险：`requirement_spec.engineering` 只是扩展字段，下游 Planner 尚未系统消费；质量收益需要继续做 Subsystem LLM adapter 后才能充分体现
  - 回滚：关闭 `LLM_ENHANCEMENT_ENABLED`、`ANALYSIS_USE_ENGINEERING_COMPILER`、`RETRIEVAL_USE_LLM_REWRITE`、`RETRIEVAL_USE_CROSS_ENCODER_RERANK` 即可恢复 deterministic 基线；必要时回退本记录关联代码文件
- 文档同步：`工作流演进记录.md` 已同步；`工作流总结文档.md` 与 `工作流节点输入输出说明.md` 后续如需展示 `requirement_spec.engineering` 和 retrieval metadata 诊断，可再补充
- 后续动作：
  - 继续按 `docs/生成能力改进/LLM节点增强落地方案.md` 推进 `SubsystemPlanner` LLM interface adapter
  - 增加 fake LLM / fake reranker workflow 级 smoke，观察 `retrieval_bundle.metadata.llm_rewrite / reranker`
  - 增加真实 LLM 与真实 Cross-Encoder 本地 smoke，但不纳入默认 CI
  - 评估 `workflow_trace.py` 是否需要把 `llm_rewrite / reranker` 摘要提升到 trace summary
- DoD：Analysis A0 结构化补丁 `[x]`；工程补丁合并 `[x]`；澄清信号增强 `[x]`；Retrieval LLM rewrite `[x]`；Cross-Encoder rerank 工具层 `[x]`；fake LLM / fake reranker 测试 `[x]`；默认关闭回归 `[x]`；Subsystem fallback schema maximum 修复 `[x]`

### [2026-04-25] 完成第一轮子流程 body 编译闭环与平台 flow 校验底座（Record ID: REC-2026-04-25-GEN-CLOSE1）

- 状态：`已完成`
- 修改人：messi
- 关联文件：`utils/knowledge_contract_loader.py`、`utils/platform_flow_schema.py`、`utils/graph_ir.py`、`agents/assembly_shared.py`、`agents/coding_agent.py`、`agents/verifier_agent.py`、`agents/global_assembler.py`、`workflow_trace.py`、`workflow.py`、`tests/test_module_contract_loader.py`、`tests/test_platform_flow_schema.py`、`tests/test_coding_subflow_body_expansion.py`、`tests/test_phase3_global_assembler.py`、`tests/test_phase3_workflow.py`、`AHU程序/pattern_library/manifest.json`、`docs/生成能力改进/下一步开工计划.md`、`docs/生成能力改进/复杂AHU流程JSON生成能力改进方案.md`、`工作流总结文档.md`、`工作流节点输入输出说明.md`
- 背景：4 月 24 日形成的 [docs/生成能力改进/下一步开工计划.md](docs/生成能力改进/下一步开工计划.md) 明确了第一轮最小闭环目标：先不扩面 `RepairAgent` 和 LLM 增强，优先把“真实子流程模板 -> 合同加载 -> Graph IR 携带 body -> Coding 展开 body -> Verifier / trace 能验能解释”的 deterministic 主链打通。
- 目标：完成工作包 0/B/C/D/E/F/G/H 的首轮落地，建立平台 flow schema 最小校验、原子模块/子流程合同加载器、子流程 body 编译与 strict compile 诊断，让系统至少能以“末端组空送风机标准控制”模板为代表生成带完整 body 的平台 JSON。
- 代码/契约变化：
  - 新增 `utils/knowledge_contract_loader.py`，支持按 `module_type` 从 `schemas/*.json` 加载原子模块合同，并按 `template_id` / pattern library 加载子流程模板与 `internal_flow_objects`
  - 新增 `utils/platform_flow_schema.py` 与 `validate_flow_document(...)`，对平台 flow 顶层结构、唯一 id、`tab/subflow/subflow:<id>`、`z`、`wires`、占位符残留，以及 `swInput / quote / pid / modbusOutput` 等首批模块合同进行结构化校验
  - `SubflowDefinitionIR` 新增 `internal_flow_objects`；`AssemblySharedMixin` 在构建 subflow definition 时把模板 body 深拷贝入 Graph IR
  - `CodingAgent` 新增子流程 body 编译路径：稳定 body id 重映射、`z` 改写到真实 subflow id、body 内部 `wires` 重写、`quote.labelName` 中的 `[id:port]` 引用改写、`subflow.in/out.wires` 指向 body 节点重映射
  - `CompileReport` 新增 `body_node_count / dropped_node_count / missing_template_count / unresolved_placeholder_count / body_expansion_errors`，不再只靠 warning 表达高风险编译问题
  - `VerifierAgent` 接入 `validate_flow_document(...)`，新增 strict compile hard fail；compile_report 中出现节点跳过、模板缺失、占位符残留或 body 展开错误时直接形成 `retryable_error`
  - `workflow_trace.py` 新增 `subflow_instance_count` 以及 body / strict compile 相关摘要字段，trace markdown 可直接显示 pages / subflows / instances / nodes / body_nodes 与 strict compile 计数
  - `GlobalAssembler` 修正 external placeholder instance id 生成规则，避免同一外部信号接入多个消费端时在编译后产生重复 id
  - 测试面新增 `test_module_contract_loader.py`、`test_platform_flow_schema.py`、`test_coding_subflow_body_expansion.py`，并扩展 `test_phase3_global_assembler.py`、`test_phase3_workflow.py` 覆盖 body 编译闭环
  - 文档面新增第一轮开工计划与复杂 AHU 流程 JSON 改进方案，原 `AHU程序/subflow设计.json` 删除，转为文档化约束与计划入口
- 影响：
  - 正式主链开始具备“子流程模板 body 进入编译产物并被平台合同验收”的能力，`compiled_artifact["json_text"]` 不再只是一层 subflow 壳
  - compile 阶段的高风险缺陷从“warning 可忽略”升级为可观测、可验收、可稳定拒绝的结构化诊断
  - 第一轮仍是最小闭环，不代表复杂 AHU 900+ 对象程序已可稳定生成；模块合同覆盖面、真实样例质量与后续 LLM 增强仍需继续推进
- 验证：
  - 提交内新增/更新测试：`tests.test_module_contract_loader`、`tests.test_platform_flow_schema`、`tests.test_coding_subflow_body_expansion`、`tests.test_phase3_global_assembler`、`tests.test_phase3_workflow`
  - 本次补核尝试命令：`python -m unittest tests.test_module_contract_loader tests.test_platform_flow_schema tests.test_coding_subflow_body_expansion tests.test_phase3_global_assembler tests.test_phase3_verifier_native_contract tests.test_phase3_workflow`
  - 本次补核结果：当前 Windows 环境在导入 `pydantic/langgraph` 时触发 `WinError 10106`（`asyncio.windows_events -> _overlapped`），未能在本机会话完成复跑；记录中对“已新增测试覆盖”和“本次补核通过”做区分
- 风险与回滚：
  - 主要风险：当前合同消费只覆盖首批高频模块，若模板或节点类型超出覆盖范围，仍可能出现“能编译但合同语义不完整”的情况
  - 主要风险：body 编译已依赖 pattern library 中的 `internal_flow_objects` 质量；若事实源或缓存不一致，会直接影响最终 JSON 质量
  - 回滚：可回退 `knowledge_contract_loader / platform_flow_schema / graph_ir / coding / verifier / trace` 相关改动，恢复为不展开 body、仅依赖旧 warning 的编译路径
- 文档同步：`工作流总结文档.md`、`工作流节点输入输出说明.md` 与 `docs/生成能力改进/*.md` 已同步；本条记录补入 `工作流演进记录.md`
- 后续动作：按开工计划继续推进子流程接口语义抽取、system pattern 蓝图增强、LLM requirement enrichment / template rerank / interface adapter、semantic verifier 与 golden diff suite；在扩大模板/模块覆盖前先补真实 AHU 样例回归
- DoD：知识合同加载器 `[x]`；平台 flow schema `[x]`；`internal_flow_objects` 入 Graph IR `[x]`；Coding body 展开 `[x]`；strict compile 诊断 `[x]`；Verifier body 校验 `[x]`；trace 编译诊断 `[x]`；最小闭环测试 `[x]`

### [2026-04-24] RepairAgent 改为默认关闭，需显式启用（Record ID: REC-2026-04-24-REPAIR-FLAG）

- 状态：`已完成`
- 修改人：messi
- 关联文件：`workflow.py`、`workflow_trace.py`、`agents/repair_router.py`、`app/api/models.py`、`app/repositories/workflow_run_repository.py`、`app/services/workflow_runner.py`、`app/services/workflow_service.py`、`frontend/src/features/workflow/components/RunComposer.tsx`、`frontend/src/types/workflow.ts`、`tests/test_phase4_repair_router.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase7_runtime_contract.py`、`tests/test_workflow_api.py`、`工作流总结文档.md`、`工作流节点输入输出说明.md`
- 背景：在生成能力改进第一轮里，策略从“默认尝试 repair”改为“默认先做 deterministic compile / verify 闭环，RepairAgent 只作为显式实验能力保留”，避免在平台合同和 body 编译尚未收紧前，让 repair 掩盖主链质量问题。
- 目标：新增统一的 `enable_repair_agent` 运行时开关，默认值为 `False`；只有显式启用时，`repair_router` 才会把 `retryable_error` 路由到 `repair_agent`。
- 代码/契约变化：
  - `WorkflowState`、`build_initial_state(...)`、`run_workflow(...)`、`workflow_trace.run_workflow(...)` 新增 `enable_repair_agent`，默认 `False`
  - `RepairRouter` 在预算和 scope 合法前提下，若 `enable_repair_agent=False`，则对本可修复问题直接给出 `reject`，`reason=repair_agent_disabled`
  - API 服务层、运行器、仓储与前端运行表单同步透传该开关，前端与 API DTO 可显式控制是否开启 repair
  - Phase 4/7/API 测试同步调整：默认路径断言为 reject，只有显式开启后才验证 repair loop 行为
- 影响：
  - 正式默认运行口径从“验收失败后尽量修”切换为“先严格暴露 compile / verify 问题，再按需开启 repair 实验”
  - 这为 4 月 25 日的 strict compile / 平台 flow 校验收口提供了更干净的主链观测面，避免 RepairAgent 把首轮 deterministic 缺陷掩盖掉
  - Repair 闭环能力并未删除，只是从默认行为改为显式能力
- 验证：
  - 提交内新增/更新测试：`tests.test_phase4_repair_router`、`tests.test_phase4_state_contract`、`tests.test_phase4_workflow_repair_loop`、`tests.test_phase7_runtime_contract`、`tests.test_workflow_api`
  - 本次补核尝试命令：`python -m unittest tests.test_phase4_repair_router tests.test_phase4_state_contract tests.test_phase4_workflow_repair_loop tests.test_phase7_runtime_contract tests.test_workflow_api`
  - 本次补核结果：当前 Windows 环境在导入 `langgraph/pydantic` 时同样触发 `WinError 10106`，未能在本机会话完成复跑
- 风险与回滚：
  - 主要风险：默认关闭 repair 后，更多 `retryable_error` 会直接以 reject 暴露给调用方；若上层仍默认预期“自动修”，需要同步口径
  - 回滚：可回退 `enable_repair_agent` 状态字段与透传链路，恢复 repair 默认启用策略
- 文档同步：`工作流总结文档.md`、`工作流节点输入输出说明.md` 已在该次提交同步；本条记录补入 `工作流演进记录.md`
- 后续动作：继续把 repair 定位为显式实验能力；等平台合同、body 编译和失败桶分类更稳定后，再评估是否扩大默认启用范围
- DoD：运行时开关 `[x]`；RepairRouter 默认 reject `[x]`；API 透传 `[x]`；前端透传 `[x]`；测试覆盖 `[x]`

### [2026-04-23] 完成 Workflow Frontend V1 工作流驾驶舱（Record ID: REC-2026-04-23-FE-V1）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`frontend/package.json`、`frontend/package-lock.json`、`frontend/index.html`、`frontend/vite.config.ts`、`frontend/tsconfig*.json`、`frontend/.env.example`、`frontend/README.md`、`frontend/fixtures/*.json`、`frontend/src/main.tsx`、`frontend/src/app/*.tsx`、`frontend/src/api/*.ts`、`frontend/src/types/workflow.ts`、`frontend/src/features/workflow/**/*.tsx`、`frontend/src/features/workflow/**/*.ts`、`frontend/src/styles/*.css`、`frontend/src/test/setup.ts`、`app/main.py`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`
- 背景：Workflow API V1 已冻结 `thread_id / attempt_id / review_id`、REST 轮询、review resume、trace/state-history 投影和统一错误结构；前端目录此前为空，仍缺可实际联调的工作流运行界面。
- 目标：按 `docs/前端设计/工作流前端下一步实施方案.md` 完成首版“工作流运行驾驶舱”，跑通 `输入需求 -> 创建运行 -> 轮询详情 -> interrupted review -> resume -> 查看终态结果 -> 查看 trace/state-history` 的前端闭环，同时不引入图编辑器、SSE、完整 `WorkflowState` 直读或固定百分比进度。
- 代码/契约变化：
  - 新增 Vite + React + TypeScript 前端工程，路由包括 `/workflow`、`/workflow/:threadId/:attemptId`、`/workflow/:threadId/:attemptId/result`、`/workflow/:threadId/:attemptId/debug`
  - 新增 `frontend/src/types/workflow.ts`，只定义 Workflow API 投影 DTO，不引入完整 `WorkflowState`
  - 新增 `workflowClient.ts` 与 `workflowErrors.ts`，统一封装 `/api/workflow` 请求、超时、取消、错误 envelope 与网络错误归一化
  - 新增 `useWorkflowAttempt` 轮询 hook：`queued / running` 自动轮询，连续无变化后退避；`interrupted / completed / rejected / failed` 停止高频轮询
  - 新增启动页、健康检查、最近运行、本地 URL 恢复、运行详情页、阶段轨道、诊断面板、ReviewCard、结果页、TraceSummaryPanel、StateHistoryPanel、AttemptHistoryRail
  - ReviewCard 固定提交 `attempt_id / review_id / decision / answers / feedback / updated_constraints`，并在前端校验 `feedback / clarify` 说明和 JSON 对象合法性
  - 阶段轨道按产品阶段映射 `current_step`，未知值降级显示原始值，不按节点数量计算百分比
  - 新增 fixture 覆盖 queued、running、interrupted、completed、failed、result、trace、state-history、conflict
  - 新增 Vitest / Testing Library 测试，覆盖 current step 映射、review 表单校验、API client 错误 envelope 与 ReviewCard 行为
  - `app/main.py` 新增 `WORKFLOW_CORS_ORIGINS` 配置，默认允许 `http://127.0.0.1:5173` 与 `http://localhost:5173` 访问后端，支撑本地 Vite 联调
- 影响：
  - 前端现在可以直接基于后端真实 Workflow API V1 联调，无需调用 LangGraph 图对象或读取 `outputs/` trace 文件
  - 主链拓扑、`WorkflowState` 字段和节点 IO 契约未改变；新增内容是 API DTO 的消费层和本地开发跨域配置
  - 前端仍是 V1 联调/演示界面，不包含用户系统、鉴权、租户隔离、图编辑器、SSE、并行子任务详情和生产运行治理页
- 验证：
  - `cd frontend; npm install`
  - `cd frontend; npm run typecheck`
  - `cd frontend; npm run test`
  - `cd frontend; npm run build`
  - `python -m unittest tests.test_workflow_api`
  - `python -m unittest tests.test_phase7_runtime_contract`
  - `python -m unittest tests.test_phase8_trace_review_contract`
  - `python -m compileall app`
  - `python -m pip check`
  - `TestClient(create_app()).get("/api/workflow/health")`
  - 结果：全部通过；健康检查返回 `ok=True`，SQLite checkpointer、local_thread worker、trace 写权限与 Chroma collections 均 ready
- 风险与回滚：
  - 主要风险：尚未完成真实浏览器端到端人工点击验收；自动化已覆盖类型、构建、组件/API client 和后端合同，但真实长链路仍需按联调脚本人工跑一次
  - 主要风险：前端当前使用轮询而非 SSE；后续接入 SSE 时应保留 hook 封装边界，避免组件重写
  - 主要风险：`localStorage` 最近运行只作为本地便利入口，不是后端会话事实源
  - 回滚：可删除 `frontend/` 并回退 `app/main.py` 的 CORS 配置；正式 `workflow.py / workflow_trace.py` 主链无需回滚
- 文档同步：`工作流演进记录.md`、`工作流总结文档.md`、`工作流节点输入输出说明.md` 已同步；`docs/前端设计/工作流前端下一步实施方案.md` 仍作为实施来源保留
- 后续动作：执行真实浏览器端到端手工验收；根据联调结果补充字段缺口和错误码；后续再评估 SSE、`subtasks` 可视化、结果下载接口、鉴权与审计
- DoD：前端工程 `[x]`；DTO/API client `[x]`；启动页 `[x]`；详情轮询 `[x]`；Review resume `[x]`；结果页 `[x]`；Trace/state-history `[x]`；attempt 历史 `[x]`；测试与构建 `[x]`；README `[x]`

### [2026-04-23] 完成 Workflow API V1 联调版与运行契约冻结（Record ID: REC-2026-04-23-API-V1）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`app/main.py`、`app/api/models.py`、`app/api/workflow_api.py`、`app/repositories/workflow_run_repository.py`、`app/services/checkpointer_factory.py`、`app/services/workflow_executor.py`、`app/services/workflow_runner.py`、`app/services/workflow_service.py`、`app/services/workflow_state_projection.py`、`tests/test_workflow_api.py`、`requirements.txt`、`.env.example`、`.gitignore`、`docs/前端设计/工作流后端API草图.md`、`docs/前端设计/工作流后端下一步实施计划.md`、`docs/前端设计/工作流后端启动与联调说明.md`、`工作流总结文档.md`、`工作流节点输入输出说明.md`、`工作流演进记录.md`
- 背景：正式主链已具备 `thread_id + checkpointer + interrupt + Command(resume=...) + trace` 的运行内核，但前端需要一层稳定 REST 服务，而不是直接接触 LangGraph 图对象、磁盘 trace 文件或完整 `WorkflowState`。此前 API 草图和下一步实施计划已明确 V1 先走 `REST + 轮询 + review 恢复`，SSE 与生产级 worker 后置。
- 目标：把已有 API 骨架推进到前后端可稳定联调版，冻结 `thread_id / attempt_id / review_id` 外部主键关系、attempt 状态机、错误结构、trace 摘要和启动说明，同时不改变正式工作流主链拓扑。
- 代码/契约变化：
  - `thread_id` 冻结为 LangGraph checkpoint 恢复指针；`attempt_id` 冻结为 API / trace / 前端展示层的一次运行主键；`review_id` 冻结为当前人工 review 卡片主键
  - V1 对外 attempt 状态固定为 `queued / running / interrupted / completed / rejected / failed`
  - 同一 `thread_id` 同一时刻只允许一个 active attempt；active 状态为 `queued / running / interrupted`
  - `POST /api/workflow/runs` 在已有 active attempt 时返回 `409 Conflict`
  - `POST /api/workflow/threads/{thread_id}/resume` 只允许恢复当前 active interrupted attempt，并同时校验 `attempt_id` 与 `review_id`
  - 终态 `completed / rejected / failed` attempt 不允许再次 `resume`
  - API 错误响应统一为 `{ "error": { "code": "...", "message": "...", "details": {...} } }`
  - 新增 `GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/trace`，返回 trace 摘要投影，不要求前端读取 `summary_json`
  - 新增 `GET /api/workflow/threads/{thread_id}/attempts/{attempt_id}/state-history?limit=...`，仅供调试页消费
  - `attempt detail` 投影中预留 `subtasks: []`，为后续 `Send / reducer` 子任务展示留口
  - `CheckpointerFactory` 默认从 `InMemorySaver` 升级为 SQLite checkpointer；`memory` 只作为单测或临时 demo 配置
  - `WorkflowRunRepository` 支持 JSON 文件落盘，作为本地联调阶段 thread / attempt 元数据真相源
  - 新增 `WorkflowTaskExecutor` 抽象与本地线程执行器；保留同步执行器用于单测
  - `health` 增加 checkpointer backend、worker readiness、trace 根目录写权限等信息
  - `.env.example` 新增 `WORKFLOW_CHECKPOINTER_BACKEND`、`WORKFLOW_CHECKPOINT_DB_PATH`、`WORKFLOW_RUN_REPOSITORY_PATH`
  - `requirements.txt` 增加 `langgraph-checkpoint-sqlite`
- 影响：
  - 前端可以按固定 API 开始实现发起、轮询、review 卡片、resume、结果页、trace 调试页
  - API 返回的是投影 DTO，不直接暴露完整 `WorkflowState`；主链状态契约不因服务层新增而改变
  - 本地开发与联调具备跨进程重启后的最小 checkpoint / attempt 元数据恢复基础
  - 当前仍不是生产级多实例执行模型：本地线程执行器、JSON 元数据仓储与 SQLite checkpointer 适合开发联调，不代表最终生产部署方案
- 验证：
  - `python -m unittest tests.test_workflow_api`
  - `python -m unittest tests.test_phase7_runtime_contract`
  - `python -m unittest tests.test_phase8_trace_review_contract`
  - `python -m compileall app workflow.py workflow_trace.py agents utils tests scripts`
  - `python -m pip check`
  - 结果：全部通过
- 风险与回滚：
  - 主要风险：JSON 仓储与本地线程 worker 不是生产级方案；多进程、多实例、长任务治理与异常恢复仍需后续队列 / worker / 数据库存储设计
  - 主要风险：SQLite checkpointer 支撑本地 durable 联调，但生产仍应预留 PostgreSQL 或等价持久化后端
  - 回滚：可回退 `app/` 服务层、`tests/test_workflow_api.py`、`requirements.txt` 与联调文档改动；正式 `workflow.py / workflow_trace.py` 主链拓扑无需回滚
- 文档同步：`工作流演进记录.md`、`工作流总结文档.md`、`工作流节点输入输出说明.md`、`docs/前端设计/工作流后端启动与联调说明.md` 已同步；API 草图与实施计划作为设计来源保留
- 后续动作：前端按 V1 DTO 开始页面联调；后端后续补 SSE、队列/worker、生产 checkpointer、持久化元数据仓储和更完整 health/persistence 恢复测试
- DoD：运行契约冻结 `[x]`；状态机测试 `[x]`；统一错误结构 `[x]`；SQLite checkpointer `[x]`；worker 抽象 `[x]`；trace/state-history 接口 `[x]`；联调文档 `[x]`

### [2026-04-21] 完成仓库级 compat / legacy 清退结案与历史文档收口（Record ID: REC-2026-04-21-CLEANUP）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`agents/retrieval_agent.py`、`agents/coding_agent.py`、`agents/global_assembler.py`、`agents/subsystem_planner.py`、`agents/repair_agent.py`、`agents/assembly_shared.py`、`utils/retrieval_bundle_utils.py`、`utils/context_formatter.py`、`tests/contracts/phase2_retrieval_bundle_contract.py`、`tests/test_phase2_retrieval_agent.py`、`tests/test_phase3_workflow.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py`、`仓库级彻底清退执行清单.md`、`兼容层清退清单.md`、`README.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`
- 背景：正式主链早已不再依赖 `retrieval_context / execution_plan / generated_code`，但演进记录、清退清单和部分历史表述仍混杂“主链已解耦”和“仓库级仍保留 compat surface”两种口径，容易误判当前仓库是否真的完成清退。
- 目标：把仓库级 Batch 0-3 清退结论与当前代码树对齐，明确哪些 compat / legacy 资产已经删除，哪些字段仍是正式状态契约，并把历史文档口径收紧到当前事实。
- 代码/契约变化：
  - 正式运行时状态已不再包含：`retrieval_context`、`execution_plan`、`generated_code`、`execution_result`、`validation_result`、`next_step`
  - 正式 IR 已不再包含：`assembled_graph_ir.source_execution_plan`
  - 仓库级 public compat API 已删除：`RetrievalAgent.retrieve()`、`CodingAgent.compile_graph(...)`、`CodingAgent.generate_json(...)`
  - root wrapper 已删除：`agents/assembly_agent.py`、`agents/planning_agent.py`、`agents/validation_agent.py`、`agents/debugging_agent.py`、`agents/retrieval_agent_old.py`
  - legacy 目录与 legacy 测试入口壳已删除：`agents/legacy/`、`tests/legacy/`、`tests/test_phase1_workflow.py`、`tests/test_phase2_bundle_consumers.py`、`tests/test_phase2_planning_bundle.py`、`tests/test_phase7_coding_determinism.py` 等
  - formal helper 已收口为 bundle-only：`utils/retrieval_bundle_utils.py`、`utils/context_formatter.py`、`agents/assembly_shared.py`
  - formal 测试当前已显式要求结果状态不回流 `retrieval_context / execution_plan / generated_code`
- 影响：
  - “正式主链已不依赖 compat 字段”与“仓库级 compat / legacy 面已删除”现在是同一结论，不再分裂
  - 当前剩余工作已从 runtime legacy 清退转成历史文档术语持续收缩
  - 正式主链后续若出问题，不应再把原因归咎于仓内 legacy wrapper 或 compat helper 托底
- 验证：
  - 代码树核对：`agents/legacy/`、`tests/legacy/`、`tests/test_phase1_workflow.py`、`tests/test_phase2_bundle_consumers.py`、`tests/test_phase7_coding_determinism.py`、`agents/assembly_agent.py`、`agents/planning_agent.py`、`agents/validation_agent.py`、`agents/debugging_agent.py`、`agents/retrieval_agent_old.py` 当前均不存在
  - 代码搜索核对：正式代码与 formal 测试当前只保留“结果中不应出现 compat 字段”的负向断言，不再保留 runtime fallback
  - 相关护栏：`tests/contracts/phase2_retrieval_bundle_contract.py`、`tests/test_phase2_retrieval_agent.py`、`tests/test_phase3_workflow.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase8_clarification_contract.py`、`tests/test_phase8_architecture_review_contract.py`
  - 本次更新未新增执行单测；文档结论依据当前仓库代码、当前测试文件与两份清退清单交叉核对
- 风险与回滚：
  - 当前主要风险已不是 runtime compat 误用，而是历史文档中若仍继续使用 `retrieval_context / execution_plan / generated_code` 术语，可能重新制造认知偏差
  - 回滚：若仅回滚本文档，不影响正式主链行为；若回滚对应代码删除，则会重新引入已关闭的 compat surface
- 文档同步：`README.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`仓库级彻底清退执行清单.md`、`兼容层清退清单.md` 已同步到当前口径
- 后续动作：后续演进记录不再重复把 compat 清退写成进行中事项；除非重新引入新的过渡层，否则该主题只保留历史记录
- DoD：Batch 0 `[x]`；Batch 1 `[x]`；Batch 2 `[x]`；Batch 3 `[x]`；总文档同步 `[x]`

### [2026-04-17] 完成工作包 E/F/G：persistence smoke、review 索引与文档收口（Record ID: REC-2026-04-17-P8-EFG）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow_trace.py`、`utils/review_index.py`、`agents/clarification_apply_agent.py`、`agents/architecture_feedback_apply_agent.py`、`scripts/run_phase8_hitl_smoke.py`、`tests/test_phase7_runtime_contract.py`、`tests/test_phase8_persistence_smoke.py`、`tests/test_phase8_trace_review_contract.py`、`README.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第八阶段实施清单.md`
- 背景：工作包 C 完成后，正式主链已有两个 review 点，但还缺最小 persistence smoke、review 结构化索引与最终文档收口，导致“暂停后如何追踪、如何反查 review”仍不够稳定。
- 目标：在不前推 `repair_review` 的前提下，先把 `checkpointer + thread_id + Command(resume=...)` 的最小闭环、review 记录落盘与文档状态统一补齐。
- 代码/契约变化：
  - E：新增 `scripts/run_phase8_hitl_smoke.py` 与 `tests/test_phase8_persistence_smoke.py`，正式覆盖 `clarification_review / architecture_review` 的 pause、resume、`get_state` 与 `get_state_history`
  - F：新增 `utils/review_index.py`；`workflow_trace.py` 现在会把 `review_history` 落成 `review_records.json`、`approval_record.json`，并在 `outputs/_trace_index/` 下补 `review_attempts / review_threads / reviews`
  - F：`clarification_apply` 与 `architecture_feedback_apply` 会把 `review_history` 从 `answered` 推进到 `applied / rejected`，避免最终审计状态停留在中间态
  - G：README 与三份总文档已统一到“Phase 8 A/B/C/E/F/G 已完成，D 暂不抢前”的口径
- 影响：
  - `workflow_trace.final_output.workflow_trace` 现在可直接拿到 `review_records_json`、`approval_record_json`、`review_attempt_index_json`、`review_thread_index_json`
  - thread/attempt/review 三层关系已能反查，`GraphInterrupt` 继续保持“暂停而非失败”
  - 第八阶段当前剩余项被收敛为 `repair_review` 是否前推，而不是继续补 A/B/C/E/F/G 的收尾
- 验证：
  - `python -m unittest tests.test_phase8_trace_review_contract tests.test_phase8_persistence_smoke tests.test_phase8_architecture_review_contract tests.test_phase8_clarification_contract tests.test_phase7_runtime_contract tests.test_phase7_trace_index_contract tests.test_phase4_state_contract tests.test_phase4_trace_summary`
  - `python -m compileall workflow.py workflow_trace.py agents utils tests scripts`
  - 结果：`32` 个相关单测通过，`compileall` 通过
- 风险与回滚：
  - 当前 smoke 仍以 `InMemorySaver` 与脚本级演示为主，尚未扩到生产级持久化后端
  - review 索引已足够支撑现阶段审计与排障，但还不是完整审批系统
  - 回滚：回退 `workflow_trace / review_index / phase8 smoke / tests / docs` 相关改动即可回到仅有 review 节点、无 review 索引的状态
- 文档同步：`README.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第八阶段实施清单.md` 已同步
- 后续动作：若继续推进第八阶段，优先审慎评估工作包 D；否则当前已形成可交接状态
- DoD：E `[x]`；F `[x]`；G `[x]`

### [2026-04-17] 完成工作包 C：`architecture_planning` 后主评审节点（Record ID: REC-2026-04-17-P8-C）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`agents/architecture_review_agent.py`、`agents/architecture_feedback_apply_agent.py`、`utils/phase3_contracts.py`、`tests/test_phase8_architecture_review_contract.py`、`tests/test_phase8_clarification_contract.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase7_runtime_contract.py`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第八阶段实施清单.md`、`README.md`
- 背景：工作包 B 已让高歧义需求在检索前进入条件式澄清，但在进入 `subsystem_planning` 前仍缺少默认主评审点，结构方向问题只能更晚在 repair 阶段暴露。
- 目标：在 `architecture_planning` 之后引入主评审节点，允许用户基于页面/子系统/共享信号/全局约束摘要做批准或反馈，并严格遵守“反馈只能回写需求侧输入”的 A0 冻结边界。
- 代码/契约变化：
  - `workflow.py` 正式接入 `architecture_review -> architecture_feedback_apply`，并允许 `feedback / clarify` 回跳 `architecture_planning`
  - `agents/architecture_review_agent.py` 负责冻结用户可读的结构摘要；`agents/architecture_feedback_apply_agent.py` 只允许更新 `requirement_spec / architecture_feedback_patch`
  - `utils/phase3_contracts.py` 补了通用 review history helper，并继续用 forbid list 阻止直接 patch `architecture_plan / decomposition_result / subsystem_plan_map`
  - `workflow_trace.py` 已能把 `architecture_review` 的中断记为 `interrupted`
- 影响：
  - 正式主链现在已有两个 review 点：前置澄清和架构主评审
  - 结构反馈会先回到需求/约束层，再重新规划 architecture，不会绕过结构层直接改后续产物
  - 默认未开启 `enable_hitl_architecture_review` 时，节点走自动批准旁路，兼容旧调用
- 验证：
  - `python -m unittest tests.test_phase8_clarification_contract tests.test_phase8_architecture_review_contract tests.test_phase4_state_contract tests.test_phase7_runtime_contract tests.test_phase7_trace_index_contract tests.test_phase4_repair_router tests.test_phase4_trace_summary`
  - 结果：`31` 个相关单测通过
- 风险与回滚：
  - 当前 review 与 persistence 的后续配套已在工作包 E/F/G 中补齐；剩余风险主要集中在是否继续前推 `repair_review`
  - 回滚：回退 `workflow / workflow_trace / architecture review agents / tests / docs` 相关改动即可恢复到仅有前置澄清的 Phase 8 状态
- 文档同步：`README.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第八阶段实施清单.md` 已同步
- 后续动作：视需要继续评估工作包 D
- DoD：C `[x]`

### [2026-04-17] 完成工作包 A/B：第七阶段收口与 `analysis` 后条件式澄清（Record ID: REC-2026-04-17-P8-AB）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`agents/analysis_agent.py`、`agents/ambiguity_router.py`、`agents/clarification_review_agent.py`、`agents/clarification_apply_agent.py`、`utils/phase3_contracts.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase7_baseline_contract.py`、`tests/test_phase8_clarification_contract.py`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第七阶段实施清单.md`
- 背景：Phase 7 已把 runtime/trace/stable-id/repair-boundary 等工程准备项补齐，但正式主链仍缺少早期用户澄清点，且 `workflow_trace.py` 对 review 中断仍没有“暂停而非失败”的正式语义。
- 目标：先冻结第七阶段与第八阶段之间的统一口径，再把 `analysis` 后的条件式澄清节点正式接入主链，并复用统一的 review/resume 合同。
- 代码/契约变化：
  - A：将 Phase 7 明确收口为“工程准备项已完成，但不等于正式主链已切到 `Send` 或完整 HITL”的历史基线
  - A0：`utils/phase3_contracts.py` 新增 `hitl_stage / review_request / review_response / review_history / review_*` 默认合同，以及 review payload 规范化与需求侧 patch 护栏
  - B：`workflow.py` 在 `analysis` 与 `retrieval` 之间新增 `ambiguity_router -> clarification_review -> clarification_apply` 分支；高歧义需求会进入澄清，低歧义需求保持单轮直通
  - B：`clarification_review` 采用“先冻结 request，再自回环进入 `interrupt`”的方式，避免 `interrupt` 前的 state 写入丢失；`clarification_apply` 只允许回写 `analysis_result / requirement_spec`
  - 最小 F 前移：`workflow_trace.py` 对 `GraphInterrupt` 单独记为 `interrupted`，`workflow_status` 可为 `interrupted`，不再误记为 error
- 影响：
  - 正式主链已经具备第一个可恢复的 HITL 入口，但仍只覆盖前置澄清，不代表 `architecture_review` / `repair_review` 已完成
  - trace 摘要现在能直接看到 `hitl_stage / review_status / review_id`
  - 恢复澄清后不会重跑 `analysis` 节点
- 验证：
  - `python -m unittest tests.test_phase7_baseline_contract tests.test_phase4_state_contract`
  - `python -m unittest tests.test_phase8_clarification_contract`
  - 结果：`12` 个相关单测通过
- 风险与回滚：
  - 当前只前移了 `GraphInterrupt` 的最小 trace 兼容，尚未扩面到完整 review 索引
  - `clarification_apply` 仍依赖 `updated_constraints` 或文本补充；若后续需要更强结构化回写，应继续在需求侧合同内扩展，而不是直接改规划产物
  - 回滚：回退 `workflow / workflow_trace / clarification agents / tests / docs` 相关改动即可恢复第八阶段开工前基线
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第七阶段实施清单.md` 已同步
- 后续动作：继续按第八阶段主清单推进工作包 E/F
- DoD：A `[x]`；B `[x]`

### [2026-04-17] 完成工作包 F：独立测试图中的 `Send + reducer` 试点（Record ID: REC-2026-04-17-P7-F）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`utils/phase3_contracts.py`、`scripts/poc_phase7_send_parallel.py`、`tests/test_phase7_send_reducer_contract.py`、`docs/ahu/Phase4_Send并行预留设计.md`、`docs/ahu/第七阶段实施清单.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`
- 背景：Phase 7 A/B/C/D/E/G 已完成，但正式主链仍没有 reducer state key、`Send` 试点结果和并行 merge contract，尚不足以讨论正式主链切换。
- 目标：在不改正式主链拓扑的前提下，先把 `Send + reducer` 的最小可迁移合同在独立测试图里跑通，并为后续主链化固定 reducer、冲突与排序语义。
- 代码/契约变化：
  - `utils/phase3_contracts.py` 新增 `ParallelMergeConflict`、`SubsystemPlanningDispatch`、`merge_subsystem_plan_map(...)`、`merge_parallel_conflicts(...)` 与 `order_subsystem_plan_map(...)`
  - `subsystem_plan_map` reducer 语义已定为：按 `subsystem_id` merge，重复 `subsystem_id` 直接 `duplicate_subsystem_id:*` fail-fast，不允许 `last-write-wins`
  - `parallel_merge_conflicts` reducer 语义已定为 append-only
  - 新增 `scripts/poc_phase7_send_parallel.py`，以独立 `StateGraph + Send` 测试图验证 fan-out/fan-in，不接入正式 `workflow.py`
  - worker contract 已定为“只返回局部 state update”，不原地改整份 state
  - 稳定排序规则已定为：`dispatch_index -> subsystem_id`
- 影响：
  - 正式主链与 trace 主链仍未切换到 `Send`
  - 但后续若要主链化，`subsystem_plan_map` reducer、冲突列表和 duplicate fail-fast 已有可直接迁移的合同
  - `docs/ahu/Phase4_Send并行预留设计.md` 中的预留决策已从“纯设计”升级为“已有独立试点验证”
- 验证：
  - `python -m unittest tests.test_phase7_send_reducer_contract`
  - `python -m unittest tests.test_phase6_readiness_contract tests.test_phase7_baseline_contract`
  - 结果：`12` 个相关测试通过
- 风险与回滚：
  - 当前只是独立 PoC / 合同测试，不等于正式主链已具备并行 trace 顺序、repair 局部重派和 persistence 联动能力
  - 当前 `parallel_merge_conflicts` 仅定义了 append-only 聚合语义，正式主链化前还需决定哪些冲突由 merge reject、哪些冲突进入 repair
  - 回滚：回退 `phase3_contracts / poc_phase7_send_parallel.py / test_phase7_send_reducer_contract.py / docs` 改动即可恢复工作包 F 落地前状态
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/Phase4_Send并行预留设计.md`、`docs/ahu/第七阶段实施清单.md` 已同步
- 后续动作：在不破坏正式主链基线的前提下，评估 `workflow_trace.py` 的并行记录顺序、planning repair 的局部重派边界，以及 `subsystem_merge` 是否应主链化
- DoD：F `[x]`

### [2026-04-17] 完成 Phase 7 最小工程改造：runtime/trace/stable-id/repair-boundary/docs（Record ID: REC-2026-04-17-P7-MIN）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`utils/workflow_runtime.py`、`utils/trace_index.py`、`agents/coding_agent.py`、`agents/coding_utils.py`、`agents/repair_agent.py`、`utils/phase3_contracts.py`、`scripts/poc_phase4_hitl.py`、`tests/test_phase6_readiness_contract.py`、`tests/test_phase7_baseline_contract.py`、`tests/test_phase7_runtime_contract.py`、`tests/test_phase7_trace_index_contract.py`、`tests/test_phase7_coding_determinism.py`、`tests/test_phase7_repair_patch_contract.py`、`tests/test_phase4_repair_agent.py`、`tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase1_workflow.py`、`tests/test_phase2_bundle_consumers.py`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第七阶段实施清单.md`、`docs/ahu/第六阶段当前状态交接.md`
- 背景：Phase 6 准入资料已经证明下一步的真实阻塞点不在规划质量，而在正式入口 runtime 契约、trace 索引、非确定性编译边界和 repair 节点可拆分边界。
- 目标：在不切换正式主链拓扑的前提下，完成 Phase 7 的最小工程改造 A/B/C/D/E/G，给后续 `Send / HITL / persistence` 主链化补齐基础设施。
- 代码/契约变化：
  - A：新增 `tests/test_phase7_baseline_contract.py`，并在 `docs/ahu/第六阶段当前状态交接.md` 固定 Phase 6 retained 绿态与 3 条 trace 证据路径
  - B：新增 `utils/workflow_runtime.py`，统一正式入口的 `run_name / tags / metadata / recursion_limit / configurable.thread_id`；`workflow.py` 与 `workflow_trace.py` 已支持显式 `thread_id` 与可选 `checkpointer`
  - C：新增 `utils/trace_index.py`；`workflow_trace.py` 现在为每次运行生成 `attempt_id`，并在传入 `thread_id` 时落盘 thread/attempt 级索引
  - D：`CodingAgent` 已移除随机短 UUID，改为稳定 ID 生成策略；同一输入下 `id_map` 与 `json_text` 保持稳定
  - E：`RepairAgent` 已拆成 `prepare_repair_patch(...)` 与 `apply_repair_patch(...)` 两段，并引入轻量 `RepairPatchPlan`
  - G：核心总文档已同步到 “最小工程改造已完成、正式主链尚未切 `Send / HITL`” 的口径
- 影响：
  - 正式主链入口现在具备最小 persistence 契约，但默认旧调用保持兼容
  - trace 已能稳定关联 `thread_id + attempt_id`
  - `coding` 不再是 durable execution 的关键随机阻塞点
  - `repair_agent` 后续若插入 `human_review`，不需要再从零拆边界
- 验证：
  - `D:\Anaconda\envs\midea\python.exe -m unittest tests.test_phase7_coding_determinism tests.test_phase7_repair_patch_contract tests.test_phase4_repair_agent tests.test_phase4_workflow_repair_loop tests.test_phase1_workflow tests.test_phase2_bundle_consumers tests.test_phase6_readiness_contract tests.test_phase7_baseline_contract tests.test_phase7_runtime_contract tests.test_phase7_trace_index_contract tests.test_phase4_state_contract tests.test_phase2_retrieval_bundle`
  - `D:\Anaconda\envs\midea\python.exe -m compileall workflow.py workflow_trace.py agents utils tests scripts`
  - 结果：`60` 个相关单测通过，`compileall` 通过
- 风险与回滚：
  - 当前仍未完成工作包 F；正式主链还没有 reducer state key 与 `Send` 试点落地
  - 当前 `thread_id + checkpointer` 只是最小工程接线，不等于正式 `interrupt / resume` 业务流程已主链化
  - 回滚：回退 `workflow / workflow_trace / coding / repair / tests / docs` 相关改动即可恢复 Phase 6 末状态
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第七阶段实施清单.md`、`docs/ahu/第六阶段当前状态交接.md` 已同步
- 后续动作：继续做工作包 F，验证 `Send + reducer` 在试点图中的 reducer、冲突和排序语义
- DoD：A `[x]`；B `[x]`；C `[x]`；D `[x]`；E `[x]`；G `[x]`

### [2026-04-17] 完成第六阶段 A/B/C 状态入档并补齐 Phase 7 准入包（Record ID: REC-2026-04-17-P6）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`agents/architecture_planner.py`、`agents/subsystem_planner.py`、`utils/ahu_knowledge_builder.py`、`scripts/run_phase6_real_query_suite.py`、`scripts/run_phase6_retrieval_eval.py`、`scripts/phase6_shared.py`、`workflow_trace.py`、`tests/test_phase3_architecture_planner.py`、`tests/test_phase3_subsystem_planner.py`、`tests/test_phase3_workflow.py`、`tests/test_phase2_ahu_knowledge_builder.py`、`tests/test_phase6_real_query_suite_contract.py`、`tests/test_phase6_retrieval_eval_contract.py`、`tests/test_phase6_readiness_contract.py`、`AHU程序/phase6_real_query_cases.json`、`docs/ahu/Phase6_副作用幂等性清单.md`、`docs/ahu/Phase6_thread_id与恢复契约.md`、`docs/ahu/Phase6_Send_HITL准入评审表.md`、`docs/ahu/第六阶段实施清单.md`、`docs/ahu/第六阶段当前状态交接.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`
- 背景：第六阶段的 A/B/C 代码与正式产物已经落地，但三份总文档还停在 2026-04-15 的 Phase 5 口径；同时 Phase 7 所需的 `thread_id / reducer / 副作用幂等性` 资料还未形成正式裁决。
- 目标：把第六阶段真实质量、检索评估与规划硬化的正式状态同步入档，并补齐 Phase 7 的准入包，使下一轮不再需要重复做摸底文档。
- 代码/契约变化：
  - A：`scripts/run_phase6_real_query_suite.py`、`scripts/phase6_shared.py` 与 `AHU程序/phase6_real_query_cases.json` 已形成唯一正式 case 源、retained 机制与真实 suite 摘要；当前正式结果为 `12/12 passed`
  - B：`scripts/run_phase6_retrieval_eval.py` 已形成稳定 `retrieval_diagnosis`、golden 命中率、`ready_for_c` 与 asset-gap 根因分析；当前正式结果为 `healthy=12`
  - C：`ArchitecturePlanner` 新增模板 `imports / exports / 端口覆盖度` 加权与 pattern 缺页负分；`SubsystemPlanner` 在模板接口 mismatch 时提前降级到 `atomic_assembly` 并写清 `degrade_reason`；`ahu_knowledge_builder` 已补齐 `exhaust_fan_control` alias 模板资产
  - F：新增 `docs/ahu/Phase6_副作用幂等性清单.md`、`docs/ahu/Phase6_thread_id与恢复契约.md`、`docs/ahu/Phase6_Send_HITL准入评审表.md`，明确当前 `Send` 为 `conditional-go`、`HITL / persistence` 为 `no-go`
  - 新增 `tests/test_phase6_readiness_contract.py`，对 readiness 文档存在性与“正式主链尚未接 `checkpointer / thread_id`、POC 已接入”这一事实加轻量护栏
- 影响：
  - 第六阶段的正式口径从“是否继续做 C”转为“D gate 当前不进入，F 已完成，Phase 7 可按准入包继续最小工程改造”
  - 排风机历史 `asset_gap` 已从失败桶转为正式成功基线，`reject_exhaust_fan_only`、`reject_supply_and_exhaust_fan`、`reject_exhaust_and_damper_mix` 现在都是 `observed_success`
  - Phase 7 所需的副作用清单、`thread_id` 契约、reducer 风险与准入裁决已经形成正式资料
- 验证：
  - `python -m unittest tests.test_phase6_real_query_suite_contract`
  - `python -m unittest tests.test_phase6_retrieval_eval_contract`
  - `python -m unittest tests.test_phase6_readiness_contract`
  - `python scripts/run_phase6_retrieval_eval.py`
  - `python scripts/run_phase6_real_query_suite.py`
  - 结果：`phase6_real_query_suite` 固定 `12` 条 case 全部通过，`phase6_retrieval_eval` 固定 `12` 条 case 全部 `healthy`，readiness contract 通过
- 风险与回滚：
  - 主要风险：准入包完成不等于已允许正式主链化；当前 `workflow.py` / `workflow_trace.py` 仍未接 `checkpointer / thread_id / interrupt`
  - 主要风险：若未来 Phase 7 直接在未收口 `analysis / retrieval / coding / repair_agent / workflow_trace` 边界的情况下上主链，仍会放大 durable execution 风险
  - 回滚：回退 Phase 6 文档、测试与状态同步改动即可恢复 2026-04-15 文档基线
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第六阶段实施清单.md`、`docs/ahu/第六阶段当前状态交接.md` 已同步
- 后续动作：先按 readiness 文档推进 Phase 7 的最小工程改造；只有在出现新的稳定失败桶时，才重新评估工作包 D
- DoD：Phase 6 状态入档 `[x]`；Phase 7 准入包落盘 `[x]`；正式产物与 retained 路径对齐 `[x]`；轻量 readiness contract 建立 `[x]`

### [2026-04-15] 完成第五阶段第四批：Query Suite、Trace 摘要与文档同步（Record ID: REC-2026-04-15-P5-BATCH4）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow_trace.py`、`utils/phase3_contracts.py`、`scripts/run_phase5_query_suite.py`、`tests/test_phase4_trace_summary.py`、`tests/test_phase5_workflow_query_suite.py`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第五阶段实施清单.md`
- 背景：第五阶段前三批已完成共享信号归属前移、模板选择解释、Verifier/Repair 结构化 payload、`ambiguous_shared_signal` 有限修复或稳定 reject；第四批需要把这些语义纳入可重复验收、trace 摘要和协作文档。
- 目标：补齐 Phase 5 deterministic query suite，扩展 trace 摘要诊断字段，同步核心文档，确保不依赖在线波动也能复现成功、reject、repair、budget exhausted 等结果。
- 代码/契约变化：
  - `workflow_trace.py` 新增 `planning_unresolved_by_type`、`ambiguous_signal_count`、`repair_reject_category`
  - `workflow_trace.py` 的 trace 目录时间戳加入微秒，避免 query suite 连续运行时覆盖同一秒 trace
  - `utils/phase3_contracts.py` 补齐 `SubsystemSignalBinding` 的 `candidate_exporters / resolution_status / resolution_evidence`，并为 `SubsystemPlanEdge` 补 `edge_id`
  - 新增 `scripts/run_phase5_query_suite.py`，以固定 fake analysis payload、固定内存检索资产和 workflow 级 deterministic stubs 跑 6 类 case
  - 新增 `tests/test_phase5_workflow_query_suite.py`，覆盖 Phase 5 query suite 输出、稳定 reject / budget exhausted 观测，以及额外多轮 repair case
  - `tests/test_phase4_trace_summary.py` 增加 Phase 5 trace 字段断言
- 影响：
  - 单看 trace 摘要即可区分成功通过、已修复、歧义未收敛、无可修或不支持、预算耗尽等终态类别
  - Phase 5 query suite 不依赖真实 Chroma/在线 LLM 波动，默认作为确定性阶段基线
  - `outputs/phase5_query_suite/` 开始保存 query suite JSON/Markdown 摘要，并记录每个 case 的 trace 路径
- 验证：
  - `python -m py_compile workflow_trace.py scripts\run_phase5_query_suite.py tests\test_phase4_trace_summary.py tests\test_phase5_workflow_query_suite.py`
  - `python -m unittest tests.test_phase4_trace_summary`
  - `python -m unittest tests.test_phase5_workflow_query_suite`
  - `python scripts\run_phase5_query_suite.py`
  - `python -m unittest tests.test_phase4_workflow_repair_loop`
  - `python -m unittest tests.test_phase1_workflow`
  - `python -m compileall workflow.py workflow_trace.py agents utils tests scripts`
  - 结果：新增/相邻回归全部通过；Phase 5 query suite 固定 `6` 条 case 均通过，摘要落盘到 `outputs/phase5_query_suite/suite_20260415_131312_103038/`
- 风险与回滚：
  - 主要风险：当前 query suite 是 deterministic 基线，不代表在线 LLM、真实 Chroma 召回和真实 AHU 项目质量已验收
  - 主要风险：手工要求保留的 3 条真实 `workflow_trace.run_workflow(...)` 证据 trace 仍需单独执行和人工复核
  - 回滚：回退 `workflow_trace / phase3_contracts / scripts/run_phase5_query_suite.py / tests/test_phase4_trace_summary.py / tests/test_phase5_workflow_query_suite.py / docs` 相关改动即可恢复 2026-04-14 基线
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`、`docs/ahu/第五阶段实施清单.md` 已同步
- 后续动作：用真实 `workflow_trace.run_workflow(...)` 补 3 条 Phase 5 证据 trace；继续用 query suite 守住 Phase 5 契约；再评估 `Send` 并行或正式 HITL 准入条件
- DoD：Phase 5 query suite 落盘 `[x]`；trace 摘要新增字段 `[x]`；相关测试通过 `[x]`；核心文档同步 `[x]`

### [2026-04-14] 完成第四阶段第一轮整改：接口语义收口、结构化 Repair、Query Smoke（Record ID: REC-2026-04-14-P4-HARDEN）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`utils/signal_semantics.py`、`utils/phase3_contracts.py`、`utils/graph_ir.py`、`agents/architecture_planner.py`、`agents/subsystem_planner.py`、`agents/global_assembler.py`、`agents/verifier_agent.py`、`agents/repair_agent.py`、`tests/test_phase3_architecture_planner.py`、`tests/test_phase3_subsystem_planner.py`、`tests/test_phase3_global_assembler.py`、`tests/test_phase3_verifier_native_contract.py`、`tests/test_phase4_repair_agent.py`、`tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase4_signal_semantics.py`、`scripts/run_phase4_query_smoke.py`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`
- 背景：第四阶段评估复查确认存在 4 类收口缺口：模板接口缺少统一语义层，external 类接口会混入 shared-signal owner 推断；`Verifier -> RepairAgent` 缺少结构化 repair 载荷；`ambiguous_shared_signal` 被误列入自动修复口径；缺少固定 query smoke 验证整改后的正式链路。
- 目标：按整改方案完成前四步，把接口语义、repair 输入、repair 支持边界和 smoke 验收统一到同一套真实代码语义。
- 代码/契约变化：
  - 新增 `utils/signal_semantics.py`，把模板接口统一归类为 `shared_signal / external_input / external_parameter / external_command / subsystem_output`
  - `utils/phase3_contracts.py` 新增 `InterfaceBindingSpec`，`decomposition_result.subsystem_descriptors[*]` 与 `subsystem_plan_map[*]` 正式带上 `interface_bindings / template_interface_bindings`
  - `ArchitecturePlanner` 改为先基于模板真实端口和显式 imports/exports 生成 `interface_bindings`；`shared_signal_registry` 只收 `binding_kind == shared_signal` 的接口
  - `SubsystemPlanner` 改为从 `interface_bindings` 投影 `imported_signals / exported_signals`，并保留 `canonical_signal_key / binding_kind / allowed_external / owner_subsystem_id`
  - `GlobalAssembler` 只让 `shared_signal` 进入 exporter / owner / ambiguity 链路；`external_input / external_parameter / external_command` 直接注入 external placeholder，不再产出 `synthetic_shared_signal_source`
  - `VerificationIssue` 新增 `repair_payload`；`VerifierAgent` 为 planning unresolved item 产出 `signal_name / canonical_signal_key / binding_kind / allowed_external / candidate_exporters / consumer_subsystem_ids / owner_subsystem_id`，为 `compile.wire.port.range` 产出 `source_real_id / target_real_id / invalid_target_port / target_input_count`
  - `RepairAgent` planning repair 改为优先消费结构化 `repair_payload`；当 payload 指向 external 类接口时，直接把共享信号重分类为 external；`ambiguous_shared_signal` 已从 planning 自动修复白名单移除
  - `RepairAgent` compile repair 改为优先走结构化 payload，只保留一版 regex fallback 兼容旧 message
  - 新增 `scripts/run_phase4_query_smoke.py`，用真实 workflow + 临时 Chroma + 固定 fake analysis payload 做固定 2 条查询 smoke
- 影响：
  - external 类接口不再被误当作必须寻找真实 exporter 的 shared signal
  - `Verifier -> RepairAgent` 的数据链从字符串消息驱动升级为结构化 payload 驱动
  - `ambiguous_shared_signal` 不再做猜测式自动修复，失败语义更清晰
  - 第四阶段整改后有了可重复执行的 query smoke，而不只依赖 baseline trace
- 验证：
  - `python -m unittest discover -s tests -p "test_phase3*.py"`
  - `python -m unittest tests.test_phase4_state_contract tests.test_phase4_repair_router tests.test_phase4_repair_agent tests.test_phase4_workflow_repair_loop tests.test_phase4_trace_summary tests.test_phase4_signal_semantics`
  - `python -m unittest tests.test_phase3_verifier_native_contract`
  - `python scripts/run_phase4_query_smoke.py`
  - `python -m compileall workflow.py workflow_trace.py agents utils tests scripts`
  - 结果：`25` 个 Phase 3 测试通过；`29` 个 Phase 4 测试通过；固定 `2` 条 query smoke 均为 `verification_status=passed` / `route_decision=accept`；`compileall` 通过
  - smoke 产物：`outputs/phase4_smoke/smoke_20260414_214748/phase4_smoke_summary.md`
- 风险与回滚：
  - 主要风险：当前仍未把 `ambiguous_shared_signal` 纳入稳定自动修复，复杂共享信号歧义仍会走 reject
  - 主要风险：query smoke 依赖 fake analysis payload 与临时 embedding，不等于在线 LLM 质量已完成验收
  - 回滚：回退 `signal_semantics / phase3_contracts / graph_ir / architecture / subsystem / assembler / verifier / repair / tests / smoke script / docs` 相关改动即可恢复 2026-04-11 基线
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md` 已同步
- 后续动作：继续扩大 structured repair payload 覆盖面；决定是否为 `ambiguous_shared_signal` 设计稳定 reject 辅助诊断或新 repair 策略；把 smoke 查询扩展到更多真实 AHU 场景
- DoD：接口语义层入链 `[x]`；structured repair payload 入链 `[x]`；unsupported repair 白名单收紧 `[x]`；固定 query smoke 落盘 `[x]`

### [2026-04-11] 完成 Phase 4 首版：Repair 闭环、Trace 增强、并行预留与 HITL POC（Record ID: REC-2026-04-11-P4）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`agents/repair_router.py`、`agents/repair_agent.py`、`utils/phase3_contracts.py`、`tests/test_phase4_state_contract.py`、`tests/test_phase4_repair_router.py`、`tests/test_phase4_repair_agent.py`、`tests/test_phase4_workflow_repair_loop.py`、`tests/test_phase4_trace_summary.py`、`tests/test_phase3_workflow.py`、`scripts/poc_phase4_hitl.py`、`docs/ahu/Phase4_Send并行预留设计.md`、`docs/ahu/Phase4_HITL_POC记录.md`、`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md`
- 背景：Phase 3 主链与前三级文档基线已经稳定，但 `verification_report` 之后仍停在单向验收，trace 也还不足以快速定位 repair 轮次、终止原因与 scope 级预算状态。
- 目标：在不引入 `Send` 并行、HITL 正式入链和完整 body 编译的前提下，完成 Phase 4 首版的 Repair 闭环，并同步补齐 trace 观测、设计预留和基线产物。
- 代码/契约变化：
  - `WorkflowState` 新增 `repair_context`、`repair_history`、`route_decision`、`retry_budget`、`retry_counts_by_scope`；`retry_count` 降级为聚合统计值
  - `workflow.py` / `workflow_trace.py` 共用 Phase 4 拓扑注册函数，主链升级为 `verification -> repair_router -> (accept|repair_agent|reject)`，并添加 `recursion_limit=40`
  - 新增 `RepairRouter`，按 `verification_report.status`、`repair_scope`、`retry_counts_by_scope` 和 `retry_budget` 产出稳定 `route_decision`
  - 新增 `RepairAgent` V1，支持：
    - `planning`: `synthetic_shared_signal_source`、`ambiguous_shared_signal`、`shared_signal_owner_mismatch`
    - `assembly`: `missing_local_edge_endpoint`
    - `compile`: `compile.wire.port.range`
  - `workflow_trace.py` 摘要新增 `repair_round_count`、`repair_scopes_seen`、`final_route_decision`、`retry_exhausted`、`retry_counts_by_scope`、`last_repair_issue_ids`、`last_repair_actions`、`reject_reason`
  - 新增 `docs/ahu/Phase4_Send并行预留设计.md`，收敛未来 `Send` fan-out、reducer、冲突规则与稳定排序
  - 新增 `scripts/poc_phase4_hitl.py` 与 `docs/ahu/Phase4_HITL_POC记录.md`，独立验证 `InMemorySaver + thread_id + interrupt + Command(resume=...) + get_state/get_state_history`
  - 生成 `outputs/phase4_baseline/baseline_20260411_164711/`，落盘固定 3 条查询的 baseline 摘要与 trace 路径
- 影响：
  - 正式主链不再在 `verification` 之后直接 `END`
  - Repair 只修补会被下一轮真正消费的上游主对象，避免对派生产物做无效 patch
  - trace 摘要无需翻全量 JSON，即可定位 repair 轮次、scope、最终路由与 reject 原因
  - 后续 `Send` 并行与 HITL 迭代已有可直接接续的设计与 POC 基础
- 验证：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests scripts`
  - `python -m unittest tests.test_phase4_state_contract`
  - `python -m unittest tests.test_phase4_repair_router`
  - `python -m unittest tests.test_phase4_repair_agent`
  - `python -m unittest tests.test_phase4_workflow_repair_loop`
  - `python -m unittest tests.test_phase4_trace_summary`
  - `python -m unittest discover -s tests -p "test_phase3*.py"`
  - `python -m unittest discover -s tests -p "test_phase2*.py"`
  - `python -m unittest discover -s tests -p "test_phase1_workflow.py"`
  - `python -m unittest tests.test_runtime_versions`
  - `python -m pip check`
  - `python scripts/poc_phase4_hitl.py --thread-id phase4-hitl-ci --resume approve`
  - 结果：`4` 个 Phase 4 状态契约 / trace 摘要测试通过；`6` 个 Phase 4 router 测试通过；`4` 个 Phase 4 repair agent 测试通过；`3` 个 Phase 4 repair loop 测试通过；`23` 个 Phase 3 测试通过；`23` 个 Phase 2 测试通过；`11` 个 Phase 1 回归测试通过；`2` 个 runtime 版本 / 依赖一致性测试通过；`compileall` 通过；`pip check` 通过；HITL POC 通过
  - baseline 结果：固定 3 条查询均为 `verification_status=passed` / `route_decision=accept`；对应 trace 已落盘
  - 补充说明：baseline 生成脚本里的第一次 `phase3_tests` 子进程曾出现 `1` 次失败，立即复跑通过；当前原因未定，已记录在 baseline 摘要
- 风险与回滚：
  - 主要风险：Phase 4 首版不等于所有 verifier failure 类型都能自动修复；当前只是 deterministic patch 的 V1 子集
  - 主要风险：`Send` 并行与 HITL 仍未接入正式主链，当前相关结果只是设计预留与 POC
  - 回滚：回退 `workflow/workflow_trace/repair/tests/scripts/docs` 相关改动即可恢复 Phase 3 基线
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`工作流节点输入输出说明.md` 已同步
- 后续动作：补齐 `ambiguous_shared_signal`、`missing_local_edge_endpoint` 等 repair 的更强端到端用例；继续提高 Phase 3 规划质量；评估 baseline 中 Phase 3 单次波动的原因
- DoD：Repair 闭环正式入链 `[x]`；trace 摘要补齐 Phase 4 字段 `[x]`；`Send` 并行设计落盘 `[x]`；HITL POC 落盘 `[x]`；固定 3 条 baseline trace 落盘 `[x]`

### [2026-04-09] 完成第三批收口：文档、环境基线与 Trace 摘要统一（Record ID: REC-2026-04-09-P3-DOCS-ENV）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow_trace.py`、`tests/test_phase3_workflow.py`、`tests/test_runtime_versions.py`、`requirements.txt`、`README.md`、`docs/analysis_agent_summary.md`、`docs/new_retrieval_agent_summary.md`、`docs/planning_agent_summary.md`、`docs/knowledge_base_architecture.md`、`agents/planning_agent.py`、`agents/assembly_agent.py`、`agents/validation_agent.py`、`agents/debugging_agent.py`、`agents/retrieval_agent_old.py`、`工作流总结文档.md`、`工作流演进记录.md`、`docs/ahu/前三阶段遗留问题集中整改实施清单.md`
- 背景：第一批和第二批收口后，README、部分摘要文档、遗留模块定位、`requirements.txt` 与 trace 摘要仍停留在旧口径，容易让新同事误判当前主链、资产链和可运行环境。
- 目标：完成第三批工作包 D/E/F，把当前真实主链、正式资产链、环境基线与 trace 观测口径统一到同一套事实。
- 代码/契约变化：
  - `workflow_trace.py` 新增 `_build_trace_summary()`，把短诊断字段从端到端落盘逻辑中抽离，补充 `workflow_status`、失败节点、最后成功节点、检索计数、子系统实现模式计数、`unresolved_items` 摘要、`verification_*` 摘要与 `acceptance_summary`
  - `tests/test_phase3_workflow.py` 新增 trace 摘要单测；新增 `tests/test_runtime_versions.py`，覆盖关键依赖版本基线与 `pip check`
  - `requirements.txt` 收紧到当前已验证版本下界，移除对 `langgraph>=0.0.40` 之类旧口径的默认兼容假设，并把 `streamlit` 降为可选说明
  - 重写 `README.md`、`docs/analysis_agent_summary.md`、`docs/new_retrieval_agent_summary.md`、`docs/planning_agent_summary.md`、`docs/knowledge_base_architecture.md`，统一到 Phase 3 主链、`retrieval_bundle`、正式 `outputs/chroma_db` 落库口径
  - 为 `PlanningAgent`、`AssemblyAgent`、`ValidationAgent`、`DebuggingAgent`、`retrieval_agent_old` 增加显式状态说明，区分 compat、基础能力复用与历史保留
  - 同步 `工作流总结文档.md` 与整改清单，回写第三批完成状态、环境基线和最新验收结果
- 影响：
  - 顶层文档不再把 `PlanningAgent` 当作当前正式主链节点
  - 当前 `requirements.txt` 与已验证环境不再明显冲突
  - trace 落盘文件无需翻完整 node IO，也能快速定位 pattern、退化点、placeholder / unresolved error 与失败节点
  - 第三批收口完成后，后续迭代可以把注意力重新放回规划质量与 Repair 闭环
- 验证：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests`
  - `python -m unittest discover -s tests -p "test_phase3*.py"`
  - `python -m unittest discover -s tests -p "test_phase2*.py"`
  - `python -m unittest discover -s tests -p "test_phase1_workflow.py"`
  - `python -m unittest tests.test_runtime_versions`
  - `python -m pip check`
  - 结果：`23` 个 Phase 3 测试通过；`23` 个 Phase 2 测试通过；`11` 个 Phase 1 回归测试通过；`2` 个 runtime 版本 / 依赖一致性测试通过；`compileall` 通过；`pip check` 通过
- 风险与回滚：
  - 主要风险：本次主要是文档、环境与 trace 观测收口，不等于在线 LLM 质量或系统级规划质量已经完成
  - 回滚：回退 `workflow_trace/tests/docs/requirements/legacy module` 相关改动即可恢复第三批收口前状态
- 文档同步：`README.md`、`工作流总结文档.md`、`工作流演进记录.md`、整改清单与相关 agent/docs 摘要均已同步
- 后续动作：继续提高真实 AHU 复杂场景的规划质量；若进入 HITL / `interrupt` / persistence / `Send`，从当前版本基线重新验证 LangGraph 依赖
- DoD：README 与核心摘要文档已同步 `[x]`；遗留模块定位已收口 `[x]`；依赖与环境基线已冻结 `[x]`；trace 摘要已补强 `[x]`；Phase 1/2/3 回归与 runtime smoke 已通过 `[x]`

### [2026-04-08] 验收加固：真实集成回归、模板匹配修正、共享信号未解析即失败（Record ID: REC-2026-04-08-P3-HARDEN）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`agents/architecture_planner.py`、`agents/global_assembler.py`、`agents/subsystem_planner.py`、`agents/verifier_agent.py`、`tests/test_phase3_architecture_planner.py`、`tests/test_phase3_global_assembler.py`、`tests/test_phase3_workflow.py`、`工作流总结文档.md`、`工作流演进记录.md`
- 背景：第三阶段首版主链已落地，但验收复查发现两个可信度缺口：一是缺少真实 `AnalysisAgent + RetrievalAgent` 的 Phase 3 集成回归；二是共享信号缺少真实导出方时，`GlobalAssembler` 可能注入 placeholder 后仍被 `VerifierAgent` 判为 `passed`。
- 目标：提高第三阶段验收可信度，避免“结构可编译但共享信号来源不真实”被误判为通过，并补上真实 AHU 资产驱动的端到端回归。
- 代码/契约变化：
  - `ArchitecturePlanner` 修正模板打分逻辑，优先命中 `template_role == subsystem_type` 的模板，避免真实 AHU 资产场景下错绑到无关模板
  - `GlobalAssembler` 新增外部输入/全局模式白名单判断；对未声明为外部输入且缺少真实导出方的共享信号，写入 `synthetic_shared_signal_source`
  - `GlobalAssembler` / `SubsystemPlanner` 为关键 `unresolved_items` 补充 `severity=error` 与 `suggested_fix`
  - `VerifierAgent` 对 `severity=error` 的 `unresolved_items` 生成正式 issue，并收敛到 `retryable_error`
  - Phase 3 测试新增：
    - 真实 `AnalysisAgent + RetrievalAgent + 临时 Chroma + AHU程序/flows_*.json` 集成回归
    - 缺少真实共享信号导出方的负向验收
    - 多导出方共享信号歧义的负向验收
    - `ArchitecturePlanner` 首选模板角色的回归断言
- 影响：
  - 第三阶段“通过”口径从“仅结构跑通”收紧到“关键共享信号未解析时不能静默通过”
  - 真实 AHU 资产参与的 Phase 3 端到端回归已建立，测试可信度高于纯 stub 前半链
  - 未改变 Phase 3 主链拓扑；仍为线性首版
- 验证：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests`
  - `python -m unittest discover -s tests -p "test_phase3*.py"`
  - `python -m unittest discover -s tests -p "test_phase2*.py"`
  - `python -m unittest discover -s tests -p "test_phase1_workflow.py"`
  - 结果：`13` 个 Phase 3 测试通过；`22` 个 Phase 2 测试通过；`11` 个 Phase 1 回归测试通过；`compileall` 通过
- 风险与回滚：
  - 主要风险：当前真实集成回归仍依赖可控 fake LLM 与临时 embedding，不等于在线 LLM 质量已完成验收
  - 回滚：回退本次涉及的 `architecture/global_assembler/subsystem_planner/verifier/tests/docs` 改动即可恢复 2026-04-07 基线
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md` 已同步
- 后续动作：继续补真实 AHU 复杂多子系统场景；明确哪些外部输入可以合法 placeholder，哪些必须由真实子系统导出
- DoD：真实 Phase 3 集成回归已建立 `[x]`；共享信号未解析不再静默通过 `[x]`；Phase 1/2 回归未退化 `[x]`

### [2026-04-07] 接入 Phase 3 分层规划主链并建立回归基线（Record ID: REC-2026-04-07-P3）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`agents/analysis_agent.py`、`agents/architecture_planner.py`、`agents/subsystem_planner.py`、`agents/global_assembler.py`、`utils/phase3_contracts.py`、`utils/phase3_adapters.py`、`tests/test_phase3_*.py`、`tests/test_phase1_workflow.py`、`工作流总结文档.md`、`工作流演进记录.md`、`docs/coding_agent_summary.md`
- 背景：Phase 1 与 Phase 2 基线已稳定，但单体 `PlanningAgent` 无法承载 AHU 的多页签、多子系统规划需求。
- 目标：在不引入 Repair、HITL 与并行 `Send` 的前提下，把主链升级为 `ArchitecturePlanner + SubsystemPlanner + GlobalAssembler`。
- 代码/契约变化：
  - 主链从 `analysis -> retrieval -> planning -> assembly -> coding -> verification -> END` 升级为 `analysis -> retrieval -> architecture_planning -> subsystem_planning -> global_assembly -> coding -> verification -> END`
  - `WorkflowState` 新增 `requirement_spec`、`decomposition_result`、`architecture_plan`、`subsystem_plan_map`
  - `AnalysisAgent` 新增 `requirement_spec` 输出；旧 `analysis_result` 结构保持兼容
  - 新增 `utils/phase3_contracts.py` 与 `utils/phase3_adapters.py`
  - 新增 `ArchitecturePlanner`、`SubsystemPlanner`、`GlobalAssembler`
  - `GlobalAssembler` 负责回填兼容 `execution_plan` 与 `assembled_graph_ir.source_execution_plan`
  - 新增 `tests/test_phase3_requirement_spec.py`、`tests/test_phase3_architecture_planner.py`、`tests/test_phase3_subsystem_planner.py`、`tests/test_phase3_global_assembler.py`、`tests/test_phase3_workflow.py`
- 影响：
  - 主规划链从单体弱计划升级为分层规划
  - `CodingAgent` 与 `VerifierAgent` 的真实输入边界不变，仍分别消费 `assembled_graph_ir` 与 `compiled_artifact`
  - `PlanningAgent` / `AssemblyAgent` 退居历史兼容路径，不再是正式主链节点
- 验证：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests`
  - `python -m unittest discover -s tests -p "test_phase3*.py"`
  - `python -m unittest discover -s tests -p "test_phase2*.py"`
  - `python -m unittest discover -s tests -p "test_phase1_workflow.py"`
  - 结果：`11` 个 Phase 3 测试通过；`22` 个 Phase 2 测试通过；`11` 个 Phase 1 回归测试通过；`compileall` 通过
- 风险与回滚：
  - 主要风险：当前 `ArchitecturePlanner` / `SubsystemPlanner` 以规则优先为主，系统级质量仍受资产覆盖率限制
  - 回滚：回退本次新增的 Phase 3 文件与 `workflow*` / `analysis_agent` / 文档改动即可恢复 Phase 2 基线
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md`、`docs/coding_agent_summary.md` 已同步
- 后续动作：继续迭代 Phase 3 规划质量；视需要进入 Repair 闭环与 `Send` 并行化
- DoD：主链已切换 `[x]`；新增状态契约已入链 `[x]`；Phase 3 自动化测试已建立并通过 `[x]`；Phase 1/2 回归未退化 `[x]`

### [2026-04-07] 关闭第二阶段整改清单并完成验收（Record ID: REC-2026-04-07-P2-CLOSE）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`agents/planning_agent.py`、`agents/verifier_agent.py`、`utils/ahu_knowledge_builder.py`、`tests/test_phase1_workflow.py`、`tests/test_phase2_ahu_knowledge_builder.py`、`tests/test_phase2_planning_bundle.py`、`工作流总结文档.md`、`工作流演进记录.md`
- 背景：第二阶段原有回归线已通过，但整改复查确认仍存在验收阻塞项、资产身份稳定性缺口和 Chroma 可重复构建缺口。
- 目标：关闭整改清单中的 `P0/P1` 项，并把第二阶段状态从“条件通过”收敛到“通过”。
- 代码/契约变化：
  - `PlanningAgent` 明确写入 Phase 2 规划优先级：先参考 `system_patterns`，优先复用 `subflow_templates`，模板不足再退化到 `atomic_modules`
  - 空 `execution_plan` 不再作为合法规划结果；规划失败保持可追踪错误语义
  - `VerifierAgent` 将规划失败、空计划、空 `assembled_graph_ir.node_instances`、零节点编译产物统一判定为 `retryable_error`
  - compile 阶段 wire 目标端口校验改为对照目标节点 `inputs`
  - `subflow_template.template_id` 纳入稳定拓扑签名；`dependency_module_types` 改为按真实内部功能对象提取
  - `write_assets_to_chroma()` 增加 stale ID 清理，支持可重复构建
  - 新增 `tests/test_phase2_planning_bundle.py`；扩展 Phase 1 与 Phase 2 验收回归
- 影响：
  - 第二阶段结构验收口径闭合，空产物不再误判为通过
  - AHU 子流程模板身份与依赖语义稳定性提升
  - Phase 2 索引写库支持重复构建，不再残留过期资产
- 验证：
  - `python -m unittest discover -s tests -p "test_phase2*.py"`
  - `python -m unittest discover -s tests -p test_phase1_workflow.py`
  - `python -m compileall agents utils scripts tests workflow.py workflow_trace.py`
  - `python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_acceptance`
  - `python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_acceptance_write --write-chroma --persist-dir outputs/test_tmp/chroma_phase2_acceptance`
  - 结果：`22` 个 Phase 2 测试通过；`11` 个 Phase 1 回归测试通过；`compileall` 通过；验收构建与写库路径均产出 `7` 个 `subflow_templates` 与 `1` 个 `system_patterns`
  - 未覆盖：在线 LLM 质量；真实 AHU 项目上的召回质量；真实 AHU 项目上的系统级规划质量；目标 `chroma_db` 的正式落库策略
- 风险与回滚：
  - 主要风险：第二阶段通过不等于系统级规划能力已完成；Phase 3 分层规划仍未启动
  - 回滚：回退本次涉及的 planning / verifier / knowledge_builder / tests / docs 改动即可恢复整改前状态
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md` 已同步
- 后续动作：进入 Phase 3 分层规划；继续迭代 AHU 资产质量与检索质量；明确正式 `chroma_db` 落库策略
- DoD：整改清单 `P0/P1` 已关闭；验收命令通过；文档口径已同步

### [2026-04-04] 补齐 Phase 2 验收硬化：脚本入口 / Windows 编码 / 临时 Chroma 回归（Record ID: REC-2026-04-04-P2-HARDEN）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`scripts/build_phase2_retrieval_indexes.py`、`utils/console_utils.py`、`utils/model_manager.py`、`agents/retrieval_agent.py`、`agents/planning_agent.py`、`agents/analysis_agent.py`、`utils/knowledge_base_manager.py`、`agents/retrieval_agent_old.py`、`tests/test_phase2_retrieval_agent.py`、`工作流总结文档.md`、`工作流演进记录.md`
- 背景：Phase 2 主功能完成后，验收暴露 3 个工程性缺口：脚本不能直接运行、Windows 控制台可能触发 `UnicodeEncodeError`、缺少真实临时 Chroma 的 `RetrievalAgent` 回归。
- 目标：补齐工程性可运行性与检索链路回归，提升第二阶段验收可信度。
- 代码/契约变化：
  - `scripts/build_phase2_retrieval_indexes.py` 增加项目根目录注入逻辑，支持 `python scripts/...` 直接执行
  - 新增 `utils/console_utils.py`，集中处理 `safe_console_text / safe_print`
  - `model_manager`、`retrieval_agent`、`planning_agent`、`analysis_agent`、`knowledge_base_manager`、`retrieval_agent_old` 的控制台输出切到 `safe_print`
  - 新增 `tests/test_phase2_retrieval_agent.py`，使用临时持久化 Chroma、可重建自定义 embedding、真实 `RetrievalAgent` 与真实 `retrieve_bundle / retrieve / __call__` 路径做回归
  - 主工作流契约不变；本次只做工程性加固与测试补齐
- 影响：
  - 主链拓扑不变，仍为 `6` 节点
  - 索引构建脚本可直接从常规终端运行
  - `RetrievalAgent` 在 Windows 默认控制台下输出更稳定
  - 第二阶段验收从“离线契约验证”扩展到“真实临时 Chroma 检索路径回归”
- 验证：
  - `python scripts/build_phase2_retrieval_indexes.py --output-dir outputs/test_tmp/pattern_library_direct_smoke`
  - `python -m unittest discover -s tests -p "test_phase2_retrieval_agent.py"`
  - `python -m unittest discover -s tests -p "test_phase2*.py"`
  - `python -m unittest discover -s tests -p test_phase1_workflow.py`
  - `python -m compileall agents utils scripts tests`
  - 结果：直接脚本入口通过，可生成 `7` 个 `subflow_templates` 与 `1` 个 `system_patterns`；`test_phase2_retrieval_agent.py` 的 `4` 个测试通过；共 `17` 个 Phase 2 测试通过；`9` 个 Phase 1 回归测试通过；`compileall` 通过
  - 未覆盖：Phase 2 collections 正式写入目标 `chroma_db` 的落库策略；真实 AHU 项目上的端到端召回质量与系统级规划质量
- 风险与回滚：
  - 主要风险：`safe_print` 会降级显示控制台不支持的字符；正式目标库的 Phase 2 collections 仍未落库
  - 回滚：仅需回退 `scripts/build_phase2_retrieval_indexes.py`、`utils/console_utils.py`、相关 `safe_print` 引用与 `tests/test_phase2_retrieval_agent.py`
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md` 已同步；`docs/coding_agent_summary.md` 待同步
- 后续动作：确定是否以及如何把 Phase 2 collections 写入目标 `chroma_db`；落库后补真实运行态验收；继续进入 Phase 3 拆分 `PlanningAgent`
- DoD：代码已落地 `[x]`；测试通过 `[x]`；文档已同步 `[x]`

### [2026-04-03] 完成 Phase 2 检索升级并接入 `retrieval_bundle` 主链（Record ID: REC-2026-04-03-P2）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`agents/retrieval_agent.py`、`agents/planning_agent.py`、`agents/assembly_agent.py`、`agents/coding_agent.py`、`utils/retrieval_bundle_utils.py`、`utils/ahu_knowledge_builder.py`、`scripts/build_phase2_retrieval_indexes.py`、`tests/test_phase2_retrieval_bundle.py`、`tests/test_phase2_ahu_knowledge_builder.py`、`tests/test_phase2_bundle_consumers.py`、`工作流总结文档.md`、`工作流演进记录.md`
- 背景：Phase 1 主链已稳定，但单层 `retrieval_context` 无法承载 AHU 子流程模板和系统模式资产。
- 目标：把 Retrieval 主契约升级为多类结果包，并在保留兼容性的前提下让下游节点消费新契约。
- 代码/契约变化：
  - `workflow.py` 与 `workflow_trace.py` 增加 `retrieval_bundle`
  - `RetrievalAgent` 同时检索原子模块、子流程模板、系统模式等资产，输出 `retrieval_bundle`，并派生兼容 `retrieval_context`
  - `PlanningAgent`、`AssemblyAgent`、`CodingAgent` 改为优先消费 bundle
  - 新增 `utils/retrieval_bundle_utils.py`，统一 bundle 识别、兼容视图与编译文档映射
  - 新增 `utils/ahu_knowledge_builder.py` 与 `scripts/build_phase2_retrieval_indexes.py`，打通 AHU 资产构建链
  - 新增 `3` 个 Phase 2 测试文件
  - Retrieval 主输出从单层 `retrieval_context` 升级为 `retrieval_bundle + retrieval_context(兼容视图)`
- 影响：
  - 主链仍为 `6` 节点；拓扑不变
  - Planning / Assembly / Coding 可直接消费 bundle 中的子流程模板和系统模式提示
  - 旧调用仍可通过 `retrieval_context` 工作
- 验证：
  - `python -m unittest discover -s tests -p "test_phase2*.py"`
  - `python -m unittest discover -s tests -p test_phase1_workflow.py`
  - 结果：`13` 个 Phase 2 测试通过；`9` 个 Phase 1 回归测试通过
  - 未覆盖：在线 LLM 质量；真实 AHU 项目上的召回与系统级规划质量
- 风险与回滚：
  - 主要风险：检索升级不等于系统级规划升级；分层 Planner 仍缺失
  - 回滚：回退 `workflow*`、`agents/retrieval/planning/assembly/coding`、`utils/retrieval_bundle_utils.py`、`utils/ahu_knowledge_builder.py`、`scripts/build_phase2_retrieval_indexes.py` 与 `3` 个 Phase 2 测试文件
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md` 已同步；`docs/coding_agent_summary.md` 待同步
- 后续动作：进入 Phase 3，拆分 `PlanningAgent`；持续提高 AHU 资产质量与检索质量
- DoD：代码已落地 `[x]`；测试通过 `[x]`；文档已同步 `[x]`

### [2026-03-31] 补齐 Phase 1 验收缺口并更新回归测试（Record ID: REC-2026-03-31-P1-FIX）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`agents/assembly_agent.py`、`agents/coding_agent.py`、`agents/verifier_agent.py`、`workflow_trace.py`、`tests/test_phase1_workflow.py`、`工作流总结文档.md`、`工作流演进记录.md`
- 背景：对照 `docs/ahu/LangGraph工作流V2架构设计.md` 的 Phase 1 要求复查后，发现实现与验收之间仍有缺口。
- 目标：让 Phase 1 在“强结构 IR + 确定性编译 + 结构验收”主链上真正闭合。
- 代码/契约变化：
  - 修复 subflow 实例在 `Assembly -> Compiler` 间的定义映射
  - 编译器对坏边改为 warning，不再直接抛异常
  - `VerifierAgent` 新增必需输入端口缺失检查
  - `workflow_trace.py` 恢复异常场景下的 trace 落盘
  - 契约层新增：`verification_report.metrics.missing_required_inputs` 开始真实生效
- 影响：
  - subflow 实例可稳定进入编译结果
  - 更多问题转入 `verification_report`，编译阶段异常减少
  - 主链拓扑不变，兼容字段 `generated_code` 不变
- 验证：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests\test_phase1_workflow.py`
  - `python -m unittest discover -s tests -p test_phase1_workflow.py`
  - 结果：编译通过；`9` 个测试通过
  - 未覆盖：在线 LLM + ChromaDB 的真实端到端质量
- 风险与回滚：
  - 主要风险：Phase 1 已稳定，但仍不等于完整 V2
  - 回滚：仅需回退本次涉及的 `5` 个代码文件与 `2` 个文档文件
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md` 已同步；`docs/coding_agent_summary.md` 待同步
- 后续动作：进入 Phase 2，扩展 `retrieval_context -> retrieval_bundle`
- DoD：代码已合并 `[ ]`；测试通过 `[x]`；文档已同步 `[x]`

### [2026-03-27] 完成 Phase 1 Graph IR 重构并建立离线验收（Record ID: REC-2026-03-27-P1）

- 状态：`已完成`
- 修改人：Codex
- 关联文件：`workflow.py`、`workflow_trace.py`、`utils/graph_ir.py`、`agents/assembly_agent.py`、`agents/coding_agent.py`、`agents/verifier_agent.py`、`agents/coding_utils.py`、`tests/test_phase1_workflow.py`
- 背景：第一阶段目标不是继续强化 V1 四节点链路，而是先把 V2 需要的 `Graph IR / Compiler / Verifier` 立住；对应设计见 `docs/ahu/LangGraph工作流V2架构设计.md`。
- 目标：在不先重写 Planner 的前提下，建立可编译、可验收、可持久化的 Graph IR 中间层。
- 代码/契约变化：
  - 主链从 `analysis -> retrieval -> planning -> coding -> END` 升级为 `analysis -> retrieval -> planning -> assembly -> coding -> verification -> END`
  - 新增 `AssemblyAgent`、`VerifierAgent`、`utils/graph_ir.py` 与 `tests/test_phase1_workflow.py`
  - 新增状态字段 `assembled_graph_ir`、`compiled_artifact`、`verification_report`
  - `CodingAgent` 的真实输入从 `execution_plan` 变为 `assembled_graph_ir`
  - 最终真实产物从 `generated_code` 升级为 `compiled_artifact`；`generated_code` 退化为兼容字段
  - 包初始化不再主动导入所有 Agent，避免离线测试被 `chromadb` 等可选依赖阻塞
- 影响：
  - 主链从“弱计划直出 JSON”变为“弱计划 -> 强结构 IR -> 确定性编译 -> 结构验收”
  - `workflow_trace.py` 与正式入口同步到同一条主链
  - 原有“四节点线性链路”描述失效
- 验证：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests\test_phase1_workflow.py`
  - `python -m unittest discover -s tests -p test_phase1_workflow.py`
  - 结果：编译通过；`5` 个测试通过
  - 用例：离线常量相加计划、子流程定义与子流程实例关系、带 stub 的 LangGraph 端到端运行
  - 未覆盖：在线 LLM 质量、ChromaDB 对复杂 AHU 样例的召回、真实 AHU 多页面/多子流程工程的系统级规划质量
- 风险与回滚：
  - 主要风险：Planner 仍是单体弱计划层；旧命名与旧模块残留仍会制造认知偏差
  - 回滚：回退上述代码文件与测试文件即可恢复 Phase 1 之前链路
- 文档同步：`工作流总结文档.md`、`工作流演进记录.md` 已同步
- 后续动作：进入 Phase 2，把单层 `retrieval_context` 升级为正式 `retrieval_bundle`，并补检索层测试
- DoD：代码已落地；测试通过；文档已同步

## 5. 决策记录

### [2026-03-27] Phase 1 不先重写 Planner，先立 Graph IR

- 结论：保留现有 `PlanningAgent -> execution_plan`，新增 `AssemblyAgent` 把 plan 升级为 `assembled_graph_ir`
- 背景：同时改检索、规划、编译、修复会让重构面过大，难以稳定验收
- 备选：`先改三层检索`、`先拆 Planner`、`先立 Graph IR + Compiler`
- 选择原因：Graph IR 是后续所有阶段的共同依赖，也是最小但最关键的基础设施
- 代价：Planner 仍是单体弱计划层，复杂 AHU 规划能力不会立即提升
- 后续：方向不会推翻，但 `assembled_graph_ir` 字段设计仍会继续扩展

### [2026-03-27] Phase 1 先做 Verifier，不做 Repair 闭环

- 结论：Phase 1 只接入 `VerifierAgent` 做规则优先验收，不接入 `RepairRouter / RepairAgent`
- 背景：当前最缺的是可靠验收标准，不是立即自动修复
- 备选：`验收与 Repair 一起做`、`先验收再 Repair`
- 选择原因：没有可靠验收标准，Repair 就没有明确目标
- 代价：当前主链拿到 `verification_report` 后不会自动修复
- 后续：不会推翻；Phase 4 再补 Repair 闭环

### [2026-03-27] 继续保留 `generated_code` 兼容字段

- 注：该决策已在 2026-04-21 仓库级清退中失效；保留这里仅用于说明当时为何没有立即删除。
- 结论：保留 `generated_code`，其值等于 `compiled_artifact["json_text"]`
- 背景：现有代码和外部调用可能仍依赖 `generated_code`
- 备选：`立即删除`、`保留兼容映射`
- 选择原因：第一阶段优先稳定新编译边界，而不是扩大外部破坏面
- 代价：字段语义暂时不够干净
- 后续：后续可能改为 `generated_flow_json`，或只暴露 `compiled_artifact`

## 6. 契约变更摘要

| 日期 | 契约 | 变更 | 影响 |
|:---|:---|:---|:---|
| 2026-04-27 | `subsystem_plan_map[*].template_binding` 可选诊断 | 新增 `llm_advisory`，记录 Subsystem LLM interface adapter 的 enabled、adopted、patch_count、rejected_patch_count、risk_flags、fallback 等诊断 | 不改变子系统局部 IR 主结构的前提下，可观察端口绑定 patch 是否采用、拒绝或回退 |
| 2026-04-27 | `architecture_plan.pattern_bindings[*]` 可选诊断 | 新增 `llm_advisory`，记录 Architecture LLM advisor 的 enabled、adopted、patch_count、rejected_patch_count、warnings、fallback 等诊断 | 不新增顶层状态字段即可追踪页面、模板偏好和 shared signal advisory 的采用情况 |
| 2026-04-27 | `architecture_plan.warnings` / `decomposition_result.warnings` | Architecture advisor 被拒绝的非法 patch、未知模板、shared signal 多 owner advisory 会进入 warnings | LLM advisory 不会强行污染主结构，风险可在节点输出和 trace 中观测 |
| 2026-04-27 | LLM 增强开关 | 新增 `SUBSYSTEM_USE_LLM_ADAPTER` 与 `ARCHITECTURE_USE_LLM_ADVISOR`，均需配合 `LLM_ENHANCEMENT_ENABLED=True` 才会真实调用 | 默认关闭时保持 deterministic 基线，支持分节点试跑和快速回滚 |
| 2026-04-26 | `requirement_spec` 扩展字段 | 新增可选 `engineering` 扩展，包含 `points / control_loops / interlocks / communication / naming_convention / missing_required_fields / confidence` | Analysis A0 可把 AHU 工程点位、回路、联锁与缺失项结构化沉淀给后续 Planner；下游暂不强依赖，兼容正式主契约 |
| 2026-04-26 | `analysis_result` 增强诊断 | `AnalysisAgent` 在 `ANALYSIS_USE_ENGINEERING_COMPILER=True` 时写入 `engineering_analysis`，记录 enabled、llm_used、fallback、adopted、patch_summary 等诊断 | trace / 节点 IO 可观察 Analysis LLM 增强是否采用或回退 |
| 2026-04-26 | `analysis_result.clarification_signals` | 新增工程缺失类 high severity 信号：设备数量、点表、通讯地址、控制目标、PID 信号等 | 前置澄清可更早暴露 AHU 工程输入缺口 |
| 2026-04-26 | `retrieval_bundle.metadata` 增强诊断 | 新增 `llm_rewrite`、`base_query_variants`、`template_query_variants`、`pattern_query_variants`、`reranker_enabled`、`reranker_model`、`reranker_fallback_used`、`reranker` | 不改变 bundle 正式结构的前提下，能观察查询改写与 rerank 是否启用、采用、失败回退 |
| 2026-04-26 | Retrieval LLM 开关语义 | `RETRIEVAL_USE_LLM_REWRITE` 需要配合 `LLM_ENHANCEMENT_ENABLED=True` 才会真实调用 LLM | 避免本地 `.env` 单独打开 rewrite 时误触真实 LLM；便于分节点试跑 |
| 2026-04-26 | `SubsystemPlanner` atomic fallback | 动态输入参数按 `parameters_schema.maximum` 裁剪；容量不足时写 `atomic_fallback_input_capacity_limited` warning | 避免 fallback 生成违反平台合同的 `inputCount / inputs`，恢复 `fan_damper` workflow 回归 |
| 2026-04-25 | 子流程定义编译契约 | `SubflowDefinitionIR` 新增 `internal_flow_objects`；子流程模板 body 从 pattern library 进入 Graph IR，并在 `CodingAgent` 中展开为真实 body 节点 | `compiled_artifact.flow_objects` 可输出带 body 的 subflow 定义，不再只是壳 |
| 2026-04-25 | 平台 flow schema / 模块合同校验 | 新增 `validate_flow_document(...)`，覆盖 flow 顶层结构、`tab/subflow/subflow:<id>`、`z`、`wires`、占位符残留以及首批高频模块合同 | 编译产物开始具备平台合同级结构验收，`VerifierAgent` 可对 compile 缺陷稳定失败 |
| 2026-04-25 | `compiled_artifact.compile_report` | 新增 `body_node_count / dropped_node_count / missing_template_count / unresolved_placeholder_count / body_expansion_errors` | compile 风险从 warning 提升为结构化诊断，可进入 trace 和 verifier |
| 2026-04-25 | Trace 编译诊断摘要 | `workflow_trace` 新增 `subflow_instance_count` 与 strict compile / body 编译相关摘要字段 | 不打开完整 final state 也能判断 body 是否展开、strict compile 是否失败 |
| 2026-04-24 | `WorkflowState` / 运行时开关 | 新增 `enable_repair_agent`，默认 `False`，仅显式开启时允许 repair 路由 | 正式默认运行口径从“默认尝试修复”切到“默认严格暴露问题” |
| 2026-04-24 | `route_decision.reason` | `RepairRouter` 新增 `repair_agent_disabled` | 调用方可以区分“可修但未启用 repair”与“不可修 / 超预算” |
| 2026-04-24 | Workflow API / Frontend 运行请求 | create run / runner / service / 前端 RunComposer 同步透传 `enable_repair_agent` | API 与前端可显式控制 repair 是否开启，避免隐式默认值分叉 |
| 2026-04-23 | Workflow Frontend V1 消费契约 | 新增 `frontend/` V1 驾驶舱，只消费 Workflow API 投影 DTO；前端类型定义不引入完整 `WorkflowState`，ReviewCard 固定提交 `attempt_id / review_id / decision / answers / feedback / updated_constraints` | 前端与 LangGraph 内部状态隔离，后续主链内部演进不应迫使页面重写 |
| 2026-04-23 | 本地前后端联调 CORS | `app.main` 新增 `WORKFLOW_CORS_ORIGINS`，默认允许 Vite dev server 的 `127.0.0.1:5173` / `localhost:5173` | 本地浏览器可直接访问 FastAPI Workflow API |
| 2026-04-23 | Workflow API 外部资源模型 | 新增 `thread_id / attempt_id / review_id` 作为服务层稳定主键；`thread_id` 对应 LangGraph checkpoint 指针，`attempt_id` 对应一次 API/trace 运行，`review_id` 对应当前人工 review 卡片 | 前端不直接依赖 LangGraph 内部 checkpoint 或完整 `WorkflowState` |
| 2026-04-23 | Workflow API 状态机 | V1 对外状态固定为 `queued / running / interrupted / completed / rejected / failed`；同一 `thread_id` 只允许一个 active attempt | 前端可用稳定产品态轮询，不绑定内部节点拓扑 |
| 2026-04-23 | Workflow API 错误结构 | API 错误统一为 `{ "error": { "code": "...", "message": "...", "details": {...} } }` | 前端错误渲染不再依赖 FastAPI 默认 `detail` 格式 |
| 2026-04-23 | Workflow API trace 投影 | 新增 `/trace` 与 `/state-history` 调试接口；`/trace` 返回摘要投影，不要求前端读取磁盘 `summary_json` | trace 文件格式变化时可由服务层隔离前端 |
| 2026-04-23 | API checkpointer / 元数据仓储 | API 默认使用 SQLite checkpointer 与 JSON attempt 仓储，本地联调可跨重启恢复基础元数据与 checkpoint | 开发联调具备最小 durable 能力；生产仍需更强持久化后端 |
| 2026-04-21 | 正式状态字段 | `retrieval_context`、`execution_plan`、`generated_code`、`execution_result`、`validation_result`、`next_step` 已全部退出正式运行时状态 | 正式主链与 legacy 历史术语彻底解耦 |
| 2026-04-21 | 仓库级 public surface | `RetrievalAgent.retrieve()`、`CodingAgent.compile_graph(...)`、`CodingAgent.generate_json(...)` 与 root wrapper 全部删除 | 仓内不再保留 public compat API 与 wrapper 托底 |
| 2026-04-21 | 正式 helper 输入边界 | `retrieval_bundle_utils`、`context_formatter`、`AssemblyShared` 全部收口为 bundle-only | formal helper 不再承载 bundle-or-context 双形态兼容 |
| 2026-04-15 | `shared_signal_registry` / `subsystem_plan_map` | 共享信号补齐 `candidate_exporters / resolution_status / resolution_evidence`；子系统槽位与计划补齐 `selection_reason / degrade_reason` | planning 决策与降级原因可追踪 |
| 2026-04-15 | `assembled_graph_ir.unresolved_items[*]` / `verification_report.issues[*].repair_payload` | planning / assembly / compile 三类 repair payload 结构化收口；assembly unresolved 补齐边定位字段 | RepairAgent 不再依赖 message 文本或隐式重扫作为主路径 |
| 2026-04-15 | `route_decision.reason` | `RepairAgent` 对无法收敛的 ambiguous shared signal 写入 `ambiguous_shared_signal_unresolved` | 歧义未收敛成为稳定 reject 语义 |
| 2026-04-15 | Trace 摘要 | 新增 `planning_unresolved_by_type / ambiguous_signal_count / repair_reject_category` | 单看 trace 摘要即可区分成功、已修、歧义未收敛、无可修、超预算等终态 |
| 2026-04-14 | `InterfaceBindingSpec` / Phase 3 规划契约 | `subsystem_descriptors[*]` 与 `subsystem_plan_map[*]` 新增接口语义层字段，统一携带 `canonical_signal_key / binding_kind / allowed_external` | shared signal 与 external 接口正式分流 |
| 2026-04-14 | `verification_report.issues[*]` | 新增 `repair_payload`，planning 与 compile issue 可携带结构化修补上下文 | `RepairAgent` 不再依赖 message 解析作为主路径 |
| 2026-04-14 | `GlobalAssembler` shared signal 语义 | external 类接口直接注入 placeholder，不再产出 `synthetic_shared_signal_source`；只有 `shared_signal` 进入 owner/exporter 判定 | shared/external 边界收紧 |
| 2026-04-07 | `WorkflowState` | 新增 `requirement_spec`、`decomposition_result`、`architecture_plan`、`subsystem_plan_map` | Phase 3 分层规划正式入链 |
| 2026-04-07 | 主链拓扑 | `planning -> assembly` 升级为 `architecture_planning -> subsystem_planning -> global_assembly` | 正式主链切到分层规划 |
| 2026-04-07 | `execution_plan` | 从主规划产物退化为兼容投影，由 `GlobalAssembler` 回填 | 保持 `VerifierAgent` 兼容语义 |
| 2026-04-11 | `WorkflowState` | 新增 `repair_context`、`repair_history`、`route_decision`、`retry_budget`、`retry_counts_by_scope`；`retry_count` 改为聚合值 | Phase 4 Repair 状态正式入链 |
| 2026-04-11 | 主链拓扑 | `verification -> END` 升级为 `verification -> repair_router -> (accept|repair_agent|reject)` | Phase 4 Repair 闭环正式入链 |
| 2026-04-11 | Trace 摘要 | 新增 `repair_round_count`、`final_route_decision`、`retry_counts_by_scope`、`reject_reason` 等 repair 字段 | Repair 轮次与终止原因可观测 |
| 2026-03-27 | `WorkflowState` | 新增 `assembled_graph_ir`、`compiled_artifact`、`verification_report` | 主链从弱计划直出 JSON 升级为 IR -> 编译 -> 验收 |
| 2026-03-27 | `execution_plan -> coding` | 不改 `execution_plan` 字段结构，但不再直接供 Coding 生成 JSON，而是先供 Assembly 生成 Graph IR | `CodingAgent` 输入边界改变 |
| 2026-03-27 | 最终产物 | 真实产物从 `generated_code` 升级为 `compiled_artifact`；`generated_code` 仅保留兼容语义 | 调用方和文档语义需要更新 |
| 2026-03-31 | `verification_report.metrics` | `missing_required_inputs` 开始真实生效 | Phase 1 验收粒度提升 |
| 2026-04-03 | `WorkflowState` | 新增 `retrieval_bundle` | Retrieval 主契约升级 |
| 2026-04-03 | Retrieval | 主输出从单层 `retrieval_context` 升级为 `retrieval_bundle + retrieval_context(兼容视图)` | Planning / Assembly / Coding 可直接消费多类资产 |
| 2026-04-07 | Planning / Verification | 空计划、空 IR、零节点编译产物进入 `retryable_error`；compile wire 目标端口按目标 `inputs` 校验 | 第二阶段验收口径闭合 |
| 2026-04-07 | AHU 资产构建 | `template_id` 纳入稳定拓扑签名；`dependency_module_types` 按真实内部功能对象提取；写库前清理 stale IDs | 资产身份与可重复构建稳定 |

## 7. 验收现状

### 已回归

- Phase 4 `WorkflowState` repair 字段与 `recursion_limit`
- 接口语义层 `shared_signal / external_* / subsystem_output` 的分类与归一
- `RepairRouter` 的 scope 路由、预算裁决、非法 scope reject、预算耗尽 reject
- `RepairAgent` 对 planning / assembly / compile 三类 deterministic patch、external 重分类路径，以及 `ambiguous_shared_signal` 可收敛/不可收敛两类结果
- `workflow.py` / `workflow_trace.py` 的 Repair 闭环运行
- trace 摘要对 repair 轮次、scope、最终路由、planning unresolved 分类、歧义信号数量与 reject 类别的观测
- `VerifierAgent` 到 `RepairAgent` 的 structured `repair_payload` 传递
- Phase 3 `requirement_spec` 适配
- `ArchitecturePlanner` 对 `system_patterns` 的显式绑定
- `SubsystemPlanner` 的模板复用与 atomic fallback
- `GlobalAssembler` 的多页签装配、共享信号连接与 external 占位源注入
- 真实 `AnalysisAgent + RetrievalAgent + 临时 Chroma + AHU程序/flows_*.json` 的 Phase 3 集成回归
- 共享信号缺少真实导出方、多导出方歧义的负向验收
- 固定 2 条查询的 Phase 4 query smoke
- 固定 7 条 deterministic case 的 Phase 5 query suite
- workflow 级 planning / assembly / compile repair 成功、多轮 repair、稳定 reject 与 budget exhausted 回归
- `workflow.py` / `workflow_trace.py` 的 Phase 3 主链运行
- Phase 1 Graph IR 生成、编译、结构验收
- 子流程定义与实例关系
- `workflow.py` / `workflow_trace.py` 带 stub 主链运行
- Planning / Assembly / Coding / AssemblyShared 的 bundle-only 消费链路
- AHU 子流程模板与系统模式资产构建
- 空计划 / 空 IR / 零节点编译产物拦截
- compile wire 目标端口按目标 `inputs` 校验
- 子流程模板稳定拓扑签名、真实依赖抽取与 Chroma stale 清理
- `RetrievalAgent` 在临时持久化 Chroma 上的真实 bundle 检索
- 索引构建脚本直接入口
- Windows 控制台安全输出
- formal 路径对 `retrieval_context / execution_plan / generated_code` 不回流结果状态的负向合同断言
- `enable_repair_agent` 默认关闭、显式开启透传到 `workflow / workflow_trace / API / frontend` 的运行时合同
- `RepairRouter` 对“可修但未启用 repair”的稳定 reject 语义：`reason=repair_agent_disabled`
- `knowledge_contract_loader` 对 `schemas/*.json` 原子模块合同与 pattern library 子流程模板的加载
- `validate_flow_document(...)` 对平台 flow 顶层结构、`subflow:<id>` 引用、`z`、`wires`、占位符残留和首批模块合同的正负样本校验
- 子流程模板 `internal_flow_objects` 进入 Graph IR、body id / `z` / `wires` 重映射，以及 `subflow.in/out.wires` 指向 body 节点的编译闭环
- strict compile 诊断字段、trace 编译摘要字段与 body 计数观测
- `AnalysisAgent` 工程需求补丁、`requirement_spec.engineering` 合并、工程缺失澄清信号与 LLM fallback
- `RetrievalAgent` LLM 查询改写接线、metadata 诊断、默认关闭保护与 fake LLM fallback
- `RetrievalAgent` Cross-Encoder rerank 工具层、fake scorer 重排、失败回退与“不创造新资产 ID”约束
- `SubsystemPlanner` atomic fallback 动态输入端口按 schema maximum 裁剪，`fan_damper` 回归恢复通过
- `SubsystemPlanner` LLM interface adapter 的端口绑定 patch 采用、越界端口拒绝、未知模板 ID 拒绝、fallback_required 不强制降级、LLM 失败 fallback 与开关关闭路径
- `ArchitecturePlanner` LLM advisor 的页面补充、模板偏好重排、必需页删除拒绝、未知模板 ID 拒绝、shared signal 多 owner warning、LLM 失败 fallback 与开关关闭路径
- `thread_id / attempt_id / review index / persistence smoke` 的正式合同与冒烟验证
- Workflow API 的 active attempt 冲突、`attempt_id / review_id` resume 校验、终态禁止 resume、统一错误结构、trace 摘要接口与 state-history 调试接口
- Workflow Frontend V1 的 DTO 类型、API client、错误 envelope、ReviewCard 表单校验、current_step 阶段映射、生产构建与后端 health 联调

### 未回归

- 在线 LLM + ChromaDB 的复杂 AHU 场景端到端验证
- 基于真实 AHU 样例的系统级规划能力验证
- `Send` 并行与 reducer fan-in 的正式运行态
- 生产级多进程 worker / PostgreSQL checkpointer / 持久化元数据仓储上的 HITL / persistence 运行态
- 所有 Repair 支持问题类型的真实端到端回归
- 基于真实 AHU 模板与真实 `schemas` 覆盖范围的更大样本 body 编译回归
- semantic verifier、golden diff suite 与更高覆盖率模块合同的正式回归
- 新一轮真实 `workflow_trace.run_workflow(...)` 交付证据 trace 的人工复核
- 真实浏览器中的前端端到端人工点击验收与长链路运行演示
- 真实 LLM 查询改写、真实 Cross-Encoder rerank 的本地 smoke 与质量指标对比
- Verification semantic critic 的正式回归

## 8. 下次进入新对话前建议补充

- 新一轮真实 `workflow_trace.run_workflow(...)` 的交付证据 trace 路径与人工复核结果
- Phase 5 query suite 是否继续扩充真实资产/真实 Chroma 场景
- Phase 4 baseline 中 `phase3_tests` 单次失败的排查结果
- AHU 资产质量与检索质量的持续迭代结果
- 活动 `CHROMA_PERSIST_DIR` 与正式 `outputs/chroma_db` 的最终统一策略
- 历史文档中 `retrieval_context / execution_plan / generated_code` 等术语的继续收缩结果
- Workflow Frontend V1 真实浏览器端到端验收后的字段缺口、错误码补充和 SSE 事件流设计

