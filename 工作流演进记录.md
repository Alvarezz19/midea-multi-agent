# 工作流演进记录

> 用途：记录工作流的持续改动、当前进度、关键决策、验证情况与下一步计划。
> 使用建议：每完成一轮较明确的改动，就至少更新一次“快速对齐区”并新增一条“演进记录”。
> 配合文档：
> - [工作流总结文档](d:\yjsproject\midea\工作流总结文档.md)：回答“系统当前是什么样”
> - [工作流演进记录](d:\yjsproject\midea\工作流演进记录.md)：回答“最近改了什么、做到哪了、接下来做什么”

---

## 0. 变更索引表

> 用途：先快速定位“哪次改动影响了什么”，再下钻到对应演进记录详情。
> 维护规则：每新增一条“演进记录”，同步在本表新增一行。

| 记录ID | 日期 | 阶段 | 标题 | 状态 | 影响范围 | 测试状态 | 文档同步 |
|:---|:---|:---|:---|:---:|:---|:---:|:---:|
| REC-2026-03-31-P1-FIX | 2026-03-31 | Phase 1 | 补齐 Phase 1 验收缺口并更新回归测试 | 已完成 | assembly / coding / verifier / workflow_trace / tests / docs | 通过 | 已同步 |
| REC-2026-03-27-P1 | 2026-03-27 | Phase 1 | 完成 Graph IR 重构并建立离线验收 | 已完成 | workflow / assembly / coding / verifier / tests | 通过 | 已同步 |

---

## 1. 当前看板

### 当前状态

- 当前版本状态：`Phase 1 已完成，验收缺口已补齐`
- 当前正式主链：`analysis -> retrieval -> planning -> assembly -> coding -> verification -> END`
- 最近一次重大改动：修复了子流程实例编译、必需输入验收、坏边容错、失败 trace 落盘 4 个 Phase 1 缺口
- 当前最大问题：还没有三层检索和分层规划，复杂 AHU 结构的“承载骨架”已就位，但“系统级规划能力”还未升级
- 当前最大风险：V2 后续阶段尚未启动，新旧命名和旧模块残留仍较多
- 下一优先任务：进入 V2 第二阶段，扩展检索层次，把当前单层 `retrieval_context` 升级为三层 `retrieval_bundle`

### 里程碑进度

- 已经稳定的部分：
  - `WorkflowState` 已扩展到 Phase 1 结构
  - `assembled_graph_ir` 已成为正式中间产物
  - `CodingAgent` 已变成确定性编译器
  - `VerifierAgent` 已接入主链
  - `workflow.py` 与 `workflow_trace.py` 已同步到同一条 Phase 1 主链
  - 子流程定义/实例链路已从 `Assembly` 打通到 `Compiler`
  - `VerifierAgent` 已覆盖必需输入缺失
  - 编译器遇到坏边会降级为 warning，而不是提前崩溃
  - `workflow_trace.py` 在失败场景下也会落盘
  - 离线验收测试已建立并通过
- 还在演进中的部分：
  - 检索层次
  - Planning 分层拆分
  - Repair 闭环
- 明确未接入/未完成的部分：
  - `Execution Tool`
  - `Validation Agent`
  - `Debugging Agent`
  - `RepairRouter / RepairAgent`
  - 人工澄清与中断节点

### 当前优先级与待定事项

- 近期任务：
  - [ ] 第二阶段：扩展为三层检索结果包 `retrieval_bundle`
  - [ ] 第三阶段：把 `PlanningAgent` 拆为 `ArchitecturePlanner + SubsystemPlanner + GlobalAssembler`
  - [ ] 第四阶段：新增 `RepairRouter / RepairAgent`
- 暂缓 / 待定：
  - [ ] `Execution / Validation / Debugging` 是否复用旧实现
  - [ ] 是否重命名 `generated_code`
  - [ ] 是否引入人工澄清节点

### 当前风险与技术债

- 已知问题：
  - `generated_code` 命名仍然保留，和真实语义不完全一致
  - `Execution Tool / Validation Agent / Debugging Agent` 尚未与 Phase 1 主链对齐
  - 当前 `PlanningAgent` 仍是单体规划器，不适合直接承接复杂 AHU 系统级规划
- 技术债：
  - `docs/coding_agent_summary.md` 等旧文档尚未同步到 Phase 1 语义
  - `workflow_trace.py` 已同步主链，但其 trace 展示仍较偏通用结构，未来可以增加对 IR/Compile/Verify 的专门摘要
  - 当前 `VerificationReport` 还未参与自动路由和自动修复
- 建议优先级：

| 优先级 | 项目 | 原因 |
|:---|:---|:---|
| P0 | 第二阶段三层检索 | 这是复杂 AHU 程序可规划的前提 |
| P1 | 第三阶段拆分 Planner | 没有分层规划，就难以稳定输出多页面/多子系统工程 |
| P1 | 同步 `docs/coding_agent_summary.md` | 避免后续继续误解 CodingAgent 职责 |
| P2 | 清理旧命名与旧模块残留 | 提升代码可维护性，但不是最紧迫阻塞项 |

### 本次进入新对话前建议先看

1. [工作流总结文档](d:\yjsproject\midea\工作流总结文档.md)
2. [工作流演进记录](d:\yjsproject\midea\工作流演进记录.md)
3. `workflow.py`
4. `utils/graph_ir.py`
5. `agents/assembly_agent.py`
6. `agents/coding_agent.py`
7. `agents/verifier_agent.py`
8. `tests/test_phase1_workflow.py`

---

## 2. 当前架构快照

### 工作流主链

```text
user_query
  -> analysis
  -> retrieval
  -> planning
  -> assembly
  -> coding
  -> verification
  -> END
```

### 当前状态字段

| 字段 | 是否在主链使用 | 备注 |
|:---|:---:|:---|
| `user_query` | 是 | 用户输入 |
| `analysis_result` | 是 | Analysis 输出 |
| `retrieval_context` | 是 | Retrieval 输出 |
| `execution_plan` | 是 | Planning 输出 |
| `assembled_graph_ir` | 是 | Assembly 输出 |
| `compiled_artifact` | 是 | Coding 输出 |
| `verification_report` | 是 | Verifier 输出 |
| `generated_code` | 是 | 兼容字段，等于 `compiled_artifact.json_text` |
| `execution_result` | 否 | 历史预留 |
| `validation_result` | 否 | 历史预留 |
| `debug_history` | 否 | 历史预留 |
| `retry_count` | 否 | 为未来 Repair 预留 |
| `current_step` | 是 | 节点都会更新 |
| `next_step` | 否 | 当前未使用 |
| `final_output` | 是 | Verifier 和 Trace 入口会填充 |

### 当前关键约束

- Analysis Agent 的职责边界：只做需求理解，不做检索
- Retrieval Agent 的职责边界：只做向量检索与模块定义返回，不做后续编译
- Planning Agent 的硬约束来源：`retrieval_context.relevant_nodes[*].module_type`
- Assembly Agent 的核心职责：把弱 `execution_plan` 升级成强结构 `assembled_graph_ir`
- Coding Agent 的最终产物：`compiled_artifact`
- 当前与旧版逻辑不一致但保留的历史命名：`generated_code`

---

## 3. 演进记录

### 演进记录最小模板（强约束）

> 要求：后续每条演进记录必须至少填写以下 8 项，避免记录风格漂移。

```markdown
### [YYYY-MM-DD] 标题（Record ID: REC-xxxx）

- 改动状态：`计划中 / 进行中 / 已完成 / 已回滚`
- 修改人：
- 关联文件：
- 关联任务/背景：

#### 1. 改动目标
- 为什么改：
- 目标结果：

#### 2. 实际改动
- 代码/配置变更：
- 契约变化（输入/输出/状态字段）：

#### 3. 影响评估
- 主链影响：
- 上下游影响：
- 兼容性影响：

#### 4. 验证情况
- 执行的验证：
- 结果：
- 未覆盖：

#### 5. 风险与回滚
- 当前风险：
- 回滚方案：

#### 6. 文档同步
- 已同步文档：
- 待同步文档：

#### 7. 后续动作
- 下一步：
- 责任人/计划时间：

#### 8. 完成定义（DoD）
- [ ] 代码已合并
- [ ] 测试通过
- [ ] 文档已同步
```

### [2026-03-27] 完成 Phase 1 Graph IR 重构并建立离线验收

- 改动状态：`已完成`
- 修改人：Codex
- 主题：完成 V2 路线下第一阶段落地
- 关联文件：
  - `workflow.py`
  - `workflow_trace.py`
  - `utils/graph_ir.py`
  - `agents/assembly_agent.py`
  - `agents/coding_agent.py`
  - `agents/verifier_agent.py`
  - `agents/coding_utils.py`
  - `tests/test_phase1_workflow.py`
- 关联任务/背景：
  - 目标不是继续强化 V1 的四节点链路，而是先把 V2 路线里最关键的 Graph IR、Compiler、Verifier 立住
  - 对应设计背景见 `AHU程序/LangGraph工作流V2架构设计.md`

#### 1. 改动目标

- 这次为什么要改：
  - 当前 V1 的核心问题不是单个 Agent 不够强，而是 `execution_plan -> generated_code` 之间缺乏稳定的工程级中间层
- 原来存在什么问题：
  - Planner 直接把弱计划交给 Coding
  - Coding 既承担模板装配，又承担 JSON 编译，职责边界过宽
  - 主链没有显式验收出口
  - 后续如果要支持 AHU 多页面、多子流程、多实例结构，很难继续演进
- 希望达到什么效果：
  - 在不先重写 Planner 的前提下，先建立可编译、可验收、可持久化的 Graph IR 层

#### 2. 实际改动

- 改了哪些文件：
  - `workflow.py`
  - `workflow_trace.py`
  - `agents/coding_agent.py`
  - `agents/coding_utils.py`
  - `agents/__init__.py`
  - 新增 `agents/assembly_agent.py`
  - 新增 `agents/verifier_agent.py`
  - 新增 `utils/graph_ir.py`
  - 新增 `tests/test_phase1_workflow.py`
- 改了哪些逻辑：
  - 主链从 4 节点升级为 6 节点
  - 新增 `assembly` 节点，把 `execution_plan` 装配成 `assembled_graph_ir`
  - `coding` 节点改为确定性编译器，只消费 IR
  - 新增 `verification` 节点，对 IR 和编译结果做规则优先验收
  - `workflow_trace.py` 跟正式入口同步到同一条主链
- 新增了哪些字段 / 配置 / 节点 / 约束：
  - 新增状态字段：
    - `assembled_graph_ir`
    - `compiled_artifact`
    - `verification_report`
  - 新增节点：
    - `AssemblyAgent`
    - `VerifierAgent`
  - 新增结构模型：
    - `PageIR`
    - `SubflowDefinitionIR`
    - `NodeInstanceIR`
    - `EdgeIR`
    - `AssembledGraphIR`
    - `CompiledArtifact`
    - `VerificationReport`
- 删除或废弃了哪些旧逻辑：
  - 废弃了“Coding 直接从 `execution_plan` 生成最终 JSON”的职责边界
  - 包初始化不再主动导入所有 Agent，避免离线测试被 `chromadb` 等可选依赖阻塞

#### 3. 对工作流的影响

- 主链是否变化：`是`
  - 从 `analysis -> retrieval -> planning -> coding -> END`
  - 变为 `analysis -> retrieval -> planning -> assembly -> coding -> verification -> END`
- 状态字段是否变化：`是`
  - 增加了 `assembled_graph_ir`
  - 增加了 `compiled_artifact`
  - 增加了 `verification_report`
- 上下游契约是否变化：`是`
  - `CodingAgent` 的真实输入由 `execution_plan` 变成 `assembled_graph_ir`
  - 最终真实产物由 `generated_code` 转为 `compiled_artifact`
  - `generated_code` 退化为兼容字段
- 是否影响已有文档描述：`是`
  - 原有“当前是四节点线性链路”的描述已过期

#### 4. 验证情况

- 已做验证：
  - 语法编译验证
  - Phase 1 离线结构验收
  - 带 stub 的 LangGraph 端到端运行
- 验证输入：
  - 离线构造的常量相加计划
  - 离线构造的子流程定义与子流程实例关系
- 验证结果：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests\test_phase1_workflow.py` 通过
  - `python -m unittest discover -s tests -p test_phase1_workflow.py` 通过
  - 共 5 个测试全部通过
- 未验证部分：
  - 在线 LLM 场景下的真实主链质量
  - ChromaDB 实际检索结果对复杂 AHU 样例的召回效果
  - 真实 AHU 多页面/多子流程工程的系统级规划质量

#### 5. 风险与问题

- 当前已知问题：
  - 当前 Phase 1 只是“承载复杂结构的编译骨架”，并没有完成复杂 AHU 级别的系统规划升级
  - `generated_code` 等旧命名仍然保留，容易造成误解
  - 仓库中仍保留旧版 `Execution / Validation / Debugging` 模块，语义尚未对齐
- 临时处理方式：
  - 通过总结文档和演进记录明确当前真实主链与字段语义
  - 保留兼容字段，避免一次性破坏外部调用
- 潜在风险：
  - 如果第二阶段不尽快把检索层次升级，Planner 的能力瓶颈仍然会很快暴露
  - 如果后续继续增加能力但不同步文档，会再次出现“代码和认知模型漂移”

#### 6. 后续动作

- 下一步准备做什么：
  - 进入第二阶段，把当前单层 `retrieval_context` 扩展成三层 `retrieval_bundle`
- 是否需要同步文档：`已同步`
- 是否需要补测试：`需要`
  - 第二阶段完成后需要补检索层级相关测试
  - 第三阶段完成后需要补多子系统规划和合并测试

---

### [2026-03-31] 补齐 Phase 1 验收缺口并更新回归测试

- 改动状态：`已完成`
- 修改人：Codex
- 主题：补齐 Phase 1 剩余验收缺口
- 关联文件：
  - `agents/assembly_agent.py`
  - `agents/coding_agent.py`
  - `agents/verifier_agent.py`
  - `workflow_trace.py`
  - `tests/test_phase1_workflow.py`
  - `工作流总结文档.md`
  - `工作流演进记录.md`
- 关联任务/背景：
  - 对照 `AHU程序/LangGraph工作流V2架构设计.md` 第 1 阶段要求复查后，补齐实现与验收之间的缺口

#### 1. 改动目标

- 为什么改：
  - 虽然 Phase 1 主链已落地，但仍有几个会影响验收可信度的缺口
- 目标结果：
  - 让 Phase 1 在“强结构 IR + 确定性编译 + 结构验收”这条主链上真正闭合

#### 2. 实际改动

- 代码/配置变更：
  - 修复 subflow 实例在 `Assembly -> Compiler` 间的定义映射
  - 为编译器补充坏边容错，改为 warning 而不是直接抛异常
  - 为 `VerifierAgent` 增加必需输入端口缺失检查
  - 恢复 `workflow_trace.py` 在异常场景下的 trace 落盘
  - 为以上场景补充回归测试
- 契约变化：
  - 无新增状态字段
  - `verification_report.metrics.missing_required_inputs` 开始真实生效

#### 3. 影响评估

- 主链影响：
  - 无节点增减，仍是 6 节点 Phase 1 主链
- 上下游影响：
  - subflow 实例现在能稳定进入编译结果
  - 编译阶段异常更少，更多问题会进入 `verification_report`
- 兼容性影响：
  - 兼容字段 `generated_code` 保持不变

#### 4. 验证情况

- 执行的验证：
  - `python -m compileall workflow.py workflow_trace.py agents utils tests\test_phase1_workflow.py`
  - `python -m unittest discover -s tests -p test_phase1_workflow.py`
- 结果：
  - 编译通过
  - 共 9 个测试通过
- 未覆盖：
  - 在线 LLM + ChromaDB 的真实端到端质量

#### 5. 风险与回滚

- 当前风险：
  - Phase 1 已稳定，但仍不等于完整 V2
- 回滚方案：
  - 如需回滚，仅回退本次涉及的 5 个代码文件与 2 个文档文件

#### 6. 文档同步

- 已同步文档：
  - `工作流总结文档.md`
  - `工作流演进记录.md`
- 待同步文档：
  - `docs/coding_agent_summary.md`

#### 7. 后续动作

- 下一步：
  - 进入 Phase 2，扩展 `retrieval_context -> retrieval_bundle`
- 责任人/计划时间：
  - 待下一轮实现

#### 8. 完成定义（DoD）
- [ ] 代码已合并
- [x] 测试通过
- [x] 文档已同步

---

## 4. 决策记录

### [2026-03-27] Phase 1 不先重写 Planner，先立 Graph IR

- 决策主题：第一阶段的切入点
- 决策结论：先保留现有 `PlanningAgent` 输出 `execution_plan`，新增 `AssemblyAgent` 作为过渡层，把 plan 升级成 `assembled_graph_ir`
- 背景：
  - 如果一开始就同时改检索、规划、编译、修复，重构面太大，难以稳定验收
- 备选方案：
  - 方案 A：先改三层检索
  - 方案 B：先拆 Planner
  - 方案 C：先立 Graph IR 与 Compiler
- 为什么最终选择这个方案：
  - Graph IR 是后续所有阶段的共同依赖
  - 它是最小但最关键的基础设施
  - 先立 IR 后，后续扩检索、拆 Planner、加 Repair 都能在同一编译边界内迭代
- 代价 / 缺点：
  - Planner 仍然是单体弱计划层，复杂 AHU 规划能力暂时不会立刻提升
- 后续是否可能推翻：
  - 不太会推翻方向，但 `assembled_graph_ir` 的字段设计还会继续扩展

### [2026-03-27] Phase 1 先做 Verifier，不做 Repair 闭环

- 决策主题：是否同时接入自动修复
- 决策结论：Phase 1 只接入 `VerifierAgent` 做规则优先验收，不接入 `RepairRouter / RepairAgent`
- 背景：
  - 当前最缺的是“知道输出是否结构正确”，不是“立即自动修复一切问题”
- 备选方案：
  - 方案 A：验收和 Repair 一起做
  - 方案 B：先做验收，再做 Repair
- 为什么最终选择这个方案：
  - 先建立可靠验收标准，后续 Repair 才有明确目标
  - 否则容易陷入“修了但不知道修得对不对”
- 代价 / 缺点：
  - 当前验收报告出来后，主链不会自动修复
- 后续是否可能推翻：
  - 不会推翻，只会在 Phase 4 增加 Repair 闭环

### [2026-03-27] 继续保留 `generated_code` 兼容字段

- 决策主题：是否立即删除旧命名
- 决策结论：保留 `generated_code`，其值等于 `compiled_artifact["json_text"]`
- 背景：
  - 现有代码和外部调用可能仍依赖 `generated_code`
- 备选方案：
  - 方案 A：立即删除
  - 方案 B：保留兼容映射
- 为什么最终选择这个方案：
  - 第一阶段的首要目标是让新编译边界稳定落地，而不是扩大外部破坏面
- 代价 / 缺点：
  - 会让字段语义暂时不够干净
- 后续是否可能推翻：
  - 是，后续很可能改成 `generated_flow_json` 或直接只暴露 `compiled_artifact`

---

## 5. 契约变更记录

### `WorkflowState` 变更

| 日期 | 变更内容 | 影响范围 | 是否已同步文档 |
|:---|:---|:---|:---:|
| 2026-03-27 | 新增 `assembled_graph_ir`、`compiled_artifact`、`verification_report` | `workflow.py`、`workflow_trace.py`、`coding_agent.py`、调用方理解模型 | 是 |

### `execution_plan` 变更

| 日期 | 变更内容 | 影响范围 | 是否已同步文档 |
|:---|:---|:---|:---:|
| 2026-03-27 | 不改字段结构，但从“直接供 Coding 生成 JSON”变为“先供 Assembly 生成 Graph IR” | `AssemblyAgent`、Planning/Coding 之间的契约理解 | 是 |

### 最终产物格式变更

| 日期 | 变更内容 | 影响范围 | 是否已同步文档 |
|:---|:---|:---|:---:|
| 2026-03-27 | 最终真实产物从单一 `generated_code` 升级为 `compiled_artifact`，`generated_code` 仅保留兼容语义 | `CodingAgent`、`VerifierAgent`、工作流调用方 | 是 |

---

## 6. 文档同步记录

| 日期 | 文档 | 状态 | 备注 |
|:---|:---|:---|:---|
| 2026-03-31 | `工作流总结文档.md` | 已同步 | 已改为修复后 Phase 1 状态 |
| 2026-03-31 | `工作流演进记录.md` | 已同步 | 已补 2026-03-31 修复记录 |
| 2026-03-27 | `工作流总结文档.md` | 已同步 | 已改为 Phase 1 当前画像 |
| 2026-03-27 | `工作流演进记录.md` | 已同步 | 已记录本次 Phase 1 改动与验收 |
| 2026-03-27 | `docs/analysis_agent_summary.md` | 未确认 | 本次未修改 |
| 2026-03-27 | `docs/new_retrieval_agent_summary.md` | 未确认 | 本次未修改 |
| 2026-03-27 | `docs/planning_agent_summary.md` | 未确认 | 本次未修改 |
| 2026-03-27 | `docs/coding_agent_summary.md` | 待更新 | 其内容还偏旧版“模板落地器”语义 |

---

## 7. 验证与测试记录

### 回归验证清单

- [x] Phase 1 Graph IR 可生成
- [x] 编译器可消费 IR 并输出 JSON
- [x] 编译器可表达子流程定义与子流程实例
- [x] 验收器可对 IR 和编译结果做结构检查
- [x] 主链在带 stub 的前半段条件下可完整跑通
- [ ] 在线 LLM + ChromaDB 的复杂 AHU 场景端到端验证
- [ ] 基于真实 AHU 样例的系统级规划能力验证

### 典型验证样例

| 日期 | 用例 | 目的 | 结果 | 备注 |
|:---|:---|:---|:---:|:---|
| 2026-03-27 | 常量相加计划 -> Graph IR -> JSON | 验证 assembler/compiler/verifier 主干 | 通过 | 离线 |
| 2026-03-27 | 子流程定义 + 子流程实例 | 验证 `subflow` 结构承载能力 | 通过 | 离线 |
| 2026-03-27 | Stub Analysis/Retrieval/Planning 的 LangGraph 主链运行 | 验证正式工作流编排 | 通过 | 离线 |

### 未覆盖风险

- 当前还未用真实 AHU 复杂需求驱动新主链做系统级编排
- 当前测试更偏结构正确性，不代表业务正确性已经达到 AHU V2 目标
- `RetrievalAgent` 的召回能力瓶颈仍是第二阶段的主要风险

---

## 8. 下次进入新对话时建议先补充的内容

- 真实 AHU 复杂需求在新主链上的一次端到端跑通结果
- 第二阶段三层检索设计与实施记录
- `retrieval_context -> retrieval_bundle` 的契约变更
- `docs/coding_agent_summary.md` 的同步状态

---

## 9. 当前维护建议

现阶段最推荐的维护方式是：

1. 用 [工作流总结文档](d:\yjsproject\midea\工作流总结文档.md) 维护“当前系统画像”
2. 用 [工作流演进记录](d:\yjsproject\midea\工作流演进记录.md) 维护“改了什么、为什么改、做到哪了、怎么验收的”

这样后续新对话进入时，不需要再从旧版 V1 认知模型重新梳理一遍上下文。
